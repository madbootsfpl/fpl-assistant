# ADR Index

An index of all Architectural Decision Records in this project. **Add a row for each new ADR as it is
created, and keep the table in ascending order** — appending wherever the cursor happened to be is how it
drifted into `…069 068 067 089 122…070 066` and then stopped being maintained at all.

*Backfilled 2026-08-31 (Sprint 231): the index had stopped at **ADR-122** while 171 ADRs existed — 49 missing,
found while filing ADR-171. Re-sorted ascending at the same time, because appending 49 rows to a scrambled
table would have produced something still unusable. Every link is checked to resolve to a real file.*

| ADR | Title | Status |
|-----|-------|--------|
| [001](./ADR-001-single-user-vs-multi-user.md) | Single-user vs Multi-user architecture | Accepted |
| [002](./ADR-002-ui-approach.md) | UI approach | Accepted |
| [003](./ADR-003-cli-approach.md) | Command-line interface approach | Accepted |
| [004](./ADR-004-fixtures-and-fdr.md) | Fixtures data model & FDR approach | Accepted |
| [005](./ADR-005-custom-fdr.md) | Custom (overall) fixture difficulty | Accepted |
| [006](./ADR-006-expected-points-v0.md) | Expected Points (xP v0) | Accepted |
| [007](./ADR-007-multi-week-xp.md) | Multi-week xP (fixture horizon) | Accepted |
| [008](./ADR-008-squad-selector.md) | Optimal squad selector (ILP) | Accepted |
| [009](./ADR-009-squad-include-exclude.md) | Squad selector include / exclude | Accepted |
| [010](./ADR-010-clubelo-external-source.md) | ClubElo — first external data source | Accepted |
| [011](./ADR-011-squad-objective.md) | Squad objective toggle (points/value/xp) | Accepted |
| [012](./ADR-012-full-squad.md) | The full 15-man squad (`squad --full`) | Accepted |
| [013](./ADR-013-declared-bench.md) | A declared bench (`squad --bench`) | Accepted |
| [014](./ADR-014-flexible-formations.md) | Flexible formations (`squad --formation`) | Accepted |
| [015](./ADR-015-expected-goals.md) | Expected goals (xG/xA/xGI) from the FPL API | Accepted |
| [016](./ADR-016-soccerdata-evaluation.md) | soccerdata as a data source — **Defer** | Accepted |
| [017](./ADR-017-over-under-performance.md) | Over/under-performance (expected vs actual attacking points) | Accepted |
| [018](./ADR-018-defensive-contribution.md) | Defensive Contribution (DefCon reliability) | Accepted |
| [019](./ADR-019-clean-sheet-solidity.md) | Clean-sheet / defensive-solidity lens (xGC) | Accepted |
| [020](./ADR-020-clubelo-retry.md) | ClubElo retry-with-backoff (transient failures) | Accepted |
| [021](./ADR-021-importance-scaled-retry.md) | Importance-scaled retry (FPL tries hard, ClubElo fails fast) | Accepted |
| [022](./ADR-022-validate-legal-bench.md) | Validate a legal bench (warn, not block) | Accepted |
| [023](./ADR-023-player-availability.md) | Player availability (skip injured; opt-out) | Accepted |
| [024](./ADR-024-saved-squad.md) | Saved / persistent squad (user state, JSON) | Accepted |
| [025](./ADR-025-shared-table-renderer.md) | Shared table renderer (`Col` + `render_rows`) | Accepted |
| [026](./ADR-026-phase1-cli-mvp.md) | Declare Phase 1 (CLI Analytics MVP) complete; reframe the Roadmap | Accepted |
| [027](./ADR-027-historical-past-seasons.md) | Historical past-season data (`history --backfill`) | Accepted |
| [028](./ADR-028-xp-historical-baseline.md) | xP historical baseline (multi-season rate) | Accepted |
| [029](./ADR-029-captain-suggestions.md) | Captain suggestions (recommend + explain; xP, GK-excluded) | Accepted |
| [030](./ADR-030-transfer-suggestions.md) | Transfer suggestions (best single legal upgrades; xP gain, `--bank`) | Accepted |
| [031](./ADR-031-team-analyser.md) | Team Analyser (a saved squad's health check; indicators, not a grade) | Accepted |
| [032](./ADR-032-per-gameweek-xp.md) | Per-gameweek xP breakdown (+ `analyse --sort xp`) | Accepted |
| [033](./ADR-033-llm-grounded-narration-spike.md) | LLM grounded-narration spike (local Ollama; analytics decide, LLM narrates) | Accepted |
| [034](./ADR-034-ask-command-grounded-nl.md) | The `ask` command — grounded NL answers (routing; LLM optional) | Accepted |
| [035](./ADR-035-multi-transfer-plan.md) | Multi-transfer plan (coordinated, greedy; `transfer --count` / `ask` N transfers) | Accepted |
| [036](./ADR-036-per-gameweek-plan-table.md) | Per-gameweek transfer-plan table + a structured detail in `ask` | Accepted |
| [037](./ADR-037-grounding-verification.md) | Grounding verification (verify the LLM narration; a ✓/⚠ trust line) | Accepted |
| [038](./ADR-038-expected-minutes-v0.md) | Expected minutes (xMins) v0 — weight recommendations by playing time (chance% × minutes share; default-on at the decision edge) | Accepted |
| [039](./ADR-039-start-bench-compare-intents.md) | Two new `ask` intents — start/bench (best legal XI vs declared) + compare (robust name-matching; analytics decide, LLM narrates) | Accepted |
| [040](./ADR-040-low-evidence-xp-and-transfer-dedup.md) | Sane low-evidence xP (shrink the no-baseline fallback toward a replacement prior) + transfer dedup (no repeated incoming) + consistency clarity | Accepted |
| [041](./ADR-041-one-xp-metric-and-squad-build-intent.md) | One xP metric — unify the optimiser with the decision layer (`decision_xp`; `xp` the default `squad` objective) + `ask "build me a squad"` | Accepted |
| [042](./ADR-042-shortlist-intent.md) | A "best players" shortlist `ask` intent — `best <position> [under £X]` ranked by xP (or xP/£m for "value"); grounded | Accepted |
| [043](./ADR-043-squad-archetype-constraints.md) | Squad archetypes — min-count price-band constraints in the optimiser (`--cheap`/`--premium`, NL); differential defined + deferred (needs ownership) | Accepted |
| [044](./ADR-044-differential-archetype.md) | The differential archetype — ingest ownership; a ≤5%-owned min-count constraint (`--differential N`, NL); completes ADR-043 | Accepted |
| [045](./ADR-045-bench-aware-optimisation.md) | Bench-aware squad optimisation — a `start`-variable ILP + weighted objective; `--weekly` (max XI + playing bench) / `--bench-boost` (max-15); default unchanged | Accepted |
| [046](./ADR-046-xi-aware-transfers.md) | XI-aware transfers — rank swaps by XI-gain (best-XI change) via a fast `best_xi_points`; the default (`--raw` for the old ranking) | Accepted |
| [047](./ADR-047-conversational-ask-follow-ups.md) | Conversational `ask` — a `chat` REPL with grounded follow-ups (why / next / what-about); a pre-route resolver + rank offset; analytics still decide, verifier runs each turn | Accepted |
| [048](./ADR-048-fixtures-fdr-ask-intent.md) | A fixtures / FDR `ask` intent — league FDR ranking + single-team schedule (reuse `team_fdr`/`team_schedule` + renderers); team resolution never guesses; FPL difficulty; squad-scoped deferred | Accepted |
| [049](./ADR-049-squad-scoped-fixtures.md) | Squad-scoped fixtures — a third `fixtures` mode: rank a saved squad's **players** by their team's FDR (player-level); precedence team→squad→league; `_squad_name` possessive-aware; works in `ask` + `chat` | Accepted |
| [050](./ADR-050-thin-web-ui.md) | A thin web UI — a read-only, local-only **FastAPI** (sync) edge in `src/web/` reusing the analytics/`ask` (core stays web-free); slice 1 reuses the text renderers in `<pre>`; `/` + `/ask` flagship | Accepted |
| [051](./ADR-051-web-track-streamlit.md) | The web track — a measured spike (Streamlit vs FastAPI) decides: **adopt Streamlit** (pure-Python, interactive; ~⅓ the code) and **freeze the FastAPI edge** as a lean reference; graduate the spike to `src/web_streamlit/` next | Accepted |
| [052](./ADR-052-streamlit-edge-structure.md) | The Streamlit edge structure — multipage `pages/`; a clean `python -m src.web_streamlit` runner (no `sys.path` hack); filterable table + chat-style Ask; `streamlit` web-only; the two-edge guardrail; FastAPI frozen | Accepted |
| [053](./ADR-053-deploy-streamlit-community-cloud.md) | Deploy & share — **Streamlit Community Cloud** (owner relaxed the custom domain), a committed **seed DB**, fully **public/read-only**; no secrets, Ask degrades without Ollama; going live is owner-executed via a runbook | Accepted |
| [054](./ADR-054-cloud-squads-session-and-files.md) | Cloud squads — a session **"active squad"** (build/upload) + **download/upload** persistence (the user's file; no server writes); a committed demo seed (`SQUADS_PATH` fallback); Transfer/Analyse/Captain consume the dict | Accepted |
| [055](./ADR-055-editable-session-squad.md) | An **editable** session squad — mutate in `session_state` (name · apply-transfer · manual-swap · set-captain · bench; no server writes); a generic **`squad_15_issues`** validator (budget = warn, not block); `captain_id` superset + **(C)**; edit inline (Transfer/Captain) + a **My Squad** hub; fixes the Build xP/xMins bug | Accepted |
| [056](./ADR-056-local-refresh-and-freshness.md) | A **local-only** data-refresh button (gated by `FPL_LOCAL`, set by the runner; writable non-seed DB) reusing `ingest.refresh` + a **"Data as of \<date\>"** freshness caption on every tab (DB mtime); the cloud stays read-only (caption only) — the first, narrowly-scoped web write path | Accepted |
| [057](./ADR-057-crowd-signals-lens.md) | **Phase 6** opener — crowd/sentiment signals as a **complementary lens + flags, never blended into xP**; Tier-1 **free FPL** fields (`transfers_*_event` · `cost_change_*` · `form` · `ict_index`+components · `value_form`); a pure `crowd_flags` helper (template ≥20% / differential ≤5% / trending / price / in-form; tunable, GW1-calibrated); external social + pundit deferred (Tier 2/3) | Accepted |
| [058](./ADR-058-tier2-fpl-news-and-manager-import.md) | **Phase 6 Tier 2** (start) — an FPL official-**news lens** (surface the ingested `player.news` + scout links) + **import a team by manager-ID** (public FPL entry API → a `SquadStore`-shaped session squad; **GW1-gated** picks; no auth/secret); degrade-gracefully (ClubElo pattern); not xP; no server writes; keyed social (Reddit/X) + pundit NLP deferred | Accepted |
| [059](./ADR-059-community-signals-reddit-rss.md) | **Community Signals** — a Reddit **RSS** buzz counter (`r/FantasyPL/.rss`, **no auth/secret** — the `.json` API 403s); count current-player mentions → "most talked about"; **degrade-gracefully** (403/429/parse → "unavailable"), **cached + button-gated** + a descriptive UA; a Trending "💬 Talked about" board; buzz (frequency) not sentiment; not xP; cloud-IP may block → degrades | Accepted |
| [060](./ADR-060-per-gameweek-history-and-dormant-form-blend.md) | **Data Hardening** step 1 — a per-GW `player_history` table (`PlayerGameweek`, keyed code+round; additive/idempotent) filled by the **existing** throttled `element-summary` walk (empty preseason → live GW1); a **dormant** in-season **form blend** in the one `decision_xp` recipe (a computed minutes-aware **rolling pp90**, not FPL's `form`) behind `FORM_WEIGHT = 0` → `rate` unchanged today; the one-xP invariant (ADR-041) preserved, pinned by an invariance test; GW1 flip = backfill + set the weight | Accepted |
| [061](./ADR-061-differentials-and-value-ask-intent.md) | **Differentials / value `ask` intent** — extend the shortlist (ADR-042) with a **differential** lens (filter ≤ `DIFFERENTIAL_OWN` owned, +Own% column; the plain shortlist stays byte-identical) + a new single-player **`worth`** intent ("is X worth the money?" → xP/£m · rank among position peers · vs the position median · a tiered fact-derived verdict; degrades on no match); routing `worth` before `shortlist`, "differentials"→shortlist, "most owned"→trends; grounded (ADR-037); preseason-honest (ownership sharpens at GW1) | Accepted |
| [062](./ADR-062-web-build-parity-and-tab-reorg.md) | **Web build parity + tab reorg** — expose the full CLI `squad` options as **form widgets on "Build Squad"** (include/exclude/bench/objective/no-xmins/weekly/bench-boost/include-unavailable) feeding the *same* `select_squad`/`decision_xp` (no engine change); the saveable build stays a full **15** (formation = an XI-only **display preview**); My Squad stays the tweaker + a "Rebuild in Build Squad →" link; rename Squads→**Squad Health**, Build→**Build Squad**, grouped; an optional Ask-build → session-squad bridge; no server writes | Accepted |
| [063](./ADR-063-player-stats-page-and-pagination.md) | **Player Stats page + pagination** — a new **Player Stats** page (tabs: Over/under · DefCon · Clean sheets · xG) reusing `over_under`/`defcon_reliability`/`defensive_solidity` + the xGI ranking (no analytics change; season-to-date, team badges), placed at sidebar **position 2** (renumber); a shared **`paginate`** helper (`page_labels` pure) so **Players** (page all + sort by team/position) and **Trending** (four boards page past 30) can see everything; no server writes | Accepted |
| [064](./ADR-064-shared-player-filter.md) | **Shared player filter** — one `web_streamlit/filters.py` (`filter_controls` + `apply`): **Team · Position · Player** multiselects (+ optional max-price), **AND**-combinable (empty dim = any), tolerant of Row/dict rows; applied to **Player Stats** (all four tabs) and **Players**; the Players scatter → a filter-responsive **top-15 bar** (Altair, rank-ordered) by the sort metric; no analytics change, no server writes | Accepted |
| [065](./ADR-065-help-tooltips.md) | **Help tooltips (ⓘ)** — a concise, action-oriented `help=` on **every input control** (selectbox/multiselect/slider/number_input/text_input/checkbox/radio) across the web, added at the shared components (`filters`/`paginate`/`squads`) + per page; key buttons too; `st.tabs`/`st.chat_input` exempt (captions cover them); a coverage test (`.help` via AppTest) enforces "all options have a tooltip"; no behaviour change | Accepted |
| [066](./ADR-066-tech-debt-pulp-and-squad-renderer.md) | **Tech debt — PuLP + squad renderer** (revises 2 Backlog items on real behaviour): migrate `LpVariable`→`problem.add_variable`, **keep `PULP_CBC_CMD`** (COIN_CMD fails — needs an external CBC; would break the Cloud), and narrow the blanket DeprecationWarning ignore to a **targeted** PULP_CBC_CMD filter; share the squad renderers' **header/divider + bench-heading** but **don't** fold into `render_rows` (not byte-identical-feasible — mid-table heading, glued markers, divergent price cells); no behaviour/output change | Accepted |
| [067](./ADR-067-team-level-squad-fixtures.md) | **Team-level squad fixtures** — implements the ADR-049 deferral: a 4th `fixtures` ask mode ranking a squad's **teams** (with player-counts) by their `team_fdr` run; a **"teams"/"clubs"/"by team"** cue routes to it within the squad branch (else the player-level view); a dedicated `render_squad_team_fixtures`; grounded (ADR-037); reuses `team_fdr` (no new analytics); other modes unchanged | Accepted |
| [068](./ADR-068-help-guide-tab.md) | **A Help guide tab** — a static, step-by-step **Help** page (`pages/12_Help.py`, placed last) walking a user through *build → make it yours → check → improve → research → ask → save*, with `st.expander` steps + copy-paste `ask` examples + tab pointers; complements Home (which gains a pointer); no analytics/data dependency, no input controls (outside the tooltip test); a render + key-content test | Accepted |
| [069](./ADR-069-sidebar-consolidation.md) | **Sidebar consolidation** — merge Players+Player Stats → **Players** and Build/My Squad/Health/Transfer/Captain → **Squads** (12 → 7 tabs: Players · Fixtures · Squads · Ask · News · Trending · Help); a lazy **`st.segmented_control`** sub-nav (only the selected view computes — not `st.tabs`, which runs all); view bodies extracted to `render_*` functions (`web_streamlit/views/`); a shared filter (Players) / picker (Squads manage views); no behaviour/output change; tests rewired (every assertion kept) | Accepted |
| [070](./ADR-070-ai-gameweek-recommendation.md) | **AI gameweek recommendation ("this week")** — a grounded weekly plan for a squad: an assembler `gameweek_plan` orchestrating the existing primitives (captain `captain_picks` next GW · lineup `best_legal_xi` vs the declared bench · one self-funding `suggest_transfers` · availability flags), a new `ask`/`chat` **gameweek** intent (`_decide_gameweek`, phrase-routed after the specific intents) that humanises + verifies it (✓/⚠, ADR-037), a `render_gameweek_plan` block, and a **Squads → This week** view (routes through `ask.answer`, degrades without Ollama); no new analytics, no server writes | Accepted |
| [071](./ADR-071-interpretable-stat-boards.md) | **Interpretable stat boards** — a display-only quality **rating** on the Players stat boards: `web_streamlit/ratings.py::quality_band` rates a value **relative to the players shown** (quintile 🟢 excellent…🔴 very poor) with the **percentile** inline ("top N%"); a **Rating** column on **Clean sheets** (xGC/90, lower=better) + **xG** (xGI, higher=better) + a legend; clearer captions + per-column `help=` tooltips on all four boards (absolute season total vs per-90). Relative (not fixed) bands because real FPL xGC/90 (median 1.36) makes ChatGPT's fixed table mislabel 91/117 "poor"; analytics untouched, no server writes | Accepted |
| [072](./ADR-072-web-number-formatting.md) | **Consistent number formatting (web tables)** — a shared convention (`web_streamlit/formats.py`: a `FORMATS` label→printf map + `column_config(...)`) formatting the Streamlit tables via `st.column_config.NumberColumn` (right-aligned + **still numeric/sortable**, not pre-rounded strings): money/value/%/form/ICT → **1dp** (`6.0`, `24.2`), counts (Pts/Mins) → **integer**, the xG family (xG/xA/xGI/xGC/xGC/90) → **2dp**, signed diffs (Diff/Margin/+xP) → **`%+.1f`**. Applied to the Pool, the four stat boards, and the squad tables; the raw analytics values are untouched (display-only); CLI unchanged; no server writes | Accepted |
| [073](./ADR-073-xg-rating-meaningful-only.md) | **Rate the xG board only where xGI is meaningful** (refines ADR-071) — a tester spotted keepers (xGI ≈ 0) rated "🟢 excellent". The xG board now rates `xGI` **only for outfield players with ≥900 mins**, with the percentile over **that** pool; goalkeepers / low-minutes / no-data rows show a blank **—**. The column is renamed **"xGI rating"** and moved **right after xGI** (away from xGC, which caused the "how can 0 be good and 56 be good?" ambiguity). `quality_band` + the analytics + Clean sheets are unchanged — only which rows are rated and against what pool | Accepted |
| [074](./ADR-074-availability-flags-in-tables.md) | **Availability flags in the player tables** — a shared `availability_flag(player)` (in `analytics/crowd.py`, next to `crowd_flags`) → an emoji per FPL status (🚑 injured · 🚫 suspended · ⛔ unavailable · ❓ doubtful; `""` = available), chosen distinct from the rating circles. A compact **Fit** column + a shared `AVAILABILITY_LEGEND` on the web player tables (the Pool, then the four stat boards). The Pool/xG flag raw rows directly; the trimmed boards look the flag up from the full `players` list each render func already has — **no analytics change**; display-only, no server writes | Accepted |
| [075](./ADR-075-formation-preview-xi-score.md) | **The XI score in the formation preview** — the Build page's "🔎 Preview the best XI in a shape" now shows the shape's **Projected XI xP** total (a sum of the previewed XI's `xP`; free — the solve already runs), and — behind a **default-off "Compare all formations"** checkbox — a table ranking all 7 shapes by XI xP with **Δ vs best**. Gated because a Streamlit expander body runs even collapsed (7 ILP solves would hit every Build render). Display-only; the saved build is still a full 15; reuses `select_squad`/the build's `scores`; no analytics change, no server writes | Accepted |
| [076](./ADR-076-talked-about-bigger-sample.md) | **"Talked about" — count mentions across a bigger sample** (refines ADR-059) — a tester saw "1 mention regardless". Verified the counter is fine (it counts 1–4); the cause is the sample: Reddit's default `.rss` is only **25 posts**, so 35/51 players sit at "1". Fix: request **`?limit=100`** (new `config.REDDIT_RSS_LIMIT`) so `community_buzz` counts across ~100 posts (the counter already sums every match across every entry); and **paginate** the now-longer board (per_page=30, like the other Trending boards). Stays a best-effort, cached, button-gated **buzz lens** that degrades on failure; `decision_xp` untouched; no server writes | Accepted |
| [077](./ADR-077-configurable-squad-horizon.md) | **A configurable prediction horizon on the Squads tab** — a shared **"Gameweeks ahead"** dropdown (1–8, default 5) that drives the prediction horizon across **Build · My Squad · Health · Transfer · AI Tips** (Captain stays next-GW — captaincy is a one-week decision). Threads the chosen `horizon` into `decision_xp`/`analyse_squad`/the transfer renderers (US-237) and adds a backward-compatible `horizon` param to `ask.answer` → `_decide_gameweek` → `_squad_xp` for AI Tips (US-238). Default 5 = unchanged behaviour; reuses the existing horizon params (no analytics change); no server writes | Accepted |
| [078](./ADR-078-bench-order.md) | **Bench order — the auto-sub priority** — a pure `analytics.bench_order(bench, scores)` (next to `best_legal_xi`): the **outfield** bench ranked by xP → roles "1st"/"2nd"/"3rd" (your most valuable sub first), then the **bench GK** → "GK" (it only ever replaces the starting keeper). Shown on **My Squad** as a "Bench order (auto-subs)" line (horizon-aware xP), with a caption noting FPL brings on "the first that keeps a legal XI". A recommendation (order by value), not a per-blank simulator; display-only, no analytics drift | Accepted |
| [079](./ADR-079-set-bench-order.md) | **Set the bench order** (refines ADR-055/078) — make `bench_ids` **order** the sub priority: `set_bench` now **preserves** the given order (was squad-position); a pure `move_bench_sub(squad, player_id, direction, by_id)` swaps an **outfield** sub up/down in priority (the bench GK is keeper-only → excluded + kept last). My Squad shows the **stored** order + a ⬆/⬇ reorder control + a "Use recommended (xP) order" button (ADR-078). Copy-not-mutate; the order rides in the download; the analytics use `bench_ids` as a set (unchanged); no server writes | Accepted |
| [080](./ADR-080-pronoun-aware-chat.md) | **Pronoun-aware chat** (refines ADR-047) — a pure `_resolve_pronoun(question, context)` rewrites a pronoun (he·him·his·she·her·they·them·their) → the last turn's **sole** subject (unambiguous only; possessives → `name's`), then routes — so "is Haaland worth it?" → "compare **him** to Isak" resolves. Wired into `_fresh` (fires in `converse`/`chat`, a no-op for the context-less one-shot `answer`). US-248 threads `Context` through the web **Ask** (`converse` not `answer`) so pronouns **and** the why/next/what-about follow-ups work there. Substitutes the player's *name* for the pronoun (never assigns one); analytics decide, LLM narrates, every turn still verified; no server writes | Accepted |
| [081](./ADR-081-set-piece-takers.md) | **Set-piece takers (ingest + a differential lens)** — who takes **penalties · corners · free-kicks** per team. Ingest `corners_order` (`corners_and_indirect_freekicks_order`) + `freekicks_order` (`direct_freekicks_order`) onto the `Player` model + `from_api` + storage (auto-migrated; `get_players` uses `SELECT p.*`) — `penalties_order` already existed. A pure, empty-safe `analytics.set_piece_flags(player)` → ⚽ pens · 🚩 corners · 🎯 FK for the **first-choice** (order == 1) taker. A Players **"Set pieces"** view (Pen/Corners/FK order + **Own% · Val/£m**, shared filter, differential caption) + a compact **"Set"** flag on the Pool. **Display-only** — no scoring change; the differential lens reuses ownership + value; penalty duty already feeds captaincy. `refresh` + `reseed` populate real data | Accepted |
| [082](./ADR-082-chip-strategy-advisor.md) | **Chip-strategy advisor (an assembler + a grounded intent)** — WHEN to play each chip (Triple Captain · Bench Boost · Free Hit · Wildcard) for a squad. A pure `analytics/chips.py::chip_advisor(owned, by_gameweek_by_id, gameweeks)` reduces the per-GW xP (`by_gameweek`, ADR-032) + `best_legal_xi` per GW into a best GW/window per chip — **TC** = max single-starter ceiling · **BB** = best all-15 GW (+ bench share) · **FH** = weakest best-XI GW · **WC** = weakest rolling 3-GW window. A `chips` `ask`/`chat` intent (`_decide_chips`, reuses `_squad_xp`) narrated + **verified** (ADR-037); `ui/chips.py::render_chip_advice` is the shared block; a Squads "Chips" view routes through `ask.answer` (degrades without Ollama). Routing placed first with distinctive chip phrases (not bare `bench boost`/`wildcard`/`captain`/`bench`). An **assembler** (the ADR-070 shape) — no new analytics, so it can't drift. Fixture-run + xP based; DGW/BGW + mini-league position deferred (in-season / GW1) | Accepted |
| [083](./ADR-083-captain-double-in-projection.md) | **Captain double-points in the My Squad projection (next-GW only)** — the "Projected XI (N GW)" total now adds the captain's ×2 for the **next gameweek only** (a pure `captain_bonus` from `by_gameweek`/ADR-032), applied only when the captain is set + in the XI (benched/unset → 0); "Captain (2×)" reframed to next-GW doubled; a caption states the ×2 is a one-week thing when horizon > 1 (owner steer). Whole-horizon doubling rejected — captaincy is a weekly decision. Display-only; `decision_xp` unchanged; chips deferred to the Chips tab (ADR-082) | Accepted |
| [084](./ADR-084-styled-pitch-view.md) | **A styled (CSS) pitch view for My Squad** — `render_pitch` emits one self-contained HTML/CSS block (`st.markdown(unsafe_allow_html=True)`): a green football pitch with formation rows (GK/DEF/MID/FWD) + a bench strip, each player a kit card (photo/club-shirt image · name · xP chip · £ · next opponent · crowd/set-piece flags), with a (C) armband + sub-number badges. Names HTML-escaped; responsive; theme-readable; no JS; **display-only** (the edit controls stay native widgets). Revisits the informal Sprint-062 "native cards, no custom CSS" call; kept to one contained block so a visual regression can't break a control; tests read the HTML via `AppTest.markdown` | Accepted |
| [085](./ADR-085-chat-assistant-rules-kb.md) | **AI Chat Assistant — a curated rules KB + a labelled free-form mode** — `src/fpl_rules.py` (authoritative FPL-rules facts) + a grounded **`rules`** intent (routed first on question-shaped cues; answers from the KB, narrated + **verified ✓**, degrades to the facts without a model — rules never come from the LLM's memory, which hallucinates them). A **free-form** tail for open tactics / unrecognised questions → a scoped LLM answer tagged **ℹ not verified** (a third `_trust_line` state), never a specific squad/player decision, degrades to the help message. Both funnel through one `{free_form:True}` branch in `assemble`, so the Ask tab + CLI `chat` inherit it. Grounded questions unchanged | Accepted |
| [086](./ADR-086-deadline-banner.md) | **A season countdown / deadline banner derived from fixtures** — a pure `analytics/deadline.py::next_deadline(fixtures, now)` computes the next FPL deadline from the stored `kickoff_time` (each gameweek's earliest kickoff − 90 min; the first still ahead of `now`, rolling forward each GW; empty-safe) — **no `events` ingest** (the derivation matches the API's `deadline_time`). `get_upcoming_fixtures` returns `kickoff_time`; `ui/deadline.py::deadline_banner` formats a days/hours countdown + the date in UK time (`zoneinfo`). Shown on Home + Squads; `now`-injected (deterministic tests); no live tick | Accepted |
| [087](./ADR-087-beta-access-and-feedback.md) | **Beta enablement — an opt-in access gate + feedback capture** — a shared **`FPL_ACCESS_CODE`** gates entry via `require_access()` on every page (no accounts; a session flag; open when unset); an in-app **📣 Feedback** form POSTs to the owner's **`FPL_FEEDBACK_WEBHOOK`** (degrades to a GitHub issue); a **"Join the beta"** link to **`FPL_SIGNUP_URL`** captures founding-tester emails. All opt-in via secrets, **off by default**; a safe `secret()` getter (st.secrets *raises* without a file → try/except → env); no user data on our infra (the read-only guardrail holds). Accounts/DB/payments deferred (DIRECTION §1); runbook: `docs/BETA.md` | Accepted |
| [088](./ADR-088-live-countdown-clock.md) | **A live client-side countdown clock (`st.iframe`)** — `web_streamlit/countdown.py::countdown_html` builds a self-contained Days:Hrs:Mins:Secs clock (urgency-coloured; cells server-filled so it reads without JS) that ticks via `setInterval` off the embedded deadline ISO; `render_countdown` embeds it with `st.iframe` (JS-enabled; the modern replacement for the deprecated `components.v1.html`). The **first client-side JS** in the app — one self-contained block, no external scripts, only our own ISO/int, display-only; the static text line (ADR-086/US-267) is the no-JS fallback. Revisits ADR-086's "no live tick" deferral | Accepted |
| [089](./ADR-089-explainability.md) | **Explainability — grounded Why / Risk / Confidence** — a pure `analytics/explain.py` turns a decision's signals into an `Explanation` (✓ reasons · ⚠ risks · a transparent confidence score + band). `explain_captain`/`explain_transfer` build the ✓/⚠ from real data (xP · penalties · xMins · fixture · ownership · form · set-pieces); a **documented** confidence heuristic (auditable, self-tempering via the xP lead over the runner-up, capped by chance/doubt). Rendered as a Confidence·Why·Risk block in Ask/CLI/web; the values enter the facts so a narrated number still **verifies** (ADR-037). Every reason + the number is computed from the data — the LLM only phrases, never invents. A heuristic, not a probability; gated signals light up at GW1 | Accepted |
| [090](./ADR-090-default-squad-scoping.md) | **`ask` defaults squad questions to the loaded squad** — when a squad is active, the question names no known squad, and there's no explicit-global cue (`all players`/`everyone`/`best overall`/`from all`/`any player`), the squad-scoped intents (captain·transfer·analyse·start_bench·gameweek·chips) default to the loaded squad. Fixes *"who should I captain from my-team?"* answering globally (a regex matched "my team" but not the hyphenated "my-team" the examples use) and makes a bare question scope to your team; captaincy's global **best-picks** mode stays reachable via the explicit cue; gated to squad intents so a global fixtures/compare question isn't scoped; the CLI (no `active_squad`) is unchanged. Refines Sprint 066 + ADR-047/080; routing/display only | Accepted |
| [091](./ADR-091-persisted-chat-context.md) | **Persist the chat context across CLI runs (local-only)** — a small `src/chat_context.py` saves/loads one `Context` ↔ a local, git-ignored JSON (`config.CHAT_CONTEXT_PATH`) with a timestamp + a **2h TTL** (stale/missing/corrupt → `None`; best-effort — never crashes a turn). The CLI `ask` loads→converse→saves (so *"ask …"* then a separate *"ask why?"* works) and `chat` resumes + persists per turn; `--forget`/"forget" clears it. The pure `ask.answer`/`converse` API is unchanged; the **multi-user web keeps `st.session_state`** and never imports the store (a guardrail test enforces it), so the read-only deploy is untouched. Completes the open half of ADR-047/080 | Accepted |
| [092](./ADR-092-price-change-predictor.md) | **A Price Change Predictor (a directional lens, dormant → GW1)** — `analytics/price.py`: `price_pressure` = `net_transfers ÷ selected_by%` (net per 1% ownership — comparable across players; the constant total-manager count cancels, so no `total_players`/ingest), `price_prediction` → rise/fall/stable (GW1-calibrated thresholds), `price_flag` → 🔺/🔻/"" (distinct from the **retrospective** crowd 💰/💸). A *directional flag, not the exact price/timing* (that needs an unpublished threshold + a since-last-change counter). Surfaced as a Price column on the Pool + a My Squad transfer-timing nudge; **never** `decision_xp` (an invariance test pins it); 0 preseason → live at GW1. Extends ADR-057; follows the wired-dormant pattern (ADR-060) | Accepted |
| [093](./ADR-093-signal-sources-and-media-lens.md) | **External signal-source policy + a media-headlines lens** — a review of ~12 external feeds → **adopt** public, no-auth, FPL-relevant **RSS/Atom** (Fantasy Football Scout, BBC Football, Reddit `top/.rss?t=week`, YouTube creator RSS); **defer** Reddit `.json` (OAuth), HTML scraping (Premier League/Sky/club sites/Premier Injuries — FPL's own status/news grounds injuries), Transfermarkt (ToS), and **betting/odds** (auth + crosses the lens→xP line → a possible Tier-3 modelling item). A generic `api/feeds.py` (`MediaFeedsClient` + `parse_feed` for RSS `<item>` + Atom `<entry>`, stdlib, no new dep) + `config.MEDIA_FEEDS` → a button-gated, cached, per-feed-degrading **Headlines** section on News + a Reddit weekly-top list on Trending. Display-only (never xP); best-effort (cache + gate + degrade). Extends ADR-058/059 | Accepted |
| [094](./ADR-094-cross-device-squad-persistence.md) | **Cross-device squad persistence — a handle-keyed cloud store** (design gate; build = Sprint 123) — save on one device, load on another via a user-chosen **handle** (no login), backed by a free **Supabase** REST store (`FPL_STORE_URL`/`FPL_STORE_KEY` secrets, ~£0). A thin, swappable `cloud_store` adapter (`save_squad`/`load_squad`); the handle can later become an authenticated `st.login()` user id without re-architecting. **Revises the read-only invariant** (ADR-054): the sole server write is this opt-in, secret-gated, tested squad save — off by default (unset secret → the feature hides, the app stays read-only). Trade-off: a handle isn't security (public FPL data, no PII); accounts/auth stay deferred (the "product" path). Extends ADR-054 | Accepted |
| [095](./ADR-095-running-a-wider-beta.md) | **Running a wider beta — environments, code protection, backup, monitoring** — the ops decisions for ~20–50 testers (all ~£0, opt-in, stay hobby). **(1) Envs:** a two-app **prod/staging** split — `master` (working) → a staging app, `main` (stable) → the prod app testers use; promote by **merge** (a mid-sprint push can't break the beta). **(2) Protection:** stay **public** + a **PolyForm Noncommercial** LICENSE (visible, non-commercial reuse; deploy unaffected); private deferred (Community-Cloud reconnection friction). **(3) Backup:** a **mirror remote** + a `mirror.yml` Action (`push --mirror`, secret-gated → inert until `MIRROR_URL` set) + a `git bundle` fallback; kills the single-point-of-failure. **(4) Monitoring:** a free external **uptime/latency** monitor + the Cloud Analytics tab; server-timing to logs + a paid host **deferred** until the free tier demonstrably struggles. Extends ADR-053/087 | Accepted |
| [096](./ADR-096-gated-set-piece-xp-term.md) | **A gated set-piece xP term (tier-restricted, wired-dormant)** — reward penalty/corner/FK duty in `decision_xp` (a **modelling** change to the rate, not a lens — ADR-057 still governs crowd/price). A pure `set_piece_bonus(player)` (pens > corners/FK) added to the `player_xp` rate, **gated by `SET_PIECE_WEIGHT` (default 0 → wired-dormant, ADR-060 pattern; invariance-at-0 pinned)**. **Key insight — no double-counting:** the baseline rate already includes an established taker's pens, so the bonus applies **only to the fallback/current tiers** (`rate_source != "hist"`), i.e. players whose history doesn't capture the current duty (new signings/role changes). **Auditable:** when active the contribution is an explanation reason + enters the grounding facts (ADR-037/089). A coarse proxy (no per-team pen rate); calibrate + backtest at GW1. Extends ADR-041/060/081 | Accepted 🚫 **CLOSED 2026-09-13 (ADR-190) — removed, not left dormant.** Unmeasurable, not wrong: ⭐ **THE DESIGN DECISION THAT MADE IT CORRECT IS THE ONE THAT MADE IT UNMEASURABLE** — the tier restriction (never on the `hist` baseline, or it double-counts an established taker's pens) means that of **43** duty-holders the term reaches **9**, fixed for the season because `history_by_code` holds only completed seasons; at weight 0.5 it produced a ranking **0.99998** correlated with the unweighted one, so §B0's bar was **unreachable**. Deleted (weight · `player_xp` param · rate branch · `set_piece_xp` · the explain clause · the sweep entry · `setpieces.py`) rather than left at 0 = furniture; verified **657 players, 0 projections changed**. ⭐ The duty is still SHOWN (⚽ glyphs, Scout board) — **the app says who takes the penalties and declines to say what that is worth**, which is ADR-057's lens rule reached from the other direction: that ADR bars a signal entering `decision_xp` on assertion; this one tried to enter on evidence and the evidence could not be gathered | 🚫 Closed |
| [097](./ADR-097-defcon-opposition-magnifier.md) | **A fixture-context DefCon magnifier** (design gate Sprint 127; **refined + built Sprint 129, wired-dormant**) — reward the owner's insight that **defensive-contribution** points rise when a team defends more (vs a *strong* opponent) and fall when it dominates a weak one. **Refined to the "delta" approach:** the baseline *already includes* DefCon points, so rather than add a new component, the magnifier **re-weights the DefCon portion already in the baseline by fixture** — `defcon_pts_per_match × (magnifier − 1)`, **0 at neutral → no double-count, dormant by default**. `defcon_points_per_match` (2·P(clear) from `defcon_per90` vs `THRESHOLD`) + `defcon_magnifier(FDR difficulty)` (clamped ~0.5–1.5, a clean-sheet proxy — **no betting odds**), wired into `player_xp` behind `DEFCON_MAGNIFIER_WEIGHT = 0` (invariance-pinned) + `defcon_xp` on the row + a grounded reason. Traps recorded: clean-sheet vs DefCon move **oppositely** (separate multipliers); the **transferred-player** baseline (deferred team-share adjustment). A **modelling** change (not a lens); **calibrate at GW1**. Extends ADR-018/019/041/096 | Accepted |
| [098](./ADR-098-capped-email-registration-gate.md) | **A capped email-registration gate (soft control, not accounts)** — control tester numbers before the ramp: a third access mode (extends ADR-087) where a visitor enters the **shared invite code + their email** and is admitted up to a **variable `FPL_USER_CAP`**; at the cap → a "beta full — waitlist" note (`FPL_SIGNUP_URL`). A second small `beta_users(email, created_at)` table in the **existing Supabase** (endpoint derived from `FPL_STORE_URL`, reusing the anon key — no new secret) via `web_streamlit/user_store.py`. **Soft, not hard:** the email is self-declared (no password/verification — Proton has no free SMTP); the code gates *who can* register, the cap bounds *how many*, the email is *who*. **Off by default** (unset cap → today's gate, invariance-pinned); the registration write is the **second** opt-in, secret-gated server write (after the ADR-094 squad save) — the read-only invariant now names two exceptions. **Softens** DIRECTION §1 without the accounts/paid pivot; native `st.login()` = the deferred hard-auth path. Extends ADR-087/094 | Accepted |
| [099](./ADR-099-remember-me-cookie.md) | **A persistent "remember me" cookie for the beta gate** — kill the friction where **every browser refresh re-prompts** for the code + email (`st.session_state` is wiped on a full refresh/new tab). A first-party **`fpl_beta` cookie** persists a *passed gate* (~**30 days**; **~7 on iOS** — Safari ITP caps JS cookies), storing **what proves the pass**: the tester's **email** (registration mode) or the **code** (shared-code mode). **Grants no new access** — **re-validated on load** (`user_store.is_registered(email)` / value `== FPL_ACCESS_CODE`), so a **pruned tester / rotated code** invalidates the cookie. Streamlit 1.61.1 **reads** cookies (`st.context.cookies`) but can't **write** (a DIY `document.cookie` is iframe-sandboxed) → one small **cookie component**, quarantined behind a guarded seam `web_streamlit/remember.py` (`read`/`write`/`clear`, lazy-import + `try/except` → **no-op when absent**). **Off by default / fail safe** — unavailable/blocked/expired → today's per-session gate, **839 byte-identical** (invariance-pinned). Not identity: native `st.login()` = the deferred hard-auth path; `remember.clear()` ships the plumbing for a later "not you? / log out". Layers on ADR-087/098 | Accepted |
| [100](./ADR-100-beta-usage-analytics.md) | **Beta usage & experience analytics — an opt-in, anonymous, fail-silent write path** — answer the owner's *"what do testers use, do they return, does it feel fast/reliable?"* **without a separate analytics project**. A `web_streamlit/analytics.py` client `track()`s small **usage** (`session_started`·`page_viewed`·`squad_*`·`analysis_run`·`feedback_*`·`error`) + **`perf`** events (duration + ok) to a Supabase **`events`** table (endpoint derived from `FPL_STORE_URL`, reusing the key — **no new secret**). **The #1 rule — can never affect the app:** every `track` is **fire-and-forget** (a daemon thread + tight timeout) and **swallows everything**; disabled → an immediate no-op. **Off by default** behind a dedicated **`FPL_ANALYTICS`** flag *and* the store being configured — the **3rd** opt-in, secret-gated server write (after ADR-094/098), a guardrail test pins "no secrets → zero writes, byte-identical". **Anonymous + minimal:** a per-session `uuid4` + a persistent **`fpl_anon`** UUID cookie (returning users, via the ADR-099 component) — **no PII/email/IP, not the handle, no full squad, no click/mouse/screen tracking, no 3rd-party service**. Observes *the app's usage*, never the FPL decisions. Admin = SQL in `docs/ANALYTICS.md` now, a minimal gated view next. 2-sprint split (136 foundation · 137 coverage+admin). Extends ADR-094/098 | Accepted |
| [101](./ADR-101-calibration-methodology.md) | **Calibration methodology — how we tune the dormant weights on real returns** — before flipping on the wired-dormant `FORM_WEIGHT`/`SET_PIECE_WEIGHT`/`DEFCON_MAGNIFIER_WEIGHT` (ADR-060/096/097), record *how a weight value is chosen*. A **walk-forward backtest** (predict GW N from data ≤ N-1, never in-sample) that maximises **Spearman rank correlation** between predicted xP and actual `total_points` (it's a **ranking** tool — MAE + a top-N hit-rate are secondary sanity checks); **one weight at a time** (interpretable); **overfitting guards** (need ≥K GWs, prefer the smaller weight on a flat curve, report the whole curve). Calibration **recommends**, the **owner commits** the value — the repo stays **dormant (0)** until then (the one-xP invariant, ADR-041, intact). A hobby-sample **decision aid, not a probability/significance claim** (ADR-037). Built as prep: `analytics/backtest.py` (synthetic-tested) + a `calibrate` CLI + `docs/GW1_RUNBOOK.md`; real calibration is GW4–6+ (no data preseason). Governs ADR-060/096/097 | Accepted |
| [102](./ADR-102-beta-waitlist.md) | **A beta waitlist — capturing failed-registration emails** — when a registration *fails* (the cap is **full** or the invite **code is wrong**), record the email so the owner can invite later (owner's steer: **one** waitlist for **both**, not two mechanisms). A `beta_waitlist(email, reason ∈ {full,bad_code}, created_at)` table in the **existing Supabase** (endpoint derived from `FPL_STORE_URL`, reusing the key — **no new secret**, like `beta_users`) via `web_streamlit/waitlist.py::add(email, reason)`; wired into the two `access._registration_gate` failure branches. **Best-effort + never blocks** the gate (wrapped, like the analytics write); **off by default** (registration mode + store), the **4th** opt-in secret-gated server write (read-only invariant names four exceptions). **Privacy — recorded honestly:** it holds emails of the **non-admitted**, incl. **wrong-code** attempts (possible typos/randoms) — minimal PII, owner-only, "remove me = delete the row"; the owner opted into the wrong-code capture knowingly. Not accounts/auth; manual invites. Extends ADR-098 | Accepted |
| [103](./ADR-103-madboots-brand-identity.md) | **The MADBOOTS brand identity** — rename the product's **user-facing** surfaces **FPL Assistant → MADBOOTS** (one word — the two-tone MAD-purple/BOOTS-orange colour split carries the word-break; `madboots` = the domain/handle/technical id), tagline **"Fantasy Football, Calculated."**. A **mark system**: the approved **transparent badge** (`madboots.png`) = the **favicon** + small mark (Home header · player-card band); a **CSS wordmark** = headers; the boots **illustration** = a later big-surface hero (deferred). **Approach B — accents, not a reskin:** folded into the current **light/theme-aware** app (a dark reskin, approach A, is deferred). **Keep the internal identity** — the `fpl-assistant` package/repo + the **14 `FPL_*` secrets** unchanged (invisible; renaming `FPL_*` breaks every deploy secret); only the borderline `USER_AGENT`/CLI `prog` flip to `madboots`. A **not-affiliated** disclaimer (named product on official FPL data). Honest: an **AI-origin** mark, a **raster PNG** (fine at ~330px/32px; a true-SVG re-export + designer redraw are deferred). One `web_streamlit/brand.py` source of truth. Display/asset-only — **no** engine/xP/secret change; owner-approved via a real-badge Artifact preview. The repo-transfer + `madboots.com` changeover is a **separate** bundled backlog item. Extends ADR-050/084 | Accepted |
| [104](./ADR-104-cold-start-xp-floor.md) | **A cold-start xP floor from FPL's `ep_next`** — new/promoted players with **no FPL history** were projecting at **0** (preseason `points_per_game` 0 → rate 0 → xP 0; **69 available players** at GW1). Fix: in `player_xp`'s last rate tier, **`rate = max(points_per_game, ep_next)`** — floor with **FPL's own expected-points-next-GW** (already on the row; honest, not invented). **Targeted** to the no-history/current-ppg tier only — the ≥900-min **baseline** + shrunk **fallback** tiers (ADR-028/040) are **unchanged** (O'Shea/Haaland byte-identical). `ep_next` already prices minutes (not re-scaled by our weight); availability still gates (a flagged player stays 0); a new **`rate_source="ep_next"`** keeps grounding honest. **Reframe recorded:** we do **not** chase FFH — for a player *with* history (O'Shea ≈1.6) our number is *above* FPL's own `ep_next` (1.0), so FFH's 4.7 is the outlier; the genuine defect was only the 0s. Deliberate change to the one-xP metric (ADR-041) for that cohort → the invariance tests update. Extends ADR-028/040 | Accepted |
| [105](./ADR-105-squad-ia-split.md) | **Split the Squads tab — Squad Lab + My Squad** (revises ADR-069) — the single *Squads* page grew to a **7-way "Tool" switch** (`Build·My Squad·AI Tips·Chips·Health·Transfer·Captain`, defaulting to Build) — busy, and *Build* (a **create** tool) sits first among six **manage-your-team** tools. Split into **two top-level tabs**: **Squad Lab** (the old *Build*, renamed — create/optimise a fresh 15; mascot-themed page header; "Use this squad →" hands off) + **My Squad** (the pitch/edit **plus the five tools as sub-tabs** in workflow order — `My Squad·AI Tips·Captain·Transfer·Chips·Health`; the squad picker + horizon live here). A no-squad → **Squad Lab** pointer keeps new users guided. **Naming:** functional nav labels + brand on the *page* (mascot), not the label ("Draft" rejected — FPL Draft clash; full MADBOOTS vocab deferred to branding-E). **Reuses the `views/squads.py` renderers unchanged** — IA only, no engine/analytics change. Cost: split `3_Squads.py` into two pages + a nav renumber + re-point ~20 tests (the shared harness). Extends ADR-055 | Accepted |
| [106](./ADR-106-google-auth-and-per-user-persistence.md) | **Google auth (`st.login`) + per-user squad persistence** — the hard-identity upgrade deferred by ADR-087/098/099, to fix the tester *save/persist* cluster (C2 iPhone session-wipe · C3 cross-device · C4 clunky code login). Google **`st.login()`** (OIDC) becomes the **primary gate when `[auth]` is configured**, **allow-listed by `beta_users`** (ADR-098; a logged-in email *in* it → admitted, else → the **waitlist** ADR-102); the auth cookie survives reconnects. **Per-user auto-persistence:** the squad is linked to a **hash of the email** and **auto-saved on edit** (extends the S145 auto-sync) + **auto-restored on load** (logged-in + no active squad → fetch from `cloud_store`) — so it follows you across devices/reconnects (the mobile-wipe fix). **Off by default** — no `[auth]` → today's shared-code/registration/open gate + manual handle save/load, **byte-identical** (local/CI/open unchanged; a guardrail pins it). **Cost: none** (`st.login`/Google OAuth/Supabase free). **Privacy:** email-only + basic scopes, squad keyed by a hash, remove-me = delete the rows. The auth cookie subsumes the remember-me cookie (ADR-099) in auth mode; the shared code/cap become the no-auth fallback. Extends ADR-094/054 | Accepted |
| [107](./ADR-107-madboots-vocabulary.md) | **MADBOOTS vocabulary — a brand lexicon for the tool labels** (extends ADR-103; the follow-on ADR-105 deferred) — turn the last generic analytics labels into a small brand lexicon, governed by the owner's *clean, modern, **not gimmicky*** principle (brand as a light signature, never glued onto every label; a rename must be **at least as clear** as the word it replaces). **Adopt now:** **Radar** — `🎯 Target by fixtures` → **`🎯 Radar`** (players to watch); **Edge** — the `explain` **"Why"** heading → **"Edge"** (the advantage), giving a coherent **Edge · Risk** pairing. **Keep:** **Risk** (already on-brand — but reconcile the `Risks`/`Risk` inconsistency between the captain card + build explanation to the singular) and **Captain** (already `Captain Pick`). **Defer "Pick"** — its home would be the *"AI Tips"* sub-tab, but that renders a **whole-week plan**, so "Pick" would mis-size it (owner's call); AI Tips stays. **Display-only** — no analytics/`decision_xp`/code-identifier change (`render_ai_tips`, `target_by_fixtures`, the `target_*` session keys, the `elif view==…` branch strings all kept); no nav-label churn (ADR-105 keeps top-level labels functional). Reference ADR for future brand-word decisions | Accepted |
| [108](./ADR-108-player-actions-panel.md) | **The My Squad player-actions panel** (extends ADR-055/084/105) — My Squad is the golden page, but picking a player is scattered across **three** places and captaining one means leaving the pitch: the card picker `👤 View your player's card`, the 🔁 Substitute expander (re-pick), and Set-captain over in the **Captain sub-tab** (re-pick again). Tester (2026-08-12 w2): FFH pops a **menu on clicking a player** (full card · substitute · make captain) — the real want = *open the full card*, not the truncated hover. ⚠ **Platform wall** — the pitch is one static `st.markdown` block (no callback), so no literal click; and the rich card is reachable on the pitch only via the **desktop-only CSS hover** (a mobile gap). **Decision:** one **inline "player-actions" panel** on `render_my_squad` — a single **"Select a player"** → the **full card** + **👑 Make captain** (one click, `set_captain`; moved onto the pitch) + **🔁 Substitute** (selected = bring-off; reveal the legal bring-on picker + confirm; a benched pick flips it). **Absorbs** the card picker + the Substitute expander; **retires** `_sub_prefill_for`. **Transfer stays separate** (heavier flow, not in the menu); the **Captain sub-tab keeps its recommendation**. Native `selectbox`+`button` → **taps on phone/tablet**, closing the hover gap. **Reuses** `substitute()`/`set_captain()`/the card renderer — no engine/analytics change. **Deferred (committed next, not vague): "My Squad v2: tap-the-pitch"** — a custom **JS component** so a shirt-tap returns the id → opens this same panel (~90% reused); its **own spike + ADR** (full React vs a lightweight click-detector reusing the pitch HTML + a Cloud-deploy check), **post-GW1**, feedback-driven | Accepted |
| [109](./ADR-109-per-gameweek-xp-card.md) | **Per-gameweek xP in the player card** (extends ADR-084/032) — tester (2026-08-12 w2, with an image): under a shirt, show the **per-GW points** as columns (up to 3 GWs) — **xP on top** + **fixture below** (Mbeumo `5.1` HUL(A) · `6.2` IPS(H) · `4.5` EVE(A)). **No Total column** (owner dropped it after previewing — cleaner; the shirt chip already shows the horizon total). **Data is free** — `decision_xp` already returns `by_gameweek` (`{event: xP}`); `team_schedule` gives per-fixture `{event, opponent, venue, difficulty}`; align xP↔fixture by **event number**. **Decision:** turn `card_body`'s fixture row into a **per-GW row** (xP over an FDR-tinted `OPP (H/A)`, up to 3 GWs, no Total) — shows in **both** the hover popover **and** the ⚙ panel card (ADR-108), one change. Backward-compatible (no `xp` on fixtures → today's pills + Proj chip). **Not on the kit chip** (cramped). Build: US-367 (`card_body` row + panel card) · US-368 (thread `fixtures_by_id` into `_kit_html`). Display-only, no xP math. DGW/BGW = graceful (align by event; GW1-era polish) | Accepted |
| [110](./ADR-110-player-card-compare.md) | **Compare two players on the card** (extends ADR-084) — tester (2026-08-12 w2, UX H, with an image): a **side-by-side comparison** to decide between two players. **Decision:** a **same-position, winner-highlighted** compare on the Players Card view — pick A, then a **🔍 "Compare with"** typeable picker (Streamlit selectboxes filter as you type; scoped **same-position** so the stat rows align) → a merged **A · stat · B** grid with the **better value tinted**. Direction: higher-better default; **xGC lower-better**; **ownership neutral**; ties/missing → no tint (`—`). Reuses the card's stat catalog + `decision_xp` (xP for all players already computed) — **display-only, no new xP math** (the per-stat winner is a diff of existing values; `ask`'s compare has no per-stat winner to reuse). Build: US-369 (`_stat_catalog` exposes `(key,raw)` + `compare_rows`/`compare_card_html`/`render_player_compare`) · US-370 (wire the Card view; preview for sign-off). Single-card path unchanged. **Not** cross-position; the ⚙ panel "compare with…" (owned 15) is a follow-on | Accepted |
| [111](./ADR-111-help-revamp.md) | **Help revamp + Boot Battle everywhere + the MadBoots Explainer** (revises ADR-068; extends ADR-110) — the owner drafted a full Help-page rewrite; the current Help carries **stale claims** (esp. *"no accounts / nothing saved server-side"* — wrong since auth went live, ADR-106). **Decision:** (1) **Boot Battle everywhere** — rebrand the compare control **⚔️ Boot Battle** and add it to the My Squad **⚙ panel** (a typeable picker scoped to same-position **owned** players → `render_player_compare` in place of the single card; 👑/🔁 still act on the primary), so the copy is real; reuses `compare_card_html` + `fixtures_by_id`/`xp_by_id` — no analytics change. (2) **Help rewrite** — the owner's copy (intro · Quick start · Your squad · deadline · 8 expanders), **reconciled against the live app** (Save/Import → auth-live: account save · auto-sync · Download · Upload · Manager-ID from GW1; the stale no-accounts line removed); **keep the app's icons** (override draft conflicts). (3) **MadBoots Explainer** — a glossary in **one expander with category subheaders** (all visible → **Ctrl-F**-able; Streamlit expanders can't nest). Static content + one display-only feature add. Build: US-377/378/379 + a content preview for sign-off. Not this ADR: cloud-LLM narration (P2), admin graphs (P2) | Accepted |
| [112](./ADR-112-ask-maddie-video-hub.md) | **Maddie Explains — a scripted FAQ-by-video hub** — a grounded, dashboard-managed video hub (not a live chatbot): `web_streamlit/maddie.py` reads a Supabase `maddie_videos` table (endpoint from `FPL_STORE_URL`, no new secret, best-effort + built-in fallback), one `st.video` per published clip (unlisted on YouTube); read-only → RLS stays on with a public-read policy | Accepted |
| [113](./ADR-113-unified-your-team-persistence.md) | **One account-backed team (unified "Your Team" persistence)** — fixes a refresh-revert bug (two cloud stores fought over one session squad — account vs handle); signed-in = the account IS the store (`render_cloud_sync` early-returns), so a loaded team survives a refresh; a single ⚙ Your team panel (sync · get · backup) | Accepted |
| [114](./ADR-114-brand-token-foundation.md) | **Brand-token foundation** — `brand.py` becomes a design-token source of truth: semantic GOOD/WARN/BAD (solid/tint/fg) trios, `FDR_STYLE`, neutrals + spacing/radius scales, a canonical `MANTRA`; `[theme] primaryColor` = brand purple; AA-contrast chips source the tokens. Partial-on-purpose (the full retro-fit is incremental) | Accepted |
| [115](./ADR-115-my-squad-density-redesign.md) | **My Squad density redesign** — the golden page decluttered via progressive disclosure: a 3-number strip (was a 5-across metric wall) + one availability/price line; a ⚙ Players & lineup panel + a flat ⚙ Manage expander; in-page transfer → the Transfer tab; ~half the function, no feature/engine change | Accepted |
| [116](./ADR-116-scrollable-tables-honest-sort.md) | **Scrollable tables replace pagination (honest column-sort)** — the browse tables paginated before sorting, so a native header-sort only sorted the visible page (misleading); now one scrollable, fully-sorted `st.dataframe` per table (header-sort orders the whole set); retires `paginate()`; supersedes ADR-063 | Accepted |
| [117](./ADR-117-watchlist.md) | **⭐ Watchlist** — a per-user shortlist of players to watch: `web_streamlit/watchlist.py` (session + best-effort per-user Supabase sync keyed by `auth.user_key`, no new secret, off by default, capped 30); ⭐ add on Players (card toggle + pool multi-select) + view/act on My Squad ▸ Transfer | Accepted |
| [118](./ADR-118-player-dna-page.md) | **Player DNA — a rich single-player analysis page** — the player card evolved into AI Verdict → 8-axis percentile-within-position radar → AI Insights → performance trend; `analytics/player_dna.py` + `explain.py` verdict + SVG cards + `player_dna_view.py`; on Players ▸ Card + reused on My Squad ▸ Players & lineup (owned-aware Hold/Sell); all on data held today, no `decision_xp` change; trend/sparklines auto-populate from GW1; shot map deferred (soccerdata) | Accepted |
| [119](./ADR-119-team-dna.md) | **Team DNA — a team-level analysis companion** — `analytics/team_dna.py` `team_dna_all` = an 8-axis percentile-across-the-league club fingerprint + grade + grounded insights, reusing the Player-DNA engine; two doorways (Fixtures ▸ 🧬 Team DNA browse card · My Squad ▸ Health "Your teams" strip); labelled proxies, preseason-safe; no new tab / no `decision_xp`/FDR change | Accepted |
| [120](./ADR-120-admin-tester-activity-and-load.md) | **Admin — tester-activity + load/concurrency views** — owner-only reads: which testers actually test (join `beta_users` → `user_key` hash → account-store `updated_at`) + a load/health proxy (active-in-last-10-min · peak · P95 · 🟢/🟡/🔴); analytics stay anonymous; no new table/secret/`decision_xp` change | Accepted (build post-GW1) |
| [121](./ADR-121-per-gameweek-xp-toggle.md) | **My Squad — a per-gameweek xP toggle** — offered when the horizon spans >1 GW: `GW1→N (cumulative)` or `GW N only` (from `by_gameweek`), switching the **displayed** xP on the 3 metrics + pitch chips; the XI/captain *selection* stays cumulative. Display-only, no `decision_xp` change | Accepted |
| [122](./ADR-122-self-service-unsubscribe.md) | **Self-service "Remove me" / unsubscribe** — makes the *"remove me = we delete your rows"* promise (ADR-106/102/098) self-service: `web_streamlit/unsubscribe.py` (requests-only, best-effort, fail-silent, **no new secret**) `DELETE`s a person's rows — `beta_waitlist`+`beta_users` by email, `squads`+`player_watchlist` by the hashed key — behind an in-app **"Leave the beta"** confirm (both gate modes) + a **"Remove me from the waitlist"** on the not-invited screen. **In-app only** (the waitlist isn't emailed → no token link, deferred). Needs a `beta_users` **delete** policy (BETA.md §4); the other tables are RLS-off | Accepted |
| [123](./ADR-123-upcoming-fixtures-by-deadline.md) | **"Upcoming" means the gameweeks you can still act on** — a live GW1 bug: fixtures were cut by FPL's `finished` flag, which stays false for ~48h *after* a deadline, so mid-gameweek every surface offered advice on a team already locked. `get_upcoming_fixtures` now cuts by **deadline**, and `DEADLINE_LEAD` is defined once so the rule cannot drift. Uses only stored data; no new source | Accepted |
| [124](./ADR-124-cold-start-rate-shrinks-by-evidence.md) | **The cold-start xP rate shrinks toward `ep_next` by evidence** — real data activated a dormant branch and it was worst exactly where hype lives (new signings, promoted clubs). The rate now shrinks by **how much evidence exists**, not by how big the number is, reusing the shape ADR-040 already used for the neighbouring tier. Verified invariant on live GW1 data | Accepted |
| [125](./ADR-125-in-season-minutes-share.md) | **In-season minutes in the xMins share — DEFERRED to ~GW4-6.** Investigated and measured, deliberately not built: ADR-124 already absorbs most of the harm (all 93 no-history players sit on the forced-1.0 tier, `c ≈ 0.08` after one GW). ⚠️ Records the trap — **FPL writes a per-GW row when a fixture is SCHEDULED, not played**, so a naive `minutes ÷ 90×GWs` scores two whole clubs at zero. Trigger: the `FORM_WEIGHT` sitting | Accepted (defer) |
| [126](./ADR-126-last-season-fallback-stat-boards.md) | **The season-to-date boards fall back to last season until they can answer** — at rollover the browse surfaces went blank exactly when a new user decides whether to trust the tool. They now show last season **and name the season they are showing**, rather than lowering the ≥900-minute gate, which would make them wrong instead of empty | Accepted |
| [127](./ADR-127-percentile-ties.md) | **Percentiles must not rank a tie at the top** — user-reported, on the differentiator feature. The same flaw was written twice (`player_dna._percentile` and `team_dna._rank`); fixed as a **midrank** percentile, with the resulting team-grade drop accepted rather than re-tuned away | Accepted |
| [128](./ADR-128-per-gameweek-columns.md) | **Store the gameweek, not just the season total** — `player_history` widened 8 → 27 columns and re-backfilled. The expensive part is the walk (one `element-summary` call per player), not the columns, so a generous set came in one pass. The enabling change for the DNA sparklines and W-D-L dots | Accepted |
| [129](./ADR-129-per-gameweek-key-is-the-fixture.md) | **A per-gameweek row is keyed by the fixture, not the gameweek** — found by a deliberate double/blank-gameweek audit *before* the season's first double. The old primary key silently discarded one half of a DGW: no error, no gap, just a smaller number nobody would report | Accepted |
| [130](./ADR-130-squad-risk-and-grade.md) | **A squad triage view and a squad-level grade** — pure `analytics/squad_risk.py` feeding two cards. "% chance he doesn't reach 60" is **empirical, never invented** (no probability model is added), and a player who cannot be assessed shows **"—", not 100% risk** | Accepted |
| [131](./ADR-131-forward-gameweek-planner.md) | **A forward planner that leads with what actually varies** — the rival's projected-points-vs-average framing is *noise* on our numbers (a squad's per-GW xP moves ±3%), so ours leads with **fixture exposure**, which swings 2→7. The prototype changed the feature | Accepted |
| [132](./ADR-132-transfer-timing.md) | **Transfer *timing* arithmetic, not a transfer-path search** — the path search is **declined on evidence**: the best sell was the same player in all six gameweeks and the market yielded *one* beneficial move, a tree with one branch. Shipped the rule-driven arithmetic (bank or spend, is it worth a hit) instead. Carries an explicit re-open trigger | Accepted (path search declined) |
| [133](./ADR-133-tap-the-pitch.md) | **Tap a shirt to select a player** — the gesture is an *additional* input, never a replacement: the dropdown stays permanently, so AppTest keeps its coverage and keyboard users keep a path. Cloud-verified. ⚠️ One claim in it was wrong and is corrected in place — an invisible fallback is one users never learn exists | Accepted |
| [134](./ADR-134-fixtures-ia-restructure.md) | **Split the IA by level — team things on one tab, player things on another.** `📅 Fixtures` → `🧬 Team DNA & FDR` (keeping "FDR", the term people scan a sidebar for), Team DNA first, and the 🎯 Radar moved to Players, where players live | Accepted |
| [135](./ADR-135-actions-on-the-entity.md) | ↩️ **Put actions on the entity, to take widgets off the page — BUILT, HIT ITS NUMBER EXACTLY (6-7 widgets → 3), AND REVERTED THE SAME DAY.** The most valuable record here: the latency is **architectural** — every tap is a full Streamlit rerun, so a floating menu *advertises* client-side responsiveness and cannot deliver it. **Widget count was a proxy for clutter and a bad one.** Tap-to-select survives; do not rebuild the menu | ↩️ Reverted |
| [136](./ADR-136-replace-the-dead-slot.md) | **Transfer advice must name the dead slot** — a pure `analytics/dead_slot.py` parsing a return date out of `news`, so advice can say *"this player cannot play for 3 gameweeks"* rather than ranking him on a projection he will never earn | Accepted |
| [137](./ADR-137-two-build-modes-named-truthfully.md) | **Squad Lab has two build modes, not three — and they were named backwards.** "Weaker bench" became **"cheap bench"**: the £19.0m bench is not worse by accident, it is deliberately cheaper so the money goes into the XI | Accepted |
| [138](./ADR-138-value-frontier.md) | **The value frontier — position everyone, don't rank fifteen.** Ours carries a tested **verdict** in the hover, not a coordinate. Building it exposed an xMins blind spot no ranked surface had surfaced, because a frontier *promotes* cheap-and-high numbers rather than burying them. Two rounds of owner review on a live preview changed the design | Accepted |
| [139](./ADR-139-tap-to-reveal.md) | **Hover exists only where tapping doesn't** — the popover is suppressed exactly where the tap works and kept everywhere it doesn't (the ADR-133 fallback, the Build preview). Revised the next day: the compact card is back, bound to the **selection** rather than the cursor | Accepted |
| [140](./ADR-140-price-arrows-use-the-colour-channel.md) | **Price arrows use the colour channel — green up, red down.** One glyph pair (`▲`/`▼`) defined once and imported by the CLI: plain glyphs *inherit* the surrounding colour, which is what makes the rest possible. One rule written twice always drifts | Accepted |
| [141](./ADR-141-league-import-and-elite-comparison.md) | **Import a league — and compare against it.** Two layers priced separately because they cost three orders of magnitude apart: the table always (1 call), the insight behind an explicit action (N calls). ⭐ **The standing rule: nothing that costs N network calls happens because someone navigated to a tab** | Accepted |
| [142](./ADR-142-signing-in-is-not-saving-a-squad.md) | **Signing in is not saving a squad** — `last_seen` is stamped once at admit, not per page view, so the roster can answer "which testers actually test?" without touching the anonymous ADR-100 event stream | Accepted |
| [143](./ADR-143-one-chip-per-gameweek.md) | **One chip per gameweek — the sequence scan, scoped to what the data supports.** The ranking is **declined on evidence** (worth 0.3 xP); the legality defect it would have hidden — **two chips advised for one gameweek, 28% of squads** — was the real find and is fixed in `chip_advisor`, so every caller inherits legal advice | Accepted (ranking declined) |
| [144](./ADR-144-captain-margin.md) | **The captain margin, calibrated against its own distribution** — every card states the gap, not only the narrow ones, and the thresholds are the **measured quartiles** (p25 0.3, p75 1.0), so "a clear pick" means something | Accepted |
| [145](./ADR-145-fixture-concentration.md) | **Fixture concentration — the honest version of "player clashes".** Clashes are rejected on evidence: they would have fired for **100% of squads every week**. What is actionable is how much of one gameweek's XI rides on a single match (median 29%, max 64%) — which *subsumes* clashes | Accepted (clashes rejected) |
| [146](./ADR-146-the-crowd-knows-what-the-feed-does-not.md) | **The crowd knows what the feed does not** — `crowd_exodus` fires only when a sell-off is heavy **and** our own `status`/`news` cannot account for it, so the handful nobody can explain are not buried under the many already covered | Accepted |
| [147](./ADR-147-cross-device-preferences.md) | **A remembered manager id — per-user preferences that follow you.** Storing the *manager* id rather than a league id restores the whole **list** of leagues, so the picker reopens where you left off | Accepted |
| [148](./ADR-148-a-promise-must-cover-every-row-we-create.md) | **A promise must cover every row we create** — `remove_me` now deletes `user_prefs` too. ⚠️ Corrects ADR-147's SQL, which shipped without a delete policy. Fail-silent is right at the edge, but **it must not mean unverifiable** | Accepted |
| [149](./ADR-149-the-filter-reaches-the-buzz-tab.md) | **The shared filter reaches Community Signals** — and filters *after* the scan, deliberately: **"6 of 47"** says something that "6" alone does not | Accepted |
| [150](./ADR-150-one-signals-page.md) | **One Signals page, ordered by what the source actually knows** — News + buzz merge into 📡 Signals, descending by evidentiary strength (official fact → media report → crowd behaviour). The ordering *is* the honesty mechanism. Establishes the **doing-vs-saying** axis ADR-170 later leans on | Accepted |
| [151](./ADR-151-headline-extraction.md) | **Read the headlines we already fetch — resolve, then extract.** The LLM proposes a `{player, kind}` candidate and the app keeps it only if the kind is in a closed set and the name resolves — the same shape as `verify_grounding`. **The model never decides** | Accepted |
| [152](./ADR-152-resolving-a-player-name.md) | **Resolving a player name in free text** — longest-match-first with span consumption, so "Cole Palmer" claims those characters and "James Maddison" stops being a Reece James sighting. **Ambiguity resolves to silence, not to a guess** | Accepted |
| [153](./ADR-153-a-reported-departure-is-a-dead-slot.md) | **A reported departure is a dead slot we know about first** — two independent signals must agree (a transfer headline *and* an unexplained heavy sell-off), because being wrong costs a real transfer. Reaches advice through ADR-136 rather than a new path | Accepted |
| [154](./ADR-154-only-react-inside-a-window.md) | **Bench a departing player — but only while a window is open.** A *local copy* of the xP map is zeroed for leavers and passed to `best_legal_xi` and nowhere else: `decision_xp` is untouched and every other surface still shows the real projection | Accepted |
| [155](./ADR-155-health-reads-the-same-fact.md) | **Health reads the same departure fact as everything else** — four copies of the same three-line loop existed in `ask`, the CLI, the Risk Monitor and Signals: three chances to disagree. Now one function | Accepted |
| [156](./ADR-156-a-dead-slot-takes-the-free-transfer.md) | **The transfer ranking values a leaver at zero — and a dead slot takes the free transfer.** Reuses ADR-154's local-copy idiom rather than reinventing it; a test asserts the caller's dict is never mutated | Accepted |
| [157](./ADR-157-the-extraction-model-is-its-own-choice.md) | **Extraction gets its own model, timeout and budget** — narration runs while a person waits and wants warmth; extraction runs unattended in `refresh` and wants the same answer every time. One constant had been deciding for both. ⚠️ Also records: **I pitched a sprint on an unverified remembered number** (16-vs-7 was really 13-vs-13) — re-run a remembered measurement before it justifies work | Accepted |
| [158](./ADR-158-the-tap-leaves-the-pitch.md) | **The tap leaves the pitch — league-scan rows select.** ADR-133's gesture unchanged, so the card downstream is reused entirely. Deliberately a tap that **selects**, never one that opens a menu — that is ADR-135, which stays reverted | Accepted |
| [159](./ADR-159-two-windows-say-which-way.md) | **Two rolling form windows — and the refusal to draw a direction we can't measure.** Both windows are `form_rate` called twice, never a second rate written beside it: identical weighting is what makes them comparable. Only ~GW4 populates them | Accepted |
| [160](./ADR-160-the-price-journey.md) | **The price journey — the change now, the line later.** An unchanged price yields **one point, not a flat pair**, which would imply two observations of the same thing. ⚠️ Shipped on a lukewarm sign-off: thin until prices actually move; revisit ~GW4 | Accepted |
| [161](./ADR-161-head-to-head.md) | **Head-to-head — ship the decomposition, gate the probability.** The players you both start cancel, so the output is the **differential set, priced**. ⏳ The win-probability sim is **gated on evidence**: one starter's points have sd 3.51 and a 3-differential gap sd ≈ 8.6 against typical margins of 2-5 — it would say *"it's close"* every week | Accepted (sim gated) |
| [162](./ADR-162-transfer-flow.md) | **Transfer flow — split the free half from the paid one.** `movers` rather than a transfer total, because a total hides one manager taking a −12 among thirty who did nothing | Accepted |
| [163](./ADR-163-shared-presentation-primitives.md) | **A stat strip and a banner that every page shares** — `st.columns` lays out server-side at a fixed ratio, so a 3-across row slivers on a phone and **no Python-side fix exists, because nothing on the Python side knows the viewport**. CSS does. One shared component, not per-page tweaks | Accepted |
| [164](./ADR-164-one-squad-lens.md) | **One squad lens per page, not one per section** — one control above everything it governs. It reaches headlines by a different mechanism, because a headline is a sentence, not a player row | Accepted |
| [165](./ADR-165-the-player-panel-is-a-fragment.md) | **The pitch and player panel rerun on their own** — `st.fragment` around everything that *reads* or *selects*; the two things that **mutate** (Make captain, Substitute) still rerun the whole app, because the strip above has to change with them. Owner-verified on an iPhone: *"feels a little quicker."* (Measured first: caching `decision_xp` was the obvious fix and was wrong — 7 ms of a 56 ms render) | Accepted |
| [166](./ADR-166-order-by-how-often-you-need-it.md) | **Order the app by how often you need it** — sidebar 12 → 9; Squad Lab and Leagues fold into My Squad tabs. Folding two pages together must not fold their horizons together (per-mode widget keys). ⚠️ Its 4.4 s latency claim was environment-bound and is corrected by ADR-171 — the *reasoning* stands, the input number did not | Accepted |
| [167](./ADR-167-worth-a-look.md) | **Five boards become one shortlist — "worth a look", not "worth points".** The counterweight is the point: two of its signals are unpriced, so it must not imply xP. ⭐ **When a page has three or more leaderboards, the next feature it needs is probably not a fourth — build a READER** | Accepted |
| [168](./ADR-168-retire-ask-and-the-promise-with-it.md) | **Retire Ask — and the promise that went with it.** *"Ask is not being used"*, and **there is no AI in it on Cloud** — no Ollama there, so the prose paragraph is the entire local/Cloud difference. Kept owner-gated in Admin. Also fixed the mantra that promised it, and 🔬 **found the test suite was calling a live Ollama** — hence `tests/conftest.py` | Accepted |
| [169](./ADR-169-split-fdr-from-team-dna.md) | **Split FDR from Team DNA, and order the sidebar by use** — two pages, each owning its own squad lens: the FDR half had been reading a checkbox declared on the DNA half, legal while they shared a script and a bug the moment they didn't | Accepted |
| [170](./ADR-170-worth-noticing.md) | **The signals that live between the boards** — three patterns each needing two boards at once, so **a player can top none of the four and still be the most interesting name on the page**. Every threshold already existed; it says what the crowd is **doing**, never why | Accepted |
| [171](./ADR-171-one-screen-for-the-week.md) | **One screen for the week — and the number that unblocked it.** My Squad merges into ① the answer · pitch + ⚙ panel · ② captaincy inline · ③ chips button; sub-tabs 7 → 5; three captain setters → one. ⭐ **A latency number is only a fact about the machine that produced it** — ADR-166's blocking 4.4 s is **123 ms on Cloud** and 27,300 ms only on a dev box. ⚠️ And 1,642 green unit tests hid a **49-second** landing that only the smoke test found | Accepted |
| [172](./ADR-172-a-shrink-needs-something-to-shrink-toward.md) | **A shrink needs something to shrink toward** — owner-reported: Sangaré projected **9.9 xP** off two games. ADR-124 shrinks a no-history player's `ppg` toward FPL's `ep_next`, but **FPL publishes `ep_next` == `points_per_game` for 513 of 626 players** (verified on the live API), so the blend cancels and returns `ppg` unchanged — **the protection is inert**. 8 of the top 20 by xP are affected and the **top 3 are all of them**, with Haaland 4th. Fix: when `ep_next` carries no information (equals `ppg`, `ppg > 0`) shrink toward the existing `_FALLBACK_PRIOR` instead — no new constant, both ADR-104 ends preserved, and it self-repairs when FPL improves. ⏳ The cold-start **xMins forced to 1.0** is a second stacked over-estimate, measured and deliberately gated separately. ✅ **Built** — top 20 inert players 8 → 0; Haaland back to #1; the phantom +6.4 transfer now worth 0. 🔧 **Amended same day**: the prior is a pp90 *rate* so it must carry the minutes weight (`ep_next` must not — ADR-104), and removing the unscaled prior exposed a **second bug that had been cancelling it** — `minutes_share` read an all-empty history as *never plays*, zeroing 7 players who have played this season | Accepted |
| [173](./ADR-173-minutes-you-have-actually-played.md) | **The minutes you have actually played** — owner rejected his own gameweek plan: *"there is no chance I would transfer Calafiori."* Right, but his proposed fix (a 3-5 GW horizon) was **wrong and measured as wrong** — the transfer gets *stronger* further out (+2.30 → +10.00), because a longer window multiplies a suppressed rate rather than correcting it. The real cause is **xMins 0.43 from last season's injury-hit minutes vs 0.94 actually played**. Fix: use the in-season share, but **only for a player who appeared in EVERY completed gameweek** — the guard is the decision, since a player rested once falls back to history and cannot be cratered. Also surfaces ADR-132's `bank_or_use` in the week's answer, where it was never wired. **186 players move; Calafiori→Truffert +2.30 → −0.10 (gone); ⭐ the Kinsky bug, open since 2026-08-18, finally closes (0.50 → 2.60)**. ✅ **Built** — the answer now carries a *Longer view* line and, where banking wins, an *Or bank it* line | Accepted |
| [174](./ADR-174-act-on-the-answer.md) | **Act on the answer, do not move the tab** — owner: *"we cannot transfer on that page… are we in danger of making our golden page too long?"* **Both halves right, pointing opposite ways.** ADR-171 put the recommendation on My Squad and left the reader crossing to a tab to do what they had just been told. But the page measures **41 blocks** against the **14** ADR-115 called *"a wall"*. The reframe: the move is already named, so this is **one button, not a tab** — the Transfer tab is ~10 widgets and folding it in re-creates the redundancy ADR-115 removed. `AskResult` gains a `plan` (mirroring `squad`, ADR-062) so the button applies **the object the text was rendered from** and cannot name different players. ADR-135's line, applied to a recommendation: the page acts on the move named, the tab finds alternatives | Accepted |
| [175](./ADR-175-value-above-the-fold.md) | **Value above the fold** — owner: *"our golden page has a lot of real estate used up before we get to the value"*. **Ten blocks precede the first useful thing.** Four changes in one pass: cut the stale caption (still says *health*, renamed six days ago) and the picker that duplicates the banner; horizon **1/2/3/4/5/10 → GW1 | GW1-3** on the pitch (US-374 already defaults these tools to 1 and the Lab to 5); backup/import to the sidebar once you have a squad; and **This week · Captain · Transfer · Chips become one selector** under the pitch, so the pitch stays visible while you switch. ⚠️ **Reverses ADR-171 §3** (answer-first — whose measurement was about *cost*, not order) and **supersedes ADR-174** one day on (its density objection dissolves behind a selector). Owner steer recorded: on the golden page, arriving at the right layout beats holding a settled one. ✅ **Built** — top nav 5→4, horizon 1/2/3/4/5/10→GW1·GW1–3, no picker when you have one squad, backup/import to the sidebar once you have a team | Accepted |
| [176](./ADR-176-one-navigation-primitive.md) | **One navigation primitive** — owner: *"can we add this structure and layout to all our pages?"* Surveying them, **four already have ADR-175's structure and are wearing Streamlit's default clothes** (Players · Trending · Team DNA · FDR), so they get `brand.nav_css(key)` — the purple full-width selector defined **once**, joining the stat strip and banner as a shared primitive (ADR-163) — plus a chrome pass each. Stays **scoped CSS on a keyed container, never a theme**: ADR-114 measured that any `[theme]` block pins light mode and removes the viewer's toggle. ⚠️ **📡 Signals is deliberately EXCLUDED** — ADR-150 orders it by *evidentiary strength* and says that ordering **is** the answer to the risk; behind a selector you could open *crowd chatter* without seeing it sits below *official news*. **The stacking carries the honesty.** My Squad untouched, so tester feedback stays about the page it is about. ✅ **Built** — 5 pages call one helper, each with its own key; 4 guards, including one asserting Signals is **not** behind a selector and still reads in order | Accepted |
| [177](./ADR-177-project-the-team-they-will-field.md) | **Project the team they will field** — owner: *"you are showing MICKA at 59.9 and TS at 70, he is above me in the league?"* Two faults stacked. The card is a **next-gameweek forecast sitting under a table of banked points** and never says so. And the real one: `_starters` reads FPL's `multiplier` at face value, so **a spent Bench Boost is projected into next week** — TS priced on **15** players, Micka on **11**. Diagnosed from the screenshot's arithmetic alone (their five differentials sum to exactly their total minus the shared xP, so that side is complete at 11; 14 − 5 = 9 puts ours at 15) and reproduced: two *identical* squads, one bench-boosted, project **+16.0**. ⚠️ **Amends ADR-161 §3**, which defended face-value multipliers — correct for reconstructing what a squad **scored**, wrong for projecting the week **still to come**. Fix: derive the XI from `position` + `is_captain` (**a no-op for an unchipped squad**, so it is safe unconditionally); name the chip; **Free Hit reverts, so read GW−1's picks**; put the season gap beside the projection. One existing test pins the defect and is rewritten — *a test that fails when you fix the bug defends the error* ✅ **Built** — verified on a **live bench-boosted payload**: positions survive the chip, the multiplier does not, and the projection drops **67.6 → 58.9 xP** (four bench players). Sweep of `effective_ownership` / `captain_split` came back **clean** — recorded as a negative result ✅ **OWNER-VERIFIED same day**: *"gap is now +1.4"* — 70.0 → 61.3 against Micka's unchanged 59.9, a **−8.7** correction (four bench players). The expectation was **pre-registered in the ADR before the code was written** and held; ⚠️ the decimal match to the live analogue is **luck, not precision** — the band was the prediction | Accepted |
| [178](./ADR-178-plan-in-the-lab-play-on-the-pitch.md) | **Plan in the Lab, play on the pitch** — owner UX feedback: *"the players on the pitch have a lot of emojis… maybe reduce to corners, pens, FKs"* and *"should [multi-week planning] be done in the Lab? Then the Lab becomes more useful."* Measured first: **every player carries ≥1 flag** (the ownership tier always fires) and the 6-flag players are Palmer · Szoboszlai · Groß · Gibbs-White — **the clutter concentrates on the players a good squad owns**; set-pieces-only leaves **94% of shirts clean**. The market flags are a **third copy** of things already on the page by name (the price line, the Flagged line, Trending/Signals); set pieces survive on merit — a fact about **role**, not market. Three changes: pitch flags → set pieces (all of them stay in the Lab) · the Lab's "Squad name" becomes a **picker** (Current squad · New squad), holding ADR-132's declined path-search line — **it ships the read, not the search** · **per-gameweek xP instead of one total**, because *a cumulative number hides a blank* (5·5·5 and 15·0·0 read identically), breakout capped at 3-5 GWs. ⏳ **GATED — dropping GW1-3 from My Squad**, at the owner's request. 589 legal fifteens priced at both horizons: the XI differs in **63.7%** — so not decorative — but fielding the GW1-3 XI costs **0.32 xP mean / 0.60 p90**, an order of magnitude inside the model's own noise (ADR-161's sd 3.51) — so not load-bearing either. **Both obvious arguments fail; it is a judgement about page cost, now made on numbers.** ⚠️ Also a correction: a 32% captain difference was nearly written up as a fault — `captain.py` hard-codes `horizon=1`, so nothing the app advises was affected. **A measurement is not a finding until you check which code path consumes it** ✅ **Built** (3 of 4) — revised at preview by the owner (*"just the emoji and a key, for both pages?"*), and **surveying it made the change smaller**: the Lab **already** carried every flag in a table with words, so the line was never My Squad vs Lab but **pitch vs table** — which deleted the per-page flag mode from the design. 🔬 **Removing the market flags from every shirt broke NO existing test**; and the key was added to `render_pitch`, which **My Squad does not call** (it draws through the tappable component) — the one pitch with no key would have been the only one that mattered, found by mutation-testing. ⚠️ **Three guards written for this ADR passed while protecting nothing**: a word blacklist (`">pens<"` missed `"⚽ pens"`), a source-scanning test (`if False:` leaves every string in the file), and one that **skipped** (no injected squad → empty state → returned before asserting). ⏳ **GW1-3 still gated** — owner leaning toward the Lab, and *there is nothing to move*: the Lab has offered 1-10 all along | Accepted |
| [179](./ADR-179-one-week-on-the-pitch-every-week-in-the-lab.md) | **One week on the pitch, every week in the Lab** — owner: *"let's remove GW1-3 from My Squad… Bruno Fernandes was Vice Captain on My Squad but not shown in Lab… Lab should show all Emojis, not just Set Pieces… Lab should show all GW scores rather than totaling, maybe limit 3."* 🔴 **The vice-captain report is a CRASH, not a missing badge**: `render_pitch` has **no `vice_captain_id` parameter at all**, so every surface using it loses the badge — and ADR-133's degrade path forwards `**kw` into it, meaning **My Squad raises instead of degrading** on any deploy without the click-detector component. *A reported symptom is a place to start looking, not the size of the problem* — third time in two days. **Closes ADR-178's gate** (GW1-3 off My Squad — **two** controls, since the Cumulative/GW-only switch only renders above horizon 1) and **reverses ADR-175's own compromise four days on**. 🧭 **Correction: ADR-178 over-generalised.** It concluded *"the pitch is a team sheet, the table is a reference"* and applied it to **both** pitches — but the justification was about **page purpose** (a phone, minutes before a deadline), and none of that is true of the Lab. Restated where it holds: **both pitches render glyphs with a key; which glyphs depends on what the page is for.** The Lab's key groups ~12 symbols and writes ownership as an **ordered scale** (💎→⭐→🟦→👑), which answers ADR-178's own objection that four tiers were *"four unrelated pictures"*. Per-GW: **3 on the shirt, 5 in the table** — each cap from that surface's real limit, not one number for both ✅ **Built** — defect fix shipped **alone** (`e32a56b`) ahead of the three changes. ⚠️ **Four tests pinned the removed control**; each rewritten to assert the requirement, and one had its expectation computed at `horizon=3` so it would have compared the page against **a different window's sum**. The best is now inverted into evidence: it used to prove *"the card is independent of the horizon"* and now proves *"the card shows 3 GWs at a one-gameweek page horizon"* — **the reason removing the control was safe**. 🧭 A caption pointed at a control that no longer exists, and was deliberately **not** put through `RETIRED`: *"Gameweeks ahead"* is still the Lab's live label, so a global entry would fail where the phrase is right — **the mechanism has to match the scope of the change**. 🔬 And a **fifth** fixture that could not express the thing under test (`selected_by_percent` vs `selected_by` → no ownership tier at all). Nine mutations, all caught, clean suite re-run against a recorded baseline after every restore | Accepted |
| [180](./ADR-180-the-accent-belongs-to-the-theme.md) | **The accent belongs to the theme** — owner, on the deployed app **in dark mode**: *"lots of inconsistency with colours vs the style guide, should be purple"* + *"lot of real estate used in Lab when new build."* ⭐ **RE-MEASURED ADR-114 AND ITS BLOCKER IS GONE.** That ADR removed the `[theme]` block because *"any `[theme]` pins the theme — `base` defaults to light"*, killing the viewer's Light/Dark/System toggle. **Streamlit 1.61 has per-mode `[theme.light]` / `[theme.dark]` sections that did not exist when it was written**: `theme.base` stays `None`, and on the **wire format** — the check that actually decides it — the top-level `CustomThemeConfig` (the one carrying `base`) has **no fields set at all** while each mode gets its own `primary_color`. So two config lines turn every control purple in **both** themes, including widgets nobody has written yet, and dark mode gets `PURPLE_LT` which `brand.py` already keeps for dark grounds. ⚠️ **The rendered pixel is NOT verified here** (no browser in this environment) — the owner's reboot is the check, and the one-line fallback is recorded. **Narrows ADR-176**: `nav_css` keeps its full-width-segments layout and hands the colour back — keeping both would *visibly clash in dark mode*, since the CSS hard-codes `#8B2FC9` against the theme's `#B45CF0`. Also: the **Tool nav** finally joins the primitive (the one selector ADR-176 never wrapped), and the Lab's **eight inert controls** fold into ⚙ Constraints — *the line is not "advanced-looking", it is checkable: every one defaults to no constraint, so on the default path they provably did not affect the squad*. ⭐ **A CONSTRAINT RECORDED IN AN ADR IS A FACT ABOUT A VERSION, NOT A LAW** (ADR-157's rule earning its keep — I was one step from a fourth batch of scoped CSS on a 17-day-old finding) ✅ **Built** — two config lines replace five stylesheets. 🔬 Removing the colour rule **found something depending on it**: the *Apply this transfer* button lost its fill, now `type="primary"` so Streamlit paints it from the theme — *a hand-painted "primary" button was describing itself as primary in CSS while telling Streamlit it was ordinary*. ⚠️ **And a guard I wrote contained a NO-OP assertion** — `assert labels or True`, a hedge around AppTest's expander support, replaced with an **AST check** that the eight widgets sit inside the expander's body (a mutation lifting one back out proves a string grep would have passed). ⭐ **A hedge is not a weaker assertion; it is the absence of one** — fourth of the last five sprints to surface a guard that protected nothing. ✅ **ADR-114's finding is now a TEST**: re-introduce `base` and the suite fails, naming the toggle it would remove. Seven mutations, all caught ✅ **Owner-verified same day: the accent RENDERS** (purple on the Tool nav + answer selector, in dark mode) — the three-level measurement predicted it and the recorded fallback is not needed. 🔁 **Follow-up the same day: the truncation fix was not enough.** Trimming padding left `flex: 1 1 0` + `min-width: 0`, so a segment could still be squeezed below its own text (*"we are still truncating some of the selectors"* — **My … | DNA | Lea… | Lab**). ⭐ **The first fix treated a symptom (too little space); the defect was a rule (a segment may be narrower than its own label).** Floor is now `min-width: max-content` — segments grow to fill the row, never shrink past the label, and **wrap** when four will not fit. *Equal width and legible labels are not both achievable at phone width, and I optimised the wrong one* | Accepted |
| [181](./ADR-181-one-recipe-means-every-caller.md) | **One recipe means every caller** — owner: *"bug: different recommendations from My Squad 'what should I do this week' and captaincy."* Two surfaces, one page, one squad, one gameweek — **different captains**: Captain tab `B.Fernandes 4.8`, the week's answer `João Pedro 6.3`, with B.Fernandes himself priced 4.8 vs 5.4. Cause: `render_captain` called `minutes_weight_from_history(history)` **without `gw_history`**, so it was still running the **pre-ADR-173** model while every other caller (`ask` ×2, CLI, `decision_xp`) ran the current one. Invisible for four days because the argument is optional — omitting it is not an error, not a warning, not a crash; **it silently prices a different player**. ⭐ **LESSON: "ONE RECIPE" IS A CLAIM ABOUT EVERY CALL SITE, NOT ABOUT THE FUNCTION** — a shared primitive with an optional refinement gives every caller a silent opt-out, and the one that takes it does not look broken, it looks like an older plausible answer. Mechanically: **when a correction lands in a shared helper behind an optional argument, the argument stops being optional** — a sweep guard now fails any single-argument call site by name. ⚠️ Same shape as ADR-151→156 (one fact, six surfaces, every one found by the owner). 🔬 **And the cross-surface guard was testing itself**: written first as a *recomputation* of the Captain tab's picks, it **passed with the bug restored**, because it never touched `render_captain`. **A test that rebuilds the thing under test is testing the test** — rewritten to drive the page and read the rendered card, it now fails at both layers | Accepted |
| [182](./ADR-182-name-the-two-halves.md) | **Name the two halves** — owner, relaying testers: *"they're asking what this means; when you explain it it's OK, but that should not be necessary"* — of *"the FPL assistant that shows its working"*. **Copy that needs a gloss has already failed.** Three faults, the third decisive: a **British schoolroom idiom**; *"working"* is a noun almost nobody uses; and ⭐ **spoken aloud it is identical to "shows it's working"**, which a listener reaches first and which claims only that the app functions. **The mantra is the spoken close of all ten videos** — the most distinctive claim in the product, delivered ten times in a form whose default hearing is banal. 🔬 The owner asked whether *"AI explains"* could return; **checked in the code, and no**: `explain_captain` is plain Python appending hand-written strings, the LLM path is gated on a socket probe that fails on Cloud, and Ask is behind `FPL_ADMIN_KEY` — **no tester has ever seen AI output**. It would also be *weaker* if true, since the hero script's hook is *"AI that just… guesses"*. ✅ **The owner's line, better than the one I proposed**: *"Analytics decide. Logic explains. You make the call."* ⭐ **It names the two halves of the system in the order they run** — `decision_xp` decides (ADR-041), `explain.py` explains (ADR-089) — **a description of the architecture rather than a metaphor about it**, which is why it cannot drift the way both predecessors did. ⭐ **LESSON: A SLOGAN THAT DESCRIBES THE ARCHITECTURE CANNOT DRIFT; ONE THAT DESCRIBES A FEELING ALWAYS WILL.** Guards rewritten to assert the requirements, not the sentence — it has now changed twice. ⚠️ §0 is already rendered, so its script and its video now differ deliberately; the re-record is sequenced after the other nine so the stale-UI re-shoot happens once | Accepted |
| [183](./ADR-183-the-same-build-twice.md) | **The same build, twice, is the same squad** — owner: *"when toggling between Build mode there are no changes to the team."* ⚠️ **This ADR was first written with the WRONG diagnosis and the correction is its substance.** Draft 1 said *"the modes are simply inert at a full budget"*, built on measurements that were each **a single sample of a nondeterministic process**. The real cause: the objective has **exact ties** and CBC picked among them arbitrarily *between processes* — six identical runs of one build returned **two different fifteens, both scoring 401.400**. So (a) **the same build run twice returned different squads** — the worse defect, which nobody had reported — and (b) one tied optimum happens to be the squad Strong XI picks, so ~half the time the toggle looked dead. Fix: break ties toward the **cheaper** squad (same points, more in the bank). ⚠️ **The magnitude was measured, not guessed** — `1e-6` was tried and **failed** (the gap between squads £0.2m apart is 2e-7, under CBC's tolerance); `1e-3` resolves and gives up **0.000 xP** across 5 budgets × 2 modes. Re-measured on a deterministic solver, the modes differ at **every** budget including £100m (+1.5 XI xP) — *there was no collision, there was a coin flip*. The page now **prices** the mode (*"+12.8 xP in your XI, £5.4m less bench"*). ⭐ **LESSON: A MEASUREMENT OF A NONDETERMINISTIC PROCESS IS NOT A MEASUREMENT** — I tabulated one roll per cell as fact; the contradiction was available all along and I explained it away three times (Row-vs-dict, `keep_ids`, state leakage) before testing determinism itself. 🔬 And two guards were wrong first: the determinism test ran **in one process** so it passed with the fix reverted, and the fixture was itself one enormous tie, then had a **£2m** gap where reality has £0.2m — letting a constant pass in test that fails in production | Accepted |
| [184](./ADR-184-sweep-for-the-claim.md) | **Sweep for the claim, not the places you remember** — owner, reading the Lab: *"note the model note: **'the recommendation is data-driven; AI explains the reasoning'**."* That is the claim **ADR-168 retired on 2026-08-29**, still rendering — in `MODEL_NOTE`, on **six** surfaces (Lab · captain · chips · gameweek plan · three `ask` intents). False for every tester: no Ollama on Cloud, Ask is admin-gated, and the Edge/Risk/Confidence block it annotates is rule-based Python. ⭐ **Why TWO ADRs missed it: both guards checked the same two files** (`brand.MANTRA`, `7_Help.py`) — *"this file must not say this"* rather than *"nothing may say this"*. **A guard against a claim must sweep for the claim, not check the places you thought of.** Fix: `MODEL_NOTE` → *"Analytics decide the recommendation; logic explains it"* (ADR-182's two halves, heuristic caveat kept verbatim), plus a sweep over `src/` that permits a mention **only where the copy names the condition** — Help's *"available when you run it yourself… the hosted app runs data-only"* earns its exemption by scoping, not by filename. 🔬 **The guard was wrong twice first**: v1 exempted any line with an ADR number within ±5 lines, so a mutation stripping Help's caveat *passed* because an unrelated `ADR-168` comment sat four lines below — ⭐ **an escape hatch keyed on proximity exempts whatever is nearby**. And one of my own mutations was invalid (it left both scoping phrases intact): **a mutation that does not break the property proves nothing about the test** | Accepted |
| [185](./ADR-185-price-the-rebuild.md) | **Price the rebuild, not just the fixtures** — the owner runs **two teams** (one on MADBOOTS, one on his own judgement) and is **40 points ahead** after four gameweeks. Auditing the MADBOOTS squad: **3/15 overlap** with a fresh £100m build, **+102.3 xP** of wildcard value, **£14.1m that cannot play** — and the chip advisor says *"Wildcard GW5-7, your weakest stretch · **Confidence 42/100 · Low**"*. `chips.py` picks the wildcard week as the **lowest rolling window of best-XI xP** — *when are your fixtures worst* and nothing else. **It never asks what a wildcard would gain**, so it is least confident exactly when the case is overwhelming. Fix: run the optimiser (one solve, ~0.08s), lead with the gap · overlap · idle money · the transfer alternative (*"free transfers recover 30%, over six weeks"*), and key confidence on the **gap** rather than the fixture margin — two different questions were sharing one number. ⭐ **LESSON: A RECOMMENDATION THAT MEASURES ONLY *WHEN* WILL PRESENT ITSELF AS AN ANSWER TO *WHETHER*** — the reader supplies the missing half without noticing ✅ **Built** — the same call now reads *"worth **+99.6 xP** — a fresh build beats your squad; you keep only **3 of 15**; **£14.1m** cannot play. Your weakest stretch GW5–GW7 — reset before it · **Confidence 95/100 · High**"*. **42 → 95.** The fixture window survives, demoted. `rebuild=None` leaves every existing caller byte-identical, so a squad that cannot be priced degrades instead of failing. ⚠️ **Two guards tested components and not the wiring** and both survived their first mutation: one called `rebuild_confidence` **directly** (never executing the branch in `explain_chips` that selects it), the other passed `idle_spend` as a **literal** (never executing the code that computes it). ⭐ **TESTING A COMPONENT IS NOT TESTING THAT ANYTHING USES IT** — the same shape as ADR-181, one level out. Six mutations, all caught, incl. a no-op control | Accepted |
| [186](./ADR-186-bank-to-afford.md) | **Bank to afford, not only to stack** — owner: *"you are not suggesting to hold on making transfers for a couple of weeks to buy a more expensive player than you can afford."* Measured on his squad at £0.0m bank: the best move is **Watkins → Havertz +7.4**, but **£1.5m more makes it Watkins → Isak +13.8**. **£1.5m is worth +6.4 xP** and the app cannot see the cliff — `bank_or_use` (ADR-132) only ever weighs *banking a transfer to make a second move*, and **nowhere does the codebase ask what a larger budget would afford**. ⭐ **"Bank" meant a spare transfer everywhere in the code and money to the user — a term that means one thing in the code and two to the user hides the half you did not build.** Ships the arithmetic (*"£1.5m more and this becomes +13.8"*) and **refuses the forecast** — modelling the price accumulation would stack an unverified predictor on top of a heuristic, the same line ADR-161 drew when it gated the win-probability sim ✅ **Built** — the week's answer now reads *"Transfer: Watkins → Havertz (+7.4)… **Worth saving for: £1.5m more makes this Watkins → Isak (+13.8 XI xP, +6.4 on the move above)**"*. The move you can make today stays the headline; the cliff is placed after it, never instead. `gameweek_plan` gains a `cliff` key, `None` when there is nothing to say — which is most weeks. ⚠️ **The wiring guard SKIPPED instead of failing**: it asserted `"cliff" in plan` then returned early when the value was `None`, which is exactly what the mutation produces. ⭐ **A test that skips is not a test that passes** — third sprint running where my new-feature guard protected nothing first time. Rewritten to assert **the call** (with the squad's real bank), which kills both dropping it and hard-coding `bank=0.0`. 🔬 And the fixture had to grow three times — `team`, then `status`, then `points_per_game` — because `gameweek_plan` also picks a captain: ⭐ **a fixture aimed at one function has to satisfy every function on the path to it**. Five mutations, all caught 🔧 **AMENDED THE DAY IT SHIPPED — the line never appeared.** It was priced against `xp_by_id`, which on My Squad is **a single gameweek** (ADR-179 fixed that page's horizon at 1 four days earlier): the best move was **+1.7 over one week** and **+7.4 over five**, so a 2.0 xP threshold could never be cleared. ⭐ **I TUNED THE THRESHOLDS AGAINST A FIVE-GAMEWEEK MEASUREMENT AND SHIPPED ONTO A ONE-GAMEWEEK PAGE** — every number in the ADR came from a `horizon=5` console session. **The fix is the window, not the threshold**: *"is it worth waiting a fortnight?"* is a multi-week question, now priced over the same `horizon_xp` ADR-173 uses for *Longer view*, with the span named on the line because the headline above it uses a different one. ⚠️ **The tests could not have caught it** — every guard drove `gameweek_plan` with an xP map *I* supplied, so the horizon the **page** passes was never in the picture. ⭐ **A test that chooses its own inputs cannot catch a caller passing the wrong ones** | Accepted |
| [187](./ADR-187-reopen-multi-gameweek-planning.md) | **Reopen multi-gameweek transfer planning** — owner: *"you are looking one GW ahead… not looking at fixture runs and making 2 or 3 transfers to target a run."* **ADR-132 declined exactly this on 2026-08-24**, having prototyped properly and found *"one beneficial move — a tree with one branch."* Re-run today: **five distinct beneficial moves, and the ranking reorders with the horizon** (Rice→Foden overtakes Watkins→Wissa at h=5, falls back at h=6) — which *is* the planning signal ADR-132 concluded was absent. ⚠️ **Its evidence expired**: it was measured **preseason** on an xP model since corrected twice — ADR-172 (a shrink that was inert for 513 of 626 players) and ADR-173 (**186 players moved**). *A search finds one branch when most players are mispriced.* **ADR-180's lesson, landing a second time.** Reopens as a **measurement, not a planner**: does a 2-3 move sequence beat the greedy move repeated, under real budget/free-transfer constraints? If not, the decline stands. ⭐ **LESSON: A DECLINE NEEDS A RE-MEASURE DATE, THE SAME AS A FEATURE NEEDS A REVIEW DATE** — ADR-132 did everything right except record *what would have to change for the answer to change* ✅ **RESOLVED — CLOSED, the decline stands.** The measurement ran and says no. 24 squads, 6 gameweeks, **both strategies given the same three transfers**: `do nothing 178.8 → greedy 220.1 (+41.3) → planned 222.0 (+1.9)`. **Greedy captures 96% of the available gain; planning adds 4%.** **54% of squads gained NOTHING at all**, median **+0.0** (p90 +6.2, max +14.3) — which is ADR-132's own wording vindicated: *"the gain moves; the decision does not."* ⚠️ **Two design faults in the measurement gave a confident wrong answer first**: (1) greedy got **six** transfers to planned's three, so the first run reported planning *losing* by 12/squad — ⭐ **a comparison in which the two strategies get different budgets measures the budget**; (2) planned could *lose* to greedy, structurally impossible for a larger search space — its shortlist was computed once while greedy re-scanned after every move, so it measured **shortlist size, not planning**. Both produced plausible tabulated numbers; ⭐ **the tell each time was a result that should have been impossible.** Honest limit recorded: the search is bounded (10 candidates, ≤3 moves), so **+1.9 is a lower bound** — what is established is that *the easy wins are not there*. Closed **not deferred** (ADR-101's rule against furniture), with what would reopen it written down: a **double/blank gameweek** (none in this window), **chips**, or a larger search beating +1.9. Spike + result committed at `spikes/187-transfer-paths/` | Accepted |
| [188](./ADR-188-a-defender-plays-for-a-team.md) | **A defender plays for a team** — owner, on an ADR-186 suggestion to sell Konsa (ARS) for Ballard (SUN): *"that is a transfer I would not make… I have a cheap Arsenal fullback, likelihood of clean sheets probably >50%. How can we factor that knowledge in?"* ⭐ **The app already has the knowledge and does not use it**: Team DNA shows **Arsenal 75% clean-sheet potential (97th pct, grade A)** against **Sunderland 25% (42nd, grade C)** — he guessed *">50%"* and it is 75%, on a page the app renders. But a defender's xP is **his own points-per-90 × minutes × opponent difficulty**: no term for his own team's defence, and `FORM_WEIGHT` / `SET_PIECE_WEIGHT` / `DEFCON_MAGNIFIER_WEIGHT` are all **0**. Sizing: the model prefers selling Konsa over Hume by **1.7 xP**, while the signal it cannot see is worth **~10 points** over five gameweeks — *the margin is a sixth of the blind spot* (an order-of-magnitude check, not a measurement — it extrapolates a four-game rate). Proposes `CLEAN_SHEET_WEIGHT`, DEF/GK only, **defaulting to 0 and swept by ADR-101's harness at the GW6 sitting** against §B0's four criteria unchanged — with its prediction recorded first: *small positive ≈0.05-0.15, quite possibly zero, and **a large gain is a warning not a win** because much of it is already inside points-per-90*. If it fails §B0, **Option 3 is adopted rather than forgotten** — a lens naming both clubs' rates. ⚠️ Correlated defensive risk (two defenders from one club failing together — the owner's second, unprompted point) named and **explicitly out of scope**: a portfolio constraint, not a weight. ⭐ **LESSON: A NUMBER THE APP DISPLAYS AND DOES NOT USE IS A CLAIM IT IS MAKING TO THE READER AND NOT TO ITSELF** — the two surfaces share a database and not a model, and **the gap between what a system measures and what it acts on is invisible from inside the system** ✅ **Built dormant** — `cleansheet.py` (delta vs the league mean, DEF/GK only), a 4th `_CALIBRATE_WEIGHTS` entry, 5 guards, all mutation-tested. 📉 **First GW4 read: 0 of 4 criteria** — ρ 0.621 → 0.614, **monotonically down** across 0→0.30, with the whole spread a *sixth of one SE*. The prediction's cautious half was right and the mechanism is the one it named: the clean sheets are **already inside points-per-90**, so the term restates rather than adds. ⭐ **A DESCENDING CURVE AND A FLAT ONE SAY DIFFERENT THINGS** — flat is *"no effect at this sample size"* and will be re-asked at GW6; descending is *"it costs accuracy in proportion to how much you use"*, which more gameweeks will not fix. GW6 inherits a prior of zero **with a reason**, and Option 3 (a transfer-surface lens) is the likely end state | ✅ Accepted — built, **weight held at 0** 🔬 **RE-SCORED ON DEF/GK ALONE (2026-09-15) — MY HYPOTHESIS REFUTED.** ADR-190 says a whole-board metric cannot see a sub-population term, and this one touches **DEF/GK only** while being scored across **626** players, so the decline might have been dilution. It is not: restricted to the **277** rows it applies to the term declines **nearly twice as steeply** (ρ 0.546→0.520 vs 0.619→0.605; MAE 1.33→1.38 vs 1.22→1.24) — **the double-count reading confirmed from a second direction**, doing the most damage exactly where it is applied. ⚠️ One number moves the other way and is **recorded, not used**: `hit@10` among DEF/GK rises 0.15→0.23 at 0.15–0.30 — the *top* of the defender board improving while the ordering worsens, which is tempting because a transfer cares about the top of the board. It is **3 players** (6.0→9.2 of 40 across 4 GWs) on a **secondary** criterion while the primary declines on the same rows; ⭐⭐ **TREATING IT AS THE ANSWER WOULD BE CHOOSING THE METRIC AFTER SEEING THE CURVE**, the one thing pre-registration exists to stop — if it survives GW6 the question is *"does a top-of-board metric belong in §B0?"*, argued in advance. ⭐ **The owner's distrust of selling his Arsenal defender was NOT this term** — adding it ranks defenders worse; the cause was a 2nd transfer priced on **one gameweek** with no longer view (ADR-191) | ✅ Accepted — built, **weight held at 0** |
| [189](./ADR-189-two-defenders-one-bet.md) | **Two defenders, one bet** — the owner's second, unprompted point: *"I have Hume in my squad, a Sunderland player — he would be a better option to transfer to Ballard maybe."* Two defenders at one club are **one bet twice**: the clean sheet arrives for both or neither. ⚠️ **The warning is DECLINED on evidence, following ADR-145's method.** Test 1 passes — **38% of 400 squads** have 2+ defensive assets from one club, so unlike *clashes* (100%, therefore wallpaper) it discriminates. **Test 2 kills it**: correlation **never moves expected points**, it multiplies that component's spread by exactly **√2** — **1.17 pts at worst** against an XI spread of ~11.6, *ten percent of one component* — and ADR-145 already established lower variance is not automatically better. Sizing the owner's own two reasons: the **clean-sheet blind spot ~10 pts (ADR-188)** vs **doubling up ~1.2 pts of spread** — **the weight dominates 8:1**. ✅ **What ships instead is a tie-break**, because the real defect was that the ranking separated the two sells by **1.7 xP over five GWs** — 0.34 a week against a per-player weekly sd of **3.51** (ADR-161): *a coin flip presented as a ranking*. Near-ties now break toward less correlated defence, **quantised not subtracted** so the primary ordering survives any real difference (ADR-183's lesson). Measured: fires on **7%** of squads, never gives up more than the band. ⭐ **LESSON: A RANKING THAT SEPARATES TWO OPTIONS BY LESS THAN ITS OWN NOISE IS NOT RANKING THEM — IT IS PICKING ONE.** Third use of this shape (ADR-183 cheaper squad, ADR-186 the cliff): **where a model cannot tell two options apart, the right move is not a better model — it is a stated preference** 🔧 **CORRECTED SAME DAY — the first implementation never fired.** It shipped as a sort key, `round(gain / TIE_NOISE)`, and the owner saw the app still sell his Arsenal defender: his two moves gained **11.4** and **9.7** — 1.7 apart, inside a 2.0 band — but round to **6** and **5**, so they never shared a bucket. ⭐ **QUANTISING IS NOT THE SAME AS "WITHIN NOISE OF EACH OTHER"** — a band is a statement about the *distance between two numbers*, a bucket about *where each sits on a grid*; only one of them means "too close to call". Replaced with the direct form: keep every move within `TIE_NOISE` of the best, pick the least correlated. ⚠️ **And the test passed throughout** — its fixture gained 14.0 and 13.6, which happen to round into the *same* bucket: ⭐ **a fixture that only exercises the lucky case will confirm a broken mechanism.** A second guard now pins gains that straddle a bucket edge, and asserts that they do | Accepted |
| [190](./ADR-190-the-gw4-sitting.md) | **The GW4 sitting — nothing ships, and one bar cannot be cleared** — the first calibration checkpoint, held rather than drifted past. ⚠️ It was **overdue and invisible**: the roadmap held two gates under one *"blocked on gameweeks"* heading — the 900-minute bar (still real, **0 of 657**) and the ≥4-gameweek bar (lifted) — and **nothing announces the gate that lifts first**. All four weights sweep **flat against a +0.040 bar**: form +0.005 (ascending), set-piece +0.001, DefCon +0.001, clean-sheet **−0.007** (descending, ADR-188). **All stay at 0.** ⭐⭐ **THE CHECK THAT CHANGES THE READING: ARE THE TERMS EVEN LIVE?** Measured as ρ(rank at 0, rank at w) — form **0.987** and clean-sheet **0.981** genuinely re-rank the board, DefCon **0.99977** and set-piece **0.99998** do not. **A term whose ranking is 0.99998 correlated with the baseline's cannot change that ranking's correlation with reality by 0.040** — the bar is not unmet, it is **unreachable**, at GW6, GW10 and any n. And it is by design: `set_piece_bonus` applies only off the `hist` tier (ADR-096, correctly, or it double-counts), so of **43** duty-holders the term reaches **9**, and `history_by_code` holds only completed seasons — **the eligible population is fixed for the season at 9 of 657**. ⭐⭐ **A MEASUREMENT CAN FAIL BECAUSE THE THING IS ABSENT OR BECAUSE THE INSTRUMENT CANNOT SEE IT, AND A FLAT CURVE LOOKS IDENTICAL EITHER WAY** — one column split four identical-looking lines into evidence about football (form, clean-sheet) and evidence about the harness (set-piece, DefCon). ⭐ **A WHOLE-BOARD METRIC CANNOT EVALUATE A TERM SCOPED TO A SUB-POPULATION** — §B0 checked whether its bar was strict enough and never whether it was **reachable**; pre-registration stops you choosing the answer afterwards, it does not stop you asking a question the instrument cannot answer. Constants re-measured at **two seeds** (ADR-183): `WHISKER` 0% and concentration +6%/−2% → **keep**; **`CLEAR` 1.0 → 1.3 SHIPPED** (+30%, pre-registered) because leads **widened** (max 2.80 → 4.30) as real returns replaced projections — holding 1.0 would keep the number and lose the meaning. ⚠️ **`EXODUS_PRESSURE` cannot be re-measured as §B0 instructs at all**: `price_pressure` reads current-event fields and `player_history` stores no per-round transfers, so *"re-measure on ≥4 GWs"* produced a **second single-week sample**, 51% apart — which establishes **that it varies**, not a new value (it flags 2 of 190 today against an intended worst-tenth). Refused to lower criterion 1 to fit; **a live percentile** and **closing set-piece** are both proposed and GATED | ✅ Accepted 🔬 **Option 3 tried once (2026-09-15) and it did not rescue ADR-188's term** — re-scored on DEF/GK only, the clean-sheet weight declines *more* steeply, not less. ⭐ **Worth doing anyway: a whole-board decline and a sub-population decline are different facts, and only one was on the record.** The method stands; this term does not | ✅ Accepted |
| [191](./ADR-191-spend-the-transfers-you-hold.md) | **Spend the transfers you hold** — owner, on an ADR-186 line: *"I have £1.0m in the bank, I have **2 free transfers**… is this advice the best or most effective? Should we not be **triangulating** number of available transfers, spending the money on the starting 11, looking at budget?"* ✅ **The half of the worry that was unfounded, checked first:** the buy is not a 12th man — `xi_aware=True` means a move's gain **is** the XI lift, and the recommended buy made the XI **60/60** squad-runs. The fault is not *which* player, it is **how many**. 📊 Measured (`spikes/191-two-transfers/`, 5-GW XI xP, bank £1.0m): one move **+21.7/+22.2**, two greedy **+40.1/+40.6**, two planned **+40.6/+40.9** — ⭐⭐ **THE SECOND TRANSFER IS WORTH ROUGHLY AS MUCH AGAIN AS THE FIRST** (+18.4 mean; +4.5 and +10.0 on the two real squads) **and the app already computes it and throws it away** — `gameweek.py` asks `suggest_transfers` for two moves, shows one, and spends the second on *"bank or use"*. ⭐⭐ **A NUMBER COMPUTED FOR ONE QUESTION AND DISCARDED IS INVISIBLE IN A WAY A MISSING NUMBER IS NOT** — a feature never built leaves a hole someone notices; a value computed and dropped leaves a comment explaining why two were requested. Three faults: the app **does not know you hold 2 FTs** (the Transfer tab's input feeds `bank_or_use` alone); ⭐ **the cliff's "wait" is never weighed against "use your second transfer now"** — both computed, neither compared, the reader handed whichever renders (*save £1.0m for +3.3* is dominated by a second move worth +4.5–10.0); and ⭐ **the shortlist is a MENU, not a PLAN — its gains do not add** (both priced against the same squad and the same bank). ⚠️ **Option 4 refused:** ADR-187 closed planning *across* gameweeks (*when*); this is two free transfers *within* one (*how many*) — **reusing a decline because the words overlap is how a measured "no" becomes an unexamined one**. **§1 to build** (N moves, priced sequentially; the cliff must compete). **§2 joint pairs GATED** — genuinely ambiguous: +0.4 mean across random squads (8/30) vs **+5.9 on RoboTS**, where greedy routed Ballard in through the wrong sale and blocked *Mukiele → Ballard + Watkins → Isak*; random squads have too much headroom for order to matter, and believing that is not measuring it. **§3 target-driven planning GATED** (*"what would it take to field Haaland?"* — the owner's own framing, no measurement yet) | 📋 Proposed ✅ **§1 BUILT** (Sprint 256) — and the build was **one line, because the planner already existed**: `suggest_transfer_plan` has threaded the bank and priced true marginal gains since **ADR-035**, and the CLI, `ask`'s transfer intent and the Transfer tab all use it. ⭐⭐ **A PRIMITIVE CAN BE CORRECT EVERYWHERE AND STILL BE MISSING FROM THE ONE PLACE PEOPLE READ** — the week's answer was the single caller still on the older sibling, and the comment beside it *explaining why it asked for two moves* read as care rather than as a gap. *"Is this function used?"* answers yes; the question that finds it is **"what does the surface a user actually reads call?"** ⚠️ Unplanned, found while wiring: **`free` and `bank` never reached the week's answer at all** — hard-coded to 1 and £0.0m in `ask.py` while the Transfer tab collected the real numbers three tabs away, so the recommendation was computed for a position its reader was not in. Both widgets now keyed (`tr_free`/`tr_bank`) as the app's single record of it. The cliff now **competes**: on a real squad it shows at 1 transfer (+2.6 uplift) and **vanishes at 2** because the second move is worth +9.3 — compared over the cliff's own 5-GW window, never the page's, or it would repeat ADR-186's original mistake exactly. ⚠️⚠️ **TWO OF SIX MUTANTS SURVIVED THE FIRST SWEEP, BOTH FIXTURE FAULTS**: the headline *"gains must sum to the real XI lift"* guard **could not fail**, because equal upgrades in different positions make a menu and a plan genuinely agree — ⭐ *a fixture that only exercises the lucky case will confirm a broken mechanism*; rebuilt around **bank threading**, the one thing a menu cannot model (two £6.0m buys, £1.0m banked — a menu offers both, a plan affords one). The other checked for `"then #2"` and missed the total line still rendering — ⭐ *assert the whole block is absent, not the part easiest to name* | ✅ §1 Accepted · §2/§3 gated |
| [192](./ADR-192-no-opinion-is-not-full-confidence.md) | **"No opinion" is not full confidence** — owner, on the 2nd move in his week's plan: *"not sure on Konsa — more likely to be a higher performer than a Fulham back!!"* **He was right.** Affengruber: **GW3 0 mins, GW4 90 mins/8 pts, ZERO past seasons** → `cold_start` rate **4.40**, xMins **1.00**, **22.4 xP**. Konsa: **7 seasons, two consecutive 90s** → rate 2.98, xMins 0.88, **13.1 xP**. ⭐ **A PLAYER WITH ONE APPEARANCE IN HIS CAREER OUTPROJECTS ONE WITH SEVEN SEASONS BY 9.3 xP.** Two faults compound: `points_per_game` on **n=1** is an anecdote not a rate, and the most optimistic minutes value available is the default. Measured: **124** players are modelled as nailed 90-minute starters on no evidence (108 project too low to surface, 15 at 8–12, **1 at 12+ — and it landed in the headline recommendation**); **Gboho projects 10.6 xP having never played a minute**. ⭐⭐ **`NO OPINION` IS IMPLEMENTED AS THE MOST OPTIMISTIC OPINION AVAILABLE** — ADR-173 named this exact case (*"that is the crater case; one appearance says nothing about the next"*) and refused to **promote** such players, but never **demoted** them, because the default was already 1.0 (ADR-104). Its own principle *"full weight where there is no doubt, no opinion where there is"* has **both branches landing on the same number**. ⭐⭐ **A DEFAULT IS AN OPINION, AND THE SAFEST-LOOKING DEFAULT IS USUALLY THE MOST CONFIDENT ONE** — the neutral value of a *multiplier* is not neutral, it is the maximum, which is why this survived three ADRs looking straight at it. ⭐ When a model expresses *unknown* as a number, **check which direction that number points**. Direction decided, **constant deliberately NOT chosen** — it goes through §B0 scored whole-board *and* on the 124 (ADR-190 Option 3). ⚠️ Deferral refused: ADR-173's hold rested on sample size **and no known harm**, and this is the **4th sighting** in the family (ADR-172 · ADR-173 · ADR-185's A/B · this) | 📋 Proposed |
| [193](./ADR-193-admit-up-to-the-cap.md) | **Admit up to the cap, then waitlist** — owner: *"people are getting stuck in the waitlist; we need to auto-allow access up to the max number and then enter the waitlist."* The Google gate (ADR-106) had two outcomes — on the allow-list → admit, otherwise → **waitlist forever**, because nothing put anyone on the allow-list except the owner by hand. ⭐⭐ **A QUEUE WITH NO AUTOMATIC EXIT IS A QUEUE WITH NO EXIT** — the waitlist worked exactly as designed, captured people and held them; it was **half a mechanism**, and the missing half looked like an admin task rather than a defect. ⭐ **The capability already existed:** `user_store.register(email, cap)` has admitted-under-cap since ADR-098 and the older shared-code gate calls it — **the Google gate never did**, and `BETA.md` even recorded *"no FPL_USER_CAP needed"* for auth mode. Same shape as ADR-191 four days earlier: *"is this function used?" answers yes; the question that finds these is "does the path a real user takes call it?"* Built: one branch between the two existing outcomes, a **shared `_admit()`** (two routes to one state is how the second forgets a step), and **failure lands on the waitlist, never a stack trace** — the only path to admit is an explicit `"in"`. ⭐ *The failure path of a gate is the gate.* An allow-listed tester **never consumes a registration**, and **no cap set = byte-identical** — *a change to who can get in should require saying a number*. ⚠️ **The copy moved with the posture**: *private beta* → *beta*, and the waitlist's *"we'll be in touch as spots open"* — **a promise needing a person, which is how the queue became permanent** — now says the true thing that needs nobody. ⭐ *Access copy is a claim about who can get in; it expires the moment the rule does.* 5 mutants, all red. ⚠️ **Inert until `FPL_USER_CAP` is set in the deployed secrets** | ✅ Accepted |
| [194](./ADR-194-a-start-bench-call-gets-a-margin.md) | **A start/bench call gets a margin** — owner, on *"Start Thomas over Konsa (2.7 vs 2.6)"*: **"not a chance"** (Coventry shipped 5 that week; Arsenal are 1st for xGC). ⭐ **He did not need to dispute the number** — **0.1 xP is 1/35 of a player's weekly sd of 3.51** (ADR-161), so the model could not separate them and said so **in the voice it uses when it can**. ⚠️ **Every other surface already had a margin** — captain `WHISKER 0.3`/`CLEAR 1.3`, transfers `TIE_NOISE 2.0`, chips 15% — **the lineup had none**, and toss-ups were even counted as **Edge** (*"2 lineup tweaks to bank more points"* = two coin flips presented as advantage). Measured over 500 squads at 2 seeds: a real swap is **p25 1.0 · median 1.8 · p75 2.8**, and **1 in 10 is inside 0.3** — ⭐ *the advice is usually good; the fault is the tail.* Banded on the measured quartiles (ADR-144's idiom): under 1.0 → *"too close to call … your call"*, naming both and **not instructing**; the optimiser is unchanged and still fields someone. ⭐⭐ **HANDING THE CALL BACK IS THE HONEST ANSWER AND IT IS LITERALLY THE MANTRA** — *you make the call*; on a 0.1 gap the manager's own information is better evidence than the model's. ⭐⭐ **AN UNEXAMINED SURFACE INHERITS THE CONFIDENCE OF ITS LOUDEST NEIGHBOUR** — the lineup never got a margin because **nobody had ever complained about a start/bench call**, so it sat in the same block, same voice, under the same Confidence score, sounding measured. 4 mutants, all red | ✅ Accepted |
| [195](./ADR-195-team-defence-is-xgc-not-clean-sheets.md) | **Team defence is xGC, not clean sheets** — ⚠️⚠️ **I MEASURED THIS TWICE, CONCLUDED "NO SIGNAL" TWICE, AND WAS WRONG BOTH TIMES.** ADR-188 tested a club's **clean-sheet rate**: over 4 gameweeks that is a **five-valued statistic derived from a coin flip**, so its variance swamps the signal. Re-scored on DEF/GK alone (ADR-190 Option 3, spike 192) it declined *more* steeply, and I told the owner his instinct was not explained by the missing term. **xGC/90 is continuous, minutes-normalised, and spreads 3×**: ARS **0.65** (1st) · SUN 1.39 (10th) · FUL 1.83 (16th) · COV **1.94** (19th) — *and the model rates Konsa and Thomas 0.1 xP apart.* Spearman(club defensive quality, defender's actual points) = **+0.216** over 379 player-gameweeks against 1 SE of 0.051 — **4.2 SE**, where clean-sheet rate showed nothing. ⭐⭐ **A NULL IS A STATEMENT ABOUT THE INSTRUMENT AS MUCH AS ABOUT THE WORLD** — both measurements were executed carefully and both measured a quantity that **could not have shown the effect if it existed**. ⭐⭐ **WHEN A CAREFUL MEASUREMENT AND A KNOWLEDGEABLE HUMAN DISAGREE REPEATEDLY, CHECK WHAT THE MEASUREMENT IS MADE OF — three nulls from the same instrument is one null.** Proposal changes only the **input** to ADR-188's existing gated term; ⚠️ honest limits recorded first: it is a **correlation not a backtest**, part of it is already inside points-per-90, and *good clubs have good players* is unresolved. If xGC nulls too, **the question closes for the season** — a fifth instrument would be the mistake this ADR accuses the third of | ✅ **BUILT 2026-09-17** — input swapped to xGC/90 through a Poisson step; `clean_sheet_delta`, `league_clean_sheet_rate` and ADR-188's five guards **untouched**, because the term's *shape* was never what was wrong. ⚠️ **The proposal above had a UNITS BUG that would have shipped quietly**: `clean_sheet_delta` multiplies by `CLEAN_SHEET_POINTS`, which only yields *points* when the delta is a **probability** — a raw goals-per-90 difference gives "4 × goals", a quantity in no unit at all with a plausible sign and magnitude to hide behind. ⭐⭐ **A SWAPPED INPUT HAS TO ARRIVE IN THE UNITS THE CONSUMER ALREADY ASSUMES** — and the assumption lived in a `× 4` three lines away, inside the function this ADR promised not to touch. Fixed with Poisson (`P(CS) = e^-xGC`), which ⭐ **fixes the SIGN for free** — higher is better for a rate, **lower** is better for xGC, so a raw swap would have **inverted the term** and priced Coventry's defenders above Arsenal's while every guard stayed green (they all test `team − league` and none says which way "good" points). 📊 **The thesis on live data, starker than argued**: the old rate takes **5 distinct values across 20 clubs** against xGC's 20, Spearman between them is **0.243** (6% of variance), **ARS and HUL share 0.75** on xGC 0.68 vs 1.49, and the **`0.00` bucket holds SIX clubs from BOU 1.32 (4th-best) to CRY 2.04 (the worst)** — ⭐⭐ **A TERM WHOSE INPUT PUTS THE 4TH-BEST AND THE WORST DEFENCE IN ONE BUCKET CANNOT CORRELATE WITH ANYTHING CONTINUOUS**; ADR-188's null was never evidence about football. ⚠️⚠️ **The suite could not see this change AT ALL** — `decision_xp` does not call the rate builder at weight 0, so all five ADR-188 guards stayed green through a change to the input's units, source **and** sign: ⭐ *a dormant term is not covered, it is merely quiet.* 10 new guards driving it live. **9 mutants, 2 survived the first sweep and both were fixtures**: the benched-keeper guard was **masked by the one-row-per-round guard** (list him *first* and it breaks — ⭐ *a fixture that only exercises the lucky ORDERING will confirm a broken mechanism*, 3rd sighting after ADR-189 and ADR-191 §1), and nothing tested **a match in flight** — `_played` and `minutes > 0` look redundant and are not, because a live gameweek has minutes but no scoreline (ADR-125). Deltas for scale: ARS **+1.07** → CRY **−0.44** pts/match, spread 1.51 vs a weekly sd of 3.51. ⏳ **Weight still 0 — swept at GW6**, prediction unchanged and recorded first (small positive ≈0.05–0.20, quite possibly zero; **a large gain is a warning, not a win**). If it nulls, **the question closes for the season** | ✅ Built · weight 0 · sweeps at GW6 |
| [196](./ADR-196-no-output-is-attributed-to-ai.md) | **No output is attributed to AI** — owner: *"Player DNA has both **AI Verdict** & **AI Insights** which should be just Verdict and Insights OR MadBoots Verdict and MadBoots Insights."* Worse than a naming preference: **ADR-168 removed every AI claim 20 days earlier** because there is no model on the deployed app, and these two were **headline labels on the most screenshot-shared cards in the product**, crediting a model for output `explain.py` computes in plain rule-based Python. ⚠️⚠️ **ADR-184 SWEPT FOR THIS AND MISSED IT** — that ADR exists because a retired claim survived 14 days on six surfaces, and its own remedy swept for **the mantra** (one sentence) while these are the same claim in different words. ⭐⭐ **A SWEEP WRITTEN TO CATCH ONE CLAIM WILL HAVE THE SHAPE OF THAT CLAIM** — ⭐ *when you retire a claim, write down the claim, not the sentence*: **"the app must not credit a model for output it computes"** catches both on day one; *"the string 'The AI explains' must not appear"* can only ever catch itself. Renamed to **MADBOOTS Verdict / MADBOOTS Insights** (accurate, and neither card carried branding). New guard sweeps all visible copy for **`AI` + a Capitalised word** — an *attribution*, deliberately narrow: it must NOT flag Help's true *"not an AI guess"* and *"Local AI … the hosted app runs data-only"*. ⭐ **A GUARD THAT WOULD DELETE AN HONEST EXPLANATION TO SATISFY A PATTERN IS WORSE THAN THE BUG IT IS CHASING**; ⭐ *prefer a guard that fires correctly over one that fires comprehensively and is then disabled.* 3 mutants red, including a phrasing it had never seen | ✅ Accepted |
| [197](./ADR-197-compare-two-fingerprints.md) | **Compare two fingerprints, and give it no verdict** — owner: *"could we do a compare DNA for both Team & a Player. For Player it's an extension of Boot Battle and should include Performance trend too."* Boot Battle answers *which is better at each stat*; it cannot answer **what shape is each player** (a 90th-percentile creator and a 90th-percentile finisher both read as "good" in a table and look nothing alike on a radar) or **which way each is going**. Built: **two polygons on one octagon**, not two charts — *the value is the shape difference, and two charts make the reader do the overlay in their head*; a **trend per player**; one builder for Player and Team. ⭐⭐ **THE DESIGN DECISION WAS WHAT IT REFUSES TO CONCLUDE: NO SECOND VERDICT** (owner-agreed) — a verdict is a heuristic 0–99 from signals deliberately kept **out** of `decision_xp`, and two side by side publish a **ranking the model has not earned**; the team compare drops its grade gauge for the same reason. ⭐⭐ **A NUMBER THAT IS HONEST ALONE CAN BECOME A CLAIM WHEN YOU PUT IT NEXT TO ANOTHER ONE** — it would have been the week's third overclaim, after a lineup instructing on 0.1 xP (ADR-194) and two cards crediting an absent AI (ADR-196). ⚠️ **A case that only exists with two shapes**, decided at the gate rather than discovered on camera: each side may be unranked on a **different** axis, so the pair **declines together** if either is too thin (*a radar where one shape is real and the other is guesswork is worse than no radar — it looks like a comparison*) and each unranked vertex is hollow **in its own colour**. ⭐ Named `render_dna_compare`, because `render_player_compare` is already Boot Battle's card — *two functions called the same thing on one panel is how the wrong one gets called in six months*. 4 mutants red | ✅ Accepted |
| [198](./ADR-198-why-73.md) | **"Why 73?" — invert the confidence, don't offer a dial** — owner: *"when the tool provides a confidence level, how can one interact with it to say, how can I move the confidence to 80 or 90?"* ⭐ **The score is a heuristic over known inputs, so it inverts EXACTLY**: `73 = 81 (captain) − 8 (one flagged player)`, and *"how do I get to 81?"* is **arithmetic, not advice** — which is also the only way this could exist on a deployment with no model (ADR-168). Built under the Confidence line: the sum, the levers, the ceiling. ⭐⭐ **ACTIONABLE AND FIXED ARE SEPARATED, AND THAT IS THE WHOLE POINT** — a flagged player is a *choice*; an away fixture costs the captain term a flat 6 and no decision changes it. ⭐ **TELLING SOMEONE WHAT THEY CANNOT CHANGE IS AS USEFUL AS TELLING THEM WHAT THEY CAN — IT IS THE DIFFERENCE BETWEEN A LOW NUMBER AND A BAD WEEK**: the honest answer to *"how do I get to 90"* on the owner's plan is **you cannot this week** (Isak away caps the captain term at 91; clearness saturates at a 0.8 xP lead), and what he *can* do is worth exactly 8. ⚠️ **An explainer, NEVER a dial** — *a visible score plus a way to move it is an invitation to game it*; a guard asserts text only, and another bans the vocabulary of likelihood because confidence is **a heuristic, not a probability** (raising it makes the week *clearer*, not the outcome *likelier*). ⭐⭐ **A TRANSPARENT HEURISTIC CAN ANSWER "WHAT WOULD CHANGE THIS?" AND A MODEL CANNOT — AN EXPLANATION YOU CAN INVERT IS WORTH MORE THAN A SCORE YOU CAN ONLY READ**, and invertibility is a property decided long before anyone asks for the feature. Scoped to the week's plan; **the other four surfaces are GATED**, because an explainer on all five would have to reconcile five meanings of the word — the chips footnote is the standing evidence it does not mean one thing today. 4 mutants red, including *a fixed fact dressed as an action* | ✅ Accepted |
| [199](./ADR-199-a-threshold-needs-a-provenance.md) | **A threshold needs a provenance** — owner, on the *Why 73?* explainer: *"is this more of a discussion rather than an empirical result… more like hunches or punts — tell me if I'm wrong."* **Right about the layer, wrong about the level.** The formulas invert exactly (`73 = 81 − 8` is checkable); the **constants they invert over were mostly typed** — ⚠️ **2 of 7 measured, and both exist because the owner pushed back on them**. Measured 200 squads × 2 seeds: captain lead p75 **1.30–1.42** vs chosen **0.8** (so **42–47% of captain calls saturated at the MEDIAN lead** — a 0.65 lead and a 1.30 lead both scored 91); chip margin p75 **0.04** vs chosen **0.15**, ~4× too high, so *clear* was unreachable and a typical margin scored 55 where the rule puts it at 95 — ⭐ *some of the Low the page showed was the threshold, not the football.* ⭐⭐ **TWO CONSTANTS DESCRIBING ONE QUANTITY IS ONE CONSTANT AND A BUG WAITING FOR SOMEONE TO NOTICE** — `_CLEAR_LEAD` and `captain.CLEAR` measured the same thing on the same distribution, one measured twice and one typed; now an **identity**, not a copied value. ⏳ **Two GATED on wrong population** (spike 191's finding): real best-move +1.8 vs random +3.5, real wildcard 0.40 of projection vs random 1.30 — *setting them here would repeat the clean-sheet-rate mistake exactly*. ⚠️ **Changing both broke NO test** — the two numbers most responsible for what a reader sees had nothing pinning them, which is how one drifted unnoticed. New guard: a threshold is **measured or declared unmeasured with a reason**; it may not simply exist. ⭐⭐ **A CALIBRATION CONSTANT WITH NO PROVENANCE IS AN OPINION WEARING A NUMBER** — and a number is believed in a way an opinion is not. ⭐⭐ **FLUENCY IS NOT EVIDENCE**: the chatbot he imagined would have discussed four numbers, three of them wrong, exactly as persuasively before this audit as after. 3 mutants red | ✅ Accepted |
| [200](./ADR-200-a-population-shaped-like-a-real-squad.md) | **A population shaped like a real squad** — three measurements were blocked on one missing thing (ADR-199's two thresholds, ADR-191 §2). The number that settles why random squads were wrong: **121.3 XI xP** against the owner's real squads at **231** and **244**, the template at **227**, the optimum at **291** — ⭐ *a random squad is not a bad squad, it is barely a squad, and everything looks improvable against it.* ⭐⭐ **BUT QUALITY ALONE WAS NOT ENOUGH, AND THAT IS THE FINDING**: the optimum perturbed to **227.0** gives a best-transfer p75 of **2.88** while the template at **226.6** gives **1.60** — *same quality, nearly double the improvability*, because perturbation creates **repairable holes** while a template is **uniformly mediocre**. **THE POPULATION HAD TO BE SHAPED LIKE A REAL SQUAD, NOT MERELY SCORED LIKE ONE.** ⭐⭐ ***"MEASURE IT ON REALISTIC DATA" IS NOT AN INSTRUCTION UNTIL YOU CAN SAY WHAT REALISTIC MEANS*** — ⭐ *when you reject a population, say what property made it wrong*, or you walk from one unrealistic population to another and get a confident wrong number instead of an obviously wrong one. Set from perturbing the **template** (n=25 × 2 seeds, cross-checked against the owner's two real squads to ~10%): `_CLEAR_GAIN` **3.0 → 2.3**, `_CLEAR_REBUILD` **0.25 → 0.46**. ⚠️ The second mattered most: real squads gain **0.40–0.48** of their own projection from a rebuild, so **every real squad cleared 0.25 and the wildcard read 95/High for all of them** — ⭐ *a number that never varies is not a confidence, it is a constant with a gauge drawn round it.* The old comment called 25% *"deliberately high"* and reasoned it carefully: **sound reasoning, untested premise** — a quarter of your season on the table is a lot, it is simply not rare. `population.py` is importable, because the next calibration will want it | ✅ Accepted |
| [201](./ADR-201-a-season-is-part-of-the-identity.md) | **A season is part of the identity** — the ML roadmap's Phase 0a, and it jumped the queue because of what checking the cache turned up. `player_history` was keyed `(element_code, fixture)` — correct for [ADR-129](./ADR-129-per-gameweek-key-is-the-fixture.md)'s question (a double gameweek is two rows *within* a season) — but **FPL restarts fixture ids at 1 every August**, so next season's GW1 would have overwritten this season's GW1 row for row. ⭐⭐ **THE COLLISION WOULD NOT HAVE LOOKED LIKE DATA LOSS, IT WOULD HAVE LOOKED LIKE A REFRESH**: row counts hold, every value is plausible, nothing raises. ⚠️ **And it is unrecoverable, which set the priority** — `player_history_past` is the proof: **2,097 rows for twenty seasons back to 2006/07, one row per player per season**, against **2,547 rows for four gameweeks of this one**. FPL sells the aggregate back, never the detail; a rollover turns *"12 points, 88 minutes, away to Newcastle"* into *"212 points, 2026/27"* forever. ⭐ **A model can be built next winter; the gameweek you failed to store cannot** — which is why this landed ahead of every more interesting item on the roadmap. ✅ `season` joins the key, **taken from the match's own `kickoff_time` and not from the clock** (a refresh run in late July, or a delayed fixture, would otherwise file under the wrong season — precisely the boundary the change exists for): ⭐ *a record about a point in time takes its timestamp from the event, never from the process that stored it.* 2,547 live rows migrated, rounds 1–4, none lost. ⚠️⚠️ **TWO FAULTS, BOTH FOUND BY MUTATION TESTING AND BOTH INVISIBLE IN THE OUTCOME.** First, the migration **re-ran on every open, forever**: the idempotency check compared the key against an ordered list, but `PRAGMA table_info` lists columns in *table* order and `season` was appended, so it always read last however the key was declared — ⭐⭐ **A MIGRATION THAT CANNOT TELL IT HAS ALREADY RUN IS NOT A MIGRATION, IT IS A REBUILD**, and it hid because the data it leaves behind is correct (a key is a **set** of columns and was being tested as a sequence). Second, **the test written to catch that couldn't**: it asserted the rows survived and no scaffolding was left, and **passed with the check reverted**, because a rebuild also preserves every row and drops its own temp table — ⭐⭐ **WHEN THE BROKEN AND THE WORKING VERSIONS PRODUCE THE SAME END STATE, ASSERT THE WORK AND NOT THE RESULT.** ⚠️ The rebuilt guard traced the SQL and **still passed**, because it attached the trace to `storage.conn` after `__init__` had already run the migration it was watching — ⭐ *the observer has to be in place before the thing it observes*; `sqlite3.connect` is now patched so the connection is traced from the moment it opens. Same family as ADR-189 and ADR-191 §1, one turn worse: a fixture where **both outcomes are the lucky case**. One mutant survives and is **recorded as equivalent** rather than papered over (the early `kickoff_time` guard is a fast path; the `except` returns `None` regardless). ⏳ Preserves the future, not the past — the 11-season archive import stays **gated on licensing** (`vaastav` reads NOASSERTION, which matters more under AGPL with a donations question open), and one season is not a training set: **Phase 0b measures the minutes model we already have, so anything learned later has a number to beat** | ✅ Accepted |
| [202](./ADR-202-the-minutes-baseline.md) | **The minutes baseline, and three answers to one question** — ML roadmap **Phase 0b**: measure the model we have, so anything learned later has a number to beat. ⭐ *A model with no baseline is not an improvement, it is a replacement.* 📊 **The baseline** (2026/27, rounds 1–4, walk-forward, deterministic and re-run identical): minutes MAE **24.0** on owned ≥1% · start/bench call **70.1%** · points ranking **ρ 0.605** · hit@20 **0.17** · 1 SE **0.040**. ⭐⭐ **THE APP'S MINUTES MODEL IS BEATEN BY "SAME AS LAST WEEK"** (20.5 MAE, 77.3% start calls) in **all three rounds where a comparison is possible**, at **every** ownership cut — and above 3% even the *constant 90* calls starts better. ⚠️ Its number is **flattered, not penalised**: `chance_factor` reads TODAY's injury status and applies it retrospectively (195 of 659 players carry one), a leak that **cannot be closed** because FPL serves availability as a *now* field; stripped of it the model is 25.9/+19.4/69.9%. **Diagnosis corrected once**: the first cut compared rounds where the in-season term fired against rounds where it fell back, and GW4 reversed — ⚠️ **that compares two POPULATIONS, not two methods** (the fallback group is the erratic-minutes group, harder for anyone). Re-run as an A/B on the *same* players: this season **20.0** vs last season **32.1** vs last GW **19.4** — ⭐ *a comparison between two groups is a fact about the groups until you run both methods on one of them*; the in-season term is good, **the historical fallback is the weak part**, and the model reaches for it 35–52 times per round. ⭐⭐ **BUT ACTING ON THAT TABLE WOULD HAVE BEEN WRONG: A TERM CAN BE A BAD PREDICTOR OF ITS OWN QUANTITY AND STILL BE A GOOD FEATURE** — scored on the points ranking the app actually produces, the weight is worth **+0.117 ρ (2.9 SE)** over not having it, in all four rounds: the largest single effect measured this season (form +0.005, clean-sheet −0.007). *MAE on minutes is not the question the app asks; a constant has zero variance, so it can win MAE and be unable to rank two players at all.* ⚠️⚠️ **AND THEN THE SAME QUESTION — "is a better minutes model worth building?" — GAVE A THIRD ANSWER.** Three instruments, each executed correctly: swapping in a better forecaster said **+0.018 (0.4 SE), no headroom**; an oracle on actual minutes said **+0.206 (5.1 SE), large**; the oracle net of appearance points said **+0.061 (1.5 SE), modest**. **#1 was a FALSE NEGATIVE** — the substitute is 15% better on owned players but a **dead heat board-wide**, and the ranking is board-wide (⭐ *a substitute that is not better on the population being scored cannot test whether better helps* — ADR-195's lesson again: **a null is a statement about the instrument as much as about the world**). **#2 is INFLATED** — FPL pays 1 appearance point for 1–59 minutes and 2 from 60, so ⭐⭐ **AN ORACLE OVER AN INPUT THAT IS ALSO A COMPONENT OF THE OUTPUT IS PARTLY GRADING ITS OWN ARITHMETIC.** ⭐⭐⭐ **THE SAME QUESTION, MEASURED THREE WAYS, ANSWERED "NOTHING", "HUGE" AND "MARGINAL"** — ⭐ *when a measurement will decide whether to build something, measure it more than one way before you believe the first one.* ✅ **Phase 1 keeps its place with a ceiling instead of an assumption** (+0.206 on what the app ranks, +0.061 of it information) and a **cheap gate in front**: build a forecaster better **board-wide** and re-run #1 — the test #1 was meant to be. 🅾️ **ADR-192's constant NOT SET, and that is the finding.** Direction confirmed and large — on the 130 players with no past seasons the model runs **+39.1 minutes optimistic** and calls start/bench at **49.2%, a coin flip** — but sweeping the unknown case 1.0 → 0.4 declines ρ **monotonically on the whole board AND on the sub-population it is scoped to** (0.710 → 0.686), and the term is **live** (self-ρ 0.984, not ADR-190's unreachable 0.9999). ⭐⭐ **THE ERROR IS REAL AND CORRECTING IT DOES NOT HELP** — the minutes error is largest on players who never appear, and they already rank at the bottom for other reasons; ⭐ *an input can be measurably wrong in a place where the output does not care.* The owner's actual complaint **does** reproduce — across 80 top-20 recommendation slots a player with no history appears **once**, at 1.0, and **scored 0**; at 0.8 he does not, and the mean top-20 return rises 4.11 → 4.26 — **but that is n = 1**, and setting a constant on it would breach ADR-199's own rule two weeks after writing it. 📅 **Re-measure at the GW8 review (on or after 2026-10-26).** ⚠️ Limits stated: four gameweeks, one season, GW4 anomalous for everyone; and a `status`/`chance` snapshot per refresh is **worth adding before GW8** so the next baseline has no leak | ✅ Accepted |
| [203](./ADR-203-availability-is-recorded-as-it-passes.md) | **Availability is recorded as it passes** — ADR-202 published its headline with an asterisk it could not remove: `chance_factor` reads **today's** injury status and applies it retrospectively to round 1, and that **cannot be made walk-forward** because FPL serves availability as a *now* field — no history endpoint, no "as of", nothing to backfill from. `players` holds one mutable row each, so **every refresh overwrote the previous answer**. **195 of 659 players carry a flag today**, and against a five-week-old round essentially all of them are wrong in both directions. ⭐ **THIS IS ADR-201 AGAIN** — not a rollover, but the same shape: *a field that only ever holds the present, quietly destroying the past on a schedule* — and the same consequence, because the GW8 review would have re-run the baseline and reproduced the identical asterisk. ⭐ *The observer has to be in place before the thing it observes.* ✅ Built as a **change log, not a poll log** (a row per player per refresh is ~480k a season and carries nothing extra; live data confirms a second refresh eight hours later adds **0** rows). ⭐ **BUT A CHANGE LOG ALONE CANNOT TELL "UNCHANGED" FROM "NOT OBSERVED"** — three silent weeks and three stable weeks produce identical tables, and that ambiguity lands squarely on the measurement this exists for — so each row is an **interval**: `observed_at` when first seen, `last_seen_at` when last confirmed, both returned *with* the value because **a caller that cannot see the staleness will treat a three-week-old status like this morning's**. ⚠️ **`observed_at`, NOT `changed_at`, and the distinction is the honesty of the table**: ADR-201 stamps a match with its own kickoff on the principle that *a record about a point in time takes its timestamp from the event* — availability has no such timestamp, the flag may have gone up hours before the refresh that saw it, so ⭐ **the same principle produces the OPPOSITE column name here, because it is about what is actually known and not about preferring one clock to another.** `news` counts as a change (*"75% chance"* → *"back 20 Sep"* is new information at an unchanged status). ⚠️⚠️ **THE RECORDER WOULD HAVE CRASHED THE REFRESH** — a codeless player let a `NOT NULL` violation propagate out of `refresh`, taking teams, players, fixtures and Elo with it: ⭐ *the failure path of a side-record must not take down the thing it observes.* **Found by the test fixture, which is the interesting part** — the shared bootstrap sample carries eight keys and no `code`, so it **is** the pathological payload and four existing ingest tests went red on wiring; ⚠️ **and that same thinness then made the wiring test VACUOUS** (*"every player has an observation"* against a payload where none can be keyed asserts `0 == 0`), so ⭐ *a fixture that cannot express the field under test turns the guard into decoration* — codes injected, `> 0` asserted, and the codeless case promoted to its own test. ⭐ **Testing a component is not testing that anything uses it**: all six writer tests passed with the call absent from `refresh`. 7 mutants, 7 red — ⚠️ **though the first sweep reported NOTHING and looked like a pass**: it ran under **zsh**, which does not word-split unquoted expansions, so the test paths reached pytest as one impossible argument — ⭐ *a mutation sweep that cannot fail is worth less than no sweep, because it reports confidence.* ⏳ **Fixes the future, not the past** — ADR-202's numbers keep their leak permanently and the two baselines are **not comparable on that axis**; rounds 1–4 have no observations and never will; nothing reads the log yet (⭐ *a recorder with no reader is worth building when the data is perishable, and only then*); resolution is the refresh cadence | ✅ Accepted |
| [204](./ADR-204-the-phase-1-gate.md) | **The Phase 1 gate — a candidate that works, and does not clear the bar** — ADR-202 left a specific test, because its own premise test was a **false negative**: build a minutes forecaster better **board-wide** (the substitute was better only on owned players, and the ranking is board-wide), then re-score. It also left a diagnosis, so this had a candidate rather than a guess — the weak part is the **historical fallback** (32.1 MAE vs the in-season term's 20.0 on the same players). ✅ **The candidate is shrinkage instead of a switch:** ADR-173's rule is all-or-nothing by design (*played every completed gameweek, or fall back to last season*) and its worry is real — two gameweeks cannot tell a rested player from a phased-out one — so `share = (n·this_season + k·last_season)/(n+k)` answers the same worry **continuously** rather than categorically. **It clears the gate**: board MAE **24.5 → 20.9** (−14.5%), start calls **73.5% → 78.8%**. ⚠️ **And it is WORSE on owned players** (24.0 → 25.0) — the exact mirror of the substitute it replaced: ⭐ **"MORE ACCURATE" IS A CLAIM ABOUT A POPULATION, AND TWO FORECASTERS CAN EACH BE MORE ACCURATE THAN THE OTHER** — neither candidate dominates, there is no single "better minutes model" to point at. 📊 **Scored on the ranking: ρ +0.016 = +0.4 SE. §B0's bar is +1 SE. NOT SHIPPED.** Every other criterion moves the right way — MAE 1.23 → 1.14, hit@20 0.17 → 0.26, monotone across three adjacent `k` — and ⭐ *that is not enough: the bar exists precisely to stop four gameweeks of movement in the right direction from becoming a change.* ⭐⭐ **THE STRUCTURAL FINDING, RECORDED AND NOT ACTED ON: hit@20 went 14 → 21 hits in 80 slots (+2.1 SE, improving in all three rounds where the forecasters differ at all — GW1 is identical by construction), and top-20 mean return rose 4.11 → 4.94, +20% per recommendation slot. Against ADR-202's oracle that is 93% OF THE ORACLE'S TOP-OF-BOARD GAIN ON 8% OF ITS ρ GAIN — BETTER MINUTES IS WORTH FAR MORE AT THE TOP OF THE BOARD THAN ACROSS IT**, and the top is the only part a recommendation reads. ⚠️ Reported, not used: ADR-190 declined a term whose `hit@10` rose while ρ fell, and ⭐ *a rule that only binds when it agrees with you is not a rule.* 🔁 **What it says about ADR-192:** that ADR proposed demoting players with **no past seasons**; the blend demotes players **not playing**, and the two disagree about who — the blend puts **MORE** cold-start players in the top 20 (3 vs 1) and the recommendations get **better** (+0.83 pts/slot against the demotion's +0.15, **5×**). ⭐⭐ **THE VARIABLE ADR-192 IDENTIFIED WAS "NO HISTORY"; THE VARIABLE THAT MATTERS IS "NOT PLAYING", AND THEY ARE NOT THE SAME PLAYERS** — Affengruber had no career *and had just played 90*, so ADR-192's rule demotes him and the blend does not, and the blend is right: ⭐ *a correct complaint can name the wrong variable, and the fix aimed at the named variable will underperform the fix aimed at the real one.* 📅 **GW8 RULE PRE-REGISTERED NOW, BEFORE THE DATA EXISTS** — ship if ρ ≥ +1 SE, **or** hit@20 ≥ +2 SE with ρ not falling, both across ≥2 adjacent `k`. Clause 2 is a **deliberate, argued widening of §B0 and not a loophole**: §B0 was written to switch on a **dormant term**, where a whole-board ρ is the right question; this replaces the internal method of a term **already on and already earning 2.9 SE**, and the oracle decomposition that predicts a top-of-board concentration was run *before* this result existed — the reason is independent of the outcome, which is the only thing that makes it admissible. ⚠️ **`k` must be re-fitted at GW8, not carried over** — ⭐ *a parameter selected on the data it is tested on has already spent some of its result.* **If neither clause is met, Phase 1 is declined for the season.** ⚠️ Holding has a named price: if the effect is real, eight weeks of recommendations are ~0.8 pts/slot worse than they need to be | ⏳ Held — re-decided at the GW8 review |
| [205](./ADR-205-the-archive-licence.md) | **The archive licence — the blocker was mine, not the repo's** — the owner asked how to resolve the `vaastav/Fantasy-Premier-League` licence, and the answer was to read the file. ⚠️ **IT IS MIT-LICENSED.** GitHub's API reports `"spdx_id": "NOASSERTION"` because its detector does a near-exact template match and this `LICENSE` has **two sentences appended** — *"The data provided is property of fantasy.premierleague.com and understat.com / I don't own any of the data"* — which is enough to defeat the classifier. ⭐⭐ **I READ A DETECTOR'S FAILURE TO CLASSIFY AS THE AUTHOR'S FAILURE TO GRANT, AND WROTE IT DOWN FOUR TIMES AS THOUGH IT WERE A FACT ABOUT THE REPOSITORY** — `NOASSERTION` means *"this tool could not determine a licence"*, not *"no licence was asserted"*, and ⭐ **a field whose name states a conclusion will be read as that conclusion.** It gated ML Phase 1 for a month and cost **one HTTP request** to dissolve — the same length as the request that produced the flag I trusted instead. ⭐ *A metadata field about a document is not the document.* 🎯 **The real question is the one the maintainer raises himself: he cannot license data he does not own** — and it splits cleanly. **FPL-derived files are the same source the live app already calls every refresh**, so ⭐ *importing them changes the age of the data, not its provenance*; whatever position the project holds for the live app applies unchanged, and if it did not, that would already be a live problem rather than a future one. **Understat is quarantined** in `data/<season>/understat/` and simply not imported. ⚠️ **The obvious leak was tested rather than assumed**: `expected_goals` and friends look exactly like the columns Understat is famous for, and they appear in `merged_gw.csv` **only from 2022-23** — absent in 2016-17, 2019-20, 2021-22 — which is precisely when **FPL's own API began serving expected stats**. *The column that would have been the leak is the column that proves there isn't one.* ✅ **Cleared for Phase 1**, scoped to `gws/merged_gw.csv` + `player_idlist.csv` (for the stable `code`), **2016-17 → 2026-27**, columns a near-exact match for `player_history`; MIT attribution goes in `NOTICE` if it is imported. 👤 **One call remains and it is the owner's — FPL's own terms** — which the archive does **not** create: it already applies to every refresh, and what changed is the stakes (AGPL + an open donations question), so ⭐ *treating it as a blocker on the archive confuses a decision about the project with a decision about a dependency.* ⚠️ Limits recorded: the archive is one maintainer's scrape with documented caveats (`xP` scraped from `ep_this` **after** the gameweek on an undocumented cadence), **six of eleven seasons have no expected stats at all**, `defcon` exists in one, and ⭐ **eleven seasons of data is not eleven seasons of the same game** — the scoring rules changed across it and the archive is silent about that, being a record and not an explanation. ⭐⭐ **THE LESSON: A BLOCKER I RECORDED FOUR TIMES WAS DISSOLVED BY READING THE FILE IT WAS ABOUT — it survived because it was WRITTEN DOWN, each repetition citing the last, and a claim in four documents reads as established rather than as repeated.** ⭐ *Provenance is a property of the first assertion, not of the count* — third sighting after ADR-157 and ADR-184 | ✅ Accepted |
| [206](./ADR-206-a-doubt-is-a-probability.md) | **A doubt is a probability, not a verdict** — owner, on RoboTS's week: *"it's picking up that there is an issue with João Pedro, which is great. However we are coming into an international break, **he is hot**, so I would not sell him."* The app was recommending **João Pedro → Havertz at +4.7 XI xP, Confidence 95/100**, reasoning *"higher projected points (4.9 vs **0.0**)"* — for a player on **8.2 ppg, joint-highest of any forward in the game with Haaland**, four consecutive 90s, **74.5% owned**, against a 4.2-ppg replacement. ⚠️ **Not a judgement call he disagreed with — a mechanism fault.** `xp._status_is_active` was `p["status"] == "a"`, so **every doubtful player scored exactly 0** and **a 75% doubt priced identically to a 25% one** (23 players that day). Meanwhile `chance_factor` (ADR-038) computed the correct **0.75** and deliberately excluded `d` from its unavailable set — and never got to apply it, because the binary gate ran first. ⭐⭐ **TWO MECHANISMS MODELLED THE SAME THING AND THE CRUDER ONE RAN FIRST**, making the finer one dead code for exactly the population it was written for. ⭐ **ADR-006's reasoning had EXPIRED, not been wrong** — it justified the binary with *"`chance_of_playing` is barely populated, so `status` is the signal"*, true in preseason when the column was empty; FPL now populates it and nothing re-checked: *a constraint recorded in an ADR is a fact about a version, not a law* (ADR-180, 2nd time it has cost something). ⚠️⚠️ **AND SOMEONE HAD ALREADY FOUND IT AT ONE CALL SITE** — `captain.py` carried `is_available=lambda p: not is_unavailable(p)  # count doubtful, not only 'a'`, a local override naming the exact bug, written for captaincy and left broken for transfers, lineup, the week's plan and every board: ⭐⭐ **A WORKAROUND AT ONE CALL SITE IS A BUG REPORT NOBODY FILED** (ADR-181's family). Also found **three definitions of "unavailable" that disagreed** (`xp`: only `a`; `minutes`: `{i,s,u}`; `optimizer`: `{i,s,u,n}`) — ⭐ *a fix in one of three definitions is a fix in one third of the app* — now **one**, in `minutes`, re-exported by `optimizer` so all **sixteen** call sites are untouched. ⚠️ `"n"` added, closing a hole the fix would otherwise have OPENED: an unregistered player's `chance` is None, which `chance_factor` reads as *"assume available"* → **1.0**. 📊 **Doubtful players projecting > 0: 0 of 23 → 21 of 23**; João Pedro **0.0 → 4.3** next GW and `{0,0,0,0,0}` → 21.1 over five; **the recommendation goes +4.7 → +0.6, inside the noise floor** — a completely different sentence. ⭐ **Discounted, not deleted**: no doubtful player enters the board's top 10, and he still sits *below* Havertz. ⚠️ **THE WHOLE SUITE STAYED GREEN** — 1842 tests, none red, while 23 players moved off zero and the headline reversed; nothing had ever asserted what a *doubtful* player is worth, only that an unavailable one is worth nothing: ⭐ *"all tests pass" after a behavioural change is a statement about the tests* (2nd time in two days, after ADR-195). 7 guards, 5 mutants, all red. ⭐⭐ **THE LESSON AND ITS MIRROR: ADR-192 found *no opinion* implemented as the most OPTIMISTIC opinion available (no history → multiplier 1.0, the maximum); this is the same failure pointing the other way — *a doubt* implemented as the most PESSIMISTIC verdict available (75% likely, priced 0, the minimum). WHEN A MODEL EXPRESSES UNCERTAINTY AS A NUMBER, CHECK WHICH EXTREME THAT NUMBER IS — it will be one of them, in whichever direction nobody was looking.** 📋 **§2 (a flag is a fact about NOW, priced across the whole horizon — 0.75 still applies to GW9 in November) and §3 (fixture gaps: GW6 is **19 days** after GW5) are PROPOSED and deliberately NOT built** — ⭐ **§3 falls out of §2 for free if the decay is measured in DAYS UNTIL KICKOFF rather than gameweeks ahead**, so an international break needs no concept of its own; gated on the owner and on a decay shape that must be **measured, not invented** 🔧 **§2 AND §3 BUILT THE SAME DAY, and the curve turned out not to be needed.** The plan was a **measured decay** — impossible, because it needs a history of flags against what players went on to do, and ADR-203 began recording that *on the day this was written*; waiting meant a month of known-wrong projections. ⭐⭐ **THE QUESTION WAS NOT "HOW FAST DOES A DOUBT DECAY" BUT "WHAT IS A DOUBT EVIDENCE ABOUT"** — and the second needs no curve: FPL publishes `chance_of_playing` about the **upcoming** match (a *now* field, no "as of"), so applying it five weeks out **states something the source never said**. The discount holds inside the window and the player reverts beyond it — a **declared scope, not an invented decay**. ⭐ **§3 then COSTS NOTHING because the reach is in DAYS, not gameweeks**: an international break needs no concept of its own (GW6 kicks off **19 days** after GW5), and the same mechanism covers a cup week, a postponement or a reschedule — ⭐ *a rule expressed in the unit the world actually varies in needs no special case for each way it varies.* `FLAG_HORIZON_DAYS = 8`, **declared not measured**, with a re-measure date at the GW8 review. ⚠️ **The rate tier is computed TWICE rather than the answer divided by the chance factor**: `cold_start` carries the weight *inside* its rate non-linearly (ADR-124), so the naive rescale would be wrong for exactly the players with least evidence — a guard pins it by asserting the far gameweek is **not** the rescale. 📊 João Pedro's five gameweeks go `{0,0,0,0,0}` → `{4.3,4.3,4.3,4.3,3.9}` → **`{4.3, 5.7, 5.7, 5.7, 5.2}`**, and **the recommendation now reads honestly in both directions: +0.6 over one gameweek, −2.4 over five** — which is what the owner said, and what the model can now say for itself. Unflagged control untouched. 12 mutants, 11 red + **1 recorded equivalent** (`< 1.0` → `<= 1.0` gives unflagged players a far regime, but `weight/1.0` is byte-identical). ⏳ The **gate** is still undecayed — an injured player stays 0 all horizon, since FPL's return dates live only in free-text `news`; out of scope, and guarded so the lift cannot leak to him | ✅ §1/§2/§3 all Accepted |
| [207](./ADR-207-what-would-it-take.md) | **"What would it take to field X?"** — closes ADR-191 §3, gated there as *"a new query shape… the one with no measurement at all behind it yet."* The app answers a **squad-driven** question (*for each player I own, what is the best replacement?*); the owner kept asking a **target-driven** one, **twice, unprompted, three days apart** — *"is Haaland a better option than Bruno and figure the moves needed to select him"* and *"I would use my transfers to see if I can get another forward in that is scoring rather than replacing with Havertz."* ⭐ **In this project a repeated unprompted request has been a reliable signal** (the reported-departure fact needed teaching on six surfaces, every one found by the owner using the product). Nothing answered it: `suggest_transfers` iterates the **outgoing** players, so a *named* incoming player appears only by luck — and the shortlist is deliberately **disjoint** (ADR-040), so showing up against one sale removes him from consideration against the others. ✅ **`route_to_player` fixes the incoming player and searches the ROUTE** — which of your fifteen to sell. **Three design decisions, each a refusal to do the obvious thing:** ⭐ **ranked by the effect on the starting XI, NEVER by which sale frees the most money** (ADR-191 §2 measured that failure from the other direction — the best pair routed *the same incoming player through a different sale*); ⭐ **a blocked route is INFORMATION, not an absence** — *"sell João Pedro, short by £0.6m"* is exactly ADR-186's *worth saving for*, and printing only what cleared hides every near-miss; and ⭐⭐ **NEGATIVE ROUTES ARE RETURNED LEAST-BAD FIRST AND NEVER SUPPRESSED, BECAUSE A TARGET-DRIVEN QUESTION IS ONE THE READER HAS ALREADY HALF-ANSWERED — THE JOB IS TO PRICE THE WISH, NOT TO GRANT OR REFUSE IT** (suppressing them answers *"is this wise?"*, which nobody asked; same posture as ADR-194 handing the lineup call back). 📊 **Verified on a real squad BEFORE building**, and the run changed the design: the first version printed only the one route that cleared, so Isak's **£0.6m** near-miss was invisible — ⭐ *verifying a design on real data is how you find the output you did not know you wanted.* ⚠️ Scope stated up front: **one transfer** (the two-transfer form overlaps ADR-191 §2, still gated), **CLI only** — and ⭐ *testing a component is not testing that anything uses it*, since all eight function guards passed while it had **no caller at all**, so the wiring carries its own test. A reported leaver is valued at zero when ranking routes (ADR-153/156). 7 mutants, all red | ✅ Accepted |
| [208](./ADR-208-cover-is-part-of-the-advice.md) | **"Bench him" assumed a bench worth using** — owner, after ADR-206 stopped the app selling his doubtful joint-best forward: *"that's as designed, so good. **BUT where we have a gap is that the other 2 fwds are zero to low scoring**, we should be getting a backup recommendation in there."* The plan charged **8 confidence points** for *"João Pedro is flagged — **bench or replace him**"*. ⭐⭐ **THE ADVICE ASSUMED AN OPTION IT HAD NEVER CHECKED**: FPL auto-subs a 0-minute starter with the first legal bench player in his position, and on RoboTS that is **Walle Egeli at 0.2 xP** — benching a doubtful 4.3 to field a 0.2 is not a mitigation, it IS the loss. ⭐ *An instruction is only as good as the option it assumes you have.* 🔬 **The deeper fix was measured and NOT built.** The model folds the 75% into the player and *then* picks the XI — `max(E)` — while a manager lives in one of two worlds each with its own best XI, `E(max)`, diverging by exactly `(1−chance) × what the auto-sub brings in`. On a squad built to the owner's description the app priced cover at **+0.30** against a true **+1.35**. ⚠️ **Then 64 realistic squads said ZERO** (mean +0.00, max +0.08, **0 of 40** flips) — ⭐⭐ **AND THE NULL WAS ABOUT THE POPULATION, NOT THE EFFECT**: conditioning on the best available upgrade showed **not one** of those squads had a best move under **1.5**, the band RoboTS is in (+1.2). Third time in two days after ADR-195 and ADR-202 — ⭐ *the tell is always "does the population contain the case?"* So the E[max] re-ranking stays **ungated, not declined**: it re-prices every transfer for every squad, leans on FPL's coarse 25/50/75 whose accuracy ADR-206 flagged as unmeasured, and ADR-132 warned more moves is more ways to be wrong — ⭐ *a mechanism confirmed on one squad and unmeasurable on the available population is a hypothesis, not a feature.* ✅ **Built instead: say it, don't re-rank it.** `auto_sub_cover` answers *who actually comes on, and what is he worth*; three cases **deliberately worded apart** — cover exists (*"benching him fields Walle Egeli (0.2 xP)"*), **no** cover (*"replace him — you have no cover on your bench"*), already benched (*"already on your bench"*). ⭐ **"No cover" is a different problem from "weak cover"** (one is solved by a transfer, the other by a judgement call), and the third case removes advice that *described the state the reader was already in*. **No re-ranking, no new threshold** — the trigger is the existing flag, so it adds no constant needing provenance and cannot fire where the app was silent. ⚠️⚠️ **The first smoke run on the real squad caught a bug**: *"benching E.Le Fée fields **Foden** (0.0 xP)"* — about a **suspended** player who would never come on. ⭐⭐ **A FALLBACK THAT SHARES THE FAILURE IT IS COVERING FOR IS NOT A FALLBACK.** ⚠️ **3 of 8 mutants survived the first sweep, all coverage** — including, for the third time this week, **the old unqualified advice could be restored with every test green**, because they called `_flag_action` directly and the reader never does; plus a positionless-row guard whose fixture benched only rows *with* positions, and a starters-only guard whose fixture had no fit substitute, so "no cover" held for the wrong reason: ⭐ *a guard is only tested by the case it exists to catch.* The shared gameweek fixture also carried **no `position` at all** — 5th fixture this month modelling less than reality. 📋 **A SECOND GAP FOUND AND NOT FIXED**: the app recommended Le Fée → Dewsbury-Hall at **+1.2 next GW / −1.5 over five**, while **Walle Egeli → Barry is +1.2 next GW / +3.1 over five** and turns 0.2 of cover into **4.5**. The two **tie on the number it ranks by**, and it *computes and prints* the number that separates them without letting it choose — ⭐ *a ranking that separates two options by less than its own noise is picking one* (ADR-189) and ⭐ *a number computed for one question and discarded is invisible in a way a missing one is not* (ADR-191). Proposed, gated | ✅ Accepted · E[max] + the tie-break proposed |
| [209](./ADR-209-the-longer-view-breaks-the-tie.md) | **The longer view breaks the tie, and the band fits its window** — closes the gap ADR-208 found: the app recommended **E.Le Fée → Dewsbury-Hall (+1.2 next GW)** and printed *"Longer view: **−1.5**"* beside it, over **Walle Egeli → Barry (+1.2 next GW, +3.1 over five**, and cover 0.2 → 4.5). **The two tie on the number it ranks by**, and it computes the number that separates them, prints it, and never lets it choose. 🔬 **Measured first, and it reframed the change**: across **96 realistic squads** the gap between the best and second-best next-GW move has a **median of 0.20** (p90 0.40) against a per-player weekly sd of **3.51** — ⭐⭐ **ON A ONE-GAMEWEEK WINDOW THE TRANSFER RANKING WAS NEVER RANKING, IT WAS PICKING.** Something else was always going to decide; the only question was what. ⚠️⚠️ **AND SOMETHING ALREADY WAS, UNNOTICED**: `TIE_NOISE = 2.0` was sized for a **five**-gameweek window (its own comment says so) and applied to **every** window, and My Squad narrows the horizon toward the deadline — at one gameweek that band catches **100%** of best-vs-second pairs, so **ADR-189's correlation rule had silently been deciding EVERY one-gameweek recommendation**, a rule ADR-189 itself established *never moves expected points*. ⭐⭐ **THIS IS THE MISTAKE THE CODEBASE ALREADY WARNS ABOUT THREE FUNCTIONS AWAY** (*"a threshold sized against one window applied to another"*, ADR-186, written about the cliff while the tie-break beside it did the same) — ⭐ *a lesson recorded about one number does not check the others.* ✅ **(1)** `tie_noise(window)` scales by **√window**, not linearly — the error in an N-GW total grows as √N if weekly errors are roughly independent, so a band that stays the same *fraction of the noise* scales the same way; **derived, not measured** (the independence is an approximation and the covariance cannot be estimated yet), and it **reduces to exactly 2.0 at five GWs so no existing caller moves**. ✅ **(2)** within the band the **longer view is asked BEFORE correlation**, deliberately: ADR-189 established correlated defence widens *spread* by √2 (~1.2 pts at worst) and never moves expected points, while the longer view moves **expected points** — ⭐ *a tie-break on expected points strictly dominates one on spread, so it must be asked first.* ✅ **(3)** no wider map → a constant for every candidate → a clean tie, falling through to the old rule. 📊 **What it trades, 96 squads**: fires on **94%**, gives up **−0.30** next GW (worst −0.80, band 0.89), gains **+7.04** over five (worst **+0.80**), and **0 of 90 trades lose over five** — ⭐ *94% is not a wide band, it is an honest one: with a median separation of 0.20, a tie-break that fired rarely would be wrong about how ambiguous the input is.* ⚠️⚠️ **THE INTERACTION STATED, NOT BURIED**: on the live squad it chose **Affengruber** — exactly ADR-192's case (`cold_start`, **0 past seasons, 2 appearances**, ppg 8.0 on **n=1**, 22.4 xP over five). ⭐⭐ **A TIE-BREAK INHERITS THE BIAS OF WHATEVER NUMBER IT BREAKS THE TIE ON.** Shipped anyway because it does not *introduce* the dependence — `ask` already ranks on the five-GW map, so the inflation already drives primary recommendations, and here it acts only **within noise**; measured, cold-start top picks go **4 → 6 of 96**. 📌 **It raises ADR-192's priority** — a second, independent reason to fix the input rather than the symptom. ⚠️ The owner's exact pasted pair could not be reproduced by ship time (fresher data made a suspended Foden the obvious top move); the mechanism is shown on the population and on his live second move. 8 mutants, 7 red + **1 recorded equivalent** (any constant is a clean tie, not just 0.0) | ✅ Accepted |
| [210](./ADR-210-a-threshold-reads-the-distribution-it-describes.md) | **A threshold reads the distribution it claims to describe** — closes the gate ADR-190 left open (*"a live percentile for the exodus threshold, replacing the fixed constant"*), the one open item on the roadmap that was **not waiting on gameweeks** and could never be unblocked by them. ADR-146 defined the exodus bar as **the worst tenth** of selling pressure, measured it once on live GW1 data and shipped **`EXODUS_PRESSURE = -8,000`**. ADR-190 re-measured it at GW4, got **−3,901**, correctly refused to set a new value on two samples 51% apart — and recorded the reason as *"it varies."* ⭐⭐ **IT DOES NOT VARY RANDOMLY: `transfers_in_event`/`transfers_out_event` ARE A COUNTER THAT RESETS AT EVERY DEADLINE AND FILLS UP ACROSS THE WEEK**, so `price_pressure` is an **accumulation**, not a level, and its distribution is a function of *how far into the gameweek you look*. The two "noisy" samples are **two points on a ramp**: ADR-190 read ~1 day after a deadline (p10 −3,901, the constant flagging **2 of 190**); this ADR read ~5 days in with the next deadline imminent (p10 **−14,992**, the same constant flagging **50 of 188**). ⭐ **A FIXED THRESHOLD ON AN ACCUMULATING COUNTER DOES NOT ENCODE A SEVERITY — IT ENCODES THE HOUR OF THE WEEK THE CALIBRATION HAPPENED TO RUN**, and the Signals page paid for it: *"An exodus we can't explain"* listed **33 players** on deadline day (a page of names, which is not news) and about two on a Sunday. ⭐ **The constant and the definition had come apart and only the constant was executable** — ADR-146 wrote *"the worst tenth"* in a comment beside a literal that stopped obeying it; the third time this trail has recorded that shape after `CLEAR` (ADR-190) and `TIE_NOISE` (ADR-209), which said **nine days ago** that *a lesson recorded about one number does not check the others*. ✅ **(1) `exodus_threshold(players)`** — the worst tenth of the **live** board, immune to the ramp by construction rather than by calibration: the *number* stops being stable across the week and **the fraction it flags becomes stable instead**, which is what the definition always claimed. Three guards were needed before a percentile was safe: ⭐ **a sign guard** (every distribution *has* a bottom tenth, so a bare percentile reports an exodus in a week when the whole board is being **bought**, and flags a tenth of the league preseason on flat zeros — *the percentile decides how severe, the sign decides whether there is anything to be severe about, and only the second can answer "no"*); a **population floor** (`MIN_EXODUS_POPULATION = 20` — a tenth of nine players is not a tenth of anything); and ⚠️ **the right population** — ADR-150's 1% ownership floor now *also* defines the set the cut is taken over, and the two must be the same set, because `price_pressure` divides by ownership so a 0.1%-owned player reads at −500,000 per 1%, occupies the entire bottom tenth, and **silences the flag for the template players it exists to warn about** — ⚠️ *that failure is silent, not loud.* ⭐ Where it cannot answer it returns **None** and the flag does not fire — ADR-192's direction applied **on purpose** this time: the "no opinion" value is the one that says nothing, not the one that flags everybody. ✅ **(2) `player_transfer_flow`** — **ADR-203's table, for the one other quantity that was still expiring.** The counters and `selected_by` are *now* fields on the mutable `players` row; FPL resets them at each deadline and publishes no history, so every refresh destroyed the only copy — which is *why* ADR-190's own instruction (*"re-measure on ≥4 gameweeks"*) had nothing to run on and silently produced a second one-week sample. ⭐⭐ **EVERY ROW CARRIES `hours_to_deadline`, AND THAT COLUMN IS THE WHOLE POINT: A VALUE WITHOUT ITS POSITION IN THE CYCLE IS NOT A MEASUREMENT** — it is exactly what ADR-190 had, and why it could only conclude *"it varies."* Keyed by the event the counter climbs **toward** (⚠️ not the one being played), upserted so each event settles on the **last reading before its deadline**, the one point comparable across weeks. 📊 **Live board, ~6h before the GW5 deadline: Signals goes 33 → 7**; smoke-tested on the owner's real squad (*"Hume (53,752 sold him this week — nothing in the data says why)"*). ⚠️⚠️ **STATED, NOT BURIED: `EXODUS_PERCENTILE = 10` HAS NO MORE PROVENANCE THAN −8,000 DID** — it is ADR-146's own stated definition rather than a new claim, and that is the only thing that makes it better today; 📅 **re-asked at the GW8 review (on/after 2026-10-26)** when ≥4 events of end-of-cycle readings exist and like can be compared with like for the first time (ADR-199's rule applies to the new number exactly as it did to the old). ⚠️ A bug caught **before it ran**: `next_deadline` indexes its rows because every other caller hands it `sqlite3.Row`s, while `refresh` holds `Fixture` **dataclasses**, which are not subscriptable — so the test uses the dataclass `refresh` actually builds (*a fixture that cannot express what the code reads will confirm a broken mechanism*). `crowd_exodus(player, threshold)` takes it **required** so a forgotten call site **raises** instead of quietly inheriting August (ADR-181), and the guards **sweep** rather than list (ADR-184): no direct call in `src/`, every detector bound to the whole board, the retired constant unimportable — the last checked on *code* and not on the word, since the name still appears in the prose that records why it went (⭐ *a history of a decision is not a repetition of it*, ADR-178). **12 mutants, 12 red** — two survived the first sweep and both were real gaps: the `threshold is None` path had no test at all, and the ownership floor was tested on the **list** but never on the **population** | ✅ Accepted |
| [211](./ADR-211-the-pipeline-runs-without-you.md) | **The pipeline runs without you** — ✅ **BUILT, all six stages (2a–2f).** Phase 2 of the mobile platform plan, and the item the Phase 1 audit called the largest single piece of new work: today data reaches users only when the owner runs `reseed` and commits a 956 KB binary, so ⭐ **the human is a load-bearing component** and between commits the deploy serves whatever was last pushed. ⚠️⚠️ **THE COST WAS DEMONSTRATED LIVE THE SAME DAY**: ADR-210 shipped a table to catch the transfer counters before FPL resets them at each deadline, the GW5 deadline passed at 17:30Z with nothing scheduled, and **GW5's reading is gone permanently** — ⭐⭐ **AN OBSERVER THAT HAS TO BE RUN BY HAND IS NOT AN OBSERVER, IT IS A HABIT**, and *recording machinery and scheduling machinery are different problems; building the first well does nothing about the second.* 🔬 **Measured before proposing, and one number decided the architecture**: the refresh is **three jobs differing by two orders of magnitude** — core refresh **3.6 s** (FPL API only), per-GW backfill **~3.3 min** (659 throttled calls), headline extraction **64 s and 🔴 needs a local LLM** (Ollama on `localhost:11434`). ⭐ **The job that must run often is the cheap one**, which removes most of the imagined infrastructure. 🔴 **And the obvious cheap step — keep `seed.db`, just schedule the commit — is ruled out by arithmetic**: a SQLite binary delta-compresses poorly, so scheduling it costs **1.3 GB of git per year at 6-hourly, 8.0 GB hourly, 31.9 GB at 15 minutes** — ⭐ *decisive: the DESTINATION has to change, not merely the trigger*, so Phase 2 necessarily contains the Postgres migration. **The migration is smaller than it looks**: `src/analytics` imports `Storage` **nowhere**, so the coupling is one 966-line file, of which the **9 `ON CONFLICT` upserts port unchanged**, four `PRAGMA table_info` calls become `information_schema`, and the analytics' `_get(row, key)` helpers already accept **dict or Row** so psycopg's `dict_row` satisfies them untouched. ✅ **Proposed: Postgres (Supabase) as the destination, GitHub Actions as the trigger, three jobs at three cadences.** ⚠️ **The timing caveat matters BECAUSE of ADR-210** — GH cron is best-effort and the counters must be read before a deadline or the week is lost; ⭐ **mitigation is redundancy, not precision: fire at T-60/45/30/15 and let the upsert keep whichever lands** (*a cheap job does not need accurate scheduling, it needs several chances*). ⭐ **Validation must be able to say no** — today a bad FPL response costs the owner's laptop, server-side it hits every user at once, so publish only on sane player counts/fixtures/prices and otherwise **keep the last good data and alert**: *a pipeline that cannot decline will eventually publish nonsense to everyone simultaneously.* Plus a `data_status` row so freshness is visible (ADR-203/210's lesson applied to the pipeline itself). 🔴 **ONE GENUINELY OPEN QUESTION, AND IT IS THE OWNER'S CALL**: headline extraction needs a model, Cloud has none, and today the events exist only because the owner's laptop bakes them into the seed — they feed the leavers logic on **six surfaces** (ADR-151/153/155/156) plus ADR-210's exodus lede. Options: **(a)** a hosted model API — small job, but the **first paid external dependency**, needing its own provider/cost evaluation *not made here*; **(b)** keep extraction manual while everything else goes autonomous; (c) drop events — rejected, retires shipped behaviour; (d) self-host — disproportionate. ⚠️ *This is the one place the audit's "£0 until real usage" assumption does not hold.* 💡 Recommendation **(b) then (a)** — do not let the model question block the 95% that is free. Staged **2a–2f** so nothing breaks (Postgres `Storage` with the suite green on **both** backends → flagged Streamlit cutover with `seed.db` retained as fallback → scheduled refresh + validation → per-GW backfill → headline decision → retire the manual path), with ⚠️ **the refusal path proven by feeding it a deliberately bad payload**. **Exit criterion: two weeks with no manual `reseed`, including a deadline and a live gameweek.** ⚠️ **The honest cost, stated: a pipeline that runs unattended is a thing that can fail unattended — that ops burden is permanent and does not go away** | ✅ **Gated 2026-09-18** (Postgres · GH Actions · headlines manual). **2a BUILT: `src/db.py` is the seam, 1,905 green on SQLite and 1,892 + 13 skipped on Postgres 17.2, with 0 of 659 xP values differing between backends** — ⭐ *the analytics cannot tell which backend they read.* Three things actually differed, each found by RUNNING it: `?`→`%s` placeholders **plus escaping a literal `%` first** (psycopg parses placeholders in statements with no parameters, so a `1%` in a column comment stopped the schema building); a `Row` matching `sqlite3.Row`'s **named AND positional** access (`dict_row` gives only named, and `Storage` reads `fetchone()[0]` in six places — ⭐ *a row object that is nearly the same is how two backends quietly disagree*); and bool→int for the two INTEGER columns that receive Python bools. 🐛 **Two real `Storage` bugs surfaced, not test artifacts**: ⚠️⚠️ **player search was about to become case-SENSITIVE** — `LIKE` is case-insensitive in SQLite and the comment beside it stated that as a property of SQL, so on Postgres `search haaland` would find nobody (**second time in one day a case-sensitivity assumption crossed an engine boundary**, after `beta_users` in the RLS draft); and a missing table was caught by **driver class**, turning ADR-151's deliberate degrade into a crash — plus Postgres aborts the whole transaction on any error, so the adapter must roll back or recovery poisons the connection. ⭐ **The harness caught a DESIGN flaw too**: `is_postgres` read the path string, and the suite asks for `:memory:` while connecting to Postgres, so path and connection disagreed — *a derived fact should be read from the thing it describes; the path is a request, the connection is the answer.* ⚠️ **13 tests opt out and the list is PINNED by a test** (SQLite-legacy migrations + the file-copy `reseed`) — *not applicable, not handled* (ADR-178: a skip is not a pass). CI now runs **both** backends. ⚠️ **Stated limitation: Postgres has NO migration story** — its schema is created fresh, which is why `_migrate`/`_rekey_history` are skipped rather than ported, and that stops being fine the first time a column is added while it holds real data — **2b's first decision**. **2b BUILT: one variable (`FPL_DATABASE_URL`) moves the whole app onto Postgres** — it feeds `config.DB_PATH`, and since `db.py` reads the backend from that string all **eighteen** web `Storage()` call sites follow without one being edited. 1,914 green on SQLite · 1,901 + 13 skipped on Postgres; six pages rendered against Postgres with `fallback_reason()` **None**, and `refresh` wrote a live fetch into a **fresh** Postgres (662 players · 380 fixtures · 662 availability · 662 ADR-210 transfer-flow rows). ⭐ **THE VARIABLE IS THE FALLBACK** — unset it and the app is byte-for-byte what it was, which is safer than an automatic failover because *one that silently serves last month's snapshot while the pipeline is dead is the failure this phase removes*; a configured-but-unusable Postgres still renders from the seed and the sidebar raises a **warning, not a caption** (the freshness line is already a caption and reads as routine). ⭐⭐ **TWO ASYMMETRIES TURNED OUT TO BE THE DESIGN: (1) A READER NEVER CREATES THE SCHEMA** — on Postgres the pipeline owns it, and a reader running `CREATE TABLE IF NOT EXISTS` against an unexpected DSN would build eight empty tables and render *"no data"*, **reporting an empty league instead of a misconfiguration**; it probes with one query instead. **(2) A READER MAY DEGRADE, A WRITER MUST NOT** — ⚠️ caught while wiring `cmd_refresh`, not by a failing test: the fallback target is the committed `seed.db`, so a degrading refresh would write live FPL data **into the repo's snapshot** and print success. ⭐ *The safe direction to fail differs by what the caller is for.* 🐛 **AND THE GUARD SWEPT FOR THE WRONG CONSTRUCT**: `translate` rewrites `?`→`%s` and the test looked for a `?` inside a **quoted literal** — the failure I imagined — while the real one was a `?` inside a **SQL comment** (*"did the last attempt pass validation?"* in the new `data_status` DDL), which psycopg refused as *"1 placeholders but 0 parameters were passed"* so the schema would not build. ⚠️ **I wrote that comment twenty minutes after writing the guard meant to catch it** — ⭐⭐ *a guard against a claim must sweep for the claim, not for the version of it you thought of* (ADR-184, **third time**); it now asserts every surviving `%s` is a real bind, mutation-tested by restoring the naive translation. **`data_status`** makes freshness a *value* rather than a file mtime (which has no Postgres equivalent, and anyway records when someone *wrote*, never whether the write was good): `refreshed_at` moves only on a successful publish, `attempted_at` on every run — ⭐ *recording only successes would make a dead pipeline indistinguishable from a quiet one* (ADR-203's reasoning). ⚠️ **OPEN AND STATED: Supabase performance is UNMEASURED** — local overhead is ~**35 ms/page** fixed, and connection caching is **deliberately not built** because it would be infrastructure for a latency nobody has measured; 📅 trigger = first Supabase deploy, cache if the added cost exceeds ~200 ms/rerun. **2c BUILT: the pipeline runs, decides and REFUSES.** `app.py pipeline` is one tick; `.github/workflows/data.yml` runs it every 15 min, inert until `FPL_DATABASE_URL` is set. 1,933 green on SQLite · 1,920 + 13 skipped on Postgres. ✅ **EXIT CRITERION DEMONSTRATED ON REAL POSTGRES**: published 662 players → declined the next tick in **0.19 s** with no FPL call → then FPL "returned" three players and it **REFUSED**, players **662 before and after** (nothing written), `refreshed_at` **unmoved**, `attempted_at` moved, reason recorded. ⭐⭐ **THREE JUDGEMENTS A PERSON MADE IMPLICITLY, NOW WRITTEN DOWN. (1) WHEN — the schedule is PYTHON, NOT CRON, because the thing it depends on moves every week**: GitHub cannot know when Saturday's deadline is, the fixtures can, so the workflow ticks dumbly and `cadence()` decides (in play → 10 min · **the hour before a deadline → 15 min**, four chances, *the redundancy that makes cron's drift survivable* because ADR-210's counters reset there and the pre-deadline reading is unrecoverable · ordinary → 1 h · overnight → 6 h). **(2) WHETHER — `validate()` runs BEFORE THE FIRST WRITE, for every caller**: ⭐ *once storing IS publishing, a check that runs afterwards is not a check* — the last good copy is already gone. Deliberately **not optional** (ADR-181: the caller that would have forgotten is the manual refresh, run by a person, against the database everything else reads). ⚠️ **The bounds are a SMOKE ALARM, NOT A THERMOSTAT, and that is their whole provenance** (ADR-199 asks; the honest answer is *declared, not measured*) — a real January window must **pass**, because a check that blocks good data is worse than one that stores slightly odd data; 📅 revisit only if one fires on a payload that turns out to have been fine. **(3) SAYING SO — `data_status` on every attempt**, ⭐ *recording only successes would make a dead pipeline indistinguishable from a quiet one.* 🐛 **TWO THINGS THE BUILD CAUGHT**: ⚠️ **I shipped `--no-headlines` in the workflow for a flag that does not exist** — the YAML was valid, the command was not, and nothing would have failed until the first scheduled tick, in production, unattended: ⭐⭐ *a workflow file is code that no test runs by default*, so a test now parses the command out of the YAML and puts it through the real argument parser (mutation-tested by restoring the bug; headlines are absent from this path **by construction**, `pipeline.run` never reaching `enrich_headlines`). ⚠️ **Five existing ingest tests failed the moment validation went in — on 2-PLAYER PAYLOADS**, which is the validation working: they had been exercising ingest on a response production cannot produce. **The fixture was padded, not the check loosened** — ⭐ *a fixture that models less than reality will confirm a broken mechanism*, **7th time this month**. **11 mutants, 11 red.** ⚠️ **Cost stated: 96 ticks/day** — free on a public repo; on a private one check the minutes after a week, and the mitigation is a smaller pipeline requirements set, **not a coarser cadence**, because the pre-deadline window is the part that must not slip. **2d BUILT: a gameweek's history lands with no human.** `app.py pipeline --backfill` + `backfill.yml` (hourly, its own workflow because a 3–5 min job has no business holding the 15-min tick's concurrency group). 1,941 + 1 skipped on SQLite · 1,929 + 13 skipped on Postgres. ✅ **Verified live**: core refresh bootstrapped an empty Postgres → `due=True, no stored history for GW[1,2,3,4]` → a real throttled walk wrote **25 gameweek rows from 5 players, 0 failures** → `backfilled_at`/`backfilled_event` set with the **core refresh verdict untouched**. ⭐⭐ **"DONE" IS ASKED WITH THE ANALYTICS' OWN DEFINITION**: `backfill_due` calls **`minutes.completed_gameweeks`**, the same function `in_season_share`, the backtest and ADR-203's log use, where a round counts only when its rows carry a **scoreline**. ⚠️ Had the pipeline invented its own test — *"are there rows for round N?"* — it would have **passed and been wrong**, because FPL writes a per-gameweek row when a fixture is merely **scheduled** (ADR-125/129): it would have found rows, concluded the work was done, and left the analytics with a gameweek they cannot see. ⭐ *The pipeline's "done" has to be the consumer's "have".* (A gameweek also counts as complete only when **all** its fixtures finished — a Saturday round with a Monday night game to come is not ready.) ⭐ **AND THE MIGRATION GAP IS CLOSED RATHER THAN DEFERRED AGAIN**: 2a said *"Postgres has no migration story — that stops being fine the first time a column is added while it holds real data"*, and 2d adds two to `data_status`, so it stopped being fine. `_migrate` now runs on **both** backends — the only SQLite-specific part was asking a table for its columns (`PRAGMA table_info` vs `information_schema`), now `db.columns` — verified on a Postgres table created in the old shape, where the columns appear and **the row survives**: it adds, never rebuilds. `_rekey_history` stays SQLite-only and correctly, a Postgres database never having held the key it repairs. 🐛 **THREE THINGS CAUGHT, TWO BY MUTATION**: ⚠️ **`COALESCE` on `note` broke clearing** — giving the backfill its own stamps meant `set_data_status` had to merge, and merging `note` meant **a successful refresh could no longer clear a previous failure's reason**, so the app would keep showing yesterday's refusal; ⭐ *a field cleared by writing None cannot be merged with COALESCE*, so the note now follows the verdict. ⚠️ **A guard that could not reach its own case** — the test protecting the refresh's note set `note=None` first, so a mutation clobbering it with None destroyed nothing and stayed green: ⭐ *a guard only tests the case its fixture can reach.* ⚠️ **The Postgres migration was proved by a script run by hand and disabling it broke no test** — ⭐ *verifying something once is not testing it.* **17 mutants across 2c+2d, 17 red.** **2e + 2f BUILT — PHASE 2 COMPLETE (2026-09-19).** 1,946 + 1 skipped on SQLite · 1,934 + 13 skipped on Postgres. **2e — the gate chose "headlines stay manual" (they need a language model the runner has not), and that decision had an UNEXAMINED HOLE**: extraction writes to whatever `Storage()` resolves to, so after the cutover events extracted on the owner's Mac would have landed in a local SQLite cache nobody reads. ⭐ **It already worked, for a reason worth pinning rather than rediscovering**: `cmd_refresh` opens **ONE** store and hands that same store to `enrich_headlines`, so `FPL_DATABASE_URL=… app.py refresh` on a machine with Ollama writes players **and** headlines straight into Postgres — no separate push step, no second code path. A test pins it, because 'tidying' the headline call onto its own `Storage()` would split the destination in two and ⚠️ **the symptom would be *no news*, which looks exactly like *no news*.** **Signals now prints when headlines were last read** — every other freshness signal in this phase is surfaced, and leaving the one manual input silent would be the exception that matters most: ⭐ *stale headlines do not look stale.* **2f — RETIRING A PATH MEANS RETIRING WHAT THE PRODUCT SAYS ABOUT IT.** `reseed` does not go away; its **job** changes, and the app had a sentence about that job baked in — *"🌐 A data snapshot — updates when the app is redeployed"* — **true before the cutover and false after it**, since the pipeline refreshes through the day. ⭐⭐ *That is a sentence the app says about itself while behaving differently*, the exact shape ADR-184 was written about. The sidebar now tells the truth in **both** states (a test asserts both branches), and `reseed`'s help, docstring and `DEPLOY.md` state the two eras: **before**, it IS the deploy route and nothing else reaches testers; **after**, it maintains the **fallback** the app shows when Postgres cannot be read — ⚠️ *a very stale fallback is a poor fallback*, so it stays worth running. It targets SQLite explicitly, so the DSN never redirects it. 🐛 **AND THE SWEEP WAS WRONG AGAIN, THE SAME WAY**: the guard for the retired claim required the word **"reseed" on the same line**, and missed the exact regression it exists for — the `help=` string on `p_reseed` never contains that word, the *variable name* does. ⭐⭐ *Sweep for the claim, not for a word you expect to sit beside it* — **second time in this ADR alone**, after the `?`-in-a-SQL-comment guard in 2b. **21 mutants across 2c–2f, 21 red**, two surviving a first pass and both guards that could not reach the case they described. ⏳ **OUTSTANDING IS NOT CODE**: a Supabase project, the Phase 1.5 RLS work, and the `FPL_DATABASE_URL` secret — **until it exists both workflows are inert and the app is byte-for-byte what it was**, so nothing here changes today's deploy and `reseed` → commit → push remains the way to update it. 📅 **The exit criterion starts when the secret is set: two weeks with no manual `reseed` NEEDED, including a deadline and a live gameweek** — ⚠️ *needed*, not *permitted*; needing one is the failure signal, not a rule broken |
| [212](./ADR-212-the-manual-described-the-hole-the-code-had-closed.md) | **The manual described the hole the code had closed** — a documentation review before the mobile work, which found something worse than staleness. Stages A/B/B3 hardened production on 2026-09-19; **`BETA.md`, `CLOUD_SQUADS.md` and `ANALYTICS.md` still instructed the reader to create the exact `using (true)` policies and `disable row level security` that work had removed.** Following the documentation would have rebuilt the hole — with nothing red anywhere, because no *code* was wrong. ⭐⭐ **A hardening changes the system; it does not change the documentation describing how to build one** — two artefacts, and only one of them had a test. Underneath it: **four copies of the setup SQL**, one per feature that needed a table, so *"update the docs"* meant *"remember all four places"* and September remembered none — the ADR-123/127 drift lesson reappearing in prose. ⭐⭐ **One of the three was wrong in a more interesting way than staleness**: `ANALYTICS.md` justified its read policy with *"the anon key lives in Streamlit secrets and is never sent to a browser"* — true of the **key**, irrelevant to the **policy**, which applies to anyone holding that key from any source. ⭐ *A permission is granted to a role, not to the place you keep the credential.* ✅ **One file**: `sql/setup.sql` (7 tables · 12 functions · idempotent), which every runbook now points at — *a pointer cannot drift from what it points at.* ✅ **Three guards, none a word blacklist**: `test_setup_sql.py` builds a **real Postgres from the file alone** (virgin apply · `anon` refused on all six tables · every app path still works · safe to re-run over populated data); `test_setup_docs.py` scans **fenced ```sql blocks** across all of `docs/` — ⚠️ deliberately *not* a grep, because the paragraphs that **warn** about this SQL contain it, and a guard that fires on its own warning gets weakened rather than obeyed (ADR-178); `test_readme_is_current.py` checks the README's own claims. 🔬 **The wider finding: numbers in prose are untested claims** — the README advertised **"121 ADRs · 1091 tests"** against 212 / 2,039, and **"🚨 GW1 = 2026-08-21 (tomorrow)"** a month after GW1 was played. ⭐⭐ **A document can carry a correct fact inside a claim that has expired** — the *date* was right, *"(tomorrow)"* is what rotted, so the guard tests for unplayed-season **tense**, not a date. ⭐⭐ **`PRODUCT.md` showed the richest version**: its "gated — lights up at GW1" table listed six features blocked on data that has since arrived; four are now live, and the three dormant xP terms **stopped waiting on the calendar and started waiting on a person** — *a blocker that outlives its cause is worse than one that is simply wrong, because the stated reason keeps sounding plausible.* 🔴 **The ADRs and sprint records were deliberately not rewritten** — they are the record of how the system got here. ⚠️ Accepted cost: `setup.sql` duplicates the `stage_b*.sql` *migration* files; 📅 delete those once no project remains on the old schema. Unchanged: the functions stop **enumeration**, not **guessing** — Stage C (Supabase Auth) is still what closes that. | Accepted |
| [213](./ADR-213-the-pipeline-publishes-analytics-not-just-data.md) | **The pipeline publishes analytics, not just data** — the mobile audit draws the target pipeline as *validate → **run analytics** → write Postgres*, and ADR-211 built every box except that one. §4.1 commits to **direct Flutter → Supabase with no API** for most of the read surface and lists *"per-player xP over each horizon (`decision_xp` output)"* among it — ⭐⭐ **and that table does not exist**: `decision_xp` is a Python function evaluated at read time, which a Flutter client cannot call. ⭐ *Same shape as ADR-212 one day later — a document describing a capability the system does not have; the audit is not wrong, it is ahead of the code, and nothing said so.* The other two routes are both closed by existing decisions (§4.3 forbids FastAPI becoming a proxy; reimplementing in Dart is the hazard ADRs 123/127/181 are each about). ⚠️ **NOT a performance change, and it was nearly sold as one** — the first draft of the argument claimed it would speed the web app up; measured, `decision_xp` is **8 ms** for 662 players (30 ms for four horizons), so there is no performance problem and inventing a second reason would have been exactly the unexamined claim ADR-212 spent a day removing. ✅ **One row per player holding a single horizon-8 board** — measured, the **per-gameweek values are IDENTICAL across horizons** (zero disagreements over 662 players), so the model is horizon-independent and one row serves the whole 1–8 slider; 662 rows · 242 KB · **12 ms** against a 24 s tick. ⭐⭐ **The load-bearing detail is publishing the per-gameweek values UNROUNDED**: summing the rounded breakdown does not reproduce the total — **253 of 662 players** disagree with their own breakdown by up to **0.20** — because `xp` is `round(sum(unrounded))` while `by_gameweek` rounds each value separately, so publishing rounded would make **the mobile app and the web app quote different xP for the same player**, the two-implementations failure arriving through the back door of a rounding decision. 🐛 **And the comment `by_gameweek # sums to xp` was FALSE for those same 253 players** — fixed with **largest-remainder apportionment** (round down, hand the spare tenths to the largest fractional parts), verified on 20,000 randomised cases with **0 failures**; ⚠️ this shifts displayed cells by up to 0.1 — a correction, not a regression, because the column now adds up. Validation runs **before the first write** (ADR-211: *once storing is publishing, a check that runs afterwards is not a check*) — ⚠️ **a bad xP board is more dangerous than a stale one, because nothing about it looks broken**. The pipeline calls **the same `decision_xp`** everything else calls, pinned by a test that recomputes and compares, so a divergence fails instead of drifting (ADR-181). **Scope: xP only** — DNA percentiles, stat boards and Signals deliberately deferred: ⭐ *designing four validators before knowing whether one works is how you get four wrong ones.* ⚠️ Accepted cost: the same number now lives in two places, tolerable **only** because the second is generated by the first every tick — the moment anything writes that table by another route this ADR is void. 📅 Observation, not in scope: **Tzolakis (HUL, GK) is 3rd highest xP on the board, above Haaland** — worth a look at the GW6 sitting alongside ADR-195. | Accepted |
| [214](./ADR-214-most-of-the-remaining-boards-have-nothing-in-them-yet.md) | **Most of the remaining boards have nothing in them yet** — ADR-213 said the rest of §4.1 *"become mechanical once the pattern exists"*, and ⭐⭐ **measuring the populations first turned a six-board sprint into a one-board sprint.** Counted on the live GW5 snapshot: **Team DNA 20 teams** (grades A→D, 46–87) ✅ published · **over/under 0 rows** and **DefCon reliability 0 rows** (both gate at **900 minutes**; the board's maximum is **450**) · **worth-noticing 0** · **price predictions 662 rows, ALL `stable`** · trending 10/10/10 but it is `ORDER BY` on a published column · headline events already a table. ⭐ **The deciding number is 450** — three boards are not unfinished, they are **out of season**. ⭐⭐ **Player DNA is the one worth dwelling on because it does not LOOK empty**: it returns a profile for all 662 players, but `pool_size` is **1 for 584 and 0 for 78**, 659 of 662 are flagged `low_minutes`, and every percentile comes back **50** — ⚠️ *a board that renders perfectly and says nothing is worse than an empty one, because nothing prompts you to check.* ✅ **Team DNA published** for the same reason xP was — **percentiles require the whole population**, so a client genuinely cannot compute them; validated before replacing the last good board and pinned by a recompute-and-compare test. ⬜ **`trending` is not published at all, and that is not a deferral** — ⭐ *publish a rule; do not publish a sort.* 📅 **Re-measure on/after 2026-11-01 (~GW10)** for over/under, DefCon and Player DNA — the check is one line, *does the population contain the case?*, and this is the **third time in a month** it has changed a plan (ADR-195, ADR-202, ADR-208). 🐛 **THE FINDING: a threshold nothing can reach.** `price_prediction` says `stable` for all 662 and it is not because prices are quiet — **269 of 662 have moved this season**. `price_pressure` compares against **±20,000** while the live board's range is **+5,760 to −2,660**: the rule **cannot fire**. ⭐⭐ **This is ADR-210's bug in a different constant, three ADRs later** — it divides the very same accumulating `transfers_in_event`/`transfers_out_event` counters ADR-210 was written about, and this snapshot sits **516 hours** from the deadline when they are nearly empty. ⚠️ **ADR-210 fixed the exodus threshold and never looked at the price one**, though both live off `net_transfers` two modules apart — ⭐ *a lesson recorded about one number does not check the others*, exactly what ADR-209 concluded about `TIE_NOISE`, happening again. 📅 Wants ADR-210's treatment (a live, phase-aware percentile), not a new constant; next candidate after the GW6 sitting. ⚠️ **Explicitly rejected: building the four tables now to fill later** — that ships validators calibrated against an empty population, and *a validator that has never seen a populated board is a guess with a test around it.* | Accepted |
| [215](./ADR-215-a-threshold-nothing-could-reach.md) | **A threshold nothing could reach** — `price_prediction` returned **`stable` for all 662 players**, and not because prices were quiet: **269 had moved that season**. The bar was **±20,000** against a live board spanning **+5,760 to −2,660** — ⭐⭐ **3.5× above anything that occurs, so the rule could not fire and had not all season.** The comment beside the constants said why: *"placeholders … calibrated on real net transfers at GW1"* — the first half happened, **the second was a plan written as a comment that nobody executed** (ADR-212's shape again). 🔬 **Two things kept it invisible:** ⭐⭐ **every unit test built its input FROM the constants it was checking** (`_p(net_in=int(PRICE_RISE_PRESSURE * own) + own)` passes for any threshold, including an unreachable one) — *a test that derives its fixture from the thing under test is asking whether the code agrees with itself*; and ⭐⭐ **a test was NAMED `test_price_predictor_is_dormant_preseason`**, so when dormancy stopped being the preseason state and became the permanent one, **the name read as confirmation** — *a rule that never fires looks exactly like a rule with nothing to report.* ⭐⭐⭐ **It is ADR-210's bug three ADRs later**: `price_pressure` divides the very same `transfers_in_event`/`transfers_out_event` counters that **reset at every deadline and fill across the week**, so a fixed bar encodes *the hour it was picked* (these numbers were read **516 hours** out, across an international break). ADR-210 replaced `EXODUS_PRESSURE` with a live percentile and **never looked at the price one**, though `crowd.py`'s exodus population is literally built by calling `price_pressure` — ⭐ *a lesson recorded about one number does not check the others*, exactly ADR-209's conclusion about `TIE_NOISE`, now twice. ✅ **Fixed with ADR-210's treatment: the definition stays, the number goes.** ⭐ **And the two directions are NOT symmetric** — counted from `player_history`, **~2% of prices rise and ~15% fall** per gameweek (a bad week empties a bandwagon faster than a good one fills it), so one constant for both was wrong twice over; now `PRICE_RISE_PERCENTILE = 98` / `PRICE_FALL_PERCENTILE = 15`, giving live cuts **+2,644 / −346** and flagging **2.1% rising · 15.3% falling**. ⭐ **The sign decides WHETHER, the percentile decides HOW MUCH** — a distribution always has a top 2%, so without the sign check it would name risers in a week when everyone was being sold; `None` means *this direction cannot fire*, never *use a default*. ⚠️⚠️ **The first version of this fix broke the rule ADR-210 wrote down** — cuts taken over players owned ≥1% then applied to all 662, so **43 of 72 "fall" flags landed on players nobody owns**: ⭐ *a threshold and the thing it judges must be measured in the same population.* ⚠️ **Three call sites were passing a FILTERED list** (the squad page passes **fifteen players**) — a percentile over those flags your worst two every week forever, so `price_detector` binds the whole board once and the cuts are a **required** argument (ADR-181). 📅 **Not a calibration**: the real question is what pressure *preceded* a move, which needs ADR-210's `player_transfer_flow` log — it holds **one reading** — so re-measure after GW8 (≥2026-10-26). ⭐ *This replaces a number that could not be right with a definition that is defensible and still unproven.* | Accepted |
| [216](./ADR-216-hardening-what-exists-says-nothing-about-what-arrives.md) | **Hardening what exists says nothing about what arrives** — found while preparing to run the pipeline on production after ADR-213/214 added two tables. ADR-212 hardened **seven tables by hand**; the pipeline creates its own with a plain `CREATE TABLE`, so they inherit the project's **default privileges** — and a stock Supabase project ships `alter default privileges … grant all on tables to anon`. ⭐⭐ Reproduced on a production-shaped Postgres: **all eleven tables the pipeline creates come out with DELETE, INSERT, TRUNCATE and UPDATE granted to `anon`** — *the hardening closed the front door while the pipeline kept building side doors*, and nothing anywhere said so. ⚠️ **Vandalism, not disclosure** — the data is public FPL data, but `truncate players` from a key the mobile audit says *"is compiled into the binary and can be extracted from the IPA in minutes"* takes the app down for every tester. ⭐ **The shape is ADR-212's one level down**: that was documentation describing a system that changed underneath it; this is a **permission model describing a database that keeps growing underneath it** — both are a fix scoped to what you could see at the time. ✅ **Two parts, and the second is the one that matters**: the pipeline's tables become **SELECT-only** (mobile reads them directly, audit §4.1), and `alter default privileges in schema public revoke all on tables from anon, authenticated` so **new tables arrive CLOSED** — revoking eleven tables fixes today, this stops the twelfth repeating it; now also in `sql/setup.sql` so a fresh project never has it. ⚠️ **Failing closed is deliberate** — *a new table the app cannot read is a visible bug on the first page load; a new table the world can delete is invisible until someone does.* ✅ **Verified on a production-shaped database with the tables already open**: anon still reads 667 rows, all four write verbs refused, **a real pipeline tick still published**, and a table created afterwards arrives with no grants at all. ⚠️ **Two wrong turns, both caught**: I read **staging's** grants and nearly reported them as production's (the owner caught it — staging has A/B but never B3, so `squads` open + `beta_users` closed looks exactly like an alarm) so the diagnostic now **names the database in its first row** — ⭐ *a result you cannot attribute is not a result*, the same expectation-as-observation error as the `git log` vs deployed-code incident; and **the first guard could not catch a missing revoke** because it created the tables *after* the defaults were closed, so they arrived shut and the mutation passed green — ⭐ *a fixture that models less than reality will confirm a broken mechanism*, **9th time this month**. ⏳ **NOT YET APPLIED TO PRODUCTION** — the diagnostic runs first, because a fix applied without reading the answer is how the 2026-09-19 incident happened. | Accepted |
| [217](./ADR-217-stop-fetching-the-board-on-every-click.md) | **Stop fetching the board on every click** — owner: *"still slow, useable but far from a good experience."* Production's own `perf` events gave the number: `data_load` p50 **16 ms** on the SQLite seed, **3,026 ms** on Supabase — **fifteen times** ADR-211's ~200 ms bar, so the rule fired without argument. ⭐⭐ **But ADR-211 ALSO pre-registered the remedy — *"cache the connection"* — and it was aimed at the wrong mechanism**: nine round-trips at even 100 ms each is 900 ms, not 3,000. Measuring the payload found the cause — **2.41 MB per render** (`gw_history` 1.2 MB · `history` 747 KB · `players` 518 KB) crossing the Atlantic on a framework that re-runs the whole script on every click. A pool would have saved the handshakes and left the megabytes moving, been found not to help, and the conclusion would have been *"Postgres is just slow"*, which is false. ⭐ *The decision rule was right and its guessed remedy was wrong, and only measuring could separate them.* ✅ **Cache the DATA, not the connection**: one `dataload.py` owns every board-wide read; per page render **9–13 queries → 0** and **2–3 connections → 0** when warm, the first visitor after an expiry filling it for everyone. ⭐⭐ **`TTL = 300` is a product decision wearing a constant** — ADR-211's cadence is 10 min at its fastest, so a reader is never more than half a tick behind; a test pins it below that cadence so the two cannot drift. ⭐⭐ **The caption is cached WITH the board, which REVERSES what this session first proposed** — reading the timestamp live while serving a cached board lets the sidebar announce a refresh whose data the reader cannot see, the *fresh-looking but stale* failure ADR-211 exists to prevent arriving from the other direction: *a caption describes what is on the screen or it is not a caption.* 🔬 **Three things checked before being relied on**: `sqlite3.Row` **cannot be pickled** and `st.cache_data` pickles, so loaders return dicts — verified first that nothing indexes a row positionally; **the app MUTATES rows it is handed** (`render_build` does `p["xp"] = …`), so both that the optimiser returns fresh dicts (**0 of 15** shared with the pool) and that `st.cache_data` hands each caller its own copy (**tested in a real runtime, not assumed**); and **a process-wide cache leaks between tests in a one-process suite** — a test monkeypatching `Storage` would be silently defeated by a cache filled before the patch and would **pass**, so an autouse fixture empties it. ⚠️ **Cost accepted: a COLD load is worse** (six connections, one per loader) — once per five minutes across all visitors, and the alternative of one bundled fetch would make Signals and Trending reload the history ADR-216's spike just stopped them fetching. | Accepted |
| [218](./ADR-218-read-the-board-instead-of-rebuilding-it.md) | **Read the board instead of rebuilding it** — three fixes had improved the timings and the owner's experience had not changed. Instrumenting the legs that were never timed located it: `data_load` p50 warm **110 ms**, p90 **cold 10,786 ms**, payload **2.41 MB at ~0.22 MB/s**. ⭐⭐ **The cold load is the payload crossing a slow link** — caching made every click after the first free and could never touch the first, and reducing connections 6 → 1 changed it by *nothing measurable*, which is what proved the point. **81% of that payload — 1.94 MB — was history downloaded only to recompute an xP the pipeline had already published** (ADR-213): the expensive half of the answer was sitting in a table nothing read. ✅ **Which pages, established by MEASUREMENT** — the history dicts were wrapped so every read announced its caller: *This week · Transfer · Chips* read it for nothing but `decision_xp` ✅, while *Captain* (builds its own baseline) and *DNA · Lab* (`last_season_rows`, `squad_risk_rows`, `--no-xmins`) genuinely need the raw rows. ⚠️ **Measuring only the default tab would have got this wrong and nearly did** — an earlier pass concluded *"My Squad reads history only for xP"* from the landing view alone, and Captain contradicts it; the same trap caught *"Players never reads `gw_history`"*, true of one tab out of six. 📊 **This week: 2.35 MB → 0.81 MB**, about **10.7 s → 3.7 s** on the journey people take; Captain/DNA/Lab rise to 2.71 MB, accepted because they are destinations rather than the landing page. ⚠️⚠️ **`ranked_for` falls back, and THREE FAILING TESTS found that rather than any reasoning**: an empty board is a real state — the committed snapshot has never had one, a fresh project has none until the first tick, and **ADR-211's fallback serves that snapshot whenever Postgres cannot be read** — so without it My Squad would show **zero xP with nothing saying why**, the silent degrade that ADR exists to prevent, reintroduced by a performance change. ⭐ *Declining is right when the instrument cannot answer; falling back is right when another instrument can.* ⭐ **`board` is passed, not `ranked`** — the views slice at the horizon they need, because the per-gameweek card needs a **longer** horizon than the page and a pre-sliced list no longer holds those gameweeks; and it is **required**, not optional (ADR-181), since a call site that forgot would silently fetch 1.9 MB again. 🔬 The board had to become a faithful stand-in first — **identical at every horizon 1–8, every field, all 659 players** — which needed `defcon_xp`/`clean_sheet_xp` (⭐ *a value that is zero by configuration is not the same as one that is always zero*) and `games_by_gameweek` (a double gameweek is one key with two fixtures); and `ep_next` differed by **type** not value, coerced on read because **`_migrate` can add a column and cannot change one**. ⚠️ **Also reverted here: ADR-217's one-connection bundle** — justified by handshake cost production showed was not the cost, and it forced every page to fetch everything: ⭐ *an optimisation never measured to work, which blocks the one that does, is not a trade-off, it is in the way.* 📅 Open: Captain could use the board if `captain_picks` took a ranked list. | Accepted |
| [219](./ADR-219-one-contract-two-transports.md) | **One contract, two transports** — ✅ **BUILT** (Phase 3, first endpoint). The mobile audit §4.2 proposed FastAPI *and* that Streamlit migrate onto it, because ⭐ *"a contract with one consumer is a guess"*. ⭐⭐ **The principle is right and the mechanism would have made the web app slower** — separately hosted, every squad analysis becomes a round trip, on the app where a whole day had just gone into removing them (spike 017: 3,026 ms → 20 ms warm, 2.41 MB → 0.81 MB cold). ⚠️ *The audit reasoned about coupling and ignored latency; both are real, only one had been measured.* So the contract is a **Python module** — `src/service/` plain functions over plain dicts, FastAPI wrapping them for Flutter, Streamlit importing them directly — and the guarantee the audit got structurally is **bought back with a test**: the HTTP body must equal the in-process answer *serialised* (`over_http == json.loads(json.dumps(in_process))`), so encoding is forgiven and every other difference fails. 🔬 **Building it found the contract was missing a fact the web app has had since August**: `reported_out` was not passed, and `analyse_squad` drops a reported leaver from the **captainable** set — so ⭐⭐ **the phone would have recommended captaining a player the web app already knows has agreed a move**, and ADR-151→156 is the record of this same fact being taught to six surfaces one at a time. ⭐ **Wiring Streamlit naively would have undone spike 017** (the service opens its own `Storage()`) — fixed with a duck-typed `dataload.CachedStore` rather than a `rows=` argument, because an argument would have given the two transports **two code paths**. ⭐⭐ **The ADR-155 guard fired on the refactor** — *"the stub was never called — Health is not asking the question at all"* — the question had **moved, not vanished**, so its seam moved down to `headlines.leavers`. ⚠️ **JSON has no integer object keys**: `by_gameweek` crosses as `{"6": …}` and as text `"10"` sorts before `"6"`, so a prefix sum for *the next two gameweeks* would answer for the wrong two. **28 tests, 21/21 mutations killed** — ⚠️ one first reported **NOT APPLIED** (stale pattern) and one **SURVIVED**: sizing ADR-210's exodus threshold on the *squad* rather than the league, invisible because every test stubbed `leavers` and never reached the argument. ⚠️ The seed holds **zero** reported leavers, so the case was **constructed, not sampled**. **Guarded by** `tests/test_service_squad.py`. | Accepted |
| [220](./ADR-220-the-fix-reached-the-engine-and-not-its-callers.md) | **The fix reached the engine and not its callers** — ✅ **BUILT: Phase 3's remaining five endpoints** (transfers · captain · gameweek-plan · route · build), which ADR-219 held *"until the first proved its shape"*. ⭐ `src/service/` grew **three modules, not six copies** — `requests.py` · `inputs.py` (one loader) · `answers.py` — because six endpoints each assembling their own inputs is ADR-181's failure with six chances to happen. 🔬 **And replicating the recipe found ADR-209 had been shipped into the engine and threaded through only FOUR OF TEN call sites.** ⭐⭐⭐ `window` (which sizes the tie-break band, **default five**) and `horizon_xp` (the longer view, the half that **moves expected points**) are both **optional**, so omitting them is silent — the ranking returns and simply names a different player. **Six live call sites omitted them**: the Streamlit Transfer tab ×3 — running at **horizon 1** under My Squad, so the band was more than twice too wide — the CLI ×2, and the assembler's rival plan (right by coincidence). 🔬 **Measured: 89 of 120 random legal squads got a DIFFERENT top recommendation.** ⚠️ *And what that number is not* — grading those 89 on the five-gameweek map would be **partly circular**, because the tie-break optimises that very map (ADR-202); the defensible claim is **divergence**, and *which* player is right is what ADR-209 already decided. ⭐⭐ **The shape is ADR-212's one level down** — that was documentation describing a system that changed underneath it; this is **a FIX describing a call convention most callers never adopted** — *both a correction scoped to what was visible when it was made*. ⭐ **The guard sweeps for the claim** (ADR-184) rather than fixing six sites: every transfer search in `src/` must state both arguments — **not** by making them required, which would have churned 35 engine tests for no gain in the thing being protected. ⚠️ Writing the sweep: v1 reported a call site **inside a comment** (*the paragraphs that warn about a pattern contain the pattern* — ADR-178 again); v2 widened to the enclosing function to allow `**splat` and **went blind to a single call dropping the splat** — ⭐⭐ **found only by re-breaking the guard after improving it**, which had just reported three call sites clean that were not. ✅ **75 tests · 30/30 mutations killed** — ⚠️ four first survived and each named a real gap, incl. *captain never gets the raw history* surviving **because the seed has no published board** so every assertion passed vacuously (⭐ *does the population contain the case?* — 7th time), and *build ranks on nothing* surviving because **every structural assertion passes against a squad chosen on the wrong objective**; ⚠️ one assertion was **hollow** (`wider != seen.get("xp_by_id")` compared against a key never present, since the argument is positional — ⭐ *a hedge is the absence of an assertion*, ADR-180). ⚠️ Also fixed: `route_to_player` had **never** been passed `reported_out`. **Guarded by** `tests/test_service_endpoints.py`, `tests/test_tiebreak_wiring.py`. | Accepted |
| [221](./ADR-221-hand-written-models-guarded-from-both-ends.md) | **Hand-written Dart models, guarded from both ends** — ✅ **BUILT** the night before Flutter starts. 🔴 **The mobile audit §5 planned to GENERATE the client's models from FastAPI's OpenAPI schema** — *"one contract, no hand-written duplicates"* — **and the mechanism was never checked against the schema the service emits**: every route is typed `-> dict`, so **requests are fully typed and every response is an untyped bag**; a generator would emit `Map<String, dynamic>` for all six and the response models get hand-written anyway. ⭐ *The plan named a mechanism and nobody checked it against the thing it was meant to operate on.* ⚠️ **Typing the six responses server-side is not merely expensive — it is the failure this codebase keeps writing ADRs about**: a Pydantic model beside the dict the engine already builds is **a second definition of every answer**, free to drift (ADRs 123/127/181, and ADR-219/220 both exist because a contract grew a second version of a fact). ⭐⭐ **So the contract is pinned from BOTH ENDS**: `tests/test_api_contract.py` compares each endpoint's live response **shape** against a committed sample, and `test/api_models_test.dart` parses those same samples through the real classes — ⚠️ *neither half suffices* (the Python test stays green while a Dart model reads the wrong key; a Dart test on a hand-made fixture stays green while the server moves), so ⭐ **the samples are the artefact, not a convenience — the only thing both ends can be wrong about together**. ⭐ **SHAPE, never values**: the seed refreshes constantly, and asserting a number would fail on every data update and *train everyone to regenerate without reading the diff*; verified by breaking it four ways and confirming value churn is ignored. 🔬 **Writing the models found the API returns FOUR DIFFERENT PLAYER SHAPES** — 11 curated keys in `analysis`, 5–6 in a transfer, 6 in `route.target`, and **42–45 RAW DATABASE COLUMNS** in `route.blocked[].out` and `build.selected[]`: ⚠️ *that ships `cbi` and `cost_change_event` to a phone* (16.3 KB where ~3 KB would do, on the client whose architecture was justified by measuring payload) **and makes the database schema part of the API contract**. 🔴 Normalising them is recommended and **NOT done — a contract change deserves its own agreement**, and ⭐ *a model that flatters the contract is how a client discovers the truth at runtime*. ✅ `dart analyze` clean · **14 Dart tests green against the real responses**. ⚠️ **The Dart half runs LOCALLY, not in CI** (Flutter is not installed there) — ⭐ *a test nobody runs is not a guard*. **Guarded by** `tests/test_api_contract.py`, `spikes/018-flutter-read-slice/app/test/api_models_test.dart`. | Accepted |
| [222](./ADR-222-the-landing-screen-is-a-pitch.md) | **The landing screen is a pitch, and it needed an endpoint that did not exist** — ✅ **BUILT.** The app's first screen was a *list*; the owner, on seeing a draft: *"as a landing page on my team i would prefer a pitch layout format"* — citing the Hub **and madboots.streamlit.app itself**. ⭐ **The second reference decided it**: the web app already draws this card, and a phone drawing a different one gives two surfaces two ways of describing one squad *with no way to tell which is right*. So the card is `pitch.py`'s — kit · name · white xP pill · price · fixture · the manager's own C/V — plus the **header band** the Hub has and MADBOOTS did not. ⚠️ **The Hub's colour-by-difficulty card was offered and DECLINED** — ADR-179 already refused whole-card colour (*it trades a readable number for a hue*); ⭐ *that was a judgement about a laptop and a phone could legitimately differ, so the point is it was **re-decided, not drifted into*** (ADR-180: a prior ADR is a fact about a version, not a law). 🔬 **THE FINDING: the screen could not be drawn from the contract.** `analysis` returns eleven fields and the pitch needs **ten it does not** — gameweek · deadline · both armbands · kit · opponent · venue · difficulty · bench role. ⭐⭐ **The contract was built for QUESTIONS (*ids in, analysis out*) and a landing screen needs FURNITURE too** — fetching the rest separately is **five round trips before anything renders**, on the client whose architecture was justified by measuring payload. → **`POST /api/v1/squad/my-team`**, composing `fetch_manager_team` + `analysis` + `team_schedule` + `bench_order` + `shirt_url` + `deadline_line`, **6.5 KB in one call**; ⚠️ *a screen-shaped endpoint couples the API to a layout* — named as a real cost, not hidden. ⭐ **Every map keyed by something JSON leaves alone** (kits/fixtures by **club**, bench roles by **role**) so a new screen does not repeat `by_gameweek`'s integer-key trap. ⭐ **FPL's own words survive a failure** — bad id vs unreachable API vs *not public until the first deadline* are three problems and the fetch never raises, so discarding its message loses the only diagnosis. ⚠️⚠️ **A CONSTANT COPIED IS A CONSTANT THAT CAN DIFFER**: `badges.py` imports Streamlit so the API cannot use its kit helper, and the first retype of the template **dropped the `-66` suffix** — a URL that is wrong and looks entirely plausible, ⭐ *caught only because the two were compared side by side*; moved to `src/kits.py` with `badges` re-exporting. ✅ **10/10 mutations killed** · one test pins that the embedded analysis is **identical** to `/squad/analysis` (⭐ *a composing endpoint that priced a squad differently would put two answers about one team in one app, by the back door*) · ⚠️ **the FPL fetch is stubbed everywhere** — *a test that reaches the internet is not a test* · **19 Dart tests** against the real responses. **Guarded by** `tests/test_service_endpoints.py`, `mobile/test/api_models_test.dart`. | Accepted |
| [223](./ADR-223-a-pitch-you-can-navigate-from.md) | **A pitch you can navigate from** — ✅ **BUILT**, from three things the owner found by using ADR-222's pitch. ⭐⭐ **THE MONEY WAS ONE DICTIONARY KEY AWAY THE WHOLE TIME**: `picks_to_squad` read the picks and **discarded `entry_history`**, which carries **`bank` and `value`** in the same payload — and **ADR-191** is the record of this app *advising a position its manager was not in* because `bank` was hard-coded to £0.0m while the Transfer tab collected it by hand three tabs away. ⚠️ **FPL counts money in TENTHS** (`bank: 13` = £1.3m) — a missing division hands a manager **ten times his money** and every affordability answer downstream is wrong *while looking entirely reasonable*. ⭐ **None is not zero**: an empty bank is a real position, *"we could not read your bank"* is not, so the header renders `—` and never `£0.0m`. 🔬 **Free transfers CANNOT be had** — checked, not assumed: the entry payload carries bank and value but free transfers sit **behind a login** (`event_transfers` is how many were *made*), so the client states it and the server echoes it — ⭐ *a stated assumption can be corrected where a silent one cannot*. ⭐ **The pitch is PAINTED, not a picture** — vectors scale with no asset, and the real reason is placement: lines positioned **relative to the card rows** so a five-DEF and a three-DEF formation both read as a pitch; ⚠️ **white at 22% deliberately** — *markings are orientation, not content, and the eye must land on the xP first* (ADR-135). ⭐ **Five tabs, three built** (`My team · Transfers · This week · Captain · Chips` — the web's own order): ⚠️ the unbuilt two are **greyed, not hidden**, because *a bar that grows items later moves everything under the user's thumb* and ⭐ *"not built yet" is information; an empty screen is a bug report*. **The squad is loaded ONCE and shared** — two screens fetching it separately could disagree about who you own. ⭐ **Still no Riverpod**: `setState` over one object is genuinely enough, and *adding it now would be a foundation built to a guess*. 🔬 **Found by using it**: the *on your bench* warning fires on real advice (`Leno → Tzolakis +2.0 xP` — a true gain that changes the XI by nothing), ⭐ *the number is right and would mislead alone*. ⚠️ **And a test of mine asserted FPL's arithmetic with a FABRICATED FIXTURE** — pairing a synthetic £104.6m squad with FPL's real `value: 995` from a different squad — ⭐ *a constructed fixture only confirms what you put in it*. **Guarded by** `tests/test_service_endpoints.py`. | Accepted |
| [224](./ADR-224-the-recommendation-ships-with-its-reasoning.md) | **The recommendation ships with its reasoning** — ✅ **BUILT.** ADR-223's *This week* tab showed four verdicts; the owner pasted the web app's version of the same answer, **four times as long because almost all of it is *why***. ⭐⭐ **A RECOMMENDATION WITHOUT ITS REASONING IS A DIFFERENT PRODUCT** — ADR-182 settled the mantra as *"Analytics decide. Logic explains. You make the call"* precisely because it names the two halves **in the order they run**, and a screen showing only the first clause has quietly dropped the other two (the third being the one that tells a manager the decision is still theirs). So `explain_gameweek`'s output now ships **with** the plan under one `explanation` key — its own key so a caller can ignore it *and* so nothing inside can be mistaken for a number the engine computed. ⭐ **The CEILING is the part most confidence displays leave out**: *"lifting it means a different captain, not a different week"* — *a score with no stated limit invites a manager to chase it, and the honest answer is often that it CANNOT rise this week*. ⚠️ **The model note is not boilerplate** — printing a number out of 100 without *"a heuristic from the signals, not a probability"* invites it to be read as one. 🔬 **Found by mutation: a wrong horizon here is a wrong SENTENCE, not a wrong number** — `explain_transfer` writes *"+2.3 over 1 GW"*, so a hard-coded window makes the app **state in words a span it did not rank over**: ⭐⭐ *the recommendation would be right and its justification false, which is worse than either alone*. ⭐ **Commentary must not take down the match** — the explanation is a reading of a decision already made, so a failure to build a sentence leaves the plan intact. ⚠️ **`explain_gameweek` returns dataclasses**, which `json.dumps` refuses — the endpoint would 500 **on the happy path**, a failure visible only over HTTP (exactly what ADR-219's two-transport test exists to catch). 🎨 **Also: ADR-223's pitch markings were drawn from GUESSES** — literal arc angles chosen because they looked right on one screen; ⭐⭐ *those are a property of the phone it was drawn on, not of the drawing*, so at other aspect ratios the penalty D swept most of a circle (*"some distortion on rendering"*). Fixed with **geometry**: the arc crosses the box edge at `asin((edge − spot) / r)`, so the angles fall out of the shape and are right at every size — *a constant that encodes the conditions it was measured under*, the same finding as ADR-209's band and ADR-210's threshold. ⚠️ The plan's payload grew **13 → 18 KB**, the largest in the API and the screen most likely to be opened on mobile data. **Guarded by** `tests/test_service_endpoints.py`. | Accepted |
| [225](./ADR-225-a-plan-that-knows-when-it-stopped-being-true.md) | **A plan that knows when it stopped being true** — ✅ **BUILT.** Asked whether a modified squad should survive closing the app, the owner: *"It needs too."* ⭐⭐ **That settles persistence and exposes the harder question — not WHERE to store a squad but WHAT HAPPENS WHEN THE STORED ONE AND THE REAL ONE DISAGREE.** *A saved plan can become a lie while the app is shut*: plan a transfer Friday, make it for real Saturday, reopen Sunday — a stored list of fifteen would be shown as **your team** and a manager would act on it, the one failure here with a cost attached because *the action it invites spends a transfer*. ⭐⭐⭐ **So a draft stores WHAT CHANGED, not what you ended up with**: `base_player_ids` alongside `player_ids` — *a snapshot cannot tell you whether it is still relevant; a draft that remembers its starting point can be checked against reality the moment the app reopens*. Swaps are **derived** (*two lists and a subtraction cannot disagree; a stored list of swaps could drift from the squad it describes*). ⭐ **Reality is fetched FIRST, always** — a draft is an *overlay on reality, never a substitute* — and four verdicts get **four different sentences**, because ⚠️ *"your plan is gone" and "your plan already happened" mean opposite things*; ⚠️ **compared as SETS not lists**, since FPL reorders picks when a bench is reordered and *treating order as identity would throw away a good plan for no reason*. ⭐ **The SERVER says whether this is the real team** (`draft: bool` + `fpl_player_ids`) — *a client can forget to mention it; a field cannot*. ⚠️ **The draft replaces the fifteen and NOTHING ELSE**: name, bank, deadline and armbands stay FPL's, because *a draft that invented its own bank would let a manager plan a move he cannot pay for*; kits/fixtures derive from the squad being **shown**, so a drafted-in player from a new club is not left shirtless. ⭐ **`shared_preferences`, NOT Drift** — storage added the moment something needed it and no earlier; *Drift arrives when the board is cached offline, a real trigger rather than a plan*. ⚠️⚠️ **WHAT A TAP CAN HONESTLY DO: nothing here can change your FPL team** — FPL publishes no write API, and the web app already says so (`apply_transfer`: *"No server write"*). So a tap shows **your pitch with that move** under a permanent orange banner: *"A plan — not your FPL team."* ⭐ **A second swap is built from FPL's squad, never from what is on screen** — compounding would lose the thread back to reality and the draft could no longer tell it was stale. ✅ **8/8 mutations killed · 10 Dart tests on staleness alone** (incl. *reordering a bench is not a squad change* and ⭐ *absent is not "different"*). **Guarded by** `tests/test_service_endpoints.py`, `mobile/test/draft_test.dart`. | Accepted |
| [226](./ADR-226-the-manager-chooses-we-price-it.md) | **The manager chooses; we price it** — ✅ **BUILT.** Two owner calls, both of which made the app smaller: *"manual transfer, can select a higher priced player, just flag it as over budget"* and *"For Captain, do we need it on bottom bar, surely a simple client side click … could be viable."* ⭐⭐ **OVER BUDGET IS FLAGGED, NEVER HIDDEN** — filtering by price is right when the app is **recommending** and wrong when the manager is **choosing**, because *a candidate silently removed looks like a candidate that does not exist*: a manager who cannot see Saka concludes he is **ineligible**, not that he is £2.5m dear. ⚠️ It was also inconsistent with `apply_transfer`, which has always treated over-budget as a **soft warning that never blocks**, *"because prices drift"*. **Affordability is the ONLY rule relaxed** — position, ownership, availability and the ≤3-per-club cap all hold, *because an illegal squad is not a plan, it is a squad FPL will refuse*. ⭐ **The legality rule was EXTRACTED, not copied** (`is_legal_replacement`, `_club_counts`): the manual screen needed the same notion of *legal*, so the choice was **one rule with two implementations or one rule in one place** (ADR-181) — **all 44 existing transfer tests passed unchanged**, which is the point of extracting rather than rewriting. ⭐⭐ **THE ARMBAND IS A TAP, NOT A TAB** — *setting a captain changes no number this app computes* (FPL doubles his points; our projections are the XI's own xP and do not), so it is a **display** decision needing no round trip and no screen; **Captain is gone from the bottom bar** and the pitch now offers *Make captain · Make vice · Replace him…*. ⚠️ A captain cannot also be vice — *an app that lets you build an impossible team is teaching you something untrue*. 🔬 **THE FOUR PLAYER SHAPES ARRIVED IN PRACTICE**: `transfer.py`'s `_summary` is the **five-key minimal** shape with no `status`, so the list **could not flag anybody** — ⭐⭐ *ADR-206's exact failure on a new surface: a doubt is a probability, and hiding it prices it at certainty.* (Checklist 1b, arriving as a bug rather than a tidiness argument, and still unfixed globally.) ⚠️⚠️ **Two of my own tests were wrong**: *a presence check passes against a lie* (`"status" in c` survived a mutation hard-coding every status to `"a"`), and then *a skip is not a pass* — the replacement test **skipped** when nothing was found and the same mutation caused the skip (ADR-178 again); fixed by **measuring the population first** (the seed holds 24 doubtful players, 99 of them legal replacements) so absence is now a failure. ⭐ A third version **assumed a legality it had not checked** — the club cap blocked the player it demanded — *the same species as the bug it was written to catch*. ✅ **10/10 mutations killed · 2,239 tests · 29 Dart tests.** **Guarded by** `tests/test_service_endpoints.py`. | Accepted |
| [227](./ADR-227-one-shape-for-a-player.md) | **One shape for a player** — ✅ **BUILT; closes start-checklist 1b.** The API described a player **five different ways**, and I had parked the decision on the owner **three times** before he asked what I actually needed from him. ⭐⭐ **The honest answer was "nothing"** — every consumer was code written this week, the payload only shrinks, and nothing user-visible changes: *I had been treating a refactor as a product decision, which is its own way of not doing the work.* **Why it was not merely untidy:** it cost bytes (`build` **12.2 KB where 3.4 will do**, on the client whose architecture was justified by measuring payload); it **made the database schema part of the API contract**; it **shipped the model's internals** (`captain.picks` carried `rate`, `ep_next`, `defcon_xp` — ⭐ *the working, not the answer*); and ⭐⭐ **it bit** — the five-key shape carries no `status`, so ADR-226's manual list **could not flag a doubtful player**, *a doubt hidden being a doubt priced at certainty*. → **One `player_summary()` everywhere**, eleven keys, with optional maps so every caller can use it (⭐ *absent is not wrong*: no per-GW map means `{}`). Endpoints may add keys that are **answers rather than raw data** (`bench`/`forced`, `affordable`/`over_by`, fixture facts). ⚠️ **`doubtful` dropped** — it is `status == "d"` said twice, and *two representations of one fact are two things that can disagree*. ⭐ **The guard SWEEPS** — walks every response, finds anything with `id` + `web_name`, requires **exactly** the shape (⚠️ *not "at least": a superset is how the raw row crept in last time*), identified by **content not by a list of paths** (ADR-184). 🔬 **Doing it found three things**: ⭐⭐ **a REDUNDANT OVERRIDE IS A BLIND SPOT WEARING THE COSTUME OF SAFETY** — ADR-226's explicit `status` inside `replacements_for` **masked the shared function**, so a mutation falsifying `player_summary`'s status reached **no test in the entire suite** until the redundancy was removed; ⚠️ **a FIFTH shape I had never counted** (`captain.picks`) — *a sweep finds what an inventory does not*; and ⚠️ **another hedge of mine** (`'"by_gameweek"' not in text or '": {' in text` asserts essentially nothing) — ⭐ *a hedge is the absence of an assertion*, the second found this week. ✅ **5/5 mutations killed · 2,242 tests · all 44 transfer tests unchanged.** Payloads: build **16.3 → 4.7 KB**, route **5.9 → 1.9**, captain **1.9 → 1.1**. The Dart client **lost a whole class** — ⭐ *a model that needed two classes was telling us the contract had two answers to one question*. ⚠️ Open: `gameweek-plan` is now **22 KB**, the largest response and the likeliest to be opened on mobile data. **Guarded by** `tests/test_player_shape.py`. | Accepted |
| [228](./ADR-228-more-is-for-the-app-not-for-everything-else.md) | **"More" is for the app, not for everything else** — ✅ **BUILT.** *"Do we need a more option on the bottom right too? We have lots of other functionality."* ⭐⭐ **The answer is yes, and "we have lots of other functionality" is the argument AGAINST a More drawer, not for it** — the mobile audit §6 is explicit that *the mobile app is the decision layer; the web app stays the exploration layer*, and a More list grown to eight items would undo that positioning on the smaller screen. ⚠️ This project already learned it the other way round: **ADR-166 cut the web sidebar 12 → 9 ordered by frequency**, and **ADR-167/170 answered *"we need another board"* with a READER instead** — *a phone is the wrong place to reverse both*. 🔴 **THE REAL REASON: the app had nowhere to put a setting, and one of them was lying** — `_freeTransfers` was `final int = 1` with no control, which is **ADR-191's exact failure reintroduced** (*the surface a manager reads was advising a position he was not in*); I built the endpoint to accept the number and then gave the manager no way to say it, ⭐ *because there was nowhere to put it*. The manager id had it from the other end — jammed into the **title bar**, *making a setting look like a title*. → Bar is now **`My team · Transfers · This week · More`**; More holds **Your team** (manager id + free transfers 0–5, with the reason it must be asked), **This gameweek** (bank/value/chip/deadline each labelled *from FPL* — ⭐ *two of the three header numbers are FPL's and one is yours, and a reader cannot tell by looking*), **Not here yet** (Chips, Players — named **with why**, not greyed), **On the web** (⭐ *the positioning said out loud: someone who cannot find Team DNA should learn it is a decision, not an oversight*), and **About**. ⚠️ **This deliberately reverses ADR-223's own argument** that Chips should be greyed rather than hidden (*"a bar that grows items later moves everything under the user's thumb"*): ⭐ **that held while the fourth slot was a PLACEHOLDER and stops holding when there is a REAL fourth item** — four working tabs beat three plus a dead one, and the move is cheapest before anyone builds muscle memory for a button that does nothing (*a prior ADR is a fact about a version, not a law* — ADR-180). ⚠️ **Open: feedback has no path on the phone** — the web form POSTs to a webhook secret the client must not hold, so it belongs behind the API; *a beta tester on a phone is more likely to notice something and less likely to be near a laptop*. | Accepted |
| [229](./ADR-229-a-chip-is-a-season-decision.md) | **A chip is a season decision, and the sweep only saw what it was pointed at** — ✅ **BUILT; the bottom bar is complete.** `POST /squad/chips` accepts `horizon` **and ignores it**: ⭐⭐ ADR-166's rule is that a chip expires at the end of each half-season, so the question is never *"is this week good?"* but ***"is this week better than the weeks I have left?"*** — and *that is not a smaller version of the first question, it is a different one*. The server derives the window from `chip_deadline` and **returns the window it used** (⚠️ *a client rendering "next gameweek" over a three-week answer describes someone else's question*). ⭐ **The wildcard says what it is WORTH, not only when** (ADR-185, found by the owner from a two-team A/B): the screen leads with the **overlap** — *"a rebuild would change 13 of your 15"* — because *a recommendation that measures only WHEN presents itself as an answer to WHETHER*. ⚠️⚠️ **THE FINDING: ADR-227 claimed "one shape everywhere" and was true of FIVE SURFACES OUT OF SEVEN.** The new endpoint's first run returned a **45-column raw row** as its triple-captain pick, and adding `chips` to the sweep found **four more inside `gameweek-plan`** — `captain` + `captain_ranked` still carrying the xP working (ADR-227 normalised the captain ENDPOINT and never touched the captain INSIDE the plan), and all four `lineup` lists. ⭐⭐ **A GUARD COVERS THE SURFACES IT WAS POINTED AT, and a new surface is not one of them until someone points it** — *the claim was not a lie, it was unchecked in exactly the places nobody had listed*. → **The sweep now asserts its own completeness** against `service.__all__` (⭐ *a guard that requires manual registration is a guard that will be forgotten*) and **found `gameweek` on its first run**. ⚠️ **A THIRD HEDGE in my own test**: `"gain" in w or "margin" in w` reduced to its always-true half and a mutation dropping the valuation survived — *a hedge is the absence of an assertion*, **third time this week, same `or` shape**. ✅ 6/6 mutations killed · 2,248 tests. ⚠️ Chip advice runs the optimiser over the whole market — the most expensive call in the API, acceptable only because the screen is opened rarely. ⭐ *A tab earns its slot by working* — Chips has moved three times (greyed → More → the bar). **Guarded by** `tests/test_service_endpoints.py`, `tests/test_player_shape.py`. | Accepted |
| [230](./ADR-230-the-market-and-where-a-tab-belongs.md) | **The market, and where a tab belongs** — ✅ **BUILT: Players.** `POST /api/v1/players` returns **every available player ranked, in one call** (96 KB · 481 players · 75 ms) and the client filters locally — ⭐ *the cheap thing sent once beats the small thing sent constantly*, the same payload reasoning spike 017 used to shape this client. ⭐ **Unavailable players are EXCLUDED, not flagged** (*a browse list is for finding someone to buy; leaving him in makes the reader do the filtering the app exists to do*) while ⚠️ **doubtful players STAY and are flagged** — *a doubt is a probability, not a verdict*. ⚠️ **This is NOT what §4.1 designed, on purpose**: the audit wants the browse surface read **straight from Supabase** (what ADR-213 publishes the board for, proven by spike 018) — but **it is not testable today** (staging predates the board: `PGRST205, table not found`) and *production credentials are not something to hand a browse screen in order to try it*. ⭐ **So I built the path I can verify** — *shipping code nobody has run is how this session's bugs were made* — 📅 revisit when the app carries Supabase config for a real device. ⭐⭐ **AND WHERE A TAB BELONGS: working earns a PLACE, frequency earns a SLOT.** ADR-229 put Chips in the bar on *"a tab earns its slot by working"* — ⚠️ *that rule answers the wrong question*: **"should this be hidden?" and "should this hold one of five permanent slots?" are different questions**, and the second is decided by **frequency** (ADR-166's rule, one screen smaller). A chip is a handful of decisions a season; Players is browsed weekly. ⭐ **The audit settles it without my opinion** — §6's first release is *This week · My squad · Transfers · Players*, and **Chips is not in it**. Bar is now `My team · This week · Transfers · Players · More`, with Chips a full entry in More (*no longer a graveyard — it holds working things now*). ⚠️ **Chips has moved FOUR times** — named as churn: *each move was right on the rule being applied, and the rules kept turning out to be about different questions*; the bar is now ordered by the audit's own priority, **the first ordering that came from outside this conversation**. 🔧 Also fixed: the Dart client **hardcoded `/api/v1/squad/`**, so the market was reachable only via `'../players'` — a path depending on URL normalisation. ✅ 2,254 tests. ⚠️ Open: the player **card** — §6 says *search, compare, the card*, and this is search only. | Accepted |
| [231](./ADR-231-the-server-holds-the-secret-and-never-lies-about-sending.md) | **The server holds the secret, and never lies about sending** — ✅ **BUILT; closes ADR-228's open item.** ⭐ *A beta tester on a phone is MORE likely to notice something and LESS likely to be near a laptop*, and the app had no way to tell anyone. ⚠️ The web form POSTs straight to `FPL_FEEDBACK_WEBHOOK` and **a phone cannot**: *a secret in a mobile binary is a secret every tester has* — the same reasoning that keeps the `service_role` key out of a client (ADR-211, where *"it never leaves the machine"* was true of Streamlit and explicitly noted as not transferring). → **`POST /api/v1/feedback` relays it**; the server holds the webhook, the client holds nothing. ⚠️⚠️ **IT NEVER REPORTS A BLIND "SENT"** — `relay_result` exists because that was a real bug: the form said *sent* while a relay silently refused because the target address had never been **activated**, and a form-to-email relay answers **HTTP 200** with `{"success": false}`, so *reporting the status code alone calls a refusal a success*. ⭐ **A success message that cannot fail is not a success message** — three outcomes (no sink · refused · unreachable), each with the relay's **own words**, and ⭐ **every failure carries the email address** so *a tester reporting a bug does not hit a second one*. ⭐ **Relayed verbatim** — *a server that reformatted or interpreted a bug report would be editing the evidence* — and tagged `madboots-mobile` so a phone report is distinguishable from a web one. ⚠️ **THE GAP IT SHIPS WITH, NAMED RATHER THAN DISCOVERED: no rate limit.** On localhost the only caller is the owner; **the day this is hosted it becomes an open relay to his inbox**, so the limit must arrive *with* the hosting — ⭐ *an unguarded endpoint that is currently unreachable is a problem with a date, not a problem that does not exist*. ✅ **7/7 mutations killed** (incl. the two that would restore the original bug) · 2,262 tests. 🔧 Workflow: `uvicorn` ran without `--reload`, so **three times today a new endpoint 404'd against a server that predated it** and each time the first suspicion was the client — ⭐ *a stale process is indistinguishable from a missing route from the outside*. | Accepted |
| [232](./ADR-232-signals-and-a-memory-the-web-cannot-have.md) | **Signals, and a memory the web cannot have** — ✅ **BUILT; closes the Signals review.** `POST /squad/signals`, **squad-scoped** — *that is the whole difference from the web page*, which browses the market: this answers the question a manager opens a phone to ask, **what should I know about the fifteen I hold?** ⭐⭐ **THE ORDERING IS THE DESIGN** (ADR-150): these sources are **not equally reliable**, and one list without saying so *"would present a Reddit rumour beside an injury FPL confirmed"* — so `official` (FPL's own news, **a fact**, it drives status and therefore every xP) → `departure` (press + crowd agreeing, while FPL still calls him available) → `exodus` (**our own inference**: a sell-off our fields cannot explain) → `headline` (one named outlet). ⚠️ Each signal carries its `kind`, so a client **cannot** flatten them without deliberately discarding the distinction; ⭐ the exodus label is *"Unexplained"* — *a statement about our own data, the honest thing an inference can say about itself*. ⭐⭐⭐ **THE PHONE CAN DO SOMETHING THE WEB CANNOT: REMEMBER.** The server has no idea when you last looked — but every signal has a **stable key** and the device already has a store (ADR-225), so the app badges what is **new**. *That is the what-changed view the checklist asked for, and it needed NO NEW DATA — only the observation that the client is the thing with a memory of this user.* ⚠️ Marked seen **on render, not on fetch** (*saving before showing means the badge never appears*), and the set is **replaced, not merged** (*a key that stops coming back is a signal that has passed*). ⚠️ **Lives in More, and the right answer is probably neither More nor the bar**: ⭐ *the value of a signal is BEING TOLD, not going to look* — a badge on the pitch would beat any menu placement, **not built because it costs a round trip on the landing screen** whose speed ADR-217/218 spent a day earning; *that is a measurement, not a guess*. ⭐ **A quiet week is an answer** — `checked` reports how many players were examined, so *"nothing to report across your 15"* reads as news rather than a screen that failed to load. ⚠️ Caught on first render: *"-2,762 managers sold him"* — `net` is negative by construction and **the sign is already carried by the word *sold***. ✅ 2,267 tests; the ordering test is **constructed, not sampled** (*a seed with one kind of signal cannot demonstrate an ordering*). | Accepted |
| [233](./ADR-233-a-fix-scoped-to-where-it-was-noticed.md) | **A fix scoped to where it was noticed** — ✅ **BUILT.** The owner hit `ClientException with SocketException: Connection refused … errno = 61 … /squad/gameweek-plan`. The server had stopped — *that is not the interesting part*. ⭐⭐ **A GOOD MESSAGE FOR EXACTLY THIS ALREADY EXISTED** — *"The service is not answering on …, start it with `uvicorn …`"* — in `main.dart`, on the **landing screen only**; six other screens rendered `'${snapshot.error}'`, errno and all. ⭐⭐⭐ **A FIX SCOPED TO WHERE IT WAS NOTICED IS A FIX THE NEXT SCREEN DOES NOT GET**: it was written when the landing screen was the *only* screen, five more arrived over the following hours inheriting nothing, and the failure surfaced on **This week** rather than where the handling lived. ⚠️ **Same shape as ADR-229's finding hours earlier** (*a guard covers the surfaces it was pointed at*) **and ADR-155's** (one fact re-taught to six surfaces) — ***three instances in one day of "solved in the place it was seen"***. → The message moved into the **client**, the only thing that knows the base URL *and* what a refused socket means; every screen renders `friendlyError(...)`, and the landing screen's health check — which existed only to produce that message — **is gone with it**. ⚠️ **Two ways a refused connection arrives**: `SocketException` on macOS/iOS, but **on web there is no such thing** (`package:http` raises `ClientException`) — catching only the first would have left **Chrome** showing exactly the raw text this replaces, ⭐ *on the one target `flutter doctor` reports as always available*: **the platform the fix was tested on is not the platform the fix has to work on**. ⚠️ `healthy()` no longer throws — *a health check that raises takes down the screen it was meant to protect*. ✅ **Guarded by a Python sweep over the Dart source** (*the Dart suite does not run in CI, and a guard that matters has to sit in the one that does*), pinning that the message names **the address AND the command** (⭐ *"cannot connect" without either tells a reader only that they are stuck*); **3/3 regressions caught**; a Dart test hits **port 1** — a real refused connection, not a stub. ⚠️ Open: the sweep covers **rendering**, not classification — a 500 would still surface a stack. | Accepted |
| [234](./ADR-234-which-chips-you-have-actually-got.md) | **Which chips you have actually got** — ✅ **BUILT; fixes ADR-229.** The owner reviewed Fantasy Football Hub — *"not because I want to copy like for like, but the testers use it as a benchmark"* — and across 24 screens most of what it surfaced was layout. ⚠️⚠️ **ONE WAS NOT**: the Hub labels every chip *"Played GW2"* / *"1x Not played"*, and **our advisor had no idea which chips were still in hand** — it would recommend a wildcard for GW11–13 to a manager who played it in GW4, ⭐ *not a rough edge, a wrong answer delivered confidently*. **Checked, not assumed:** `entry/{id}/history/` returns a `chips` array of what was played and when — **one endpoint away**. → `chips` takes an optional `manager_id`. ⭐⭐ **THREE STATES, NOT TWO**: `true` in hand · `false` spent · **`null` we could not check** — ⚠️ *and `null` must never render as `true`*, the same distinction as the bank's None-not-zero (ADR-223); without a manager id every chip reads **unknown** and the screen says so, because *silence would read as "you have all four"*. ⚠️ **AVAILABILITY IS PER HALF-SEASON**: `CHIP_HALVES` already knew this for *deadlines* (ADR-166) and **nothing used it for availability** — ⭐ *"have I used my wildcard?" is meaningless without saying WHICH wildcard*, and treating it as season-long would tell a manager in January he has nothing left. ⭐ **A spent chip is MARKED, not removed** (*when it would have been best is still true; hiding the card leaves a manager wondering whether the app knew*) — dimmed, with a pill that **names the gameweek**, because *"unavailable" alone invites a reader to think it is a bug*. ⭐ **Never load-bearing** — the timing advice needs no network, so a failed lookup loses the status and keeps the answer. ✅ **6/6 mutations killed** — ⚠️ one survived first (*the gameweek it was played is dropped*) because the endpoint tests **stub `_chip_status` wholesale**: ⭐ *a stub bypasses exactly the thing it stands in for*. ⚠️ Open: the same question applies to **free transfers, which FPL does not publish at all** — ⭐ *a chip we can check; a transfer we must ask about, and that asymmetry is worth remembering when someone proposes inferring one from the other*. | Accepted |
| [235](./ADR-235-one-card-three-readings.md) | **One card, three readings** — ✅ **BUILT** from the Hub review. Their pitch card carries **three gameweeks** and a menu switches the pitch between *Next GW · Next 3 · Price*. ⭐⭐ **A manager deciding whether to HOLD a player is asking about his RUN, not his Saturday** — and our per-gameweek numbers had been published since **ADR-213 and thrown away at the last step**. → one card, three readings, with the switch **out in the open** rather than behind the Hub's `⋯` menu: *it changes what every number on screen means, and that is not a thing to hide two taps deep*. ⚠️ **Fixtures matched by GAMEWEEK, never by position** — a blank gameweek means a club's third fixture is not the third week, so lining xP up by index gives ⭐ *three correct values arranged into a lie*. ⚠️⚠️ **The price mode refuses to say WHEN**: the Hub shows `↗ >1 Week · 0.6%` and **we do not compute that** (`price_detector` is a live percentile, not a progress estimate) — so the card shows **the call and the evidence** (direction + net transfers, a fact rather than a forecast), because ⭐ *inventing an ETA would be the "GW rating 88%" mistake*. The detector stays bound to **the whole board** (ADR-215: a percentile over a filtered view manufactures a top 2% inside the filter). 🔬 **What the tests found**: ⭐⭐ **a field describing another field has to be checked against it** — the answer reports `run: 3` and a mutation slicing fixtures to **one** survived, because the test asserted the *constant* not the data (*a client sizing its row from `run` would draw two empty columns*); ⚠️ **presence is not truth, a THIRD time** (`isinstance(0, int)` passes against a hard-coded zero); and ⚠️⚠️ **A BROKEN HARNESS REPORTS EVERY MUTATION AS KILLED** — a syntax error in the repaired test file made the run come back all-green for reasons unrelated to the mutations, so ⭐ *a harness that cannot run is not a harness that passes*, and the re-run now checks its own output for a syntax failure before believing a kill (**same species as the stale uvicorn and the not-applied pattern: the tool being wrong looks exactly like the code being right**). ⭐ *A seam is wherever the CALLER looks, not where the function was defined.* ⚠️ `my-team` grew **10 → 15 KB**. ⭐ Open: the Hub also colours cards by difficulty — **still declined**, and the run mode makes the case *weaker*: three numbers already show the run, and colouring them says the same thing twice while competing with the numbers. | Accepted |
| [236](./ADR-236-the-comparison-belongs-where-the-decision-is.md) | **The comparison belongs where the decision is** — ✅ **BUILT.** The owner, on the Hub: *"the way they do their AI transfers is akin to our Boot Battle / Player DNA"* — right, and more precisely than *similar feature*: **we already had the substance** (ADR-110's `compare_rows`), they had two presentations and **a placement**. ⭐⭐ **THE PLACEMENT IS THE FINDING** — theirs sits *inside the transfer flow*, ours on the Players page as a destination: *a screen that suggests `Groß → Belloumi` and cannot show you the two of them is sending you to another room to answer the question it just raised.* → `POST /api/v1/compare`, expanding **inside the transfer card**, **lazily** (*five suggestions cost five requests it never makes*). Three parts: the stat grid; **recent form WITH MINUTES** (⭐ *ten points off the bench is not ten points from a starter*); and **both projected runs on ONE SHARED SCALE** (⚠️ *normalising each line to its own maximum would draw two flat lines and call it a comparison*). ⚠️ **NO VERDICT** — ADR-197 gave the DNA comparison none on purpose and the tally is rendered small and grey: ⭐ *a count of stats won is a headline, not a recommendation; the engine already made the recommendation and this is the working behind it.* ⚠️ **THE THIRD MODULE THAT COULD NOT BE SHARED BECAUSE IT IMPORTS STREAMLIT** (after `badges.py` and `access.py`) — ⭐ *a module that mixes a rule with its rendering will eventually be needed by something that cannot render*; the pure half moved to `src/analytics/compare.py`, **verbatim not retyped** (ADR-222 lost a `-66` that way) and **all 185 existing card/DNA tests passed unchanged**. ⚠️ `_BETTER` knows a **lower** xGC is better — *a `max()` would crown the worse defence, on exactly the stat a defender is bought for* (pinned with a constructed fixture). ✅ **7/7 mutations killed** — ⚠️ one was a **NO-OP** (added an unused dict instead of changing `_BETTER`) and "survived" for reasons unrelated to the tests: ⭐ *a mutation that changes nothing proves nothing*, the same false signal as ADR-219's not-applied pattern and ADR-235's broken harness. ⚠️ **`code`, not `id`** — FPL restarts element ids each August, so history by id returns another player's season, *silently, and only after a summer*. ⏳ Open: the same comparison fits the **Players** tab — the audit's *"compare"*, still unbuilt. | Accepted |
| [237](./ADR-237-the-card-opens-where-the-row-is.md) | **The card opens where the row is** — ✅ **BUILT; completes the audit's §6 *search, compare, the card*** (bar the compare). The Hub's lists expand **in place**, and ⭐ *a list you can interrogate without leaving it beats one that sends you somewhere and loses your place in it* — which is what a separate card screen does, and what makes a browse list feel like a filing cabinet. → `POST /api/v1/player`, expanding inside the row: **the run first, with fixture difficulty** (*"is he good, or is this an easy month?" is what a row is opened to ask, and a projection alone cannot answer it*), **the last five with minutes**, then the season stats. ⭐ Fetched **on expand**, never with the list — *carrying every stat for 481 players so that one can be opened is the opposite of the trade the list was built on* (ADR-230). ⭐⭐ **THE STATS ARE ORDERED FOR THE POSITION** — a defender leads with **xGC** and **DefCon**, a forward with **goals** and **xGI**; that ordering has existed since ADR-084 and **had never reached a phone**. ⚠️ *The same twelve numbers in the same order for everyone is a table, not a card* — and it buries the one the reader opened the row for. ⭐ **Difficulty colours the FIXTURE, not the player** — *not ADR-179 reversed*: that declined colouring the **player card by points**; this colours an **opponent by difficulty**, which is the distinction ADR-179 was protecting rather than the one it refused. ⚠️⚠️ **A BRANCH THIS SNAPSHOT CANNOT REACH**: `stat_rows` drops a stat with no value, and a mutation removing that filter **survived** — measured, **no stat is missing for any player in the snapshot**, so the filter is unreachable through the API. ⭐ *An unreachable branch is not a safe branch; it is an untested one* — and it is **not dead code** (FPL returns nulls for a player yet to play), so it is pinned with a **constructed** test. **Eighth time this project has been caught by *does the population contain the case?***. ✅ 5/5 mutations killed · 2,312 tests. ⏳ Open: ⭐ **tap two rows to battle them** — Boot Battle is an endpoint now, and that is the last piece of §6. | Accepted |
| [238](./ADR-238-a-directory-holds-more-than-a-menu.md) | **A directory holds more than a menu** — two changes from the competitor review. ⭐⭐⭐ **Their More screen holds six destinations without feeling like a junk drawer because of the ONE-LINE DESCRIPTION under each name**: a menu asks you to guess what a noun means, a directory tells you, so it costs no more to read a longer one. Ours could not be a directory because half of it was controls — ⭐ *a row you CHANGE reads nothing like a row you ENTER* — so the manager id, the free-transfer stepper and the gameweek facts moved into **Settings** and feedback got a destination of its own (⭐ *two doors to one room drift apart*, ADR-184). ⭐ **Frequency decides depth** (ADR-230): free transfers change weekly, the screens they affect are opened daily. ⭐⭐ **The Settings row states its own value** (`Manager 2885974 · 2 free transfers`) — *showing the state beats offering the options*. §2: a **value-showing price filter**, and deliberately NOT everywhere — ⭐⭐ *the pattern is true where the options do not fit*: position has five and they all fit, price has sixty and must hide, and **the moment a filter is hidden the chip is the only place its setting can live**. ⭐⭐ **Splitting the screen introduced a bug that looked like nothing** — a pushed route is built from values captured at push time, so the stepper told the parent and REDREW NOTHING (*a control one route away from its state cannot hear that the state changed*). ⚠️⚠️ **The price sheet CLIPPED its most expensive options on a short screen** — found by a widget test at 800×600, smaller than any phone I had in mind and therefore the only size that asked. ⭐⭐⭐ **I "CORRECTED" A COMMENT USING A MEASUREMENT OF THE WRONG ARTEFACT**: the committed sample is 169 KB on disk and the wire carries 96 KB (indented + key-sorted = 76% bigger), so the original "~110 KB" was right and my fix was worse — ⭐ *a measurement of the wrong artefact is confidently wrong, not noisily wrong*, and **the guard I had just written caught my own correction**. ✅ 9 widget tests, **9/9 mutations killed** — ⚠️⚠️ but three first reported SURVIVED having **never applied** (`dart format` reflowed the indentation the patches matched on) and two reported a broken harness because the gate read an `info` lint as a compile error: ⭐ *four of nine first-sweep results were about the harness, not the tests.* | Accepted |
| [239](./ADR-239-a-phones-localhost-is-the-phone.md) | **A phone's localhost is the phone** — feature work stopped at the owner's call: everything from ADR-228 to ADR-238 ran on one Mac and no tester could open any of it. ⭐⭐ **THE BLOCKER WAS NOT THE SIGNING, IT WAS A `const`** — `kBaseUrl = 'http://localhost:8078'` reaches the dev server from macOS, the simulator and Chrome because all three share the host's network, and a phone does not. Baking a LAN address in would only move it (wrong on another Wi-Fi, wrong on a new DHCP lease, wrong again once hosted — ⭐ *three rebuilds for three answers to one question*), so the address became **runtime state with a compile-time default** (`--dart-define=MADBOOTS_API`, overridden by More ▸ Settings ▸ Server). ⭐⭐⭐ **THE CHECK IS THE FEATURE, NOT THE TEXT BOX**: a sleeping Mac, a server bound to loopback, a phone on 4G, a typo and a router admin page answering 200 are **five problems with one symptom**, two of them not the app's fault — so `reach()` returns four outcomes. ⭐⭐ `wrongService` earns the type: `/api/v1/health` now returns `{ok, service, version}`, because a bare `{"ok": true}` cannot tell this API from a captive portal and **reporting that as healthy is worse than reporting a failure**, every later error getting blamed on the app. A typo is answered **without touching the network**; a refusal **names all three causes**; ⚠️ and a verified address is the only kind that saves — *a setting that can brick the screen it is set from has to prove itself first.* §3: two Info.plist keys that **fail silently and look exactly like the "Connection refused" the owner already reported once** — ⭐ *a setting whose absence produces a plausible bug report needs a test more than one that crashes* — with ⚠️⚠️ **`NSAllowsArbitraryLoads` rejected BY NAME** (it disables transport security for every host and follows the app to the App Store; `NSAllowsLocalNetworking` stops at the LAN). §4: `serve_api.sh` binds `0.0.0.0` and **prints the Wi-Fi address**, because ⚠️ *a default that is correct for a laptop is the bug when the client is a handset.* ⭐ **This puts the app in your hand, not in your pocket** — Wi-Fi only, and 🔴 the free certificate expires every **7 days**. 📌 Held: Boot Battle from the list, the "Add filter" dropdown, and the signals badge — ⭐ **whose round-trip cost was about to be measured on localhost, which is ADR-238's wrong-artefact mistake again.** ✅ 16 Dart + 4 Python tests, **8/8 mutations killed**, `flutter build ios` green with both keys present **in the built bundle, not just the source plist**. | Accepted |
| [240](./ADR-240-a-risk-you-have-already-handled-is-not-a-risk.md) | **A risk you have already handled is not a risk** — first tester feedback off the phone build. The owner's own card read `51/100 Low` with **`− 8  M.Sangaré is flagged — already on your bench`**: ⚠️⚠️ **it charged 8 for a risk he had already mitigated and then told him to mitigate it**, so the score could not be improved by the action it was asking for. ⭐⭐ **THE INFORMATION WAS NEVER MISSING** — `gameweek.py` has computed `"starting": p["id"] in optimal` since ADR-208 under a comment saying *"a flagged player already on the bench costs the XI nothing"*, while the scoring counted all fifteen: ⭐ *a fact computed and then not used reads, from the outside, exactly like a fact nobody knew.* Fixed to count XI flags only — **51 Low → 59 Medium** — with the benched line **kept** at worth 0 and `kind: noted`, because ⭐ *hiding it would answer the complaint by removing the information.* Both renderers changed too: the phone printed `− 0` (the bug's own wording surviving its fix) and the CLI said *"minus 24 for 3 flagged players"* against a score docked 16 — ⚠️ **a card arguing with itself in the half written in words.** ⭐⭐⭐ **MY FIRST DIAGNOSIS WAS WRONG AND MEASURING CAUGHT IT**: I read the formula and declared the top band structurally unreachable, then measured — **39/40 of the most captainable players clear 75, median 89, and still 39/40 away to a tough side.** The claim was true and vacuous; I had reasoned from an equation about a population I had not looked at — ⭐ *the exact failure ADR-210 exists to prevent, committed in the paragraph citing ADR-210*, and the same species as ADR-238's payload size. ✅ 10 tests, 5/5 mutations killed. ⚠️⚠️ **All 2,331 existing tests passed before AND after** — not one distinguished a benched flag from a starting one, so the suite stayed green across a change that moved the headline number eight points and a whole band: ⭐ *passing tests measure what was asked, not what is true.* | Accepted |
| [241](./ADR-241-the-armband-did-not-survive-the-other-feature.md) | **The armband did not survive the other feature** — *"My team, C doesn't hold."* ⭐⭐⭐ **The armband code was fine**: it serialised, restored on reload, survived a restart and applied cleanly at render — read twice, nothing there. **What the C did not survive was `_planSwap`**, the one-click transfer the owner had just called the best thing in the app, which built a BRAND-NEW `Draft` and never carried the armbands across: the sequence he uses most — pick a captain, apply a suggested transfer — took the armband off without saying so. ⚠️ *Every line about armbands was correct; the fault was in a function that is not about armbands at all.* Fixed three ways: a swap now carries them and **drops one whose player has left** (⭐ *a C on a player you have sold is worse than no C* — it survives into the pitch and the next reload describing a squad that no longer exists); ⚠️⚠️ **`copyWith` can now CLEAR an armband** — `captainId ?? this.captainId` meant passing `null` asked for *"leave it alone"*, so promoting your vice left one player wearing **C and V at once**, which FPL would reject (⭐ *an optional argument cannot say "unset" and "no opinion" with the same value*); and ⭐⭐ **the logic moved out of the widget into `Draft.swap`** — ⚠️ *logic inside a `State`'s private method cannot be called by a test, so its test has to RE-DESCRIBE it, and a re-description drifts without either side going red.* ⚠️⚠️ **MY FIRST REPRODUCTION INCLUDED THE FIX AND PASSED, PROVING NOTHING** — the helper carried the armbands because that is what the code obviously ought to do: ⭐ *a reproduction that quietly includes the fix reproduces the fix, not the bug.* ⚠️ Two mutations again reported SURVIVED having never applied (`dart format` reflowed the patched lines — the third time this session), and ⭐ **one real gap was found by a mutation**: deleting the line that swaps the outgoing player off the bench left every test green, so a plan could hold a man you no longer own AND omit the one you bought. ✅ 8 tests, 5/5 mutations killed. 📌 Open: setting an armband still stores the ENGINE'S recommended bench as yours. | Accepted |
| [242](./ADR-242-a-space-drawn-for-data-that-never-arrived.md) | **A space drawn for data that never arrived** — six items off the first phone feedback; three were the same bug in different clothes. ⭐⭐⭐ **THE RUN CARD HAD THREE COLUMNS AND ONE NUMBER**: ADR-235's premise is *one fetch, three readings*, but `by_gameweek` follows the request's `horizon` — which is **1**, because the headline beside it is a this-week projection — so two of three columns read `—` on every card, on every squad, from the day it shipped. ⚠️ *A field that is absent renders as a dash, and a dash reads as "no data for this player" rather than "this endpoint was never asked for it"* — which is why nobody called it a bug for a month. Fixed with a **second analysis pass, not by raising the horizon**: raising it takes `projected_xp` from 44.9 to 133.9, ⭐ *a three-week total wearing a one-week label — a fix that corrupts the number beside it is not a fix.* §2 **the last five now say who they played** — `player_history` has held `opponent_team` and `was_home` since ADR-201, and ⚠️ *two blanks against City and Arsenal say something completely different from two blanks against the bottom two*; `opponent` is **null, never a guess**. §3 **Confidence · Edge · Risk become three cards** — the label was top-RIGHT where every other card puts it top-left, and ⚠️ *a layout that is unique for no reason reads as a mistake, whatever it looks like on its own.* §4 a smaller draft banner (⭐ *the colour was doing the work; the height was only making it loud*) and **"Transfer…" not "Replace him…"**, because an app that renames a manager's moves makes him translate. §5 a **My squad** filter, disabled not hidden — *a chip that vanishes looks like a bug.* ⚠️⚠️ **FOUND WHILE BUILDING: `dart format` AND THE BRAND GENERATOR WERE FIGHTING OVER ONE FILE WITH NO STABLE STATE** — formatting rewrapped the generated strings and failed the byte-for-byte test, regenerating unwrapped them and the formatter rewrapped them: ⭐ *the symptom was a test failing for a file nobody had edited.* Fixed with `// dart format off` in the generated output — ⚠️ `formatter: exclude:` does NOT cover `dart format lib/` with an explicit path, and the marker is recognised **only verbatim** (a trailing comment silently disables it). ⚠️ **One mutation survived that mattered**: making `my_team` reuse the narrow answer — the exact shipped bug — left every test green, because they all exercised `analysis` rather than the composition that drew the card: ⭐ *a test one layer away from the defect passes through it.* ✅ 9 tests, 7/7 mutations killed. | Accepted |
| [243](./ADR-243-the-app-wore-someone-elses-logo.md) | **The app wore someone else's logo** — the home-screen icon was still **Flutter's default blue mark**, and had been since `flutter create`, through sixteen ADRs of feature work. ⭐⭐ **Nobody noticed until it was on a phone**: on a simulator you know which app you just launched, and on desktop it is a window you opened — ⚠️ *an icon only has a job in a grid of other icons, so the defect could not be found anywhere but the real setting, however carefully you looked.* `scripts/generate_app_icon.py` composites the badge onto `brand.INK` for all 15 iOS + 7 macOS sizes, ⭐ **reading the size table out of Xcode's own `Contents.json` rather than hard-coding Apple's list** — a written-down table goes stale the next time Xcode adds an idiom, and the symptom is a missing icon **in one place only**. ⚠️⚠️ Opaque, because iOS rejects an alpha channel; the same script also writes the **header badge** as a transparent Flutter asset, ⭐ *one script and one master, so the mark on the home screen and the mark in the header cannot drift apart.* The header now carries the badge beside the wordmark — on a phone the badge is what a person recognises, being what they tapped a second earlier. ⚠️ **Resolution, plainly:** the master is 298×298 and the largest icon iOS renders on a device is 180×180, so every on-device size is a **downscale** and loses nothing; 🔴 the **1024 App Store asset is upscaled and soft**. 📌 Owed before any submission: a real 1024 render from `~/Downloads/madboots1.svg` (genuine vector, 5 paths) — this machine has no SVG rasteriser and installing one belongs to whoever submits. | Accepted |
| [244](./ADR-244-the-gap-and-the-missing-button-were-one-thing.md) | **The gap and the missing button were one thing** — two separate notes on one feedback list that answer each other. ⭐⭐⭐ **The app could already tell you your lineup was wrong and gave you no way to fix it**: This Week said *"3 changes"* and named them, and acting on it meant reading three lines, remembering them and tapping four shirts on another screen — ⭐ *an app that can compute the answer and makes you transcribe it has stopped halfway.* Meanwhile the bottom of My Team was **14% dead space**. **The action belongs in the gap**: `my-team` now carries `suggested_lineup` and the strip offers *"+5.5 xP from 2 changes · Start X and Y"*. ⚠️⚠️ **LINEUP ONLY, NEVER TRANSFERS** — starting a player you own is free and reversible, a transfer costs points and cannot be undone: ⭐ *one button must not do both, whatever the xP says.* Null when your XI is already best, and the strip **says so once, quietly** — ⚠️ *silence and reassurance are different answers and both are sometimes right.* ⭐⭐ Reuses `best_legal_xi` on `gameweek_plan`'s own `lineup_xp` convention, **with a test that fails when the two screens field different elevens** — ⚠️ *deriving a second, subtly different optimum is how two screens start recommending different teams.* The gain is scored on the REAL xP, never the ADR-154 zeroed map: ⭐ *the number on the button has to be one he can actually get.* ⚠️⚠️ **THREE OF FIVE MUTATIONS SURVIVED AND TWO WERE NO-OPS** — the leaver branch is unreachable because there are **zero reported leavers on the whole board today**, measured not assumed: ⭐ *a mutation that survives because the population has no instance of the thing under test says nothing, and reporting it as coverage would be a lie.* Fixed by calling the function with a constructed leaver — ⭐ **building an input is wrong when live data can reach the branch and right when it cannot**; ⚠️ the first attempt hand-built the whole squad and `best_legal_xi` returned an **empty XI**, because *a fixture the code cannot consume is not a simpler test, it is a different one.* ⚠️ The third survivor was a weak assertion (bench "has one keeper" — true of every permutation), and ⚠️ three Dart tests **skipped themselves** when the sample had nothing to suggest. ✅ 7 Python + 4 Dart tests, 5/5 killed. | Accepted |
| [245](./ADR-245-trending-was-signals-asked-in-a-different-shape.md) | **Trending was Signals asked in a different shape** — the owner asked for Trending inside Signals with a squad/global filter, and ⭐ **he is right that they are one screen**: both answer *what is the crowd doing?*, and a second would have meant a second ordering, a second idea of what counts as evidence, and two places to keep honest. ⭐⭐⭐ **THE MARKET HAS TO BE BOUNDED AND THE BOUND HAS TO BE VISIBLE**: measured first — ownership has a **median of 0.2%**, so an unbounded sweep returns **194 news items**, nearly all about players almost nobody holds (⚠️ *a list that long is not more information, it is a screen a person stops reading*); at the **75th percentile (1.2% owned)** it is **22**. The cut is that percentile of the LIVE distribution, never typed — *"the quarter of the board most managers consider"* — with a test that halves every share and fails if the cut does not follow (ADR-210). ⚠️⚠️ **The answer reports the cut it used** and the app prints it, because *a market view that silently drops four fifths of the board lies by omission* and ⭐ *a filter the reader cannot see is one he will eventually be surprised by* (ADR-215). `trending` joins the kinds ⚠️ **at the BOTTOM** — the weakest evidence here, a fact about other managers rather than about the player: ⭐ *"lots of people did this" is the reason a template forms, and it is not on its own a reason to join one* — labelled **"The crowd"**, not "Trending", which would sound like a verdict the data cannot support. ⭐ It never fires for a player you already hold (*"12,000 managers bought him"* about your own is not news — you are one of them). ⭐⭐ Every signal says whether it is **yours**, because *"yours" changes what a signal MEANS*: an exodus from a player you hold is a decision, the same exodus from one you do not is a fact about the market. ✅ 10 tests, 7/7 mutations killed. ⚠️ One first-draft assertion was `assert … or True` — vacuous, papering over `player_summary` not carrying ownership: ⭐ *a hedge in an assertion is a confession.* 📌 Open: the web Trending page is now a second implementation of this question and the two will drift. | Accepted |
| [246](./ADR-246-the-formation-rule-stays-where-it-lives.md) | **The formation rule stays where it lives** — the player sheet could make a captain and start a transfer but not do the free, reversible thing a manager does most: **change two players' places**. ⭐ The interesting part is the rule, not the gesture: FPL's limits (1 GK · 3–5 DEF · 2–5 MID · 1–3 FWD) decide which swaps are legal, and **a client offering "any bench player" would propose squads FPL rejects**. ⭐⭐⭐ **THE SERVER SAYS WHO IS LEGAL AND THE CLIENT RENDERS THE LIST** — `my-team` carries `swaps`, computed by `legal_xi_issues`, because working it out again in Dart would be a second implementation of a rule the engine owns, and ADR-151→156 is this project's record of one fact being re-taught to six surfaces one at a time. ⭐ Computed by **trying** each swap rather than reasoning about it: *ask the validator, do not re-derive what it knows.* On the sheet **"Bench him…" sits above "Transfer…"** — ⚠️ *order on a list of actions is a recommendation whether or not it was meant as one*, and one is free while the other costs points. Only legal partners appear at all (greying out the rest invites a manager to wonder what he did wrong when the answer is *"nothing"*), and the incoming player takes the outgoing one's **place in the bench order** — ⚠️ *appending would quietly demote him to last, a change he did not ask for hidden inside one he did.* ⚠️⚠️ **THE FIRST VERSION OFFERED A KEEPER A SWAP WITH A FORWARD**: it removed the wrong player and built a **TWELVE-MAN XI**, which `legal_xi_issues` validated happily because it checks position RANGES and not list size — ⭐ *a validator answers the question it was asked, and "is this eleven" was never asked.* ⚠️ The `len == 11` guard added afterwards was then **removed**, a mutation having proved it could not fire (one out, one in, is eleven by construction): ⭐ *an unreachable guard is not defence in depth, it is a comment that looks like code.* ⚠️ One test **skipped itself** because the fixture XI held two forwards, so *"you cannot bench your only striker"* was never exercised; and the suite needed the **converse** property, since *"everything offered is legal" is satisfied by a function that offers nothing.* Also: the pitch price line (`£12.0m · TOT (H)`) no longer wraps — ⭐ *a card that changes height with its contents stops being a grid.* ✅ 5 Python + 5 Dart tests, 5/5 mutations killed. | Accepted |
| [247](./ADR-247-a-club-is-half-of-what-a-player-is-worth.md) | **A club is half of what a player is worth** — the owner asked for DNA in More, and the engine has two. ⭐⭐ **Player DNA answers *what kind of player is he?*, which the app already answers TWICE** (the expanding card, ADR-237, and Boot Battle, ADR-236); **Team DNA answers *is this attack actually any good?***, which decides between two players from different sides and the app could not answer at all. So Team DNA: all twenty clubs, eight axes, best first, with the grade and the insights the web renders. ⭐ Every axis is a **percentile**, so 74 means the same thing on Attacking Threat as on Squad Depth — which is what lets eight different units share one scale. ⭐⭐ **BARS, WHERE THE WEB DRAWS A RADAR**: a radar needs width a phone does not have, and eight labels round a circle on a 390pt screen are unreadable — ⚠️ *porting a layout is not the same as porting a view.* ⚠️⚠️ The squad **marks and filters nothing** (the "Mine" toggle is off by default): ⭐ *a league table you can see yourself in is a different object from a league table of the clubs you already own*, and narrowing would make the grades meaningless since they are ranks across all twenty. ⚠️⚠️ **TWO MUTATIONS SURVIVED BY BEING NO-OPS** — measured: the board has **zero null percentiles and zero tied scores**, so turning an unranked axis into `0` and dropping the tie-break both changed nothing. ⭐ **The tie-break one is subtle**: the first test compared two runs of the same query and Python's sort is stable, so it agreed with itself no matter what — ⚠️ *stability is not determinism when the input can be reordered.* It shuffles now. ⭐⭐ **`test_player_shape.py` caught the new endpoint before I did**, naming `team_dna` on the first full run; registered in `NO_PLAYERS` with a reason, because ⚠️ *"nothing to check here" has to be written down or the next reader cannot tell it from "nobody checked".* ⚠️ This is the app's **first pure exploration surface** — ADR-238's More copy said those live on the web, and that paragraph is corrected: ⭐ *positioning copy that outlives the positioning is worse than none.* ✅ 9 tests, 5/5 killed. 📌 Player DNA on the player card is the obvious next home. | Accepted |
