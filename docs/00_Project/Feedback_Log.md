# Tester Feedback Log

A running log of tester feedback on the live app, triaged into the **Sprint 060** backlog. Raw reports
come in via GitHub Issues (https://github.com/madbootsfpl/fpl-assistant/issues); this table is the
**triaged** view — one row per distinct item, newest first.

**Severity:** 🔴 broken/blocking · 🟠 confusing/wrong · 🟡 polish/nice-to-have · 💡 idea

| Date | Tester | Tab | What happened / suggested | Severity | → Backlog? |
|------|--------|-----|---------------------------|----------|-----------|
| 2026-08-28 | Owner | Home | **US-433** — **Home page needs updating to reflect the latest changes.** | ✅ fixed | Verified: `Home.py` predates 🏆 Leagues · 📡 Signals · the H2H + transfer flow. **Same staleness as the madboots.com grid** (`docs/08_Marketing/Homepage_Copy.md`) — do both in one pass so the in-app and public copy can't drift apart again. |
| 2026-08-28 | Owner | My Squad | **US-434** — **Merge AI Tips and Chips.** Chips should default to the **chip deadline** rather than trying to calculate for 1 or 3 GWs. | 💡 | Two items. The merge is IA (see the US-435 cluster). **The deadline default is the substantive one and is right**: a chip is a season-level decision with a fixed expiry, so a 1/3-GW horizon answers a question nobody asked. Needs an ADR — it changes what `chip_advisor` optimises over. |
| 2026-08-28 | Owner | My Squad | **US-435** — **Merge My Squad, AI/Chips and Captain into one screen** under the My Squad sub-tab — most of what's needed for an informed decision is then on the golden page. | 💡 | The anchor of the IA cluster. ⚠️ **Read ADR-135 first**: consolidating the golden page hit its density target exactly and was reverted the same day. The failure was *mechanism* (a floating menu, two round-trips), not *ambition* — merging into one scrolling screen is a different mechanism. Needs a gate. |
| 2026-08-28 | Owner | My Squad | **US-436** — **Health → rename to DNA.** Squad DNA is the powerful part — lead with it and put the health check underneath, as it's less informative. | 💡 | Cheap and probably right. Note ADR-119/130 already put Squad DNA *inside* Health; this inverts the order and the name. Watch the vocabulary collision with **Team DNA** and **Player DNA** — three DNAs needs a naming rule, which is the unresolved 'Team DNA & FDR' naming question resurfacing. |
| 2026-08-28 | Owner | Leagues | **US-437** — **Leagues should be a sub-tab of My Squad**, not a sidebar page. | 💡 | IA cluster. |
| 2026-08-28 | Owner | Trending | **US-438** — **Should the whole Trending tab be a sub-tab under Players?** | 💡 | IA cluster. Note ADR-149 already merged News→Signals on a *doing vs saying* axis; Trending is the 'doing' half, so this asks whether 'doing' belongs with Players. |
| 2026-08-28 | Owner | Trending | **US-439** — **Trending sub-tabs aren't consistent with the other sub-tabs.** | 🟠 | ✅ **Verified and correct.** Trending uses `st.tabs()`; Players and My Squad use `st.segmented_control()`. A genuine inconsistency and a small fix. |
| 2026-08-28 | Owner | Team DNA | **US-440** — **No heading for FDR** on the Team DNA & FDR page. Should DNA & FDR be sub-tabs? | 🟠 | ✅ **Verified and correct.** The page has `st.title('🧬 Team DNA & FDR')` and `st.subheader('🧬 Team DNA')` — the FDR half has **no heading at all**, just a divider and a slider. Heading = trivial; sub-tabs = IA cluster. |
| 2026-08-28 | Owner | Team DNA | **US-441** — **Team DNA should have a 'my squad only' option.** | 🟠 | ✅ **Verified — half exists.** The *ticker* has the checkbox (US-302/407b); the **20-club DNA scan above it does not**. So the filter is on the FDR half and missing from the DNA half, which reads as inconsistent within one page. |
| 2026-08-28 | Owner | Signals | **US-442** — **Signals needs a global 'my squad only'** for news/signals. | 🟠 | ✅ **Verified — partial.** The shared filter reaches the buzz section (ADR-149) but is not a page-level control across all four sections. Pairs with US-441: the real ask is **one squad lens that applies page-wide**, not another per-section checkbox. |
| 2026-08-28 | Owner | Signals | **US-443** — Promote the **blue banner summary** under *'An exodus we can't explain'* nearer the top — it's real news. And should the blue banners across pages use the **standard MADBOOTS card style**? *(for discussion)* | 💡 | Two asks. Promotion is cheap. The banner restyle is a **design-system** question that spans many pages — worth doing once, properly, as its own ADR rather than page by page. |
| 2026-08-28 | Owner | My Squad | **US-444** — ✅ **FIXED (Sprint 219).** **Clicking a player shows the condensed profile but you can't dismiss it** without leaving the page and returning. Clicking the pitch or the player again should hide it. | 🔴 | ✅ **Verified — the only outright defect in this batch.** The picker has a `—` option so it *is* clearable, but the tap has no toggle: tapping the selected shirt again re-selects it. Fix in `tap.render_tappable_pitch` — a tap on the already-selected player clears the selection. Small, self-contained, no gate needed. |
| 2026-08-28 | Owner | Squad Lab | **US-445** — **Should Squad Lab be a sub-tab of My Squad?** You only use the lab a few times a season — season start, wildcards, free hits. It's a distraction on the sidebar. *(thoughts rather than action)* | 💡 | IA cluster, and it **partly reverses ADR-105** (Sprint 146), which split them deliberately. The frequency argument is new and good; re-read ADR-105's reasoning before deciding, not after. |
| 2026-08-28 | Owner | Players | **US-446** — **Players sub-tabs are getting crowded.** *(thoughts?)* | 💡 | IA cluster — and in tension with US-438, which would add Trending to this same page. |
| 2026-08-28 | Owner | Ask | **US-447** — **Most of Ask is covered by AI Tips or Chips.** How could this AI be made more relevant/powerful, and what would that mean? *(discussion)* | 💡 | The biggest item here and the only **product** question rather than an IA one. Deserves its own conversation, not a slot in a UX batch. |
| 2026-08-28 | Owner | Sidebar | **US-448** — **Reorder the sidebar** after these changes — Home, then My Squad, Players, Leagues, Signals, Team DNA, then the rest. **Reduce to 10 if possible.** *(thoughts?)* | 💡 | ⚠️ **The count target is the ADR-135 trap.** Widget count was a bad proxy for clutter once already: the target was hit exactly and the result was worse on two devices. Order by *how often you need it* — which is exactly the reasoning in US-445 — and let the number fall out. |
| 2026-08-28 | Owner | Mobile | **US-449** — **On iPhone the xP for XI / Captain / Bench wraps** where it doesn't on desktop; same on other screens including the Leagues transfer flow and head-to-head. Could it be a scalable banner that shrinks on smaller devices? | 🟠 | Responsive defect, and the metric rows added this week (H2H `st.columns(3)`, transfer flow `st.columns(4)`) made it worse — Streamlit columns don't reflow on narrow screens. Real, and the fix is one shared component, not per-page tweaks. |
| 2026-08-26 | Owner | Leagues | ✅ **A loaded league must persist between sessions and across devices.** Re-entering the manager id every visit defeats the point of importing one. | ✅ fixed | **✅ SHIPPED — ADR-147, Sprint 202.** Remembers the **manager id** (which restores the whole league list) as well as the last league. ⏳ Needs `user_prefs` created — SQL in the ADR and in Admin ▸ 🔧, which shipped with the feature. *(triage:)* The pattern already exists and is proven twice: squads (ADR-106) and the ⭐ watchlist (ADR-117) both persist per-user, keyed by `auth.user_key(email)`, in the same Supabase project — no new secret, no new table shape. So this is *"store the chosen league id (and manager id) under the user key, restore on load"*. ⚠️ Two things its ADR must settle: what happens **signed-out** (today the page works without an account — a session-only fallback keeps that true), and whether it remembers the **manager id**, the **league**, or both (remembering the manager id is the bigger win: it re-populates every league). |
| 2026-08-26 | Owner | Trending | **Community signals should have a "My squad only" filter.** | ✅ fixed | **✅ SHIPPED — ADR-149, Sprint 204.** ⚠️ **The triage below was wrong in both directions** and is kept as written, because the error is the useful part: Trending's four *boards* have honoured the filter since US-407b; the surface that did **not** is the 💬 **Community Signals** tab — precisely what the owner named. Now filtered, after the scan so *"6 of 47"* stays meaningful. *(the wrong triage:)* The shared filter `sel` carries a `my_squad` dimension and `filters.apply` already honours it; Players and the Radar both use it. Trending builds its boards from the player pool without passing through that filter, so this is wiring rather than new logic. **Why it matters more than it looks:** the Trending boards answer *"what is the crowd doing?"*, and the only version of that question a manager can act on is *"…to MY players"* — which is exactly the ADR-146 crowd-exodus insight one surface over. |
| 2026-08-26 | Owner | News · Trending | ✅ **Should "Talked about" and News be consolidated?** | ✅ fixed | **✅ ANSWERED + SHIPPED — ADR-150, Sprint 205.** Yes, but the merge is bigger than the question: there were **four** lenses across two pages, and the axis is **doing vs saying**, not official vs unofficial. 📡 Signals now holds all four, descending by evidentiary strength; Trending keeps the leaderboards. *(original triage:)* **⬆️ ROADMAP as an IA question, with the distinction stated so it is decided rather than assumed.** They are **different sources, not duplicates**: *News* (page 7) is FPL's **official** `news` field — injuries, doubts, return dates, the thing that drives `status`. *Talked about* (a tab on Trending) is a **Reddit RSS mention count** — chatter, not fact. Merging them risks presenting a rumour beside an official flag with equal weight. **But there is a real case for it**, and ADR-146 just made it stronger: the app now has three signals answering *"what should I know that the table doesn't tell me?"* — official news, community chatter, and an **unexplained transfer exodus**. Those belong together far more than chatter belongs with ownership leaderboards. Likely shape: one **Signals** page (official · chatter · crowd exodus), leaving Trending as pure leaderboards. Needs an ADR — it is a page merge. |
| 2026-08-26 | Owner | My Squad → pitch | ⚠️ **"The hover on a player is missing — great if it appeared on a click, with the teal highlight."** | ✅ fixed | **✅ CLARIFIED + SHIPPED — ADR-139 rev, Sprint 201.** Reading **(b)**: the owner wants the compact card back **in place on the pitch**, on click, *as well as* the detailed panel card. Both now render. Bound to the **selection** rather than the cursor, so only the selected kit carries a card at all — exactly one is ever open, and it is always the player you chose. **I removed too much:** the original complaint described *when* the card appeared, never *what it contained*. *(original triage:)* **THIS MAY MEAN ADR-139 DIDN'T LAND AS INTENDED.** ADR-139 (shipped hours earlier, same day) removed the hover popover *because* it misbehaved, on the reading that a tap already reveals the **full card in the panel below the pitch** — which it does, and the card was moved to the top of that panel for exactly this reason. Two possible readings and they need different work: **(a)** the panel card isn't registering as "the card appeared" — a **discoverability** problem, fixed with placement/scroll or a caption; **(b)** the want is genuinely an **in-place popover on the shirt**, which ADR-139 explicitly scoped out (it is what ran out of room on a phone and collided with neighbours). ⛔ **Not guessing this one** — two rounds were already burned guessing at the dot-blur in ADR-138. Owner to confirm which. |
| 2026-08-26 | Owner | AI tips / Health | ✅ **"Watkins is looking at a move to Saudi — no warning from AI tips or Health, despite a lot of people transferring him out."** | 🐛 correctness | **✅ FIXED — ADR-146, Sprint 200.** I had examined this case in ADR-138 and wrongly concluded there was nothing to build ("nothing in the FPL data says so") — that treated the *feed* as the boundary of what the app knows. It was already computing **net −96,095** and a ❄️ out flag, three clicks from where it mattered. Now `crowd_exodus` flags a heavy sell-off our own status/news **cannot explain** (the discrepancy is the signal: of the 8 biggest exoduses, 5 were explained and 3 — Gyökeres, Konsa, Watkins — were not). Reaches the gameweek flags and the Risk Monitor. Never claims to know *what* the news is. |
| 2026-08-26 | Owner | Admin → Tester activity | ✅ **"Not a true reflection of who is logged in."** Panel read Registered 25 · 🟢 Active 1 · ⚪ Never 18 while two testers were using the app daily. | 🐛 correctness | **✅ FIXED — ADR-142, Sprint 196.** "Active" never measured what its label said: the only signal was the `updated_at` on a **saved squad row**, so it meant *saved a squad in 7 days*, and most people never save. Now records a real sign-in time (`beta_users.last_seen`, stamped at admit; the anonymous ADR-100 stream is untouched), with *Last used* and *Last saved a squad* kept as separate columns. ⏳ Needs one line of SQL: `alter table beta_users add column if not exists last_seen timestamptz;` — until then it degrades to the old behaviour and says so. |
| 2026-08-25 | Owner | My Squad → pitch | ✅ **The hover card on a player is too much — it should open on a *click*, with the teal highlight.** The hover follows the cursor across other shirts while one is selected, so you read one player's stats beside another player's selection. | ✅ fixed | **✅ SHIPPED — ADR-139, Sprint 193 (2026-08-25).** The last loose end from ADR-135 — and the *opposite* of re-opening it: a tap that **reveals** is ONE round-trip (ADR-133's shape); it was the two-tap flows that cost two reruns and felt broken. Selection state, teal outline and click ids already exist — this changes what a selection *shows*. Hover would be suppressed entirely. Open for its ADR: where the card renders on a phone, and the keyboard path. |
| 2026-08-25 | Owner | Players / tables | ✅ **Price change arrows should be green for up, red for down.** | ✅ fixed | **✅ SHIPPED — ADR-140, Sprint 194 (2026-08-25). Verified: they were not.** `price_flag` returns 🔺/🔻 and *both are red* in Unicode (U+1F53A is "red triangle pointed up"), so colour — the fastest channel to read — currently carries nothing. ⚠️ No green triangle exists in the emoji set, so this is not a one-character swap: `:green[▲]` markdown does not render inside `st.dataframe` cells, where most of these live. Pick the mechanism (colour markdown vs pandas Styler vs inline SVG) before the colour. |
| 2026-08-25 | Owner | New feature | ✅ **Can we import leagues (like fplstats.live) and do the elite manager comparison?** | ✅ fixed | **✅ SHIPPED — ADR-141, Sprint 195 (2026-08-26), merged with "Elite Manager Comparison"**, because they share all their plumbing: import a mini-league by id (same shape as the ADR-113 Manager-ID import), then compare — effective ownership *in your league*, captain split, transfer flow, who is climbing. The elite version is the same code pointed at a public league. Unblocked since the GW1 deadline. ⚠️ First feature needing many API calls per view (one per manager per gameweek — a 20-manager league over 5 GWs is 100 calls); its ADR must settle caching/rate-limit before design. |
| 2026-08-21 | Owner | My Squad ▸ pitch (kits) | **Pitch shows stale player mugshots for just-transferred players** (Konsa, Isak, Semenyo, Mbeumo etc. appear in their *prior* club's kit vs FPL showing the new one). **NOT a data bug** — verified: team / fixtures / price are all correct in MADBOOTS (Konsa = ARS, Isak = LIV, …). Cause: the pitch draws each player's **FPL mugshot** (`p{code}.png`, ADR-084/US-255 — "photo, else club shirt"), and FPL's **photo CDN lags a transfer by weeks** while its **kit graphic** updates instantly — so FPL's kit-based pitch looks current and MADBOOTS's mugshot-based pitch looks a step behind. **Owner decision: switch the pitch to the club KIT shirt and drop the mugshot** (transfer-proof — the shirt derives from the live club). | 🟡 polish | ✅ **SHIPPED (US-429, 2026-08-22 — ADR-084 revision).** The pitch draws the **live club kit** via a new `badges.shirt_url_by_id` (transfer-proof — the shirt derives from the current club, like FPL's own pitch), so a just-signed player is never stuck in a stale mugshot; the **hover card keeps the mugshot** (`photos`, self-heals when FPL refreshes the photo). `render_pitch`/`_kit_html` gained a `kits` map (falls back to `photos` for older callers); `render_build`/`render_my_squad` build it from the live team. +4 regression tests; AppTest smoke = **19 kits, 0 mugshots as the pitch image**. |
| 2026-08-19 | Owner | Fixtures tab (IA) | **Restructure the Fixtures tab around Team DNA.** Team DNA is the unique value; FDR is "handy but available in every tool"; the Radar is player-discovery. Proposal: **(a)** rename the tab **"🧬 Team DNA & FDR"** (keep "FDR" for discoverability — "Fixtures" is a term people scan for); **(b)** reorder **Team DNA first, FDR ticker second**; **(c)** move the **🎯 Radar** to a **view on the Players tab** (player-discovery belongs there). Agreed direction (player-level vs team-level IA split). | 💡 IA | **PARKED for a morning IA sprint** (agreed shape above). Notes: it's mostly *moving existing renders* + a **page rename** (`3_Fixtures.py` → new sidebar label + URL slug; repoint "Radar's on Fixtures" signposts + Help). Watch: the Players view list hits **9** with Radar (segmented control ceiling — future group/dropdown if it grows). Gate as an ADR + plan, then build. |
| 2026-08-19 | Owner | Players ▸ History · xG board | **Two bugs.** (1) **History**: the shared filter isn't applied at all — the player picker lists everyone regardless. Cause: `render_history` never took `sel`. (2) **xGI rating** reads **"top X%"** where a *higher* X is *worse* — e.g. a very-poor player shows **"top 91%"**, which reads backwards (top sounds good). | 🔴 broken + 🟠 confusing | ✅ **US-427 + US-426 (2026-08-19).** (1) `render_history` now takes `sel` + scopes the picker via `apply_filter` (like the Card view). (2) `ratings.quality_band` reads **"top N%"** for the better half, **"bottom N%"** for the worse half (best "top 1%", worst "bottom 1%"). +regression tests; 1091 green. | **"My squad only" empties the board.** On **Clean sheets · Defensive Contribution · Over/under**, ticking *My squad only* → all players vanish (xG/xA/xGI was fine). **Cause:** those boards filter the **analytics output** (`over_under`/`defensive_solidity`/`defcon_reliability`), which built rows **without `id`** — so the id-based `my_squad` filter dropped every row. (xG filters the **raw** players, which have `id`.) | 🔴 broken | ✅ **US-425 (2026-08-19).** Carry `id` through the three analytics outputs. + a regression test (rows carry id + the my_squad filter keeps them); fixtures gain an id. 1090 green. | **Collapse the filter into a popover** (competitive look at joinonetwo.com — a "Filter" button opening a panel). "Will save so much real estate space," esp. mobile. | 💡 UX | ✅ **US-424 (2026-08-19).** `filter_controls` (the shared filter, ADR-064) now renders inside a single **🔽 Filter** `st.popover` (was an always-visible widget row) with an **active-count** in the label (`🔽 Filter · N on`) so you know it's on while collapsed — collapses on all four callers at once. Return dict + `apply` unchanged; tests green. | **Per-GW xP toggle — cumulative vs single-GW.** Today the "Gameweeks ahead" selector shows a **cumulative** projected xP (GW1, or GW1+GW2 at horizon 2, …). Testers want a **toggle** to switch between **cumulative (GW1→N, as now)** and **this-GW-only (just GW N)** — e.g. select GW2 → see GW1+GW2 *or* GW2 alone; same for GW3. | 💡 feature | ✅ **US-422 + ADR-121 (2026-08-19, pre-GW1).** The **per-GW data already existed** — `decision_xp` returns `by_gameweek` (ADR-032; already used for the player-card per-GW row, ADR-109). So it's a display/UX build: a **"Cumulative ⇄ This GW"** toggle on the projected-XI / Captain / Bench metrics (and ideally the pitch xP chips). |
| 2026-08-19 | Tester | My Squad (mobile/tablet esp.) | **Too much real estate between the header and the pitch.** The pitch (the main thing) sits *below* a tall stack: header + mark + blurb · Tool tabs · Gameweeks selector · deadline line · Squad picker · caption · the big **YOUR TEAM** banner · Download-backup · Import expander · the £/legal line · 3 xP metrics · caption. On **phone/tablet** it all stacks → you scroll a long way before seeing your team. *(3 screenshots.)* Owner: "UX review or not?" | 🟠 UX/density | ✅ **US-423 (2026-08-19, previewed + owner-approved, pre-GW1).** Condensed the above-pitch chrome so the pitch sits higher on mobile: the tall "Your team" banner → a **one-line status card** (dropped the save-explainer paragraph + foot mark; the pill conveys sync); **Download + Import folded into one collapsed ⚙ Backup / import panel**; the £legal green box → a compact caption; the "on the pitch…" caption dropped + the page blurb trimmed. Display/copy only, no logic. Preview: `scratchpad/mysquad_density_preview.html`. |
| 2026-08-19 | Owner | Squad Lab → Build mode | ✅ **Rethink the 3-mode set (follow-on to the "Strong XI" rename).** Two oddities: (1) **"Balanced"** maps to `bench_weight=None` = the **max-15** build → a **strong** bench, so the labels now imply the opposite (Balanced = strong bench · Strong XI = weak bench). (2) **"Balanced" and "Bench Boost" produce the *same* squad** — both `bench_weight=None`; `bench_boost` is only a **display** flag on `render_squad`, not a different optimisation. So there are really only **two** distinct builds (strong-XI-weak-bench vs max-15), presented as three. | ✅ fixed | **✅ FIXED — ADR-137, Sprint 191 (2026-08-25).** Both oddities confirmed on live data, and (2) was worse than reported: Bench Boost produced the same fifteen **even at `bench_weight=1.0`**, because that objective reduces exactly to the max-15 one — so it could never have been a distinct build. Took the **merge** branch: **All-round (strong bench)** + **Strong XI (cheap bench)**, with the chip answered in a caption where the question is asked. Default unchanged (checked: XI-first is +7.2 XI xP but buys a 4.9-xP near-dead bench slot — a real trade, not an error). CLI `--bench-boost` kept with honest help. The smoke also caught `--bench-boost` printing "all 15 score" and "the bench won't score" together; fixed with a test. |
| 2026-08-19 | Owner | My Squad → AI Tips / Transfer | ✅ **Transfer advice says "hold" on a squad with a dead player.** RoboTS contains Destan (departed, 0 xP, benched). The gameweek plan correctly **flags** him (Risk + Flags: "unavailable, 0%") but the transfer line still reads *"no positive-gain upgrade — hold your transfer"* — because `suggest_transfers` optimises **starting-XI xP gain**, and a benched dead player doesn't move that number. But a departed player is a **permanent dead slot with zero bench cover**, so a manager *should* swap him for any playing ~£4.5m body (pure upside). | ✅ fixed | **✅ FIXED — ADR-136, Sprint 190 (2026-08-25).** Reproduced on live data first: `suggest_transfers` ranks by starting-XI gain (ADR-046), so a departed player on the bench moved it by exactly zero and the advice fell through to "hold". Now asked as a separate question — a slot that cannot score for the whole horizon is named with its **reason** (*gone* · *out until 28 Nov* · *no return date*) plus the best legal replacement, on the CLI, in `ask`, and on web ▸ Transfer with a one-click Replace. The design turns on telling a **permanent exit from a two-week injury** (Doku, back 5 Sep, is held; Minteh, back 28 Nov, is not) — which only FPL's news text knows, since `decision_xp` scores all 94 unavailable players 0.00. No `decision_xp` change. |
| 2026-08-19 | Owner | Squad Lab / My Squad | **Built a squad with a player who's left the league — nothing warned.** "Destan" (Hull, £4.5m FWD) is in the data but not the FPL picker. **Diagnosed (verified vs live FPL API):** he's a real ex-Hull forward, **status `u`** with news *"Has joined Konyaspor permanently"* — still in FPL's raw bootstrap (so reseed won't drop him; FPL just **hides `u` players from selection**, MadBoots ingests all). Got into the squad via Squad Lab **"Must include"** (`available_players(keep_ids=…)` keeps forced players even if unavailable), and **no squad view flags an unavailable member** once in. | 🟠 data/UX gap | ✅ **US-421 (bugfix, 2026-08-19).** A display-layer **⛔ availability warning** in **My Squad** + **Squad Lab result**: `is_unavailable` members listed with a "swap them, they'll score 0" nudge — catches every entry path (force-include · Manager-ID import · old save). No optimizer/analytics change (force-include stays for advanced use; the warning is the safety net). | **"Returning: 0" looks wrong / anonymous device count suspect.** Admin showed **Sessions 40 = Devices 40, Returning 0** (last-1000-events window, 17→18, 100% ok). The **exact Sessions==Devices equality** means every session is minting a fresh device id → the anonymous **`fpl_anon` returning-cookie isn't persisting across visits in prod** (same browser cookie-jar class as the remember-me bug, Sprint 132/134). ⚠ **Can only be confirmed with a real browser** (AppTest is headless — the standing limitation). Tell: if 40 devices > actual tester count, it's over-minting. (Also: "Events 1000" = the **read cap**, not all-time; "returning" needs a device on 2+ distinct days, so a short window under-reports too.) | 🟠 metric unreliable | **Verify** the `fpl_anon` read path with a browser check (does `remember.read_cookie` resolve on return?). **But don't over-invest** — the anonymous returning metric is inherently fragile (private mode / cleared cookies). **[[ADR-120]] account-roster (`user_key` + `updated_at`) SUPERSEDES it as the trustworthy retention/"who's testing" signal** (server-side, names who, can't be fooled by cookies). Anonymous panel stays fine for *usage volume + error rate*. |
| 2026-08-18 | Owner | Players ▸ Card (Player DNA) | **Kinsky (Spurs #1 GK, now 20.7% owned) rates far too low** — ~0.5 xP/GW, AI Verdict **24 = "Avoid"**. Diagnosed: **not a data bug** — a **role-change cold-start**. His *rate* is fine (2.59, floored via ADR-104); the killer is **xMins = 0.18** (last season he was a backup, 630 mins), so `xP = rate × xMins ≈ 0.5`. The model still projects **Vicario** as Spurs #1 (xMins 0.79, but only 1.2% owned) — last-season minutes can't know the shirt changed; even FPL's own `ep_next` (1.9) lags. **Self-corrects in-season** as real starts feed xMins (~GW2–3); this is exactly the GW1 minutes/form Data Hardening (ADR-060/101). | 🧭 known limitation | **Not now** (self-heals by ~GW2–3; a preseason minutes-override would fit noise). **Post-GW1 idea (feedback-driven):** a **role-change caveat** on the card — when **crowd ownership ≫ modelled xMins**, show *"⚠ Crowd expects him to start — rating still on last season's minutes; updates as he plays."* A **display lens** (crowd never touches xP, per the invariant) so the dashboard doesn't *confidently* mis-"Sell" a newly-anointed #1. Real fix = in-season minutes. |
| 2026-08-17 | Owner | All | **Detailed post-reboot review.** 🔴 **theme stuck light** (system/dark gone); 🔴 **My Squad "no purple" banner**. Consistency: **MADBOOTS mark missing** on My Squad/Squad Lab/Ask/Help/Feedback/Admin. Home: consolidate Maddie+Testing into the New-here box; highlight the Build CTA; Demo needs an icon. Copy: Players/My Squad/Squad Lab captions. Fixtures: **FDR colours too psychedelic → mirror the FPL app** (grey middle). News: add filters (My Squad/Team/Player/Position). Trending: add a My-Squad filter. Help: §1 body icons. Fixtures naming + **Radar watchlist** = discussion. | 🔴 broken + 🟡 polish + 🧭 discussion | ✅ Sprint 166. Theme + banner reverted (regressions I introduced); marks on all 6 headers; FPL FDR colours; captions; Home hero box; News/Trending My-squad filters; Help icons. **Decisions:** keep "Fixtures" (park upcoming-fixtures); **Radar watchlist → Sprint 167 (before GW1).** |
| 2026-08-17 | Owner | Home · Fixtures · Help | UX batch: Home callouts should be **purple** not blue; **all** "explore the sidebar" bullets should show their icon; **"squad.json" too technical**; rename **"Ask Maddie" → "Maddie Explains"**. Fixtures: **colours more vibrant** (yellow looks brown); *Radar — move under Players?* Help: **consistent icons** on the dropdowns. Also asked for an **app-wide UX/style review**. | 🟡 polish + 🧭 discussion | ✅ Sprint 160 (US-389–393) — purple callouts · icon-led bullets · de-jargon · **Maddie Explains** rename · vibrant FDR + digit · Help icons; **Radar kept under Fixtures + signpost on Players** (owner call). The review → **`docs/00_Project/UX_Style_Audit.md`** (6 themes, 4-sprint sequence); Sprint A = the honesty+copy slice, done. |
| 2026-08-17 | Tester | My Squad | **Smoke test of the Sprint-158 persistence fix.** ✅ **Win:** transfer made on iPhone → refreshed Mac → change showed (**cross-device sync works**). Gaps (all visibility): **"not obvious where Save/Persist/Backup is"** (biggest); **"my team needs to stand out — box it in colour"**; upload label "…upload a squad.json backup" is unclear; a downloaded `TS.json` came back as `squad-13.json` (confusing). | 🟠 confusing / 🟡 polish | ✅ Sprint 159 (US-386/387). **US-386:** a brand-boxed **"Your team" status card** at the top of My Squad (team name + 🔄 Synced/💾 session pill + MADBOOTS mark) so it stands out + Save/backup is signposted; a muted "demo squad" prompt when it isn't yours; the ⚙ panel auto-expands when not-yours. **US-387:** upload label → "…restore your team from a backup file"; Download named after the team (`TS.json`). Owner-approved via mock. *(Old handle-saved teams not auto-migrated — recovery = upload the json backup, by design.)* |
| 2026-08-16 | Tester | My Squad | **Save & persist is inconsistent.** Loading a team via **☁ Save/Load across devices** then refreshing **reverts** it — but an **uploaded json persists**. And **Upload · Save · Manager-ID · Save/Load** feel like **4 separate tools** — make it 1 screen. Asks: (1) Upload/Download for device backup; (2) see/manage the team on other devices w/ the same login, save config or local; (3) load by FPL Manager-ID + persist across devices / save local. | 🔴 broken (revert) + 🧭 discussion | ✅ Sprint 158 (ADR-113, US-384/385). Root cause: **two cloud stores** — the **account** (`user_key`, ADR-106, restored on every load) vs the manual **handle** (ADR-094) — fought over one squad; ☁ Load wrote the *handle*, refresh restored the *account* → reverted. **Fixed:** the account is the store in signed-in mode (`render_cloud_sync` hidden when `auth.is_configured()`; handle = no-login fallback) → a loaded team survives refresh; and **one inline ⚙ "Your team" panel** (sync status · import [Manager-ID/upload] · Download backup) replaces the 4 scattered tools. 994 tests. Owner smoke post-deploy. |
| 2026-08-27 | Owner | Feedback | **Why can't we use in-app email?** | ❓ question | ✅ Answered — the in-app form **already** emails you when the **relay** (FormSubmit/Web3Forms, US-308) is set as `FPL_FEEDBACK_WEBHOOK` (in-app Send → your inbox, no mail client). The *mailto* is only the zero-setup fallback. Direct **SMTP** send isn't possible free (**Proton has no free SMTP**). Action: set the relay (BETA.md §1B). No build. |
| 2026-08-27 | Owner | Analytics | **DefCon opposition magnifier** — a transferred player won't return the same at a new team; scale DefCon by the fixture (a clean-sheet-unlikely game → more defensive actions → higher DefCon; e.g. Spurs v COV → ×0.5–0.75, ARS v Spurs → ×1.25–1.5). | 💡 idea | ◑ Sprint 127 — **ADR-097 design gate** (a fixture-context DefCon-xP magnifier via an FDR/xGC clean-sheet *proxy* — no betting odds; the transferred-player nuance; "model DefCon in xP first" prerequisite; wired-dormant; **build + calibrate at GW1**). |
| 2026-08-23 | Owner | Direction | **Strategy discussion:** cross-device squads · feedback form live · Streamlit perf metrics · dev/test/prod for 20–50 testers · hobby-vs-product (protect the code + backup). | 🧭 discussion | ✅ Sprint 122 — reviewed (all ~£0): **ADR-094** (handle-keyed persistence, build deferred) + **ADR-095** (prod/staging split · public+PolyForm-NC LICENSE · mirror backup · uptime monitor). Shipped: LICENSE, mirror Action, BACKUP.md, enriched feedback + BETA go-live checklist. |
| 2026-08-22 | Owner | Fixtures | Make **🎯 Target by fixtures** budget-aware — an affordability filter + bang-for-buck. | 💡 | ✅ Sprint 121 (US-303/304) — a **Max price** slider (caps the targets to what you can afford) + a **Val/£m** column & an **xP↔Val/£m** sort toggle. |
| 2026-08-21 | Owner | Fixtures | The Fixtures view is needed for **planning a new squad / wildcard** — in addition to what's there. | 💡 | ✅ Sprint 120 (US-301/302) — a "🎯 Target by fixtures" shortlist (best players from the easiest-run teams, by xP, + a position filter) + a "My squad" ticker lens (scope to your teams + a player-count). |
| 2026-08-20 | Owner | Squads → My Squad | The **Edit (swap)** should have a **position filter** (GK/DEF/MID/FWD) and an **affordable** checkbox. | 💡 | ✅ Sprint 119 (US-299/300) — a Position segmented control scopes "Replace"; an "Affordable only" checkbox + a bank caption scope "With". |
| 2026-08-17 | Owner | All | **Hover-over tooltips** seem to have stopped working. | 🟠 regression | ✅ Sprint 116 (US-293) — the `help=` are all still set (coverage test passes; no global CSS); **pinned `streamlit==1.61.1`** so the deploy matches the tested version (an unpinned auto-upgrade likely changed hover rendering). Verify on the live deploy after redeploy |
| 2026-08-17 | Owner | reseed | Does `reseed` call **ClubElo**? It used to — I don't see it now. | 🟡 confusing | ✅ Sprint 116 (US-293) — it always did (via `refresh`); `cmd_reseed` had dropped the Elo count from its printout. Now reports *"N Elo ratings (ClubElo)"* / *"kept last-known"* |
| 2026-08-15 | Owner | Trends | Use ownership **badges**: 💎 Differential <5% · ⭐ Popular 5–20% · 🟦 Template 20–60% · 👑 Essential >60%. (Challenge if better.) | 💡 | ✅ Sprint 114 (US-289/290, extends ADR-057/089) — adopted (verified the tiers fill real gaps: 57 unbadged "popular", 1 mis-lumped "essential"); `ownership_tier` in `crowd_flags` everywhere + the explanations speak the same tier language (Essential/Template/Differential). Differential aligned to the ≤5% filter |
| 2026-08-14 | Owner | Ask | The example-click **auto-scroll** works for *some* answers, not all — still scroll manually sometimes. | 🟠 polish | ✅ Sprint 113 (US-287, extends ADR-052) — the single smooth scroll → a multi-tick **instant** scroll ([50,200,450,800]ms), so it lands reliably after layout settles |
| 2026-08-14 | Owner | Ask | *"best differential midfielders under £8m"* needs a **why** — the benefit of a differential, and maybe why these ones. | 💡 | ✅ Sprint 113 (US-288, extends ADR-042/061) — a grounded "Why a differential?" lead (rank lever + variance) + a per-pick "standout signals" block (nailed/rotation · set-pieces · form); plain shortlist unchanged, still ✓ |
| 2026-08-12 | Owner | Ask | Rules answers are a **dense paragraph** — a multi-item fact (chips) should read as **bullets**. | 🟡 polish | ✅ Sprint 111 (US-283, extends ADR-085) — the chips/scoring/clean-sheets/leagues facts render item-per-line; grounding unchanged (still ✓) |
| 2026-08-12 | Owner | Ask | Clicking an **example question** doesn't auto-scroll to the answer (typing your own does). | 🟠 polish | ✅ Sprint 111 (US-283, extends ADR-052) — the static scroll nudge didn't re-run; now unique per turn (`/*turn N*/`) so it fires on every answer |
| 2026-08-12 | Owner | Ask | *"is X worth the money?"* should explain **why** — like the captain/transfer answers. | 💡 | ✅ Sprint 111 (US-284, extends ADR-089/061) — `explain_worth` adds a grounded Confidence · Why · Risk + Model note; explains without Ollama, verified ✓ |
| 2026-08-10 | Owner | Ask / Captain | *"who should I captain from my-team?"* returns the **same answer regardless of my players** — it's picking the best from **all** players, not my squad. (That global question is a great one in its own right.) | 🔴 wrong | ✅ Sprint 109 (US-279/280, ADR-090) — root cause: "my team" resolved via a regex that matches a space not the hyphen the examples use. Fixed by defaulting squad questions to the loaded squad (explicit "all players" escapes to global); the global answer reframed "Best Captain Picks" + a nudge; example buttons name the loaded squad |
| 2026-08-09 | Owner | Ask / Captain | **Evolve explainability** — make the captaincy answer read like a mockup: a **Captain Pick** card (medal · Team·Pos · Projected · Confidence · Why · Risks · **Alternatives** 🥈🥉) + a **Model note** ("data-driven; AI explains"). | 💡 | ✅ Sprint 108 (US-277/278, extends ADR-089) — `render_captain_pick` card in Ask/CLI/web; a shared `MODEL_NOTE` closes all five explained answers once; `render_explanation` confidence line cleaned; CLI/web captain delegate to the card |
| 2026-08-08 | Owner | Ask | Answer **sentences don't wrap** and are hard to read; the page **doesn't auto-scroll down** to the answer to the current question. | 🟠 polish | ✅ Sprint 107 (US-275, extends ADR-052) — answers render with `st.code(wrap_lines=True)` so long prose wraps (tables/blocks stay aligned) + a same-origin `st.iframe` nudge smooth-scrolls to the newest Q&A |
| 2026-08-08 | Owner | Players → Fit | Should a **fit** player show an **emoji** (✅) rather than leaving the Fit cell blank? | 💡 | ✅ Sprint 107 (US-276, extends ADR-074) — a new display-only `fit_flag` shows ✅ for a fit player (🚑/🚫/⛔/❓ for concerns) on the Pool + stat boards + CLI; `availability_flag` unchanged (still "" for fit — drives the who's-flagged logic) |
| 2026-08-07 | Owner | Ask | **Explainability** — show **why** a recommendation was made (traceable to xP · fixtures · xMins · penalties · form · injuries · ownership), with a **Confidence · Why (✓) · Risk (⚠)** so a user can trust or challenge it. | 💡 | ✅ Sprint 104 (US-269/270, ADR-089) — a grounded Confidence·Why·Risk block on captain + transfer answers (Ask/CLI/web); every ✓/⚠ and the number computed from the data (the LLM only phrases + is verified). Gated signals (form · % of team goals · opponent xGC) light up at GW1 |
| 2026-08-07 | Owner | Trending / Players | **"what does template mean?"** — the Trends flags aren't explained. | 🟠 confusing | ✅ 2026-08-07 — a shared `CROWD_LEGEND` (🟦 template = ≥20% owned · 💎 differential = ≤5% · 🔥/❄️/💰/📈) on the Trending boards + the Players Pool + a Trends tooltip |
| 2026-08-07 | Owner | Direction | **Discussion:** multi-user / registration / paid? iOS-Android apps? wider testing (50 via Reddit) with access + feedback + a "free for X years" promise? A consolidated features/gating/roadmap doc on GitHub? | 💡 | ✅ 2026-08-07 — created **docs/00_Project/PRODUCT.md** (features · gating · backlog · roadmap overlay) + **DIRECTION.md** (multi-user/paid · mobile · wider-testing options, with a recommendation). Beta-setup sprint offered when ready |
| 2026-08-07 | Owner | Squads → My Squad | Redesign the **My Squad graphic** closer to the **Fantasy Football Hub** pitch — it "looks like a **poor cousin**". | 💡 | ✅ Sprint 099 (US-257/258, ADR-084) — a green FFH-style pitch (kits in formation + a bench strip, xP chips, (C) armband + sub badges, crowd/set-piece flags) replaces the native card-grid. Owner approved the preview ("looks great") |
| 2026-08-07 | Owner | Squads → My Squad | When I select a captain, the **GW xP doesn't account for the captain double points** — should it double for next GW or all selected? (Chips are next-GW only.) | 🟠 wrong | ✅ Sprint 098 (US-256, ADR-083) — Projected XI now adds the captain's ×2 for the **next GW only** (owner steer; captaincy is a weekly decision), with a caption saying so; "Captain (2×)" reframed to next-GW |
| 2026-08-07 | Owner | Players / Squads | If a player has **no photo**, use the **club shirt** image instead of a blank. | 💡 | ✅ Sprint 098 (US-255) — the photo falls back to the club-shirt kit (GK variant for keepers) via a cached CDN existence check; ~25% of players (no photo file despite a code) now show a shirt |
| 2026-08-07 | Owner | Squads → My Squad | Each selected player should show its **Set Piece attributes** (pens/corners/FK takers), **like it does for Trends**. | 💡 | ✅ Sprint 097 (US-253/254, extends ADR-081) — a `set_piece_flags` ⚽/🚩/🎯 line on the My Squad pitch cards + a "Set" column next to "Trends" on the Build/Health/Captain tables (and "In set" on Transfer) |
| 2026-08-07 | Owner | Squads | **Chip Strategy Guidance** — AI advice on when to use **Wildcard · Free Hit · Bench Boost · Triple Captain** from your squad, fixtures, and mini-league position. (One of 5 intake requests — the fixtures-now slice.) | 💡 | ◑ Sprint 096 (US-251/252, ADR-082) — a v0 chip-timing advisor (`chip_advisor` + a `chips` `ask` intent + a Squads "Chips" view) from per-GW xP + fixture run. Deferred: DGW/BGW (in-season) + mini-league position (leagues API, GW1) |
| 2026-08-07 | Owner | Players | **Set Piece & Ownership** — clear info on who takes **penalties · corners · free-kicks** per team + **ownership combinations** to find high-value, low-ownership **differentials**. (One of 5 intake requests — the buildable-now one.) | 💡 | ✅ Sprint 095 (US-249/250, ADR-081) — ingested corner/FK orders + `set_piece_flags`; a Players **"Set pieces"** view (order + Own%/Val/£m, filterable, differential caption) + a Pool **"Set"** flag. The other 4 intake requests remain in the Backlog (Elite Manager Comparison GW1-gated; AI Chat Assistant needs an ADR; Chip Strategy / Price Predictor partly gated) |
| 2026-08-07 | Owner | Squads → My Squad | Use the banner real estate for a **quick-view team summary** — xP over the gameweeks, players injured/suspended, etc. | 💡 | ✅ Sprint 090 (US-239/240) — a metrics row (Projected XI / Captain / Bench / Unavailable / Doubtful) + a who's-flagged caption |
| 2026-08-07 | Owner | Squads (all sub-tabs) | Want to **select the number of gameweeks** predicted over — 4–6 for a wildcard/start, 1–2 mid-season; a dropdown throughout the tab. | 💡 | ✅ Sprint 089 (US-237/238, ADR-077) — a "Gameweeks ahead" dropdown (1–8, default 5) across Build · My Squad · Health · Transfer · AI Tips; Captain stays next-GW |
| 2026-08-07 | Owner | Trending → Talked about | Only shows **1 mention** regardless of the number of mentions — can we count **all mentions**? | 🟠 wrong | ✅ Sprint 087 (US-232/233, ADR-076) — the counter was fine (1–4); the default feed is only 25 posts → now `?limit=100` (~100 posts) + the board paginated |
| 2026-08-07 | Owner | Squads → Build | On the **Preview the best XI in a shape**, show the **XI score** so a user can see the effect of different formations. | 💡 | ✅ Sprint 086 (US-230/231, ADR-075) — a "Projected XI xP" metric per shape + a gated compare-all-formations table (Δ vs best) |
| 2026-08-06 | Owner | Players → xG | **Flaw in logic:** goalkeepers with `xGI 0`/`0.04` are rated **🟢 excellent** — "how can 0 be good and 56 be good?" | 🟠 wrong | ✅ Sprint 084 (US-225, ADR-073) — rate xGI only for outfield ≥900-min players (vs that pool); GKs/low-minutes show `—`; column renamed "xGI rating" beside xGI |
| 2026-08-06 | Owner | Squads | Rename the **This week** tab to **AI Tips**. | 💡 | ✅ Sprint 084 (US-226) — label renamed; the gameweek engine (ADR-070) unchanged |
| 2026-08-06 | Owner | Ask | Show a few **example prompts** in the Ask tab. | 💡 | ✅ Sprint 084 (US-227) — an expander of 7 copy-paste examples |
| 2026-08-06 | Owner | Help | Update the **Help** tab with recent changes and improvements. | 💡 | ✅ Sprint 083 (US-224) — This week / the gameweek plan, the 🟢…🔴 ratings, the table-first Pool, a new Ask example |
| 2026-08-06 | Owner | Players / tables | **Number formatting** — keep to xx.x (Val/£m `24.2345` → `24.2`; cost `6` → `6.0`) to keep tables aligned. | 🟡 polish | ✅ Sprint 083 (US-223, ADR-072) — a shared `NumberColumn` convention (1dp money/%, 2dp xG family, integer counts, signed diffs) across the Pool, stat boards & squad tables |
| 2026-08-06 | Owner | All tabs | Home has a nice **⚽ graphic** header — do the same on every other tab. | 💡 | ✅ Sprint 082 (US-222) — an emoji-led title + tagline on all 7 tabs (👟📅🧩💬📰📈🧭) |
| 2026-08-06 | Owner | Players (stat boards) | What does a raw stat mean (**xGI/xGC**) — is it absolute or just relative? Could a **colour graphic** beside the score help a casual user read it? | 💡 | ✅ Sprint 082 (US-221, ADR-071) — a relative 🟢…🔴 **quality rating** + percentile on Clean sheets (xGC/90) & xG (xGI); clearer captions + tooltips on all four boards. (Verified: a fixed ChatGPT band table would mislabel 91/117 defenders "poor" → rate relative to the pool) |
| 2026-08-06 | Owner | Squads | Want an **AI recommendation on your squad for the upcoming gameweek**. | 💡 | ✅ Sprint 081 (US-220, ADR-070) — a grounded "this week" plan (captain·lineup·transfer·flags); an `ask` intent + a **Squads → This week** view |
| 2026-08-06 | Owner | All (data) | CLI refreshed **572** players but the app shows **569–570** — how do I ensure the Streamlit app is on fresh data? | 🟠 confusing | ✅ Sprint 081 (US-219) — the freshness caption shows the **player count** + a cloud snapshot note; a one-command **`reseed`**; DEPLOY/Help explain cloud vs local refresh |
| 2026-08-06 | Owner | Players → Pool | Flip the bar chart and table — the **table matters most**, show it first. | 🟡 polish | ✅ Sprint 081 (US-218) — `render_pool` renders the table before the top-15 bar |
| 2026-08-06 | Owner | Players / Player Stats / Trending | The filter's **Player** multiselect lists all ~570 names — scope it to the selected team(s). | 🟡 polish | ✅ Sprint 077 (US-213) — scoped by team ∧ position in the shared filter |
| 2026-08-06 | Owner | Trending | Trending needs a **filter**, same as Players and Player Stats. | 💡 | ✅ Sprint 075 (US-210) — reused the shared Team/Position/Player filter (ADR-064) |
| 2026-08-06 | Owner | All | Add a small **ⓘ help tooltip** over all feature options so users understand what each does. | 💡 | ✅ Sprint 074 (US-208/209, ADR-065) — `help=` on every input control + a coverage test |
| 2026-08-06 | Owner | Players / Player Stats | Need a **filter** (by player(s) / team / position, combinable, multi-select) on both. Players' top **graph isn't adding value** — remove/replace. | 🟡 polish / 💡 | ✅ Sprint 073 (US-206/207, ADR-064) — shared Team/Position/Player filter on both; scatter → a filter-responsive top-15 bar |
| 2026-08-06 | Owner | Player Stats | The CLI's **Overperf / DefCon / Cleansheet** (and xG) aren't in the web — add a Stats tab; **Players** caps at 50 (page through all + sort by team/position); **Trending** caps at 30 (page all four boards). | 🟠 gap / 💡 | ✅ Sprint 072 (US-203/204/205, ADR-063) — Player Stats page + shared paginator |
| 2026-08-06 | Owner | Build / Ask / My Squad | The web can't build a squad with the **full CLI `squad` options** (include · exclude · bench · formation · objective · no-xmins · weekly/bench-boost · include-unavailable — only budget/cheap/premium/differential are exposed). Want to build with any/all options, save it into the session so **My Squad** picks it up to tweak, then download. | 🟠 gap / 💡 | ✅ Sprint 071 (US-200/201/202, ADR-062) — full options on **Build Squad**; Ask offers "Use this squad →"; tabs renamed/regrouped |
| 2026-08-06 | Owner | My Squad | Player photos are left-aligned in the pitch cards — centre them | 🟡 polish | ✅ Sprint 063 (US-188) |
| 2026-08-06 | Owner | Ask | Ask ignores the loaded/session squad — "captain/analyse RoboTS" falls back to "(all players)" (picks B.Fernandes, who isn't in RoboTS). Ask only sees server-side `SquadStore`, not the session active squad. | 🔴 broken | ✅ Sprint 066 |

---

## How this feeds Sprint 060

1. A report arrives (GitHub issue or direct note).
2. Add a **triaged row** here (dedupe against existing rows; group similar reports).
3. At Sprint 060 planning, promote the **🔴/🟠** items (and any high-value 💡) into the sprint backlog.

## Themes to watch (pre-seeded from the Sprint 058/059 retros)

- **Squad resets on refresh** until downloaded — the most likely confusion; the guide pre-empts it. If
  testers still trip on it, it argues for **Path 2** (server-side persistence).
- **Same-position-only swaps** — if people want cross-position reshapes, that's a multi-swap feature.
- **Data freshness** — do testers understand the "Data as of" snapshot / that refresh is local-only?
