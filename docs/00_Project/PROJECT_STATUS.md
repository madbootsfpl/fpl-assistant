Current Phase: Phase 4 complete (grounded `ask`: eight intents, verified; a conversational `chat` mode). Now building the **web UI** (Phase 2) — a thin read-only FastAPI edge (ADR-050). Phase 3 (decision support + xMins v0), Phase 1 MVP done
Current Sprint: Sprint 083 - Consistent number formatting · refresh the Help tab (✅ Complete, US-223/224, ADR-072, retro done)
Current Story: None active. Next markers: **GW1 (2026-08-21)** — momentum boards/questions + threshold calibration + the live manager-import check light up; and the **Data Hardening flip** (Sprint 069 wired it dormant): `python app.py history --backfill` (now also per-GW) → raise `config.FORM_WEIGHT` → calibrate the weight/window on real form. Also: verify Community Signals on the live cloud IP (may degrade). Tester feedback → Feedback_Log.md. Owner to steer Next markers: **GW1 (2026-08-21)** — confirm the live manager-import (picks unlock); the deferred trends `ask` intent (US-185) + calibrate `TRENDING_NET`/`FORM_MIN`; the Data Hardening flip (per-GW history + form now wired, dormant → set `FORM_WEIGHT`). Later: Tier-2b keyed social (Reddit/X — needs a Cloud secret) / pundit NLP; Tier 3 (crowd-vs-xP backtest). Tester feedback (Sprint 059 loop) → docs/00_Project/Feedback_Log.md. Owner to steer
Next Milestone: Owner to steer — more Phase 4, the web UI (Phase 2), or (at GW1, 2026-08-21) Data Hardening + the full probabilistic xMins (Phase 5)
Current Version: 0.0.1
Last Updated: 2026-08-06 (Sprint 083 close)
Commands: refresh · reseed (refresh → copy to the committed seed) · history (--backfill) · ask · chat (interactive; follow-ups) · table · search · filter · fdr (--type) · fixtures · xp (--by-gameweek) · captain (--squad/--no-xmins) · transfer (--squad/--bank/--next/--count/--raw/--no-xmins) · analyse (--squad/--next/--sort/--no-xmins) · xg · overperf · defcon · cleansheet · squad (--full/--bench/--formation/--objective[default xp]/--no-xmins/--cheap/--premium/--differential/--weekly/--bench-boost/--include[-unavailable]/--exclude/--save/--load) · ask ("build me a squad for £Xm with N low-cost and M premium" / "best midfielders under £8m" / "best fixtures next 5" / "when does Arsenal play?" / "which of TS's players have the best fixtures?")
Web UI: `python -m src.web_streamlit` → http://localhost:8501 (Streamlit, the UI we grow; interactive) · `python -m src.web` → :8000 (FastAPI, frozen reference). Deployable to **Streamlit Community Cloud** (ADR-053; runbook: docs/DEPLOY.md) — public, read-only; a committed `data/seed.db` seeds it; Ask degrades without Ollama. Both read-only, local. Streamlit — **7 tabs** (ADR-069; consolidated pages switch views with a lazy `st.segmented_control`): **Players** (Pool — filter/sort/page-all, table-first + a top-15 bar — · Over/under · DefCon · Clean sheets · xG; **Clean sheets + xG carry a 🟢…🔴 quality Rating** vs the players shown, ADR-071) · **Fixtures** (ticker) · **Squads** (**Build** the full `squad` options → 15 · **My Squad** pitch view & edit · **This week** a grounded gameweek plan — captain·lineup·transfer·flags, ADR-070 · **Health** 5-GW analysis · **Transfer** XI-aware swaps · **Captain**) · **Ask** (chat; a build answer offers **Use this squad →**) · **News** · **Trending** (+ Community Signals) · **Help** (a step-by-step guide). **Cloud squads (ADR-054/055):** a session **active squad** in `st.session_state` — build/upload it (sidebar uploader; **Download = your save**, the CLI `SquadStore` shape), then **edit** it (rename · apply-transfer · swap · bench · set-captain, all mutating `session_state`, validated by `squad_15_issues`) — used across Analyse · Transfer · Captain · My Squad; a committed `data/seed_squads.json` **demo** (a `SQUADS_PATH` fallback) populates the pages; the web **never writes** server-side (a test asserts it)
Sources: FPL — bootstrap-static + fixtures + element-summary (past-season history) — required, retries hard · ClubElo (best-effort, intermittent) · local Ollama LLM (optional, for `ask`)
Tests: 612 · ADRs: 72 · CI: GitHub Actions (ruff + pytest on push, Py 3.13/3.14) · Consistent number formatting (Sprint 083, ADR-072): a shared web-table format convention — `src/web_streamlit/formats.py` (`FORMATS` label→printf map + `column_config(...)`) formats every Streamlit table via `st.column_config.NumberColumn` (right-aligned + **still numeric/sortable**, not pre-rounded strings): money/value/%/form/ICT → **1dp** (`6.0`, `24.2`), counts (Pts/Mins) → **integer**, the xG family (xG/xA/xGI/xGC/xGC/90) → **2dp**, signed diffs (Diff/Margin/+xP) → **`%+.1f`**. Applied to the Pool, the four stat boards, and the squad tables; the raw analytics values are untouched (display-only); CLI unchanged; no server writes · Help refreshed (Sprint 083, US-224): the step-by-step guide now covers **This week** (the gameweek plan), the **🟢…🔴 quality ratings**, the **table-first Pool**, and a *"what should I do this week"* Ask example · Interpretable stat boards (Sprint 082, ADR-071): a display-only quality **rating** on the Players stat boards — `src/web_streamlit/ratings.py::quality_band(value, pool, higher_is_better)` rates a value **relative to the players shown** (quintile 🟢 excellent…🔴 very poor) with the **percentile** inline ("top N%"); a **Rating** column on **Clean sheets** (xGC/90, lower=better) + **xG** (xGI, higher=better) + a legend; clearer captions + per-column `help=` tooltips on all four boards (absolute season total vs per-90). **Relative** (not fixed) bands because real FPL xGC/90 (median 1.36) makes ChatGPT's fixed table mislabel 91/117 defenders "poor". The two *signed* boards (over/under, DefCon) keep +/- direction, no colour rating. Analytics untouched; no server writes · Per-tab header graphics (Sprint 082, US-222): a distinct emoji-led title + one-line tagline on every tab (Players 👟 · Fixtures 📅 · Squads 🧩 · Ask 💬 · News 📰 · Trending 📈 · Help 🧭), like Home's ⚽ · AI gameweek recommendation (Sprint 081, ADR-070): a grounded **"this week"** plan for a squad — **captain · lineup · a transfer · flags** — as an *assembler* (`src/analytics/gameweek.py::gameweek_plan`) over the existing primitives (`captain_picks` next-GW · `best_legal_xi` vs the declared bench · one self-funding `suggest_transfers` · availability), **no new analytics**. A phrase-routed `ask`/`chat` **gameweek** intent (`_decide_gameweek`, placed after the specific intents so "captain this week" still routes to captain) humanises it → narrated + **verified** (✓/⚠, ADR-037); a `src/ui/gameweek.py::render_gameweek_plan` block; a **Squads → This week** view routes through `ask.answer(active_squad=…)` + `render_ask`, degrading to the plan without Ollama; no server writes · Refresh clarity (Sprint 081, extends ADR-053/056): the freshness caption leads with the **player count** ("N players · data as of \<date\>") so a stale snapshot is obvious, + a **cloud snapshot note**; a one-command **`python app.py reseed`** (refresh `fpl.db` → copy to `seed.db`; new `config.LIVE_DB_PATH`) with DEPLOY.md + Help splitting the **cloud** (reseed → push → reboot) vs **local** (🔄 button / restart) refresh story · Pool: table first (Sprint 081, US-218): `views/players.py::render_pool` renders the paginated **table before** the top-15 bar (the tester's ask; the bar stays filter/sort-responsive) · Sidebar consolidation (Sprint 080, ADR-069): the web sidebar is now **7 tabs** (was 12) — **Players · Fixtures · Squads · Ask · News · Trending · Help**. **Players** merges the old Player Stats (a lazy `st.segmented_control`: Pool · Over/under · DefCon · Clean sheets · xG, shared filter). **Squads** merges Build · My Squad · Health · Transfer · Captain (one segmented control + one shared squad picker for the manage views); only the selected view computes (not `st.tabs`, which runs all). View bodies extracted to `web_streamlit/views/`; **no behaviour change** (every prior AppTest assertion kept, ~38 rewired to drive the control); no server writes · Help tab (Sprint 079, ADR-068): a new **Help** page (`pages/12_Help.py`, last in the sidebar) — a static step-by-step guide to building a team with the assistant: **7 expander steps** (build → make it yours → check health → improve → research → **ask** (copy-paste examples + the ✓/⚠ trust line) → save), a quick-start line, and an honest close on data freshness + what lights up at GW1. Complements Home (which gains a pointer). No data dependency, no input widgets · Team-level squad fixtures (Sprint 078, ADR-067): a 4th `fixtures` ask/chat mode — *"which of \<squad>'s **teams** have the best/worst fixtures?"* — ranks a squad's distinct teams by their `team_fdr` run, with a **player-count** each; a `teams`/`clubs`/`by team` cue routes to it (else the per-player view); a `render_squad_team_fixtures` renderer. Implements the ADR-049 deferral; reuses `team_fdr` (no new analytics); grounded; other fixtures modes unchanged · Team-scoped player filter (Sprint 077, refines ADR-064): the shared filter's **Player** multiselect now scopes its options to the selected **team ∧ position** (empty = all) instead of listing all ~570 names — one edit to `filter_controls`, inherited by Players · Player Stats · Trending; the stored pick is pruned when it falls out of scope. `apply` (the AND predicate) unchanged; no server writes · Tech-debt sweep (Sprint 076, ADR-066): two Backlog items cleared with **no behaviour/output change** (65 optimizer + 19 render assertions unchanged). **PuLP** — variables → `problem.add_variable(...)`; **kept `PULP_CBC_CMD`** (`COIN_CMD` needs an external CBC — verified "cannot execute cbc", would break the read-only Cloud); the blanket `DeprecationWarning` ignore → a **targeted** PULP_CBC_CMD filter (other deprecations now surface). **Squad renderer** — `render_squad`/`render_loaded_squad` share a `_header` + `_BENCH_HEADING`; the `render_rows` fold is **closed** (its flat single-space join can't reproduce the mid-table heading / glued markers / divergent price cells byte-for-byte) · Trending filter (Sprint 075, reuses ADR-064): the **Trending** page gained the same **Team · Position · Player** filter (AND-combinable) as Players & Player Stats — one `filter_controls`/`apply` above the four boards, applied before pagination; the buzz board + GW1 note unchanged. No new analytics/ADR; the filter's tooltips are inherited (ADR-065) · Help tooltips (Sprint 074, ADR-065): a concise **ⓘ tooltip** (`help=`) on **every input control** across the web — added at the shared components (`filters` · `paginate` · `squads` sidebar/picker) so all pages inherit it, + per-page controls on Players/Fixtures/Trending/Build Squad/My Squad/Transfer/Captain; key buttons too. `st.tabs`/`st.chat_input` keep captions (no `help=`). A **coverage test** (`tests/test_help_tooltips.py`, via AppTest `.help`) asserts every input widget on all nine pages carries a non-empty tooltip — so a new option can't ship without one. No behaviour change · Rich filters (Sprint 073, ADR-064): one shared `web_streamlit/filters.py` (`filter_controls` + `apply`) adds a **Team · Position · Player** multiselect filter — **AND**-combinable (empty dim = any), tolerant of Row/dict rows — to both **Player Stats** (above the four tabs) and **Players** (with max-price; team + player added to the existing position). The Players **price-vs-points scatter is replaced** by a filter-responsive **top-15 bar** (Altair, rank-ordered) of the strongest filtered players by the sort metric. No analytics change; no server writes · Player Stats + pagination (Sprint 072, ADR-063): a new **Player Stats** page (sidebar position 2) brings the CLI stat views into the web as tabs — **Over/under-perf · Defensive Contribution · Clean sheets · xG** — each reusing the *same* analytics (`over_under`/`defcon_reliability`/`defensive_solidity`; xG by xGI), rendered with a team badge + the CLI columns, paginated; **season-to-date** (preseason = last season's carryover). A shared **`web_streamlit/paginate.py`** (`page_labels` pure + `paginate` = a page selectbox + "Showing X–Y of N") powers see-everything paging: **Players** now pages through all (no 50-cap) + a **team / position** sort (on top of points/value); **Trending's** four boards page past 30. Sidebar renumbered (Player Stats at 2 → Fixtures 3 … Trending 11). No analytics/data change; no server writes · Web build parity + tab reorg (Sprint 071, ADR-062): the web now has the **full CLI `squad` build options**. **Build Squad** (renamed from Build) exposes include · exclude · declared bench · objective (points/value/xp/xgi) · no-xmins · weekly/bench-boost · include-unavailable as **form widgets** (+ the existing budget/archetypes), all feeding the *same* `select_squad`/`decision_xp` (no engine change); the saveable build stays a full **15** (Download / Use this squad →), plus a display-only **"best XI shape"** preview (formation). The **Ask** tab's "build me a squad…" answer now offers **"Use this squad →"** (a `squad` field on `AskResult`) → the session squad → **My Squad** to tweak → download. Squad tabs **renamed + regrouped**: Squads→**Squad Health**, Build→**Build Squad**, sidebar order Players · Fixtures · **Build Squad · My Squad · Squad Health** · Transfer · Captain · Ask · News · Trending; My Squad points to Build Squad for a full rebuild. No server writes (the guardrail holds) · Differentials / value `ask` intent (Sprint 070, ADR-061): two natural-language lenses over the unified xP + ownership, grounded + verified (ADR-037). (1) A **differential** lens on the shortlist — `ask "best differential [position] under £Xm"` filters to **≤`DIFFERENTIAL_OWN` (5%) owned** (0% included), xP/value-ranked, with an **Own%** column (`render_shortlist(show_own=…)` keeps the plain shortlist byte-identical). (2) A single-player **`worth`** intent — `ask "is X worth the money?"` → the player's **xP/£m**, its **rank among available same-position players**, the **position median**, and a tiered fact-derived **verdict** (good/fair/pricey); degrades on an ambiguous/absent/flagged player. Routing: `worth` before transfer ("worth buying" ≠ "buy"), "differentials"→shortlist, "most owned"→trends. Preseason-honest: ownership is flat now → the differential filter sharpens at GW1 (a caption says so) · Data Hardening prep (Sprint 069, ADR-060): the first season-start foundations, built **wired but dormant** so GW1 (2026-08-21) is a switch-flip. (1) **Per-GW history** — a `player_history` table (`PlayerGameweek`, keyed `code+round`; additive/idempotent) filled by the **existing** throttled `element-summary` walk (the one call already carries `history` — empty preseason → live GW1). (2) **A dormant in-season form blend** — a pure `src/analytics/form.py` (`form_rate` = a recency+minutes-weighted rolling **pp90**; `blend_form`) folded into the **one** `decision_xp` recipe behind `config.FORM_WEIGHT = 0` → xP **unchanged today** (an invariance test + a real-DB smoke pin it; the whole suite passing is the proof). Every `decision_xp` caller (cli/ask/web) wired → **GW1 = backfill + raise the weight**, nothing else. Not FPL's `form` field (minutes-blind); the one-xP invariant (ADR-041) preserved · Community Signals (Sprint 068, ADR-059): a Reddit **RSS** buzz counter (`r/FantasyPL/.rss`, **no auth/secret** — the `.json` API 403s) — `src/api/reddit.py` + a pure `src/community.py` `community_buzz`/`community_signals` (degrade-gracefully); a Trending **"💬 Talked about"** board (button-gated + `st.cache_data` ~30 min → "unavailable" on failure). Buzz not sentiment; display-only; cloud-IP may block → degrades · Community trending (Sprint 067): a pure `trending(players, by, limit)` (owned/in/out/form; free FPL crowd data, display-only) powering a **trends `ask`** intent (grounded; preseason-graceful → "live from GW1") + a **Trending page** (`pages/10_Trending.py` — 4 boards, photos/badges/flags). Ownership works now; momentum/form at GW1. Reddit social sentiment = a gated follow-up (US-195/ADR-059, needs a cloud secret) · Ask ↔ session squad (Sprint 066, fix): the web **Ask** tab now answers about your **loaded** squad — `ask` gained `_load_squad`/`_known_squad_names` + an optional `active_squad` threaded through (default None → CLI unchanged); "my team" resolves to the loaded squad; fixes captain/analyse falling back to "(all players)" · Phase 6 Tier 2 (Sprint 064, ADR-058): an FPL official-**news lens** (`pages/9_News.py` over the stored `player.news` + ingested `scout_news_link`; flagged players, most-serious first, degrades to "no current news") + **import your team by manager-ID** (public FPL entry API → `src/manager.py` `picks_to_squad`/`fetch_manager_team`, degrade-gracefully; a sidebar Import control → the session active squad, no server writes; picks **GW1-gated** → live 2026-08-21). Keyed social (Reddit/X) + pundit NLP deferred. Display/state only — xP untouched · UI (Sprint 062): **Fixtures** = a **fixture ticker** (teams × gameweeks grid, colour-coded by difficulty, a **1–8 weeks** selector; pure `fixture_ticker` + a pandas Styler) · **My Squad** = a **formation pitch card-grid** (`web_streamlit/pitch.py`; position rows + bench, cards with photo/name/(C)/£/xP/opponent/flags), keeping the edit controls — robust native Streamlit, no custom CSS · Phase 6 — Crowd & Sentiment Signals (Sprint 060–061, ADR-057): crowd signals as a **complementary lens + flags, never blended into xP** (a test asserts `decision_xp` unchanged). Tier-1 **free FPL** fields ingested (`transfers_in/out_event` · `cost_change_*` · `form` · `ict_index`+components · `value_form`; `seed.db` reseeded); a pure `crowd_flags` helper (🟦 template ≥20% · 💎 differential ≤5% · 💰 price · 🔥 trending · 📈 form; tunable, GW1-calibrated) shown as **Trends/Form/ICT** on Players + a **Trends** column on Build/Analyse/My Squad. Momentum/form 0 preseason → live GW1. External social/pundit deferred (Tier 2/3) · Pre-tester polish (Sprint 059): **imagery consistency** — photos + team badges on every squad tab (Build/Analyse/Transfer/Captain/My Squad), augmenting the text (shared `badges.photo_url*` + `tables.render_player_table`); **local refresh + freshness (ADR-056)** — a **"Data as of \<date\>"** caption on every tab + a **local-only "🔄 Refresh data"** button (gated by `FPL_LOCAL`, set by the runner; writable non-seed DB; reuses `ingest.refresh`), the first web write path — the **cloud stays read-only** (caption only) · Editable squad (ADR-055): the session squad is **editable in `session_state`** (no server writes) — **rename**, **apply a transfer** (Transfer), **swap any player** / set the **bench** (a new **My Squad** hub), **set a captain** (Captain → **(C)** in Analyse + the download); one generic core validator **`squad_15_issues`** (positions + ≤3/club hard, **budget a soft edge warning**); edits go through mutation helpers (`rename`/`apply_transfer`/`set_bench`/`set_captain`), pages never touch the dict inline; `name`/`captain_id` are superset keys (validated on upload); fixed the Build xP/xMins-render bug · Cloud squads (ADR-054): a session **active squad** — **build** (Build → Download a `squad.json` + Use this squad) or **upload** (sidebar) → `st.session_state`; **Analyse · Transfer · Captain · My Squad** run the engine on the squad **dict** (not `ask`-by-name); a committed `data/seed_squads.json` demo (+ a `config.SQUADS_PATH` fallback) populates the pages; persistence is the user's file, the web **never writes** server-side (a test scans both edges for `.save(`) · Web UI = **Streamlit** (ADR-051/052): a multipage `src/web_streamlit/` edge — **Home** landing · Players (filters + **photos** + a price-vs-points **scatter**) · Fixtures (+ **team badges** + an FDR **bar chart**) · Squads/**Analyse** · **Transfer** (bank slider → XI-aware swaps) · **Build** (budget/archetype → the optimal 15) · **Captain** · **Ask** (a grounded **chat**), run via `python -m src.web_streamlit`; a measured spike chose it over FastAPI (pure-Python, interactive); the **FastAPI edge (ADR-050) is frozen** as a lean reference; both are thin edges over the one engine, the core imports neither (a test asserts it) · Web UI (ADR-050): a thin **read-only, local-only FastAPI** edge (`python -m src.web` → 127.0.0.1:8000) reusing the analytics — pages Players/Fixtures/Squads/**Ask** (the grounded answer + trust line, in a browser); slice-1 reuses the CLI text renderers in `<pre>`; the core stays web-free (a test asserts it); the CLI runs without the web deps · Fixtures `ask` intent (ADR-048/049): `ask`/`chat` answer fixture questions in three modes — a league **FDR ranking** ("best/hardest fixtures next N"), a **single team's schedule** ("when does Arsenal play?"), and **squad-scoped** ("which of TS's players have the best fixtures?" → your players by their team's run); reuses `team_fdr`/`team_schedule`; team names resolve or ask, never guess; `_squad_name` is possessive-aware · Conversational `ask` (ADR-047): a `chat` REPL where follow-ups build on the last turn — **why** (re-explain), **next** (2nd/3rd pick or next page), **what-about** (shortlist-only position swap); analytics decide every turn, grounding verified · XI-aware transfers (ADR-046): `transfer` ranks swaps by **XI improvement** (best-XI gain via `best_xi_points`), so bench-fodder upgrades drop out; `--raw` for the old raw-xP-gain ranking; the plan + `ask` agree · Bench-aware squad build (ADR-045): `--weekly` maximises the XI + a cheap playing bench (rotation, +7.6 XI xP), `--bench-boost` the max-15 (or NL "for rotation"/"for a bench boost") · Squad builds show a Starting XI xP vs Bench xP breakout (Sprint 044) · Archetypes (ADR-043/044): `--cheap`/`--premium`/`--differential` · One xP metric (ADR-041); `xp` the default squad objective, table shows xMins + xP · Phase 4: grounded `ask` answers eight (…build-a-squad · best-players · fixtures), verified (ADR-037); a conversational `chat` mode (ADR-047)
Known issue: ClubElo intermittent (timed out 2026-08-04 pm; up earlier same day) — best-effort, degrades; no blocker.
Deferred (→ later): more `ask` intents (fixtures/FDR, differentials); a persisted/pronoun-aware chat; an LLM intent classifier; Data Hardening (post-GW1: full backfill + per-GW history + form blending); multi-move transfer planner; ceiling captaincy; xMins v0 DONE (Sprint 037, ADR-038); the full probabilistic xMins model (Phase 5, post-GW1); web UI
