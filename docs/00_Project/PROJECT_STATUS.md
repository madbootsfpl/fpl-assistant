Current Phase: Phase 4 complete (grounded `ask`: eight intents, verified; a conversational `chat` mode). Now building the **web UI** (Phase 2) — a thin read-only FastAPI edge (ADR-050). Phase 3 (decision support + xMins v0), Phase 1 MVP done
Current Sprint: **Sprint 211 — A DEAD SLOT TAKES THE FREE TRANSFER (ADR-156, 2026-08-27).** Owner, still testing: *"Transfer doesn't pick up Watkins, he is not recommended to transfer."* **Two faults on one page.** (1) `suggest_transfers` ranked on his stored 4.3 xP — points he will never score — so it asked *"how much better is the replacement than 4.3?"* instead of *"…than nothing?"*; now takes `reported_out` and zeroes leavers in a **LOCAL COPY** of the xP map (ADR-154's idiom reused; a test asserts the caller's dict is unmutated), and a departing player is barred from the incoming shortlist. (2) **The page contradicted itself** — the ⛔ dead-slot banner and the ADR-132 timing line were computed independently, so one said *"Watkins can't score"* and the other *"use your free transfer on Gibbs-White"*; `transfer_timing` now takes the same `dead` list the banner is built from, and a dead slot is never banked against (banking buys a transfer next week; a hole costs the same every week it stays). **The two gains are still NEVER compared** (ADR-136 — 'recovered from zero' vs 'XI improvement'): the dead slot wins on KIND, pinned by a test giving it the SMALLER number (1.1 vs 9.9) and asserting it still goes first. Live: *"Use your free transfer on Watkins → Welbeck — Watkins can't play (per Romano), so that slot recovers 3.4 xP next gameweek."* 💡 **LESSON: one owner stops surfaces DISAGREEING; it does not stop them NOT ASKING.** ADR-155 deleted four copies of the lookup and I claimed that closed the class — it couldn't: pure analytics can't fetch, so a caller must pass the fact, and `reported_out=None` means 'no departures' — the safe default AND the silent one. **Six surfaces, six found by the owner using the product.** Second lesson: **two correct answers on one screen can still be a wrong page.** **1476 → 1485 tests, ruff clean.**
Current Story: **Roadmap re-cut post-GW1 (2026-08-24)** — one forward page with an explicit **END-STATE VISION** (decision engine · explanation layer · DNA layer · triage layer · honesty layer · interaction layer) and *everything* pulled in, including things we may decline. Recovered threads the owner flagged: the **competitive analysis** (fplapex = solver / aceanalyst = viz / fplanalyser = closest in philosophy / FFH = the card + click menu) and the **FFH-style click layer** — ADR-108's deferred **tap-the-pitch custom Streamlit JS component** (~90% already built; needs its own spike+ADR comparing a full React component vs a lightweight click-detector over `pitch.py`'s HTML + a Cloud deploy check; trigger = feedback-driven). Also: 2 roadmap entries were WRONG — the price-change predictor SHIPPED (ADR-092, live: 10 🔺/9 🔻), and the **Attack/Defence FDR split is blocked AT SOURCE** (FPL still returns `strength_attack_*` = 0 after GW1) so it needs a decision, not a wait. **Agreed next: (1) Player DNA sparklines + W-D-L dots, (2) Team DNA clean-sheet rate + team form, (3) the `_percentile` midrank fix.** Prev: **ADR-125 — in-season minutes in the xMins share: DEFERRED to ~GW4-6 (2026-08-24).** Investigated + measured, deliberately not built. (1) ADR-124 already absorbed most of the harm — all 93 no-history players are on the `cold_start` tier where the outer weight is forced to 1.0 and `w` scales only the `ppg` term by `c` (≈0.08 after 1 GW); halving the 1.0 default moves xP for just 25 of them by ≤1.8 over 3 GWs and none crack the top 20 (Sangaré, the worst case, is 49th). (2) ⚠️ **FPL writes a per-GW row when the fixture is SCHEDULED, not played** — `minutes = 0` — so on 2026-08-24 all 38 CHE + 23 FUL players read 0 minutes for a fixture kicking off that evening; a naive `minutes ÷ (90 × GWs)` would score Palmer/Caicedo/Sánchez at 0.00 "never plays" and zero two clubs' xP. Any in-season minutes signal must first check `kickoff_time` has passed (ADR-123's question, one level down). Noted in the `Storage.get_gw_history_by_code` docstring. `form_rate` is safe — it filters `minutes > 0`. (3) One gameweek isn't evidence; the design (shrink in-season share toward the historical/1.0 default by GWs played) is ready but `c ≈ 0.1` would change nothing. **Trigger to revisit: when `calibrate` clears its ≥4-GW guard, gated together with FORM_WEIGHT — one sitting, same data.** Prev: **🚨 GW1 = TOMORROW (2026-08-21) — the immediate focus.** Run **`docs/GW1_RUNBOOK.md` §A** once the first gameweek's results post: `python app.py history --backfill` → `reseed` → `git add data/seed.db` → push → **Reboot app** → verify the GW1-gated features light up (real **manager-ID** import · **momentum/price** boards · **community signals**). Weight **calibration is §B (~GW4–6)** — the harness *recommends*, the owner *commits*; `FORM_WEIGHT`/`SET_PIECE_WEIGHT`/`DEFCON_MAGNIFIER_WEIGHT` stay **0** until a reviewed flip. Player/Team **DNA GW1 follow-ups** (real per-stat sparklines · W-D-L form dots · clean-sheet rate · team form) auto-populate as per-GW results accrue. — **ADR-120 Admin: tester-activity + load/concurrency views — GATED (build post-GW1).** Owner-only Admin reads answering "which testers actually test?" (join `beta_users` emails → `user_key` hash → account-store `updated_at` → a roster; analytics stay anonymous) + "am I hitting limits?" (active-in-last-10-min concurrency proxy · peak · P95 latency · 🟢/🟡/🔴 health). No new table/secret, no analytics-payload change, no `decision_xp` change. Bridge for the GW1 deadline spike = Streamlit Cloud Manage-app **Logs** + the Admin **P95** + tester reports. — **ADR-119 Team DNA — ✅ BUILT (Sprint 172, US-418/419/420).** Live on **Fixtures ▸ 🧬 Team DNA** (browse any team) + **My Squad ▸ Health "Your teams" strip** (your clubs graded, drills in). A team-level companion to Player DNA (reuses its engine). **One component, two doorways:** **Fixtures ▸ 🧬 Team DNA** (browse any team — 8-axis radar [Attacking Threat · Chance Creation · Defensive Strength · Clean-Sheet Potential · Fixture Strength · Set-Piece Threat · FPL Output · Squad Depth, percentile-vs-20-teams, proxies on data we hold] + grade + insights + fixtures + key-players) and **My Squad ▸ Health "Your teams" strip** (scoped to your clubs, drills into the card — **the lead**, most actionable). 🟢 buildable now (preseason on last-season aggregates); 🟡 GW1: real clean-sheet rate + team form; 🔴 deferred: event-data bars/zones/shot-map (future `soccerdata` ADR). No new tab; no `decision_xp`/FDR change. Preview: `scratchpad/team_dna_gen.py`. — **ADR-118 Player DNA page — BUILT (S168–S171 complete).** Arc **COMPLETE**: S168 ✅ radar · S169 ✅ AI Verdict · S170 ✅ AI Insights · S171 ✅ page + My-Squad doorway + GW1 trend. Player DNA lives on **Players ▸ Card** + **My Squad ▸ Players & lineup**. Tracked GW1 follow-ups: real per-stat sparklines + W-D-L form dots; shot map = future `soccerdata` ADR. Owner-approved from a real-data preview (Haaland): evolve the player card (ADR-084) into a **rich single-player analysis page**. 🟢 **Live now (v1 core):** an 8-axis **percentile-within-position radar** (Goal Threat · Creativity · Set Pieces · FPL Output · Consistency · Value · Bonus[ICT proxy] · Team Attack), an **AI Verdict** (Buy/Hold/Sell + 0–100 + grounded line, from existing xP/Edge/Risk signals), key-rate cards, Fixture Run, AI Insights. 🟡 **From GW1:** performance-trend line + sparklines + form W-D-L (per-GW history, dormant → auto-populate). 🔴 **Not v1:** the **shot map** (Opta/Understat event data, ADR-016 deferred → its own future `soccerdata` ADR). **Placement:** one component — **home on Players**, reused from **My Squad ▸ Players & lineup** (no new tab/subtab). Preview: `scratchpad/player_dna_preview.html`. **118 ADRs.** Next markers: **GW1 (2026-08-21)** — momentum boards/questions + threshold calibration + the live manager-import check light up; and the **Data Hardening flip** (Sprint 069 wired it dormant): `python app.py history --backfill` (now also per-GW) → raise `config.FORM_WEIGHT` → calibrate the weight/window on real form. Also raise + backtest `config.SET_PIECE_WEIGHT` (Sprint 126, ADR-096 — the set-piece xP term, dormant → calibrate on real returns). And **calibrate the DefCon opposition magnifier** (built Sprint 129, ADR-097 — set `config.DEFCON_MAGNIFIER_WEIGHT`, tune `DEFCON_P_SCALE` + the band, backtest on real DefCon returns). Also: verify Community Signals on the live cloud IP (may degrade). Tester feedback → Feedback_Log.md. Owner to steer Next markers: **GW1 (2026-08-21)** — confirm the live manager-import (picks unlock); the deferred trends `ask` intent (US-185) + calibrate `TRENDING_NET`/`FORM_MIN`; the Data Hardening flip (per-GW history + form now wired, dormant → set `FORM_WEIGHT`). Later: Tier-2b keyed social (Reddit/X — needs a Cloud secret) / pundit NLP; Tier 3 (crowd-vs-xP backtest). Tester feedback (Sprint 059 loop) → docs/00_Project/Feedback_Log.md. Owner to steer
Next Milestone: **GW1 (2026-08-21, tomorrow) — Data Hardening.** Execute GW1_RUNBOOK §A on the first results (backfill · reseed · verify the gated features), then §B weight calibration at ~GW4–6. The web UI (Phase 2) is live; the full probabilistic xMins (Phase 5) stays post-season-data. Post-GW1 competitive candidates (Roadmap): multi-GW transfer-path planner · full chip-sequence scan · value-frontier scatter · ⭐ Squad Risk Monitor · ⭐ Squad-grade DNA. Also parked: the **Fixtures IA restructure** (Team DNA-led — see Feedback_Log)
Current Version: 0.0.1
Last Updated: 2026-08-27 (Sprint 211 — a dead slot takes the free transfer; the timing line and the banner now share inputs)
Commands: refresh · reseed (refresh → copy to the committed seed) · history (--backfill) · ask · chat (interactive; follow-ups) · table · search · filter · fdr (--type) · fixtures · xp (--by-gameweek) · captain (--squad/--no-xmins) · transfer (--squad/--bank/--next/--count/--raw/--no-xmins) · analyse (--squad/--next/--sort/--no-xmins) · xg · overperf · defcon · cleansheet · squad (--full/--bench/--formation/--objective[default xp]/--no-xmins/--cheap/--premium/--differential/--weekly/--bench-boost/--include[-unavailable]/--exclude/--save/--load) · ask ("build me a squad for £Xm with N low-cost and M premium" / "best midfielders under £8m" / "best fixtures next 5" / "when does Arsenal play?" / "which of TS's players have the best fixtures?")
Web UI: `python -m src.web_streamlit` → http://localhost:8501 (Streamlit, the UI we grow; interactive) · `python -m src.web` → :8000 (FastAPI, frozen reference). Deployable to **Streamlit Community Cloud** (ADR-053; runbook: docs/DEPLOY.md) — public, read-only; a committed `data/seed.db` seeds it; Ask degrades without Ollama. Both read-only, local. Streamlit — **7 tabs** (ADR-069; consolidated pages switch views with a lazy `st.segmented_control`): **Players** (Pool — filter/sort/page-all, table-first + a top-15 bar, + a **Set** ⚽/🚩/🎯 flag column — · **Set pieces** (pen/corner/FK takers + Own%/Val/£m, differential lens, ADR-081) · Over/under · DefCon · Clean sheets · xG; **Clean sheets + xG carry a 🟢…🔴 quality Rating** vs the players shown, ADR-071/073; **every table has a Fit column** — ✅ fit · 🚑/🚫/⛔/❓ concerns, ADR-074/US-276) · **Fixtures** (ticker) · **Squads** (**Build** the full `squad` options → 15 · **My Squad** a **green FFH-style pitch** (kits in formation + a bench strip, xP chips, (C) armband + sub badges, ADR-084) & edit (each card shows Trends + **set-piece** ⚽/🚩/🎯, ADR-081; the summary's **Projected XI** includes the **captain ×2 for next GW only**, ADR-083; missing photos fall back to the **club shirt**, US-255) · **AI Tips** a grounded gameweek plan — captain·lineup·transfer·flags, ADR-070 · **Chips** a grounded chip-strategy advisor — when to play TC/BB/FH/WC, ADR-082 · **Health** 5-GW analysis · **Transfer** XI-aware swaps · **Captain**) · **Ask** (chat — grounded squad/player Qs, **FPL rules** from a curated KB (✓), and open **tactics** labelled ℹ not-verified, ADR-085; a build answer offers **Use this squad →**) · **News** · **Trending** (+ Community Signals) · **Help** (a step-by-step guide). **Cloud squads (ADR-054/055):** a session **active squad** in `st.session_state` — build/upload it (sidebar uploader; **Download = your save**, the CLI `SquadStore` shape), then **edit** it (rename · apply-transfer · swap · bench · set-captain, all mutating `session_state`, validated by `squad_15_issues`) — used across Analyse · Transfer · Captain · My Squad; a committed `data/seed_squads.json` **demo** (a `SQUADS_PATH` fallback) populates the pages; the web **never writes** server-side (a test asserts it)
Sources: FPL — bootstrap-static + fixtures + element-summary (past-season history) — required, retries hard · ClubElo (best-effort, intermittent) · local Ollama LLM (optional, for `ask`)
Tests: 1297 · ADRs: 135 · CI: GitHub Actions (ruff + pytest on push, Py 3.13/3.14) + a secret-gated mirror-backup Action · **Google auth + per-user squad persistence** (Sprint 147, ADR-106 + US-361/362): fixes the tester save/persist cluster — **iPhone session-wipe · cross-device · clunky code login**. Google **`st.login()`** (OIDC) is the primary gate **when `[auth]` is configured**, **allow-listed by `beta_users`** (in it → admitted, else → the waitlist `"not_listed"`); the squad **auto-saves/restores per user** keyed by a **hash of the email** (`squads.link_and_restore`, reusing the S145 auto-sync) — so it **follows you across devices + survives a mobile refresh**. **Off by default** — no `[auth]` → today's shared-code/registration/open gate + manual handle save/load, **byte-identical** (local/CI/open deploy unchanged; a guardrail pins it). Cost: none. Privacy: email-only + basic scopes, squad keyed by a hash. Owner runbook in **BETA.md §5**. · **Split the Squads tab → My Squad + Squad Lab** (Sprint 146, ADR-105 + US-359/360): the busy 7-way *Squads* switch (Build defaulting first) becomes **two top-level tabs** — **My Squad** (the pitch/edit + the five tools as workflow-ordered sub-tabs `My Squad · AI Tips · Captain · Transfer · Chips · Health`; the squad picker + horizon here) and **🥾 Squad Lab** (the renamed *Build* — the full optimiser; a mascot-themed header; "Use this squad →" hands off). A no-squad My Squad **points to Squad Lab**. Nav renumbered (Ask→Admin to 5–10). **Reuses the `views/squads.py` renderers unchanged — IA only, no engine/analytics change.** Functional nav labels + brand on the page (mascot); full MADBOOTS vocabulary deferred (branding-E). · **P0 quick-wins** (Sprint 145, US-356/357 + ADR-104/US-358): the first 2026-08-12 tester-feedback items. **US-356:** the My Squad **Transfer** bring-in list gains **Team + Max-price** filters. **US-357:** captain-persists-on-load via **auto-sync** — a cloud-linked squad (Saved/Loaded under a handle) mirrors every edit to the cloud (fail-silent; only when linked; the sidebar shows "🔄 Auto-syncing"), so a captain/transfer/bench change syncs across devices, not just the last manual Save. **ADR-104/US-358 — the cold-start xP floor:** new/promoted players with **no FPL history** were projecting **0** (69 at GW1); floor with FPL's own **`ep_next`** (`rate = max(ppg, ep_next)`, `rate_source="ep_next"`, not minutes-re-weighted) — honest, targeted (the ≥900-min baseline + fallback tiers unchanged; established players byte-identical; a flagged player stays 0). **We do not chase FFH** — for a player *with* history our number is *above* FPL's own `ep_next`, so FFH is the outlier. The **app now lives at `madboots.streamlit.app`** under `madbootsfpl`, with `madboots.com` the front door (2026-08-12 infra changeover). · **Brand polish** (Sprint 144, US-355, extends ADR-103/084): three tester nits on the rebrand — the My Squad picker → **"View your player's card"**; the player-card wordmark's **MAD/BOOTS gap** fixed (a US-349 regression — the band was one flex with three children so `gap` fell inside the word) via a shared **`brand.mark_html()`** (badge + wordmark as one lockup, MAD+BOOTS in a single span); the **captain card** gains the same mark. Branding the hover popovers was **skipped** (owner steer — too much). Display-only. · **MADBOOTS — the rebrand** (Sprint 141, ADR-103 + US-348/349/350): rename the product's user-facing surfaces **FPL Assistant → MADBOOTS** (one word — the two-tone MAD-purple/BOOTS-orange split carries the word-break), tagline **"Fantasy Football, Calculated."**. A single `web_streamlit/brand.py` source of truth (`NAME`/`TAGLINE`/`page_config`/`wordmark_html`/`badge_*`/`DISCLAIMER`); the **MB badge** (`assets/madboots-badge.png`, transparent) is now the **favicon** on every tab + the small mark on the Home header, the beta gate, and the player-card band; the **two-tone CSS wordmark** on the headers; a quiet **not-affiliated** disclaimer footer. **Approach B — accents on the current light/theme-aware app** (a dark reskin deferred). **Keep the internal identity** — the `fpl-assistant` package/repo + the `FPL_*` secrets unchanged (only `USER_AGENT`/CLI `prog` flipped). Owner-approved via a real-badge Artifact preview; display/asset-only. The repo-transfer + `madboots.com` changeover is a **separate bundled backlog item**. · **Clearer transfers** (Sprint 143, US-353/354, extends ADR-055/046): from tester feedback (most of it already existed → clarify + one feature). My Squad's "Swap a player" → **Transfer** (distinct from the S142 **🔁 Substitute** — a caption spells out *Substitute = lineup · Transfer = a new player*), controls read **Transfer out / Bring in / Transfer →**, with a **live overspend flag** (projected 15-cost + bank before you apply) and an opt-in **Include injured/suspended** toggle. The Transfer page can now **accept the coordinated AI plan** — a new `apply_transfer_plan()` (map all `out→in`, one legality check, soft over-budget warning, clear a sold captain) behind an **"Apply this plan →"** button (was display-only; single-swap apply already existed). Session-state only. · **An intuitive substitution on My Squad** (Sprint 142, US-351/352, extends ADR-055): from tester feedback — a direct **🔁 Substitute** control (a `substitute()` helper = `set_bench` + `legal_xi_issues`) replaces the clunky "re-pick all four" multiselect: **Bring off** (a starter) ↔ **Bring on** (a bench player) with **only legal swaps offered** (GK↔GK; swaps that keep a legal formation), and the pitch **card picker pre-fills "Bring off"** (a benched pick hints). A button *on* the hover card is impossible (the pitch is one static `st.markdown` block — the S139 wall), so it's real widgets near the pitch + the picker as the bridge. Session-state only. · **Tester-feedback polish + a beta waitlist** (Sprint 140, US-345/346/347 + ADR-102): four one-line tester fixes (US-345) — the Players **price filter** now caps at the **highest player price** (Haaland £15.5m no longer excluded) · the card band reads **"Last season"** (was the misleading "Season 24/25") · Trending shows **🔥 Top discussions first** · Help step 7 leads with **☁ Save/Load**; the card **hover popover** shrunk to fit without truncation (US-346, compact = 4 stats + a 250px kit-pop); and a **beta waitlist** (ADR-102/US-347) — a failed registration (cap **full** or a **wrong invite code**) best-effort-captures the email into a `beta_waitlist` table (`web_streamlit/waitlist.py`, reuses the store, **no new secret**, off by default) so the owner can invite later, the **4th** opt-in secret-gated server write. Display/store-only · **A rich player card, in two places** (Sprint 139, US-342/343/344, extends ADR-084): a self-contained HTML **player card** (`web_streamlit/player_card.py`) fed by our existing data — photo · badge · Team·Pos·£price · **3-GW FDR fixture pills** · a **Projected-xP** chip (our `decision_xp`) · ownership-tier/set-piece/availability flags · a **position-adaptive** stat grid (FWD/MID → goals/xGI/ICT · DEF → xGC/DefCon-90/CBI/tackles · GK → xGC/recoveries). Everything `html.escape`d, no JS. Wired as a Players **"Card"** view (selectbox → the full card) + on the **My Squad pitch** (`CARD_CSS`/`card_body` split → a pure-CSS **hover popover** per kit + a **"View a player's card"** picker → the full card, the all-device/touch path). Beats FFH's card on Projected xP/Value/Ownership/DefCon-90; Understat advanced stats (KP/Shots-in-Box) backlogged, Big Chances (Opta-paid) dropped. Display-only · **GW1 Data Hardening — calibration harness + runbook** (Sprint 138, ADR-101 + US-340/341): make GW1 (2026-08-21) a **switch-flip** for the dormant form/set-piece/DefCon weights. A pure `analytics/backtest.py` — **walk-forward** (predict GW N from data ≤ N-1, no leakage), **Spearman rank correlation** primary (MAE + top-N hit-rate as checks), a weight `sweep` (smaller value on a flat curve; `insufficient` below 4 GWs) — with the **predictor injected** (synthetic-tested now). A `python app.py calibrate --weight form|set_piece|defcon` CLI (decision_xp-backed walk-forward, `Storage.get_fixtures_by_event`; **"not enough gameweeks" until ~GW4+**) + `docs/GW1_RUNBOOK.md` + a GW1-readiness smoke. **Recommend-not-flip** — the owner commits the value; weights stay 0 (**xP byte-identical**). **⏳ GW1 owner flip (data-gated)** · **Analytics coverage + a 📊 Admin view** (Sprint 137, US-335/336/337, extends ADR-100): the **feature events** (`analysis_run`·`squad_created`·`squad_saved`·`squad_loaded`·`feedback_submitted`) + `error` at the web sites (anonymity-tested — **no handle/message** in any payload); **perf timers** (`data_load`·`analysis`·`squad_save`·`squad_load` via `analytics.timed`, timing the **compute/IO not renders** so `st.rerun` isn't misread as failure); and a gated **📊 Admin** tab (`pages/9_Admin.py`, `FPL_ADMIN_KEY` — inert/locked/unlocked) that **reads** recent events (the first analytics read — needs an anon SELECT policy; the anon key is server-side, events anonymous) + a **pure `summarise`** (sessions/devices/**returning**/top-pages/success/median-P95). **⏳ owner smoke: anon SELECT policy + `FPL_ADMIN_KEY`** · **Beta usage & experience analytics — foundation** (Sprint 136, ADR-100 + US-332/333/334): answer *"what do testers use, do they return, is it fast/reliable?"* **without a separate analytics project**. `web_streamlit/analytics.py` `track()`s small usage (`session_started`·`page_viewed`) + `perf` events to a Supabase **`events`** table (endpoint derived from `FPL_STORE_URL`, **no new secret**); **#1 rule — never affect the app:** fire-and-forget (daemon thread + tight timeout) + swallow-all, a hard **no-op when off**. **Off by default** behind **`FPL_ANALYTICS`** + the store (the **3rd** opt-in, secret-gated server write; guardrail-pinned "no secrets → no write, byte-identical"). **Anonymous + minimal:** a per-session `uuid4` + a persistent **`fpl_anon`** returning-id cookie (via the ADR-099 component, **defer-then-mint** so a loading run can't inflate unique-users) — **no PII/email/IP, not the handle, no click/mouse/screen tracking, no 3rd-party service**. `boot("<page>")` on all 9 surfaces. Admin = SQL in `docs/ANALYTICS.md` now, a gated view next. **✅ owner-verified: rows landing** · **Confirm on Log out + ☁ Save/Load in the sidebar** (Sprint 135, US-329/331, extends ADR-099/094): clicking the sidebar **"Log out"** now opens a `st.dialog` **confirm** (Log out / Cancel) so a mis-click can't reset a device — kept interactive across reruns by a `_beta_confirming` session flag; and the **☁ Save / Load across devices** moved from the My Squad body into the **Squads sidebar** (`squads.render_cloud_sync` in `render_sidebar`), visible on every sub-view — it **Saves the session's active squad** (disabled with a hint until you have one), secret-gated, moved-not-duplicated (a pointer caption left in My Squad). No analytics change · **Remember-me persistence fix** (Sprint 134, US-330, corrects ADR-099 — ✅ owner-verified on Safari + Chrome): tester feedback said the cookie didn't survive a refresh on **Safari or Chrome**; root cause = a **cookie-jar mismatch** — `streamlit-cookies-controller` writes `document.cookie` **inside its iframe**, but `remember.read()` read via `st.context.cookies` (the app's **top-level request jar**), so the read never saw the write. Fix: read through the **same component** (`_controller().get()`); the component syncs on a rerun, so a one-shot **presence-gated** `_maybe_wait_for_cookie()` shows a "🔑 Checking your device…" placeholder for one run (never hangs: no component → the gate). Native-read reverted. **AppTest can't run the component → the real proof is the owner re-smoke**; if it still fails → `st.login()` (Sprint 134 Option 2) · **"Log out" link** (Sprint 133, US-327/328, extends ADR-099): a sidebar **"Log out"** (a caption "Signed in as {email}" / "Signed in to the beta" + a button) so a tester can **reset a shared device** — it `remember.clear()`s the cookie, drops the session, and re-shows the gate. The clear is **deferred** to a clean run (a `st.rerun()` after the remove would discard it — mirrors the Sprint 132 write) and a **`_beta_forgotten`** flag stops the still-present native-read cookie re-admitting this session. Gated on `gate_active()` (registration or shared-code) → **off on the open deploy**, renders on the passed *and* the cookie-admit run; no new ADR · **"Remember me" cookie** (Sprint 132, ADR-099 + US-325/326): kill the friction where **every browser refresh re-prompts** for the code/email (`st.session_state` is wiped on refresh). A first-party **`fpl_beta` cookie** persists a *passed* gate (~30d; iOS ~7d) — the value (registered **email** / shared **code**) is **re-validated on load** (`user_store.is_registered` / `== current FPL_ACCESS_CODE`), so a **pruned tester / rotated code** is locked out; it grants no new access. Streamlit reads cookies **natively** (`st.context.cookies`, no flash) but can't write, so a small quarantined component (`streamlit-cookies-controller`) behind `web_streamlit/remember.py` (`read`/`write`/`clear`, no-op when absent) does the write (deferred to the clean post-login run so a `st.rerun()` can't drop it). **Off by default** — no cookie (empty in AppTest/CI) → the gate is byte-identical; `remember.clear()` = plumbing for a later "log out"; native `st.login()` = the deferred hard-auth path · **Capped registration gate** (Sprint 131, ADR-098): control tester numbers before the ramp — a visitor enters the **shared code + email**, admitted up to a **variable `FPL_USER_CAP`** (raise 10→20→50), backed by a `beta_users` table in the existing Supabase (`user_store`); soft control (self-declared email, not accounts/auth/paid), off by default; softens DIRECTION §1 · **Beta-readiness tidy** (Sprint 130): BETA.md documents the FormSubmit setup (the `/ajax/` endpoint, the server-side **Origin/Referer** the app sends, `FPL_FEEDBACK_ORIGIN`, the one-time **Activate Form** click, the real-result form) + a **"handle taken?"** hint on the ☁ cross-device Save (`cloud_store.exists` → new vs overwrite). **Feedback fix earlier:** the form reads FormSubmit's real reply + sends an Origin header (relay works server-side) · **DefCon opposition magnifier built** (Sprint 129, ADR-097 refined): a fixture magnifier as a **delta on the DefCon share already in the baseline** (`defcon_pts_per_match · (magnifier−1)`, no double-count) — `analytics/defcon_xp.py` + wired into `player_xp` behind `DEFCON_MAGNIFIER_WEIGHT = 0` (invariance-pinned) + `defcon_xp` on the row + a "🛡 DefCon fixture edge" reason; calibrate at GW1 · **CLI catch-up** (Sprint 128): a **CLI `chips`** command (`python app.py chips --squad X`, reuses `chip_advisor`/`render_chip_advice`) + a **price "who's about to rise?"** `ask`/`chat` intent (likely risers 🔺 / fallers 🔻 by transfer pressure; a lens; 0 preseason → live at GW1) · **Squads Gameweeks box-select** [1,2,3,4,5,10] (Sprint 127, US-315) + **ADR-097** (DefCon opposition-magnifier design gate — a fixture magnifier on a DefCon-xP component via an FDR/xGC clean-sheet proxy, no odds; build at GW1) · **A gated set-piece xP term** (Sprint 126, ADR-096): a `set_piece_bonus` (pens > corners/FK) added to the `decision_xp` rate — **only on the fallback/current tiers** (the baseline already prices an established taker's pens → no double-counting), **wired-dormant** (`SET_PIECE_WEIGHT = 0`, invariance-pinned) + **auditable** (`set_piece_xp` on the row → a grounded "Penalty taker (+X xP)" reason); a *modelling* change, calibrate at GW1 · **History polish** (Sprint 125): a coloured **Δ£** + **cross-player comparison** · **Cross-device squads** (Sprint 124, ADR-094 **built**): a handle-keyed **Supabase** `cloud_store` (save/load/delete, best-effort, secret-gated by `FPL_STORE_URL`/`FPL_STORE_KEY`) + a My-Squad **☁ Save/Load across devices** expander (no login — the handle is the key) + `docs/CLOUD_SQUADS.md`; the **first server-side write** — revised read-only invariant, off by default (a tested guardrail) · **Feedback → inbox** (Sprint 123, ADR-087): a pre-filled **mailto** (`FPL_FEEDBACK_EMAIL`, default fpl.assistant@proton.me) + form-to-email relay compatibility · **Foundations** (Sprint 122): ADR-095 beta-ops (prod/staging, PolyForm-NC LICENSE, mirror backup, uptime); LICENSE + BACKUP.md · Budget-aware "🎯 Target by fixtures" (ADR-041/042) · History on the web + a price column (Sprint 118; display only, reuses `analytics.player_history` — ADR-027/060 + ADR-069; no analytics change). Completes the Sprint-117 history feature. **US-297:** `player_history` season rows carry **`start_cost`/`end_cost`** (already £m) **+ `change`**; `render_player_history` gains a **£m** column (`£start→end`), so the CLI + `ask "X's history"` show a season's price move (empty-safe → "—"). **US-298:** a new **"History"** option on the **Players** `st.segmented_control` → `views/players.py::render_history`: a **player selectbox** → an on-demand `Storage` read → a photo/name header, a native **season `st.dataframe`** (Season · Pts · Mins · Starts · **Pts/90** · xGI · xGC · **£ start · £ end · Δ£**) + a per-GW **`st.line_chart`** (points by GW) when data exists, else a *"fills at GW1"* caption; degrades to a "run `history --backfill`" note. Native Streamlit (no bespoke CSS); display-only, a short-lived read (no server writes). The history feature is now complete across **CLI · Ask · web** · A `history <player>` view (Sprint 117; a read-view over data we already ingest — ADR-027/060 + ADR-037; no analytics change). The open half of ADR-027/060 (the ingest existed; there was no view). **US-295:** a pure `analytics/history.py::player_history(player, seasons, gameweeks)` (normalised past-season rows — season · Pts · Mins · Starts · **Pts/90** · xGI · xGC — + this-season per-GW rows; empty-safe, a lens) + `ui/history.py::render_player_history` (a season table + a per-GW trend, or a "fills at GW1" note); the CLI **`history <player>`** command resolves a name (exact web_name, else a unique substring) → the view (`history --backfill` still ingests). **Real data now** — Haaland's 4 seasons (272/217/181/239 pts); the per-GW trend lights up at GW1. **US-296:** a grounded **`history` ask/chat intent** (`_decide_history`) — *"Haaland's history"* / *"how did X do last season?"* → the view, narrated + **verified (✓, ADR-037)** with the last season's pts/mins/xGI in the facts; placed after `worth` so *"is X worth it"* still routes to worth; inherited by the Ask tab + CLI `chat`. Degrades on an ambiguous/absent player or no backfill · Two feedback fixes + a web-native Captain Pick card (Sprint 116; fixes + display, no analytics change). **US-293 (tester feedback):** (a) **tooltips** — `requirements.txt` now **pins `streamlit==1.61.1`** so the Community Cloud deploy renders on the version we build/test on (the `help=` coverage test passes + no global CSS → an unpinned auto-upgrade is the likely cause of "hover-overs stopped working"); (b) **ClubElo** — `cmd_reseed` now **reports** the Elo count (*"…and N Elo ratings (ClubElo)"* / *"kept last-known"*) — `reseed` always called ClubElo via `refresh`→`_refresh_elo` (verified reachable, 20 clubs); only the printout dropped `n_elo`. **US-294 (backlog):** a **web-native Captain Pick card** — a new `web_streamlit/captain_card.py` (the `pitch.py` self-contained HTML/CSS pattern, ADR-084): the 🥇 pick (**Team · Pos** + a **Projected-xP chip**) · a **Confidence·Band pill** (green/amber/red) · **Why** ✓ / **Risks** ⚠ columns · **Alternatives** 🥈🥉, theme-neutral + every value `html.escape`d + no JS. The web **Captain** tab renders it in place of the mono `st.code` block (the rich picks table stays above; `render_captain_picks` stays the CLI surface); reuses `explain_captain` — no analytics change (owner approved the Artifact preview) · Signal feeds — a media-headlines lens + sharper "talked about" (Sprint 115, owner intake; ADR-093, display lenses — never xP, no new dependency). Reviewed ~12 external sources → **adopt** public, no-auth, FPL-relevant **RSS/Atom** (no scraping/auth/odds — those deferred; odds also cross the lens→xP line). **US-291:** a generic best-effort `api/feeds.py` (`MediaFeedsClient` + a pure `parse_feed` handling **both** RSS `<item>` and Atom `<entry>` with stdlib — no dep) driven by `config.MEDIA_FEEDS`; `web_streamlit/media.py::media_headlines` aggregates **per-feed** (a failing/empty feed is skipped). The **News** tab gains an opt-in, **button-gated**, `st.cache_data`-cached **"📰 Headlines"** section grouped by source with links out — **Fantasy Football Scout** + **BBC Football** live (YouTube supported via a documented `MEDIA_FEEDS` slot; couldn't resolve a channel-id from the sandbox consent wall). **US-292:** `RedditRssClient.get_top_weekly()` (the `top/.rss?t=week` variant) powers a **"🔥 Top discussions this week"** list beside the buzz counter on **Trending** — button-gated + cached, reusing `parse_feed`. All best-effort (403/rate-limit → degrade to a note), no server writes, no live network in the test suite · Four-tier ownership badges — one ownership language (Sprint 114, tester feedback; display/lens, no analytics change). **US-289:** a pure `analytics/crowd.py::ownership_tier(player)` → **💎 differential** (≤5%) · **⭐ popular** (5–20%) · **🟦 template** (20–60%) · **👑 essential** (>60%), via `DIFFERENTIAL_OWN`/`TEMPLATE_OWN`/new `ESSENTIAL_OWN=60` (tunable). `crowd_flags` swaps its 2-tier 🟦/💎 block for it, so the four tiers propagate to **every** Trends-column surface (Pool · Build · Analyse · My Squad pitch · Captain · Trending) and each player shows **exactly one** tier — the 5–20% "popular" band was previously unbadged. `CROWD_LEGEND` rewritten to name all four with meanings; still a **lens** (never xP — the `decision_xp` invariant holds). Verified: 1 essential (Haaland 74.5% 👑) · 15 template · 57 popular · 500 differential. **US-290:** the recommendation **"why"** now speaks the same language — `ownership_label` + a shared `explain.py::_ownership_signal` give `explain_captain`/`explain_transfer`/`explain_worth` a **(✓ reason, ⚠ risk)** per tier (essential/template → ✓ *"Essential/Template pick (N% owned)"* · differential → ⚠ *"Differential (N% owned)"* · popular/absent → neither), so Haaland's captain/worth "why" reads **"Essential (74% owned)"** matching his 👑; the Trending page + Pool inherit the four-tier legend, and Help's ownership steer reads *"💎 differential → 👑 essential"*. No engine change · A robust Ask scroll + an explained differential shortlist (Sprint 113, tester feedback; display/rationale, no analytics change). **US-287:** the Ask example-click **auto-scroll** — which "worked for some, not all" — became a **multi-tick instant** scroll (`[50,200,450,800].forEach(setTimeout → scrollTo bottom)`), still unique per turn (`/*turn N*/`), so it lands reliably after the example expander collapses + any late layout and Streamlit's scroll-restore can't win mid-animation (extends ADR-052). **US-288:** the **differential** shortlist answer now leads with a grounded **"Why a differential?"** (the rank-lever benefit — a ≤5%-owned haul gains rank on the template, a blank costs little relative rank — + the variance trade-off) and a per-pick **"Standout signals (ranked by xP)"** block from a new `ui/shortlist.py::_pick_signals` (nailed ~N mins vs rotation risk · set-pieces ⚽/🚩/🎯 · in form — thin preseason, richer at GW1); `render_shortlist` gained a `rationale=` param, and the **plain** shortlist stays byte-identical. The per-pick signals live in the grounded **detail** (not the LLM facts), so the answer still **verifies ✓** (extends ADR-042/061). No engine change · Price Change Predictor (Sprint 112, owner intake; ADR-092, a display/analytics **lens** — never xP; wired dormant → live GW1). The last buildable-now item of the 5-request intake. **US-285:** a pure `analytics/price.py` — **`price_pressure(player)`** = `net_transfers ÷ selected_by%` (signed, `None`-safe, **0** on flat preseason data), **`price_prediction`** → `rise`/`fall`/`stable` via module thresholds `PRICE_RISE_PRESSURE`/`PRICE_FALL_PRESSURE` (placeholders that don't fire on flat data — calibrate at GW1), **`price_flag`** → **🔺 / 🔻 / ""** (a distinct **forward-looking** marker, not the retrospective crowd 💰↑/💸↓). Dividing by ownership makes players comparable **and** the constant total-manager count cancels for direction + relative magnitude, so a principled signal needs **no** `total_players`/schema change. A `decision_xp` **invariance** test (force 5M net-in → 🔺 fires) proves the lens never touches xP. **US-286:** a **Price** column on the **Players Pool** (🔺/🔻/— + `PRICE_LEGEND` + an honest *"directional pressure — a flag, not exact price/timing; live from GW1"* caption) and a **My Squad** transfer-timing **nudge** naming owned players likely to **fall** (*sell before the change*) / **rise** (*hold, or buy now*), with a dormant *"💷 No price moves flagged … live from GW1"* note preseason. Display-only; no server writes; no engine change · Ask tab polish — readable rules, reliable scroll, an explained "worth" (Sprint 111, tester feedback; display/explainability, no analytics change). **US-283:** the multi-item **rules** facts (chips · scoring · clean sheets · leagues) are authored with embedded bullet lines and `render_rules` prints a multi-line fact **verbatim** (a single-concept fact stays one `•`), so *"how does bench boost work?"* lists the four chips one-per-line — same numbers/names, so `match_rules` + the verifier are unchanged (still ✓, extends ADR-085); and the Ask **scroll nudge** now embeds a **`/*turn N*/`** token so it's unique per turn — Streamlit only re-runs a component when its inputs change, so the static US-275 nudge didn't re-fire and an **example click** never scrolled (typing used `st.chat_input`'s native scroll); now it scrolls on every answer (extends ADR-052). **US-284:** *"is X worth the money?"* now **explains why** — `analytics/explain.py::explain_worth` + `worth_confidence` build a grounded **Confidence · Why (✓) · Risk (⚠)** (projects N pts · above/below the position median · top-third / mid-pack value · penalty taker · set-pieces · template · **premium price ties up budget** · big differential) from the value/rank/median, and `_decide_worth` renders it as the `detail` (so it explains **without Ollama** — the tester saw only a raw Facts block) + the shared **Model note**, with confidence/why/risk in `facts` so a narrated number **verifies ✓** (extends ADR-089/061). No engine change · Chat robustness — remembered context + a bigger rules KB (Sprint 110, owner steer; ADR-091, no analytics change). **US-281:** a new **`src/chat_context.py`** persists the last conversational `Context` to a **local, git-ignored, TTL'd** JSON (`config.CHAT_CONTEXT_PATH`; save/load/clear are best-effort — stale >2h / missing / corrupt → `None`, never a crash). The CLI **`ask`** loads→`converse`→saves, so *"ask …"* then a **separate** *"ask why?"* resolves the follow-up; `--forget` / asking *"forget"* clears it. The CLI **`chat`** resumes the saved context and persists each turn; `chat_transcript` gained a `context=` seed and yields `(result, context)`. The pure `ask.answer`/`converse` API is unchanged, and the **multi-user web keeps `st.session_state`** and never imports the store (a guardrail test enforces it — distinct from the session_state key of the same name) so the read-only deploy is untouched (ADR-091). **US-282:** the curated rules KB grew **13 → 21** topics — player **flags** (🔴/🟡 75-50-25%) · **pre-season unlimited transfers** · **one chip per gameweek** · **bench points** · **wildcard timing** (2/season, 2nd ~GW20) · **mini-leagues** (Classic vs H2H 3/1/0) · **overall vs gameweek rank** · **team value & selling price** — plus the **routing cues** that make each verify **✓** (a rules fact only helps if the question reaches the `rules` intent; specific cues win over the squad intents without hijacking them). All grounded from the KB, never the LLM's memory · Captaincy scopes to your squad by default (Sprint 109, tester feedback; ADR-090, routing/display only) — a tester found *"who should I captain from my-team?"* answered from **all** players, not the loaded squad. Root cause: `ask` resolved "my team" via `\bmy (team|…)\b` — which matches a **space** but not the **hyphen** the app's own example prompts use. **US-279:** one rule in `ask._fresh` replaces the phrase-match — when a squad is loaded, a squad-scoped intent (`_SQUAD_DEFAULT_INTENTS` = captain·transfer·analyse·start_bench·gameweek·chips) names no squad, and there's no **explicit-global** cue (`_EXPLICIT_GLOBAL` = `all players|everyone|best overall|from all|any player`), it **defaults to the loaded squad**. Fixes the hyphen at the root **and** makes a bare *"who should I captain?"* scope to your team; the explicit cue keeps captaincy's global **best-picks** mode; gated to squad intents so a global *fixtures*/compare question isn't scoped; the CLI (no `active_squad`) is unchanged (ADR-090). **US-280:** the two questions read differently — the **global** answer reframes to **"Best Captain Picks · all players"** + a scope **nudge**, the **scoped** one stays **"Captain Pick · from squad 'X'"** (`render_captain_pick` gained `heading`/`nudge`); and the Ask **example buttons name the loaded squad** (`"…for RoboTS?"`) so a click always scopes. No analytics change · A structured "Captain Pick" answer + a shared Model note (Sprint 108, tester feedback; display-only, extends ADR-089) — evolve explainability's presentation; no analytics change. **US-277:** the captaincy answer is now the tester's mockup — a `ui/captain.py::render_captain_pick` **card**: `Captain Pick` header + scope · 🥇 pick (**Team · Pos** via a `short_name→name` map · **Projected: N pts**) · a clean `Confidence: NN/100 (Band)` · **Why** ✓ / **Risks** ⚠ · **Alternatives** 🥈🥉 (+xP) · a **Model note**. Wording nudged at the single source (`explain_captain`: "Penalty taker", "Set-piece involvement", "Expected ~N mins", "Strong fixture vs X", "Only +g pts ahead of Y", "Highest projected points" sans number). Reaches CLI `ask`/`chat` + web **Ask** from one change. **US-278:** a shared **`ui/explain.py::MODEL_NOTE`** (the honest "analytics decide, AI explains" attribution + the folded "heuristic, not a probability" caveat) closes **all five** explained answers exactly once — captain (card) · transfer · build · chips · gameweek (once at the plan's foot, never inside the composite); `render_explanation`'s confidence line went **clean** (`NN/100 (Band)`, caveat removed); `explain_transfer` phrasing aligned; and the **CLI `captain` command + the web Captain tab** now render the **same card** (`render_captain_picks` delegates to `render_captain_pick`, retiring the old mono shortlist table — the web keeps its rich photo table above the card; Alternatives grow past 🥈🥉 with "N." so `--limit N` still lists N). No engine change · Ask readability + a "fit" ✅ emoji (Sprint 107, tester feedback; display-only) — two small honest display fixes. **US-275:** the **Ask** answer renders with **`st.code(…, wrap_lines=True)`** so long narration **wraps** (the aligned tables/plan/Why-Risk blocks stay readable), + a `height=1` same-origin **`st.iframe`** nudge that smooth-scrolls the parent to the newest Q&A after the history replays (best-effort, try/catch; extends ADR-052). **US-276:** a positive **Fit** flag — a new `analytics/crowd.py::fit_flag(player)` = `availability_flag(player) or "✅"` makes a fit player read **✅** (not blank) across the **Pool** + stat boards + the **CLI** `table`/`xg`; **`availability_flag` is unchanged** (still `""` for fit — the truthiness the who's-flagged logic on My Squad + the gameweek plan relies on), so a *separate* helper keeps display and meaning apart; `AVAILABILITY_LEGEND` now leads with "✅ available" (extends ADR-074). No engine/analytics change · Explainability for the AI Tips gameweek plan (Sprint 106, extends ADR-089) — the last major decision; explainability now covers **all** the tester named (captain · transfer · squad · chips · the gameweek plan). **US-273:** `explain_gameweek(plan, players_by_id, xp_by_id, horizon)` **reuses** `explain_captain` (on the plan's `captain_ranked` — `gameweek_plan` now runs captain `limit=3` + returns it) + `explain_transfer` (on the plan's move), adds a grounded **lineup rationale** (*"Start X over Y — higher projected xP"*); `render_gameweek_plan(explanation=…)` appends **· Confidence NN/100 · Band** + a compact **Why** to the captain/transfer lines + the lineup line. **US-274:** an **overall** plan read — `gameweek_confidence` (captain-driven, −8 per flagged player) + a top-of-plan **Confidence · Why · Risk** block (the flags *are* the week's ⚠ risks); `_decide_gameweek` puts confidence/why/risk in `facts` so a narrated number **verifies ✓**. Robust to a missing id (empty-safe). Reuse, not new heuristics; the web **AI Tips** view inherits it (routes through `ask.answer`). No engine change. · Explainability for squad-build & chips (Sprint 105, extends ADR-089) — the Why·Risk·Confidence pattern now covers **all four** decisions the tester named. **US-271:** `explain_squad` + `squad_confidence` (reliability × budget-use) → ✓ optimised on xP · XI projects N · spent £X of £Y · top picks · playing bench; ⚠ £ unspent · rotation-risk starters · doubtful in the 15 · differential-heavy · weak bench — in the build answer (Ask, verifies ✓) + the Build page (above the pitch). **US-272:** `chip_advisor` exposes a per-chip **`margin`** (how clearly the recommended GW/window beats the next-best); `chip_confidence` (a **relative** separation — ≥15% → High, near-flat → Low) + `explain_chips` append a **confidence per chip** to the chip block (Ask/chat + the Chips view). Honest: preseason the gameweeks are near-uniform, so all chips read **Low** (no window is clearly best yet) — sharpening in-season. No engine change; heuristics documented in `explain.py`. · Explainability in Ask (Sprint 104, ADR-089, tester feedback) — show **why**, not just what: a **Confidence · Why (✓) · Risk (⚠)** block on a recommendation, every reason + the number **computed from the data** (never an LLM guess; the LLM only phrases + is verified). **US-269:** `analytics/explain.py` (pure) — `explain_captain(picks, players_by_id)` → grounded ✓ reasons (highest xP · on penalties · set-pieces · expected minutes · template · favourable fixture · in form) + ⚠ risks (away · doubtful · rotation · tough fixture · **narrow lead over the runner-up** · big differential); a **documented, transparent** `captain_confidence` heuristic (blends xMins · the xP lead over #2 · fixture; penalty bonus; capped by chance when doubtful; 1–99) + `confidence_band` (High ≥75 / Medium ≥55 / Low). `ui/explain.py::render_explanation` is the shared block. Wired into **Ask** (`_decide_captain` sets a self-contained `detail` + puts confidence/why/risk in `facts` so a narrated number **verifies ✓**), the **web Captain** tab, and the **CLI** `captain`. **US-270:** `explain_transfer(move, in_row)` + `transfer_confidence` (scales with the XI-gain margin; capped by a doubtful buy) → why the swap (✓ +gain to your XI · higher xP · penalties · set-pieces · frees cash · template; ⚠ costs £ from the bank · selling the out player · doubtful buy · big differential · marginal gain), in the Ask transfer answer. Honest, self-tempering (the same pick reads 99/High in-squad but 69/Medium vs the whole pool); a heuristic, not a probability; preseason-gated signals (form · % of team goals · opponent xGC) light up at GW1. No engine change. · Deadline countdown enhancements (Sprint 103) — the static banner became a real experience. **US-267 (urgency + context + nudge, extends ADR-086):** pure `deadline_urgency(time_left)` (**calm** >24h · **today** <24h · **imminent** <2h) + `gameweek_context(fixtures, gw)` (`{matches, first_kickoff}`); the banner **escalates** (⏳ *in 14 days* → 🟠 *deadline TODAY* → 🔴 *set your team!*) with a context clause (*· 10 matches · first kick-off Fri 20:00*), via a shared `deadline_line(fixtures, now)` → `(gw, deadline, text, urgency)`. Home picks the widget by urgency + a **`st.page_link` nudge** ("set captain · transfers · chips") when close; Squads shows a compact urgency caption. **US-268 (a live clock, ADR-088):** `web_streamlit/countdown.py::countdown_html` — a self-contained **Days : Hrs : Mins : Secs** clock (urgency-coloured, cells **server-filled** so it reads without JS) ticked client-side by `setInterval` off the embedded deadline ISO; `render_countdown` embeds it via **`st.iframe`** (the modern JS-enabled embed; the **first client-side JS** in the app — one self-contained block, no external scripts, display-only). On **Home** the clock is the hero, with the US-267 text line as the no-JS caption. `now`-injected → deterministic tests. · Beta enablement (Sprint 102, ADR-087) — make the deploy **beta-ready**, **opt-in / off by default** (the public app + CI unchanged until secrets are set). **US-263:** `web_streamlit/access.py` — a safe `secret()` getter (try `st.secrets` → `os.environ` → default; **never raises** without a `secrets.toml`) + `require_access()` (a no-op unless **`FPL_ACCESS_CODE`** is set, else a 🔒 private-beta prompt that `st.stop()`s the page until the code matches, remembered per session), called after `set_page_config` on **all 8 pages**. A shared code, no accounts, no server state. **US-264:** a **📣 Feedback** tab (a form → POSTs to **`FPL_FEEDBACK_WEBHOOK`** best-effort, **degrading to a GitHub-issue link** when unset/on failure) + a **"✋ Join the beta"** link to **`FPL_SIGNUP_URL`** (Home + Feedback, founding-tester email capture); a **`docs/BETA.md`** owner runbook (the three opt-in secrets · a Google Apps Script feedback sink · a signup form · recruiting on Reddit · honouring "free for X years" via the email list). No user data on our infra — the feedback POST goes to the owner's own sink (the read-only guardrail holds). Accounts/DB/payments deferred ([DIRECTION.md](DIRECTION.md) §1). · Tester feedback + direction (2026-08-07, post-Sprint 101, no sprint) — a shared **`CROWD_LEGEND`** explains the Trends flags (🟦 **template** = ≥20% owned · 💎 differential = ≤5% · 🔥/❄️/💰/📈) on the **Trending** boards + the **Players Pool** (tester: "what does template mean?"); and two GitHub-readable direction docs — **`docs/00_Project/PRODUCT.md`** (current features · gating items · backlog · roadmap overlay) + **`DIRECTION.md`** (multi-user/registration/paid · iOS-Android · wider-testing options, with a recommendation to stay hobby → validate via a beta → decide). +1 test · Pitch on Build + a deadline banner (Sprint 101, owner request) · **US-261 (pitch on Build):** `render_build` renders the optimal 15 on the **green pitch** (reuse `render_pitch`, ADR-084) — XI in formation + a bench strip in `bench_order` — **above** the sortable table (the picture *and* the detail); display-only. · **US-262 (deadline banner, ADR-086):** a pure `analytics/deadline.py::next_deadline(fixtures, now)` derives the next FPL deadline from the stored `kickoff_time` (each GW's earliest kickoff − 90 min; returns the first still ahead of `now`, **rolling forward** each GW; empty-safe) — **no `events` ingest**; `get_upcoming_fixtures` now returns `kickoff_time`; `ui/deadline.py::deadline_banner` formats a days/hours countdown + the date in **UK time** (stdlib `zoneinfo`). Shown on **Home** (a prominent `st.info`) + **Squads** (a compact caption); a GW1 countdown now, the next GW's later. `now`-injected → deterministic tests. · AI Chat Assistant (Sprint 100, ADR-085, owner intake) — the Ask tab now answers **FPL rules** + open **tactics**, keeping the trust model. **US-259:** `src/fpl_rules.py` — a curated KB of **13** authoritative rules entries (scoring · clean sheets/saves · bonus/BPS · Defensive Contribution · chips · transfers/hits · price changes · squad rules · formations · captaincy · auto-subs · deadline · DGW/BGW) + a pure `match_rules`. A **`rules`** intent placed **first** on **question-shaped** cues (how does / how many points / when is the deadline …) that don't steal squad commands; `_decide_rules` answers **from the KB**, narrated + **verified (✓)**, degrading to the facts block without Ollama — so rules come from curated facts, **never the LLM's memory** (which hallucinated chips this session). **US-260:** a **labelled free-form** tail — a rules-shaped question with no curated fact, or any unrecognised question, gets a scoped LLM answer (general rules/tactics only, **never** a specific pick) tagged **ℹ General FPL advice — not checked against your data** (a third `_trust_line` state beside ✓/⚠), degrading to the help message without a model. Both funnel through one `{free_form: True}` branch in `assemble`, so the **Ask tab + CLI `chat`** got it for free. Grounded squad/player questions unchanged. Three honest states: ✓ grounded · ✓ rules · ℹ tactics · My Squad pitch redesign (Sprint 099, ADR-084, tester feedback: "looks like a poor cousin — redesign closer to Fantasy Football Hub") · **US-257:** `pitch.py::render_pitch` now emits **one self-contained HTML/CSS block** (`st.markdown(unsafe_allow_html=True)`) — a **green football pitch** (mow-stripe gradient + a faint centre circle) with **formation rows** (GK/DEF/MID/FWD) + a **bench strip**, each player a **kit card** (photo/club-shirt image · name · **xP chip** · £m · next opponent (H/A) · crowd/set-piece flags). Every text value `html.escape`d; responsive (flex-wrap); readable on both themes; no JS; **display-only** (the edit controls are untouched separate widgets). · **US-258:** the **(C)** is a gold **armband badge** overlaid on the kit + a green **sub-number badge** (1/2/3/GK); a 👕 placeholder if even the shirt is missing; kit hover-lift + spacing polish. Replaces the Sprint-062 native card-grid (an informal "no custom CSS" call, revisited). Tests read the pitch via `AppTest.markdown` (the HTML blob). A faithful **Artifact preview** (real squad, SVG jerseys for the CDN-blocked photos) let the owner review + approve. · Club-shirt image fallback + captain double-points (Sprint 098, tester feedback) · **US-255 (image fallback):** ~a quarter of players have a valid `code` but **no photo file** on the CDN (it 403s), so cards showed a blank — `badges.photo_url_by_id(players, teams)` now returns the **photo when the CDN serves it, else the club shirt** (`shirt_{team_code}[_1]-66.png`, GK variant for keepers), via a **cached daily existence sweep** (`_missing_photo_codes`, threaded HEADs) that **degrades to "all present"** on any error; threaded through Players/Squads/Trending/News. The bootstrap `photo` field is `{code}.jpg` for everyone (no signal), so an existence check is unavoidable — kept in the edge (cached) + out of the suite via an **autouse `conftest.py`** patch (663 tests, ~45s, offline). No schema/ingest change; works immediately (one ~573-HEAD first-load sweep, cached ~daily). · **US-256 (captain double-points, ADR-083):** the My Squad **"Projected XI (N GW)"** total now adds the captain's **×2 for the next GW only** (a pure `captain_bonus`, from `by_gameweek`/ADR-032) — applied only when the captain is set + in the XI (benched/unset → 0); **"Captain (2×)"** reframed to the captain's next-GW doubled; a **caption** states the ×2 is a one-week thing when horizon > 1 (owner steer). Whole-horizon doubling rejected (captaincy is a weekly decision). Display-only; `decision_xp`/the engine unchanged. Chips deferred to the Chips tab (ADR-082, next-GW). Real: RoboTS + B.Fernandes © → Projected XI 242.5 (= 236.6 + 5.9) · Set-piece attributes on My Squad (Sprint 097, extends ADR-081, tester feedback): each selected player now shows its **set-piece attributes** (⚽ pens · 🚩 corners · 🎯 FK), in **parity with Trends**. **US-253:** `pitch.py::_card` shows a `set_piece_flags` caption line beneath the Trends line (empty-safe, a first-choice taker only) — the **My Squad** pitch cards. **US-254:** a **"Set"** column next to **"Trends"** on the squad tables (Build ×2 · Health · Captain) + an **"In set"** column beside "In trends" on Transfer (the incoming buy); `SET_PIECE_LEGEND` moved to `analytics/crowd.py` for reuse; `render_player_table` gained an optional `help=` for the tooltip. Display-only; reuses the Sprint-095 `set_piece_flags` + ingested order fields; `decision_xp`/the analytics unchanged; no server writes · Chip Strategy Guidance (Sprint 096, ADR-082): a grounded **chip advisor** — **when** to play each chip (**Triple Captain · Bench Boost · Free Hit · Wildcard**) for a squad. **US-251:** a pure `analytics/chips.py::chip_advisor(owned, by_gameweek_by_id, gameweeks)` reduces the per-GW xP (`by_gameweek`, ADR-032) + `best_legal_xi` per GW into a best GW/window per chip — **TC** = the max single-starter ceiling GW · **BB** = the best all-15 GW (+ the bench's share) · **FH** = the weakest best-XI GW · **WC** = the weakest rolling 3-GW window. An **assembler** (the ADR-070 shape), so it can't drift from the tools; no new analytics. A `chips` `ask`/`chat` intent (`_decide_chips` + `_chips_facts`, reuses `_squad_xp`) narrated + **verified** (✓/⚠, ADR-037); `ui/chips.py::render_chip_advice` is the shared block. Routing: a `chips` intent placed **first** with distinctive phrases (`chip`/`chips`/`chip strategy`/`which chip`/`triple captain`/`free hit`/`use my bench boost`/`use my wildcard`) — **not** bare `bench boost`/`wildcard`/`captain`/`bench`, so it can't hijack build/captain/start_bench (a routing test pins it). **US-252:** a Squads **"Chips"** view (after AI Tips) routing through `ask.answer(active_squad=…, horizon=…)` + `render_ask`, horizon-aware, degrading to the block without Ollama. Fixture-run + xP based — **DGW/BGW** timing and **mini-league position** deferred (in-season / GW1); preseason-flat windows sit close (mechanism correct). Display-only; no server writes · Set-piece takers (Sprint 095, ADR-081): who takes **penalties · corners · free-kicks** for each team (the FPL `bootstrap-static` order ints, **1 = first-choice**) + a **low-ownership differential** lens. **US-249** ingests `corners_order` (`corners_and_indirect_freekicks_order`) + `freekicks_order` (`direct_freekicks_order`) onto the `Player` model + `from_api` + storage (auto-migrated; `get_players` picks them up via `SELECT p.*`); a pure, empty-safe `analytics.set_piece_flags(player)` (⚽ pens · 🚩 corners · 🎯 FK for order == 1). **US-250** adds a Players **"Set pieces"** view (a board of Pen/Corners/FK order + **Own% · Val/£m**, through the shared filter, pen-takers-first, with a differential caption + tooltips) and a compact **"Set"** flag column on the Pool. Display-only — `decision_xp`/the analytics are unchanged; penalty duty already feeds captaincy. `refresh` + `reseed` populated real data (573 players, 38 first-choice takers; e.g. B.Fernandes pens+FK, plus low-own pen takers like Buendía/Wood) · Pronoun-aware chat (Sprint 094, ADR-080, refines ADR-047): `_resolve_pronoun` rewrites a pronoun (he·him·his·she·her·they·them·their; possessives → `name's`) → the last turn's **sole** subject (unambiguous only), then routes — so *"is Haaland worth it?"* → *"compare **him** to Isak"* resolves. Wired into `_fresh` (fires in `converse`/`chat`, a no-op for the one-shot `answer`). US-248: the **web Ask** now threads `Context` via `session_state` + `converse`, so **pronouns + the why/next/what-about follow-ups** work in the browser too (first turn unchanged). Substitutes the player's *name* (never assigns a pronoun); analytics decide, LLM narrates, every turn verified · Bench-order polish (Sprint 093, no new ADR — extends ADR-078/079): a **built** squad now starts in the recommended (xP) sub order (`render_build` orders `bench_ids` via `bench_order`; US-245); the **My Squad pitch** bench cards show their sub number — **🔁 1st/2nd/3rd/GK sub**, in priority order (`render_pitch(bench_roles=…)`; US-246). Display/edit only; the bench-order feature is now end-to-end (start on Build · see on the pitch + line · set via ⬆/⬇) · Set the bench order (Sprint 092, ADR-079, refines ADR-055/078): `bench_ids` **order is the sub priority** — `set_bench` now **preserves** the given order (was squad-position); a pure `move_bench_sub` swaps an **outfield** sub up/down (the bench GK is keeper-only → excluded). On **My Squad** the "🔁 Bench order" line shows the **stored** order + a **⬆/⬇ reorder** control + a **"↻ Use recommended (xP) order"** button. Copy-not-mutate; the order rides in the download; the analytics use `bench_ids` as a set (unchanged); no server writes · Bench order (Sprint 091, ADR-078): a pure `analytics.bench_order(bench, scores)` — the outfield bench ranked by xP → **1st/2nd/3rd** sub, then the **bench GK** → "GK" (keeper-only) — shown on **My Squad** as a "🔁 Bench order (auto-subs)" line (horizon-aware xP), with a caption noting FPL brings on "the first that keeps a legal XI". A recommendation, not a per-blank simulator; display-only · My Squad quick-stats summary (Sprint 090, no new ADR — reuses ADR-074/077): the **My Squad** banner now shows a metrics row above the pitch — **Projected XI (N GW)** (best legal XI, horizon-aware) · **Captain (2×)** · **Bench** xP · **Unavailable** (🚑/🚫/⛔ count) · **Doubtful** (❓ count) — plus a **who's-flagged** caption naming the injured/suspended/doubtful players (❓ carries the chance%), else "✓ all 15 available". Alongside the existing £value / legal-15 check; display-only, no analytics change · Configurable Squads horizon (Sprint 089, ADR-077): a shared **"Gameweeks ahead"** dropdown (1–8, **default 5**) on the Squads tab drives the prediction horizon across **Build · My Squad · Health · Transfer · AI Tips** (**Captain stays next-GW** — a one-week decision, with a caption). Threads the chosen `horizon` into `decision_xp`/`analyse_squad`/the transfer renderers (US-237) + a backward-compatible `horizon` param on `ask.answer` → `_decide_gameweek` → `_squad_xp` for AI Tips (US-238; the plan labels "over N GW"). Default 5 = unchanged behaviour; reuses the existing horizon params (no analytics change); the CLI/Ask-tab keep the 5-GW default; no server writes · UX polish (Sprint 088, no new ADR — extends US-227/ADR-074): **Ask example prompts are clickable** (a button per example runs it via a shared `_ask()` helper — chat box + buttons unified; US-234) · the **🚑 availability Fit flag now shows on the CLI ranking tables** too (`table`/`search`/`filter` + `xg` — a last column so an emoji's terminal width can't misalign the rest; US-235) · a **doubtful ❓ flag shows the chance%** (`❓ 75%`) across web + CLI (one `availability_flag` change; US-236) · "Talked about" bigger sample (Sprint 087, ADR-076, refines ADR-059): a tester saw "1 mention regardless" — a live fetch confirmed the counter is fine (it counts 1–4); the cause is the sample (Reddit's default `.rss` = **25 posts**, so 35/51 players sit at "1"). Now the client requests **`?limit=100`** (`config.REDDIT_RSS_LIMIT`) so mentions are counted across ~100 posts, and the (longer) board is **paginated** at 30 like the others. Stays a cached, button-gated, degrade-on-failure buzz lens; `decision_xp` untouched · Formation preview XI score (Sprint 086, ADR-075): the Build page's **🔎 Preview the best XI in a given shape** now shows the shape's **Projected XI xP** (a `st.metric`, a free sum of the previewed XI's xP) and — behind a **default-off "Compare all formations"** checkbox — a table ranking all 7 legal shapes by XI xP with **Δ vs best** (gated because a Streamlit expander body runs even collapsed, so the 7 ILP solves only fire on tick). Real spread today: 3-5-2 254.1 → 5-4-1 246.0 (8.1 xP). Display-only; reuses `select_squad`/the build's `scores`; the saved build stays a full 15; no analytics change, no server writes · Availability flags (Sprint 085, ADR-074): a shared `analytics/crowd.py::availability_flag(player)` (next to `crowd_flags`) → an emoji per FPL status — **🚑 injured · 🚫 suspended · ⛔ unavailable · ❓ doubtful** (blank = available; chosen distinct from the rating circles). A compact **Fit** column + a shared `AVAILABILITY_LEGEND` on the **Players Pool** and all **four stat boards** (over/under · DefCon · clean sheets · xG). The Pool/xG flag raw rows directly; the trimmed boards look the flag up from the full `players` list each render func already holds — **no analytics change**; display-only, no server writes (60 of 572 players flagged even preseason) · xG rating fix (Sprint 084, ADR-073, refines ADR-071): a tester spotted goalkeepers rated "🟢 excellent" on the xG board (xGI is ~0 for GKs; 172 players have 0 minutes; the pool was all shown rows, so a GK-filtered view rated 0.04 as "top 19%"). Now the xG board rates `xGI` **only for outfield players with ≥900 mins**, against that pool; GKs / low-minutes / no-data rows show a blank **—**; the column is renamed **"xGI rating"** and moved right after xGI (away from xGC). `quality_band` + the analytics + Clean sheets unchanged · This week → AI Tips (Sprint 084, US-226): the Squads gameweek tab renamed (label/dispatch/help/Help copy); the grounded gameweek plan behind it (ADR-070) is unchanged · Ask examples (Sprint 084, US-227): an expander of copy-paste example prompts on the Ask page · Consistent number formatting (Sprint 083, ADR-072): a shared web-table format convention — `src/web_streamlit/formats.py` (`FORMATS` label→printf map + `column_config(...)`) formats every Streamlit table via `st.column_config.NumberColumn` (right-aligned + **still numeric/sortable**, not pre-rounded strings): money/value/%/form/ICT → **1dp** (`6.0`, `24.2`), counts (Pts/Mins) → **integer**, the xG family (xG/xA/xGI/xGC/xGC/90) → **2dp**, signed diffs (Diff/Margin/+xP) → **`%+.1f`**. Applied to the Pool, the four stat boards, and the squad tables; the raw analytics values are untouched (display-only); CLI unchanged; no server writes · Help refreshed (Sprint 083, US-224): the step-by-step guide now covers **This week** (the gameweek plan), the **🟢…🔴 quality ratings**, the **table-first Pool**, and a *"what should I do this week"* Ask example · Interpretable stat boards (Sprint 082, ADR-071): a display-only quality **rating** on the Players stat boards — `src/web_streamlit/ratings.py::quality_band(value, pool, higher_is_better)` rates a value **relative to the players shown** (quintile 🟢 excellent…🔴 very poor) with the **percentile** inline ("top N%"); a **Rating** column on **Clean sheets** (xGC/90, lower=better) + **xG** (xGI, higher=better) + a legend; clearer captions + per-column `help=` tooltips on all four boards (absolute season total vs per-90). **Relative** (not fixed) bands because real FPL xGC/90 (median 1.36) makes ChatGPT's fixed table mislabel 91/117 defenders "poor". The two *signed* boards (over/under, DefCon) keep +/- direction, no colour rating. Analytics untouched; no server writes · Per-tab header graphics (Sprint 082, US-222): a distinct emoji-led title + one-line tagline on every tab (Players 👟 · Fixtures 📅 · Squads 🧩 · Ask 💬 · News 📰 · Trending 📈 · Help 🧭), like Home's ⚽ · AI gameweek recommendation (Sprint 081, ADR-070): a grounded **"this week"** plan for a squad — **captain · lineup · a transfer · flags** — as an *assembler* (`src/analytics/gameweek.py::gameweek_plan`) over the existing primitives (`captain_picks` next-GW · `best_legal_xi` vs the declared bench · one self-funding `suggest_transfers` · availability), **no new analytics**. A phrase-routed `ask`/`chat` **gameweek** intent (`_decide_gameweek`, placed after the specific intents so "captain this week" still routes to captain) humanises it → narrated + **verified** (✓/⚠, ADR-037); a `src/ui/gameweek.py::render_gameweek_plan` block; a **Squads → This week** view routes through `ask.answer(active_squad=…)` + `render_ask`, degrading to the plan without Ollama; no server writes · Refresh clarity (Sprint 081, extends ADR-053/056): the freshness caption leads with the **player count** ("N players · data as of \<date\>") so a stale snapshot is obvious, + a **cloud snapshot note**; a one-command **`python app.py reseed`** (refresh `fpl.db` → copy to `seed.db`; new `config.LIVE_DB_PATH`) with DEPLOY.md + Help splitting the **cloud** (reseed → push → reboot) vs **local** (🔄 button / restart) refresh story · Pool: table first (Sprint 081, US-218): `views/players.py::render_pool` renders the paginated **table before** the top-15 bar (the tester's ask; the bar stays filter/sort-responsive) · Sidebar consolidation (Sprint 080, ADR-069): the web sidebar is now **7 tabs** (was 12) — **Players · Fixtures · Squads · Ask · News · Trending · Help**. **Players** merges the old Player Stats (a lazy `st.segmented_control`: Pool · Over/under · DefCon · Clean sheets · xG, shared filter). **Squads** merges Build · My Squad · Health · Transfer · Captain (one segmented control + one shared squad picker for the manage views); only the selected view computes (not `st.tabs`, which runs all). View bodies extracted to `web_streamlit/views/`; **no behaviour change** (every prior AppTest assertion kept, ~38 rewired to drive the control); no server writes · Help tab (Sprint 079, ADR-068): a new **Help** page (`pages/12_Help.py`, last in the sidebar) — a static step-by-step guide to building a team with the assistant: **7 expander steps** (build → make it yours → check health → improve → research → **ask** (copy-paste examples + the ✓/⚠ trust line) → save), a quick-start line, and an honest close on data freshness + what lights up at GW1. Complements Home (which gains a pointer). No data dependency, no input widgets · Team-level squad fixtures (Sprint 078, ADR-067): a 4th `fixtures` ask/chat mode — *"which of \<squad>'s **teams** have the best/worst fixtures?"* — ranks a squad's distinct teams by their `team_fdr` run, with a **player-count** each; a `teams`/`clubs`/`by team` cue routes to it (else the per-player view); a `render_squad_team_fixtures` renderer. Implements the ADR-049 deferral; reuses `team_fdr` (no new analytics); grounded; other fixtures modes unchanged · Team-scoped player filter (Sprint 077, refines ADR-064): the shared filter's **Player** multiselect now scopes its options to the selected **team ∧ position** (empty = all) instead of listing all ~570 names — one edit to `filter_controls`, inherited by Players · Player Stats · Trending; the stored pick is pruned when it falls out of scope. `apply` (the AND predicate) unchanged; no server writes · Tech-debt sweep (Sprint 076, ADR-066): two Backlog items cleared with **no behaviour/output change** (65 optimizer + 19 render assertions unchanged). **PuLP** — variables → `problem.add_variable(...)`; **kept `PULP_CBC_CMD`** (`COIN_CMD` needs an external CBC — verified "cannot execute cbc", would break the read-only Cloud); the blanket `DeprecationWarning` ignore → a **targeted** PULP_CBC_CMD filter (other deprecations now surface). **Squad renderer** — `render_squad`/`render_loaded_squad` share a `_header` + `_BENCH_HEADING`; the `render_rows` fold is **closed** (its flat single-space join can't reproduce the mid-table heading / glued markers / divergent price cells byte-for-byte) · Trending filter (Sprint 075, reuses ADR-064): the **Trending** page gained the same **Team · Position · Player** filter (AND-combinable) as Players & Player Stats — one `filter_controls`/`apply` above the four boards, applied before pagination; the buzz board + GW1 note unchanged. No new analytics/ADR; the filter's tooltips are inherited (ADR-065) · Help tooltips (Sprint 074, ADR-065): a concise **ⓘ tooltip** (`help=`) on **every input control** across the web — added at the shared components (`filters` · `paginate` · `squads` sidebar/picker) so all pages inherit it, + per-page controls on Players/Fixtures/Trending/Build Squad/My Squad/Transfer/Captain; key buttons too. `st.tabs`/`st.chat_input` keep captions (no `help=`). A **coverage test** (`tests/test_help_tooltips.py`, via AppTest `.help`) asserts every input widget on all nine pages carries a non-empty tooltip — so a new option can't ship without one. No behaviour change · Rich filters (Sprint 073, ADR-064): one shared `web_streamlit/filters.py` (`filter_controls` + `apply`) adds a **Team · Position · Player** multiselect filter — **AND**-combinable (empty dim = any), tolerant of Row/dict rows — to both **Player Stats** (above the four tabs) and **Players** (with max-price; team + player added to the existing position). The Players **price-vs-points scatter is replaced** by a filter-responsive **top-15 bar** (Altair, rank-ordered) of the strongest filtered players by the sort metric. No analytics change; no server writes · Player Stats + pagination (Sprint 072, ADR-063): a new **Player Stats** page (sidebar position 2) brings the CLI stat views into the web as tabs — **Over/under-perf · Defensive Contribution · Clean sheets · xG** — each reusing the *same* analytics (`over_under`/`defcon_reliability`/`defensive_solidity`; xG by xGI), rendered with a team badge + the CLI columns, paginated; **season-to-date** (preseason = last season's carryover). A shared **`web_streamlit/paginate.py`** (`page_labels` pure + `paginate` = a page selectbox + "Showing X–Y of N") powers see-everything paging: **Players** now pages through all (no 50-cap) + a **team / position** sort (on top of points/value); **Trending's** four boards page past 30. Sidebar renumbered (Player Stats at 2 → Fixtures 3 … Trending 11). No analytics/data change; no server writes · Web build parity + tab reorg (Sprint 071, ADR-062): the web now has the **full CLI `squad` build options**. **Build Squad** (renamed from Build) exposes include · exclude · declared bench · objective (points/value/xp/xgi) · no-xmins · weekly/bench-boost · include-unavailable as **form widgets** (+ the existing budget/archetypes), all feeding the *same* `select_squad`/`decision_xp` (no engine change); the saveable build stays a full **15** (Download / Use this squad →), plus a display-only **"best XI shape"** preview (formation). The **Ask** tab's "build me a squad…" answer now offers **"Use this squad →"** (a `squad` field on `AskResult`) → the session squad → **My Squad** to tweak → download. Squad tabs **renamed + regrouped**: Squads→**Squad Health**, Build→**Build Squad**, sidebar order Players · Fixtures · **Build Squad · My Squad · Squad Health** · Transfer · Captain · Ask · News · Trending; My Squad points to Build Squad for a full rebuild. No server writes (the guardrail holds) · Differentials / value `ask` intent (Sprint 070, ADR-061): two natural-language lenses over the unified xP + ownership, grounded + verified (ADR-037). (1) A **differential** lens on the shortlist — `ask "best differential [position] under £Xm"` filters to **≤`DIFFERENTIAL_OWN` (5%) owned** (0% included), xP/value-ranked, with an **Own%** column (`render_shortlist(show_own=…)` keeps the plain shortlist byte-identical). (2) A single-player **`worth`** intent — `ask "is X worth the money?"` → the player's **xP/£m**, its **rank among available same-position players**, the **position median**, and a tiered fact-derived **verdict** (good/fair/pricey); degrades on an ambiguous/absent/flagged player. Routing: `worth` before transfer ("worth buying" ≠ "buy"), "differentials"→shortlist, "most owned"→trends. Preseason-honest: ownership is flat now → the differential filter sharpens at GW1 (a caption says so) · Data Hardening prep (Sprint 069, ADR-060): the first season-start foundations, built **wired but dormant** so GW1 (2026-08-21) is a switch-flip. (1) **Per-GW history** — a `player_history` table (`PlayerGameweek`, keyed `code+round`; additive/idempotent) filled by the **existing** throttled `element-summary` walk (the one call already carries `history` — empty preseason → live GW1). (2) **A dormant in-season form blend** — a pure `src/analytics/form.py` (`form_rate` = a recency+minutes-weighted rolling **pp90**; `blend_form`) folded into the **one** `decision_xp` recipe behind `config.FORM_WEIGHT = 0` → xP **unchanged today** (an invariance test + a real-DB smoke pin it; the whole suite passing is the proof). Every `decision_xp` caller (cli/ask/web) wired → **GW1 = backfill + raise the weight**, nothing else. Not FPL's `form` field (minutes-blind); the one-xP invariant (ADR-041) preserved · Community Signals (Sprint 068, ADR-059): a Reddit **RSS** buzz counter (`r/FantasyPL/.rss`, **no auth/secret** — the `.json` API 403s) — `src/api/reddit.py` + a pure `src/community.py` `community_buzz`/`community_signals` (degrade-gracefully); a Trending **"💬 Talked about"** board (button-gated + `st.cache_data` ~30 min → "unavailable" on failure). Buzz not sentiment; display-only; cloud-IP may block → degrades · Community trending (Sprint 067): a pure `trending(players, by, limit)` (owned/in/out/form; free FPL crowd data, display-only) powering a **trends `ask`** intent (grounded; preseason-graceful → "live from GW1") + a **Trending page** (`pages/10_Trending.py` — 4 boards, photos/badges/flags). Ownership works now; momentum/form at GW1. Reddit social sentiment = a gated follow-up (US-195/ADR-059, needs a cloud secret) · Ask ↔ session squad (Sprint 066, fix): the web **Ask** tab now answers about your **loaded** squad — `ask` gained `_load_squad`/`_known_squad_names` + an optional `active_squad` threaded through (default None → CLI unchanged); "my team" resolves to the loaded squad; fixes captain/analyse falling back to "(all players)" · Phase 6 Tier 2 (Sprint 064, ADR-058): an FPL official-**news lens** (`pages/9_News.py` over the stored `player.news` + ingested `scout_news_link`; flagged players, most-serious first, degrades to "no current news") + **import your team by manager-ID** (public FPL entry API → `src/manager.py` `picks_to_squad`/`fetch_manager_team`, degrade-gracefully; a sidebar Import control → the session active squad, no server writes; picks **GW1-gated** → live 2026-08-21). Keyed social (Reddit/X) + pundit NLP deferred. Display/state only — xP untouched · UI (Sprint 062): **Fixtures** = a **fixture ticker** (teams × gameweeks grid, colour-coded by difficulty, a **1–8 weeks** selector; pure `fixture_ticker` + a pandas Styler) · **My Squad** = a **formation pitch card-grid** (`web_streamlit/pitch.py`; position rows + bench, cards with photo/name/(C)/£/xP/opponent/flags), keeping the edit controls — robust native Streamlit, no custom CSS · Phase 6 — Crowd & Sentiment Signals (Sprint 060–061, ADR-057): crowd signals as a **complementary lens + flags, never blended into xP** (a test asserts `decision_xp` unchanged). Tier-1 **free FPL** fields ingested (`transfers_in/out_event` · `cost_change_*` · `form` · `ict_index`+components · `value_form`; `seed.db` reseeded); a pure `crowd_flags` helper (🟦 template ≥20% · 💎 differential ≤5% · 💰 price · 🔥 trending · 📈 form; tunable, GW1-calibrated) shown as **Trends/Form/ICT** on Players + a **Trends** column on Build/Analyse/My Squad. Momentum/form 0 preseason → live GW1. External social/pundit deferred (Tier 2/3) · Pre-tester polish (Sprint 059): **imagery consistency** — photos + team badges on every squad tab (Build/Analyse/Transfer/Captain/My Squad), augmenting the text (shared `badges.photo_url*` + `tables.render_player_table`); **local refresh + freshness (ADR-056)** — a **"Data as of \<date\>"** caption on every tab + a **local-only "🔄 Refresh data"** button (gated by `FPL_LOCAL`, set by the runner; writable non-seed DB; reuses `ingest.refresh`), the first web write path — the **cloud stays read-only** (caption only) · Editable squad (ADR-055): the session squad is **editable in `session_state`** (no server writes) — **rename**, **apply a transfer** (Transfer), **swap any player** / set the **bench** (a new **My Squad** hub), **set a captain** (Captain → **(C)** in Analyse + the download); one generic core validator **`squad_15_issues`** (positions + ≤3/club hard, **budget a soft edge warning**); edits go through mutation helpers (`rename`/`apply_transfer`/`set_bench`/`set_captain`), pages never touch the dict inline; `name`/`captain_id` are superset keys (validated on upload); fixed the Build xP/xMins-render bug · Cloud squads (ADR-054): a session **active squad** — **build** (Build → Download a `squad.json` + Use this squad) or **upload** (sidebar) → `st.session_state`; **Analyse · Transfer · Captain · My Squad** run the engine on the squad **dict** (not `ask`-by-name); a committed `data/seed_squads.json` demo (+ a `config.SQUADS_PATH` fallback) populates the pages; persistence is the user's file, the web **never writes** server-side (a test scans both edges for `.save(`) · Web UI = **Streamlit** (ADR-051/052): a multipage `src/web_streamlit/` edge — **Home** landing · Players (filters + **photos** + a price-vs-points **scatter**) · Fixtures (+ **team badges** + an FDR **bar chart**) · Squads/**Analyse** · **Transfer** (bank slider → XI-aware swaps) · **Build** (budget/archetype → the optimal 15) · **Captain** · **Ask** (a grounded **chat**), run via `python -m src.web_streamlit`; a measured spike chose it over FastAPI (pure-Python, interactive); the **FastAPI edge (ADR-050) is frozen** as a lean reference; both are thin edges over the one engine, the core imports neither (a test asserts it) · Web UI (ADR-050): a thin **read-only, local-only FastAPI** edge (`python -m src.web` → 127.0.0.1:8000) reusing the analytics — pages Players/Fixtures/Squads/**Ask** (the grounded answer + trust line, in a browser); slice-1 reuses the CLI text renderers in `<pre>`; the core stays web-free (a test asserts it); the CLI runs without the web deps · Fixtures `ask` intent (ADR-048/049): `ask`/`chat` answer fixture questions in three modes — a league **FDR ranking** ("best/hardest fixtures next N"), a **single team's schedule** ("when does Arsenal play?"), and **squad-scoped** ("which of TS's players have the best fixtures?" → your players by their team's run); reuses `team_fdr`/`team_schedule`; team names resolve or ask, never guess; `_squad_name` is possessive-aware · Conversational `ask` (ADR-047): a `chat` REPL where follow-ups build on the last turn — **why** (re-explain), **next** (2nd/3rd pick or next page), **what-about** (shortlist-only position swap); analytics decide every turn, grounding verified · XI-aware transfers (ADR-046): `transfer` ranks swaps by **XI improvement** (best-XI gain via `best_xi_points`), so bench-fodder upgrades drop out; `--raw` for the old raw-xP-gain ranking; the plan + `ask` agree · Bench-aware squad build (ADR-045): `--weekly` maximises the XI + a cheap playing bench (rotation, +7.6 XI xP), `--bench-boost` the max-15 (or NL "for rotation"/"for a bench boost") · Squad builds show a Starting XI xP vs Bench xP breakout (Sprint 044) · Archetypes (ADR-043/044): `--cheap`/`--premium`/`--differential` · One xP metric (ADR-041); `xp` the default squad objective, table shows xMins + xP · Phase 4: grounded `ask` answers eight (…build-a-squad · best-players · fixtures), verified (ADR-037); a conversational `chat` mode (ADR-047)
Known issue: ClubElo intermittent (timed out 2026-08-04 pm; up earlier same day) — best-effort, degrades; no blocker.
Deferred (→ later): more `ask` intents (fixtures/FDR, differentials); a persisted/pronoun-aware chat; an LLM intent classifier; Data Hardening (post-GW1: full backfill + per-GW history + form blending); multi-move transfer planner; ceiling captaincy; xMins v0 DONE (Sprint 037, ADR-038); the full probabilistic xMins model (Phase 5, post-GW1); web UI
