# Spike 188 — actions on the shirt

**Goal: less page, not more interaction.** Six or seven widgets currently sit below the pitch purely to act on
one selected player. If the shirt carries its own actions, most of that chain stops needing to exist.

```
./venv/bin/python -m streamlit run spikes/188-actions-on-the-shirt/spike_app.py
```

Tap a name or one of the ©/🔁/⚔️ actions under it. The returned id carries both.
